---
phase: quick-260610-w61
plan: 01
subsystem: tools/protobuf-decoder
tags: [protobuf, examples, encoding-override, ux]
requires: []
provides:
  - "Example chips carry per-example InputEncoding; chip click sets raw + explicit override"
affects: []
tech-stack:
  added: []
  patterns:
    - "Per-example `encoding` field in EXAMPLES data — future base64 example flips the segmented control for free"
key-files:
  created:
    - src/tools/protobuf-decoder/examples.ts
  modified:
    - src/tools/protobuf-decoder/ProtobufDecoder.tsx
    - src/tools/protobuf-decoder/ProtobufDecoder.test.tsx
    - test/e2e/protobuf-decoder.e2e.ts
decisions:
  - "SUPERSEDED (follow-up fix b3b1676b): chip click now CLEARS the override to auto-detect — the explicit override was sticky and broke typed input after chip use; determinism moved to a test-time EXAMPLES-detection contract"
metrics:
  duration: "~7 min"
  completed: "2026-06-10"
  tasks: 2
  files: 3
---

# Quick 260610-w61: Protobuf Example Chips Switch Input Encoding Summary

Example chips now carry their own `InputEncoding` and a chip click sets both `raw` and an explicit `override`, so every example decodes under its matching mode even when the user had previously forced a different encoding via the segmented control.

## What was done

- **Task 1 (`505439e9`)** — `EXAMPLES` extended to `{ label, value, encoding: InputEncoding }` (four hex payloads + the decimal array); chip `onClick` now does `setRaw(ex.value); setOverride(ex.encoding)`. Segmented control needed zero changes — its active segment already renders `result.encoding`, and `override` is already part of `decodeKey` so selection/collapsed reset correctly. Three new unit tests: forced-base64 → `{1:150}` chip flips to hex + decodes (no alert); forced-hex → "decimal bytes" chip flips to decimal + decodes; clicking the now-active segment after a chip click clears the override back to auto-detect with no regression. Existing D-03/D-10 tests pass unmodified.
- **Task 2 (`966fc5d0`)** — new real-WKWebView e2e spec in `test/e2e/protobuf-decoder.e2e.ts`: seed hex, force base64 (genuinely mismatched), click "nested message" → hex segment `aria-pressed="true"`, base64 released, field renders, no alert; then "decimal bytes" → decimal segment active, clean decode. Full e2e gate via `scripts/e2e-spike.sh`: **15/15 spec files**, exit 0; orphan vite/app processes reaped after.

## Verification (binding harness, in order)

1. Simplify pass — changes already minimal (data field + two-statement onClick); nothing to reduce.
2. `codex exec review --uncommitted` — one P3 finding, **fixed**: the forced-base64 unit test originally clicked base64 on an empty input where base64 is already the detected/active segment, so the click cleared instead of forcing (the test would have passed under the old `setRaw`-only behavior). Fixed by seeding `6869` (detected hex) before clicking base64. The e2e spec was written with the same seeding from the start.
3. Unit gate: `pnpm vitest run src/tools/protobuf-decoder/` 61/61, `tsc --noEmit` clean, `pnpm lint` clean; lefthook pre-commit ran the full suite **798/798** on both commits.
4. Real-WKWebView e2e: 15/15 spec files green.

## Binding bars

- `src/lib/protobuf/decoder.ts` + its 19 tests: **byte-for-byte untouched** (`git diff` empty under `src/lib/protobuf/`).
- Zero new runtime AND dev dependencies (`package.json`/lockfile untouched).
- Click-active-clears-override segment behavior and `raw === ex.value` chip-active check unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Unit test didn't exercise the forced-override scenario it claimed**
- **Found during:** Task 1 (codex review)
- **Issue:** Clicking base64 on empty input runs the active-segment-clears path (empty input auto-detects base64), so the override was never forced — a false-positive test.
- **Fix:** Seed `6869` (detected hex) before clicking base64, plus assert base64 starts un-pressed.
- **Files modified:** src/tools/protobuf-decoder/ProtobufDecoder.test.tsx
- **Commit:** 505439e9 (landed with the impl)

## Deferred / Observations (out of scope, pre-existing)

- `ProtobufDecoder.tsx` contains a pre-existing NUL byte (0x00) as the `decodeKey` join separator (~line 61, committed before this task) — git treats the file as binary in diffs (`Bin` in `--stat`). Functionally valid JS; left untouched per scope boundary. Worth replacing with a printable separator in a future cleanup so diffs become readable.

## Known Stubs

None.

## Threat Flags

None — pure frontend UI behavior, no new network/auth/file surface.

## Self-Check: PASSED

All 3 modified files + SUMMARY exist; commits 505439e9 and 966fc5d0 present in git log.

## Follow-up fix (2026-06-11, commit `b3b1676b`)

**Bug (user-reported, reproduced in real Chrome):** the chip click's EXPLICIT override was sticky. Repro: type decimal `10,3,50,51,52` (auto-detects decimal) → click "nested message" (hex, works) → type decimal again → input decodes under the stale forced-hex override → "Hex must have an even number of digits"; the decimal segment never re-activates.

**Fix:** chip click now does `setRaw(ex.value); setOverride(undefined)` — back to auto-detect, which resolves every EXAMPLES value to its declared format. The original decision's determinism ("stays correct if detectEncoding heuristics change") moved from runtime to test time: a new **EXAMPLES-detection contract test** iterates ALL entries asserting `detectEncoding(ex.value) === ex.encoding` (the `encoding` field is kept as the expected value). `EXAMPLES` moved to a new pure-data module `src/tools/protobuf-decoder/examples.ts` (codex P2: exporting a constant from the TSX component file trips `react-refresh/only-export-components` under `--max-warnings=0`).

**Tests:**
- Unit: two chip tests updated to clears-override semantics (forced base64 → hex chip still flips via auto-detect; forced hex → decimal chip flips); a repurposed segment test proves sticky-force + click-active-clears unchanged AFTER a chip click; NEW regression test for the exact repro (type decimal → hex chip → type decimal → decimal re-activates, no alert). 63/63 in the tool dir.
- E2e: the 260610-w61 flip spec extended with the type-after-chip step — after the "nested message" chip, `setValue("10, 3, 50, 51, 52")` asserts the decimal segment re-activates and decodes cleanly. Real-WKWebView run via `scripts/e2e-spike.sh`: **15/15 spec files, exit 0**.

**Gate (binding order):** simplify (changes minimal) → codex review (`--uncommitted`): one P2, fixed via `examples.ts` extraction → `vitest` 63/63 + `tsc --noEmit` clean + `pnpm lint` clean → real-WKWebView e2e 15/15. Lefthook pre-commit ran the full suite **800/800** on the commit. Decoder + 19 tests untouched; zero new deps. Pre-existing NUL byte in `ProtobufDecoder.tsx` (~line 67 decodeKey separator) left untouched per scope.
