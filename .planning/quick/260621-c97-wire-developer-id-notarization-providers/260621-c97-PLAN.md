# Quick Task 260621-c97: Wire Developer ID notarization (providerShortName + RELEASE.md flip)

**Date:** 2026-06-21
**Type:** Build-config + docs (no webview/source-logic change)
**Context:** Backlog 999.10 deliverable #1 — flip the direct distribution channel from ad-hoc
signing to Developer-ID-signed + notarised + stapled, now that Apple Developer enrolment is done.

## Scope (1 atomic change)

- `src-tauri/tauri.conf.json` — add `"providerShortName": "FK4HQK83WX"` (Apple Team ID) to
  `bundle.macOS`, enabling `notarytool` submission. `signingIdentity` stays `"-"` so dev builds
  remain ad-hoc/fast; `APPLE_SIGNING_IDENTITY` env overrides it for release builds.
- `docs/RELEASE.md` — flip the three "DEFERRED / post-enrolment" notarisation notes (the
  two-signatures callout, the §2 reminder, and the "Post-enrolment notarisation flip" callout) from
  deferred to **LIVE (2026-06-21)**.

## Verification

- `tauri.conf.json` parses as valid JSON; `bundle.macOS.providerShortName == FK4HQK83WX`. ✓
- No webview/unit harness applies (no source logic touched). Real verification = the notarised
  release build + `spctl -a` / `stapler validate` on the produced DMG (done at publish time).

## must_haves

- truths: providerShortName committed = Apple Team ID; signing identity driven by env at release time.
- artifacts: `src-tauri/tauri.conf.json`, `docs/RELEASE.md`.
- key_links: backlog 999.10 (`.planning/ROADMAP.md`), RELEASE.md notarisation callout.
