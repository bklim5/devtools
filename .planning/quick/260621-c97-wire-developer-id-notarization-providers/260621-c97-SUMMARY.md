# Quick Task 260621-c97 — Summary

**Completed:** 2026-06-21
**Outcome:** Developer ID notarisation wired for the direct distribution channel (999.10 #1).

## What changed

- **`src-tauri/tauri.conf.json`** — added `"providerShortName": "FK4HQK83WX"` to `bundle.macOS`.
  `signingIdentity` left `"-"` (dev builds stay ad-hoc/fast; `APPLE_SIGNING_IDENTITY` env drives
  Developer-ID signing + notarisation for release builds, per RELEASE.md's documented override).
- **`docs/RELEASE.md`** — flipped three deferred notarisation notes to **LIVE 2026-06-21**: the
  two-signatures callout, the §2 build-env reminder, and the "Post-enrolment notarisation flip"
  callout (now marked ✅ DONE, with the steps retained as the standing release procedure).

## Verification

- `tauri.conf.json` valid JSON; `bundle.macOS.providerShortName == FK4HQK83WX` ✓.
- lefthook pre-commit gate (tsc + vitest + eslint) green at commit.
- Build-verify webview/unit harness N/A (no source logic). End-to-end proof is the notarised
  `release:publish` build + `spctl -a` / `stapler validate` (runs at publish, this same session).

## Prereqs confirmed live (before this task)

Developer ID Application cert in keychain (`FK4HQK83WX`), App Store Connect notary API key
auth-validated, updater key + password sign-test passed, gh ADMIN on `bklim5/devtools-releases`.

## Next

`release:bump` → `0.4.0`, then `release:publish` (notarised universal DMG + updater `.sig` →
GitHub release + `latest.json`).
