---
quick_id: 260614-l39
phase: quick
plan: 260614-l39
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/capabilities/default.json
  - test/e2e/license-buy.e2e.ts
  - tsconfig.json
  - scripts/check-dev-strip.sh
  - server/webhook/src/keygen.ts
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03]
description: "Phase 20 webhook code-review Medium findings 6–10 — harden the now-live licensing pipeline."

must_haves:
  truths:
    - "The opener capability only permits the exact buy host (https://tinkerdev.io/*), not any https host"
    - "The existing Buy CTA still opens https://tinkerdev.io/buy under the narrowed scope"
    - "The webview app's tsc --noEmit no longer sees Node globals (setTimeout typed as number, not NodeJS.Timeout)"
    - "check-dev-strip.sh FAILS a release binary that still contains PROD_*_PLACEHOLDER sentinels"
    - "The Buy e2e asserts platform.opener.openUrl was called with the exact url, not just that the modal stayed mounted"
    - "keygen.ts searchByOrderId returns an exact metadata.orderId match (finding 6) — verified, no-op if already guarded"
  artifacts:
    - path: "src-tauri/capabilities/default.json"
      provides: "opener capability scoped to https://tinkerdev.io/* with a clear mailto seam comment"
      contains: "tinkerdev.io"
    - path: "tsconfig.json"
      provides: "root webview tsconfig with types:[] allowlist"
      contains: "\"types\""
    - path: "scripts/check-dev-strip.sh"
      provides: "placeholder-absent release assertion that fails the script"
      contains: "PLACEHOLDER"
    - path: "test/e2e/license-buy.e2e.ts"
      provides: "window-level openUrl stub + exact-url assertion"
      contains: "tinkerdev.io/buy"
  key_links:
    - from: "src/components/UpsellPanel.tsx"
      to: "src-tauri/capabilities/default.json"
      via: "platform.opener.openUrl(BUY_LICENSE_URL) must remain permitted by the narrowed scope"
      pattern: "tinkerdev\\.io"
---

<objective>
Harden five deferred Medium code-review findings on the now-LIVE Phase 20 licensing pipeline (real money: Keygen CE + Lemon Squeezy + Resend). These are not new features — they close attack surface and verification gaps in shipped code.

Purpose: the webhook + Buy + license pipeline is in production; these gaps are open-redirect surface (finding 7), a masked typecheck gate (finding 8), a placeholder-passes-release check (finding 9), a vacuous Buy e2e (finding 10), and a re-check of an idempotency guard (finding 6, likely already closed by 20-03).

Output: a narrowed opener capability, a `types:[]`-clean root tsconfig, a placeholder-absent release assertion, a non-vacuous Buy e2e, and a recorded verification of finding 6.

Constraints (binding):
- Per-task DoD in order: /simplify → /codex:review → vitest + tsc --noEmit + eslint green → real-WKWebView UI verification where the finding touches UI/e2e (tasks 1 and 4).
- The decoder's 19 tests are immovable — `src/lib/protobuf/decoder.ts` and its tests stay byte-for-byte untouched.
- HashRouter only; tools import `src/lib/platform/`, never `@tauri-apps/*` directly (unchanged here).
- NEVER widen the opener scope back to `*` or `https://*`.
- Finding 6 is a verified no-op if the exact-orderId guard already exists — record and SKIP, do not re-add.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/todos/completed/2026-06-13-phase20-webhook-review-medium.md
@.planning/todos/pending/2026-06-13-send-feedback-affordance.md
@.planning/phases/20-purchase-pipeline/20-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase; no exploration required. -->

Buy URL constant (src/components/UpsellPanel.tsx:52):
```typescript
export const BUY_LICENSE_URL = "https://tinkerdev.io/buy";
```
The Buy CTA calls `platform.opener.openUrl(BUY_LICENSE_URL)` (UpsellPanel.tsx:319) — best-effort, logs on failure, never throws. The narrowed opener scope MUST keep this exact URL permitted.

opener seam (src/lib/platform/index.ts:109 / tauri.ts:132): `opener.openUrl(url: string): Promise<void>` — tauri.ts is the SOLE `@tauri-apps/plugin-opener` importer; browser.ts is a no-op.

Current opener capability (src-tauri/capabilities/default.json):
```json
{ "identifier": "opener:allow-open-url", "allow": [{ "url": "https://*" }] }
```

keygen.ts searchByOrderId (server/webhook/src/keygen.ts:66-69) — ALREADY guards exact match:
```typescript
const match = body.data?.find((lic) => lic.attributes.metadata?.orderId === orderId);
return match ?? null;
```

Root tsconfig (tsconfig.json) — webview app, `include:["src"]`; @types/node is hoisted by the server/webhook workspace member and leaks into this gate. Node types stay scoped in server/webhook/tsconfig.json (`"types": ["node"]`).

Buy e2e (test/e2e/license-buy.e2e.ts) — currently asserts hash-unchanged + modal-still-mounted (vacuous: a broken onClick satisfies both). Run by scripts/e2e-spike.sh on the real WKWebView. NOTE: the dev-toggle → refreshEntitlements path is known-flaky on this WKWebView (deferred-items.md / [[license-walkthrough-state-pollutes-e2e]]) — do NOT be blocked by pre-existing unrelated e2e failures; the new openUrl assertion is what matters.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finding 6 re-check (no-op verify) + Finding 9 placeholder-absent release assertion</name>
  <files>server/webhook/src/keygen.ts (READ-ONLY verify), scripts/check-dev-strip.sh</files>
  <action>
FINDING 6 (re-check, likely no-op — do NOT re-add if guarded):
Re-read server/webhook/src/keygen.ts `searchByOrderId`. Confirm it returns an EXACT `metadata.orderId === orderId` match rather than blind `data[0]`. As of 20-03 it does (lines ~66-69: `body.data?.find((lic) => lic.attributes.metadata?.orderId === orderId)`). If the guard is present: change NOTHING in keygen.ts; record in the commit/summary that finding 6 is a verified no-op (closed by the 20-03 / Critical-1/4 idempotency rewrite). ONLY if the guard is genuinely missing: add the exact-match `.find(...)` filter and a matching unit test in keygen.test.ts.

FINDING 9 (scripts/check-dev-strip.sh — placeholder-absent release assertion):
In `check_prod_constants()`, after the existing `license.tinkerdev.io`-PRESENT grep, add a placeholder-ABSENT assertion that FAILS the script (return 1, not WARN) if either sentinel is embedded in the release binary:
  - `PROD_ACCOUNT_ID_PLACEHOLDER`
  - `PROD_PUBKEY_PLACEHOLDER`
Use `grep -F` (fixed-string) for the literal sentinel strings (and keep `grep -qa` for the binary). If found → echo a clear FAIL message naming the sentinel and `return 1`. Leave the existing localhost-ABSENT check WARN-only behavior unchanged (deps embed "localhost" → noisy; do not make it noisier). Keep the host-PRESENT grep as the load-bearing positive check.
Confirm the real config.rs constant names match the sentinel literals (STATE notes D-51 `PROD_*_PLACEHOLDER`); grep src-tauri/src/license/config.rs for the exact placeholder identifiers and use whatever literal sentinel value would appear in a binary string-dump, not the Rust const name.
  </action>
  <verify>
<automated>bash -n scripts/check-dev-strip.sh && grep -F "PROD_ACCOUNT_ID_PLACEHOLDER" scripts/check-dev-strip.sh && grep -F "PROD_PUBKEY_PLACEHOLDER" scripts/check-dev-strip.sh</automated>
  </verify>
  <done>Finding 6 recorded as verified no-op (keygen.ts unchanged) OR guard added with test. check-dev-strip.sh fails (return 1) when either PROD_*_PLACEHOLDER sentinel is present in the release binary, using grep -F; localhost WARN behavior unchanged; bash -n parses clean.</done>
</task>

<task type="auto">
  <name>Task 2: Finding 8 — root tsconfig types:[] allowlist (stop @types/node leaking into the webview gate)</name>
  <files>tsconfig.json, and any src/*.ts files that surface real type errors once Node globals are removed</files>
  <action>
Add `"types": []` to the ROOT tsconfig.json compilerOptions. This stops the hoisted @types/node (from the server/webhook workspace member) from leaking Node globals into the webview app's `tsc --noEmit` gate — so e.g. setTimeout is typed as browser `number`, not `NodeJS.Timeout`, and Buffer/process disappear from `src/`. Keep Node types scoped to server/webhook/tsconfig.json (`"types": ["node"]` stays — do NOT touch it).

This is the trickiest item — budget for fallout. After adding `"types": []`, run `pnpm tsc --noEmit`. Adding it will likely surface previously-MASKED real type errors (most likely around setTimeout return types and any incidental Buffer/process reference). Fix each PROPERLY for the browser:
  - `setTimeout`/`setInterval` return `number` in DOM lib — change any `NodeJS.Timeout`/`ReturnType<typeof setTimeout>` annotations that now mistype to plain `number` where the value is a browser timer id. Existing call sites already use `window.setTimeout`/`ReturnType<typeof setTimeout>` (App.tsx, RegexTool.tsx, useCopyFeedback.ts, Sidebar.tsx, SidebarResetMenu.tsx) — `ReturnType<typeof setTimeout>` resolves to `number` once Node types are gone, so it should stay correct; verify, don't blanket-rewrite.
  - Any genuine Node-only reference in `src/` is a latent bug — fix it to a browser API or remove it.
Do NOT add `@types/node` back to the root, do NOT use `// @ts-expect-error` to paper over real errors, do NOT touch decoder.ts. Verify the existing release pure-module comment in src/lib/release/publishPlan.ts (it explicitly avoids the NodeJS namespace) still typechecks.
  </action>
  <verify>
<automated>pnpm tsc --noEmit && pnpm tsc --noEmit -p server/webhook/tsconfig.json && pnpm vitest run</automated>
  </verify>
  <done>Root tsconfig.json has `"types": []`; server/webhook keeps `"types": ["node"]`. `pnpm tsc --noEmit` (webview gate) AND the server tsc gate both pass with any newly-exposed real type errors fixed properly (no @ts-expect-error, no @types/node re-added). Full vitest suite green; decoder + its 19 tests untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Finding 7 + Finding 10 — narrow opener scope to the buy host (+ mailto seam) and de-vacuum the Buy e2e</name>
  <files>src-tauri/capabilities/default.json, test/e2e/license-buy.e2e.ts</files>
  <action>
These two share the Buy/opener seam, so land them together.

FINDING 7 (src-tauri/capabilities/default.json — narrow the open-redirect surface, threat T-20-01):
Change the `opener:allow-open-url` allow list from `{ "url": "https://*" }` to the EXACT buy host:
  `{ "url": "https://tinkerdev.io/*" }`
This still permits `BUY_LICENSE_URL = "https://tinkerdev.io/buy"` (UpsellPanel.tsx:319). Add a brief JSON-adjacent comment seam (or, if strict JSON, a short note in the capability "description") documenting that a narrow `mailto:feedback@tinkerdev.io*` entry will be added later by the pending Send-feedback affordance todo — scope deliberately and narrowly, leaving that seam, and NEVER widen back to `*` or `https://*`. Do NOT add the mailto entry now (that is the separate pending todo); just leave the documented seam so the future change is obviously additive.

FINDING 10 (test/e2e/license-buy.e2e.ts — make the Buy assertion non-vacuous):
Currently the spec only asserts hash-unchanged + modal-still-mounted, which a silently-broken onClick also satisfies. Add a window-level stub of the opener so the OBSERVABLE contract is "openUrl was called with the exact URL":
  - Before clicking Buy, install a page-level spy that records calls to the opener. Since the in-Tauri runtime routes openUrl through the native plugin (non-observable in WebDriver), stub at the seam the page actually uses: inject a recorder on `window` (e.g. set `window.__openUrlCalls = []`) and monkeypatch the opener path the app invokes — prefer overriding `platform.opener.openUrl` if the platform singleton is reachable from the page (mirror the unit-test pattern in UpsellPanel.test.tsx which does `setPlatformForTest({ ...makeMemoryPlatform(), opener: { openUrl } })`). If the platform singleton is NOT reachable from a `browser.execute` context, fall back to capturing the Tauri IPC for `plugin:opener|open_url` by wrapping `window.__TAURI_INTERNALS__.invoke` (record the url arg) — whichever the runtime exposes.
  - After `clickBuy()`, assert the recorder captured exactly one call whose url === `https://tinkerdev.io/buy` (the exact buy URL). Keep the existing hash-unchanged + modal-mounted assertions as corroboration, but the openUrl-exact-url assertion is now the load-bearing one.
  - Keep the file-header note that the native browser-open stays a MANUAL walkthrough item.
  - Restore any patched globals in the `finally` cleanup (alongside the existing toggle-back-to-FULL cleanup) so later specs in the WDIO run are not polluted.
Do NOT be blocked by the pre-existing known-flaky dev-toggle/entitlements e2e failures (deferred-items.md) — if the dev-toggle retry path fails for the known-flaky reason, that is out of scope; the new openUrl-exact-url assertion is the deliverable.
  </action>
  <verify>
<automated>node -e "JSON.parse(require('fs').readFileSync('src-tauri/capabilities/default.json','utf8'))" && grep -F "https://tinkerdev.io/*" src-tauri/capabilities/default.json && grep -F "tinkerdev.io/buy" test/e2e/license-buy.e2e.ts</automated>
  </verify>
  <done>default.json opener allow is `{ "url": "https://tinkerdev.io/*" }` (valid JSON, never `*`/`https://*`), with a documented mailto seam for the future feedback todo. license-buy.e2e.ts stubs/records the opener and asserts openUrl was called once with exactly `https://tinkerdev.io/buy`; patched globals restored in finally. Real-WKWebView e2e run shows the new assertion exercised (pre-existing known-flaky toggle failures do not count against this).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Five Medium findings hardened: (6) verified no-op or guarded, (7) opener scope narrowed to https://tinkerdev.io/* with a mailto seam, (8) root tsconfig `types:[]` so the webview tsc gate no longer masks browser type errors, (9) check-dev-strip.sh now fails on PROD_*_PLACEHOLDER sentinels, (10) Buy e2e now asserts the exact openUrl call.
  </what-built>
  <how-to-verify>
1. Run `tauri dev` (real WKWebView). Open the upsell modal (toggle free tier via ⌘K dev command → "Unlock Pro" footer), click "Buy license", and confirm the OS default browser opens https://tinkerdev.io/buy (this is the native-open manual item WebDriver cannot observe). Confirm the in-app route did NOT change.
2. Confirm the Buy CTA still works under the narrowed `https://tinkerdev.io/*` opener scope (it should — the URL is in scope). If the native open is silently blocked, the scope is wrong.
3. Confirm `pnpm tsc --noEmit` is green (finding 8 did not break the webview typecheck).
4. Per the binding harness, this is a hardening pass on shipped code — confirm the per-task DoD (simplify → codex review → vitest/tsc/eslint → real-webview verify) was honored and the decoder's 19 tests are untouched.
  </how-to-verify>
  <resume-signal>Type "approved" once the Buy CTA opens the browser to https://tinkerdev.io/buy under the narrowed scope and the gates are green, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → OS browser (opener) | The Buy CTA hands a URL to the OS default browser; an over-broad scope lets any in-app code open arbitrary https hosts (open-redirect surface) |
| release binary → verification gate | check-dev-strip.sh attests a release binary embeds real prod constants, not placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering/Elevation | opener capability (default.json) | mitigate | Narrow `opener:allow-open-url` from `https://*` to exact `https://tinkerdev.io/*`; never widen to `*` (Task 3) |
| T-20-13 | Information disclosure | webview tsc gate (tsconfig.json) | mitigate | `types:[]` stops Node globals masking browser-only type errors that could ship latent runtime bugs in money-handling UI (Task 2) |
| T-20-14 | Spoofing/Repudiation | release verification (check-dev-strip.sh) | mitigate | Placeholder-absent assertion fails the gate if PROD_*_PLACEHOLDER sentinels remain — a placeholder binary can no longer pass as prod (Task 1) |
| T-20-15 | Tampering | Buy e2e (license-buy.e2e.ts) | mitigate | Assert exact openUrl(url) so a silently-broken Buy onClick can no longer pass vacuously (Task 3) |
| T-20-16 | Tampering | webhook idempotency (keygen.ts) | accept (verified) | Exact metadata.orderId match already guards against blind data[0]; finding 6 re-verified, no change needed (Task 1) |
</threat_model>

<verification>
- `pnpm vitest run` green (full suite; decoder 19 tests untouched).
- `pnpm tsc --noEmit` green AND `pnpm tsc --noEmit -p server/webhook/tsconfig.json` green.
- `pnpm eslint` 0 errors.
- `default.json` is valid JSON with opener allow `{ "url": "https://tinkerdev.io/*" }`.
- `bash -n scripts/check-dev-strip.sh` clean; the placeholder-absent assertion uses `grep -F` and returns 1 on a sentinel hit.
- Real-WKWebView e2e run exercises the new exact-openUrl assertion in license-buy.e2e.ts.
- Human walkthrough: Buy CTA opens https://tinkerdev.io/buy in the OS browser under the narrowed scope.
</verification>

<success_criteria>
- Findings 7, 8, 9, 10 closed with the specified mitigations; finding 6 recorded as a verified no-op (or guarded if it was missing).
- Opener scope is the exact buy host, never `*`/`https://*`, with a documented mailto seam for the pending feedback todo.
- Webview tsc gate no longer masks Node globals; any exposed real type errors fixed properly.
- check-dev-strip.sh fails a placeholder release binary.
- Buy e2e is non-vacuous (exact openUrl assertion).
- Per-task binding DoD honored; decoder + its 19 tests byte-for-byte untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260614-l39-phase-20-webhook-code-review-medium-find/260614-l39-SUMMARY.md` recording: finding 6 disposition (no-op vs guarded), the type errors finding 8 surfaced and how they were fixed, and the e2e stub approach used for finding 10.
</output>
