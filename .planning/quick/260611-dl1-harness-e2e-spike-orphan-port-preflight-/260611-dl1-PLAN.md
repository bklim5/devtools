---
phase: quick-260611-dl1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/e2e-spike.sh
  - lefthook.yml
  - .gitignore
  - README.md
  - docs/harness-and-decisions.md
  - docs/handoff-instructions.md
  - docs/archive/phase-0-notes.md
  - docs/archive/phase-1-ui-review.md
  - docs/archive/superpowers-specs/2026-06-02-json-xml-formatters-design.md
autonomous: true
requirements: [QUICK-260611-DL1]
must_haves:
  truths:
    - "Running scripts/e2e-spike.sh with an orphan devtools-app or a held :4445/:1420 port kills the orphans (with [spike] log lines) before launching a fresh tauri dev — WDIO can never run against a stale app"
    - "git commit is blocked when `pnpm lint` fails (eslint joins tsc + vitest in the lefthook unit gate)"
    - ".gitignore lists test/e2e/__screenshots__/ and test/e2e/__logs__/ exactly once; stale __logs__ debug files are gone"
    - "phase-0-notes.md, phase-1-ui-review.md, and superpowers/specs are under docs/archive/ and no live doc points at the old paths"
  artifacts:
    - path: "scripts/e2e-spike.sh"
      provides: "preflight() that checks :4445 + :1420 via lsof and pgrep -f devtools-app, kills orphans, runs BEFORE pnpm tauri:dev:e2e launch; PREFLIGHT_ONLY=1 dry-run mode"
      contains: "preflight"
    - path: "lefthook.yml"
      provides: "lint command running pnpm lint in the parallel pre-commit block; D-08 comment updated to name eslint"
      contains: "pnpm lint"
    - path: "docs/archive/phase-0-notes.md"
      provides: "archived stale doc (moved, not deleted)"
  key_links:
    - from: "scripts/e2e-spike.sh preflight"
      to: "pnpm tauri:dev:e2e launch"
      via: "preflight executes before the setsid launch line"
      pattern: "preflight"
    - from: "README.md / docs/harness-and-decisions.md"
      to: "docs/archive/phase-0-notes.md"
      via: "updated path references"
      pattern: "docs/archive/phase-0-notes"
---

<objective>
Batch 1 of 4 peer-review harness + hygiene fixes: (1) close the silent stale-test hole in scripts/e2e-spike.sh — an orphan devtools-app holding :4445 makes `nc -z` succeed instantly so WDIO tests pass against OLD code; (2) add eslint to the lefthook pre-commit unit gate; (3) repo hygiene — dedupe .gitignore, delete stale gitignored debug logs, archive three stale docs.

Purpose: harness trustworthiness (a green e2e run must mean the CURRENT code passed) + commit-gate completeness (STATE.md already describes the unit gate as "vitest + tsc + eslint" but lefthook only runs two of three).
Output: hardened e2e-spike.sh with preflight, lefthook.yml with lint command, clean .gitignore, docs/archive/ with three relocated artifacts and updated references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@scripts/e2e-spike.sh
@lefthook.yml
@.gitignore
@package.json

<verified_facts>
<!-- Confirmed during planning — executor should NOT re-verify these. -->
- `pnpm lint` exists ("lint": "eslint .") and completes in ~2.8s wall — well under the 30s flag threshold. No concern adding it to pre-commit.
- Dev binary name (src-tauri/Cargo.toml) is `devtools-app`; the production app is `TinkerDev`. `pgrep -f devtools-app` therefore matches only dev/e2e orphans, never the user's installed app.
- .gitignore duplicate block: lines 23-26 (first occurrence, keep) and lines 33-35 (second occurrence under "# e2e UI-gate artifacts (HRN-02 spike)" comment, remove comment + both lines).
- test/e2e/__logs__/ currently holds 9 stale gitignored files (codex-review.txt, cron-spike-run.log, sidebar-demo-dev.log, spike-hover-regression.log, spike-run.log, spike-success.log, tauri-dev.log, url-e2e-run.log, url-e2e-run2.log) — all safe to delete; regenerated by the script.
- Live references to docs being archived (must update): README.md:34 (`docs/phase-0-notes.md`), docs/harness-and-decisions.md:113 (`docs/phase-0-notes.md`), docs/handoff-instructions.md:107 and :143 (`docs/phase-0-notes.md`). NO live references exist to phase-1-ui-review.md or docs/superpowers/specs/ outside `.planning/milestones/` archives — leave .planning history byte-for-byte untouched.
- docs/superpowers/specs/ contains exactly one file: 2026-06-02-json-xml-formatters-design.md. Check whether docs/superpowers/ is empty after the move; if so remove the empty dir.
- Memory note: this shell has no `shopt`; use explicit `git mv` per file, verify targets exist before any `rm -rf`.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add orphan/port preflight to scripts/e2e-spike.sh</name>
  <files>scripts/e2e-spike.sh</files>
  <action>
Insert a `preflight()` function and call it BEFORE the `pnpm tauri:dev:e2e` launch (i.e., before the current "[spike] starting..." echo at line ~52). Match the script's existing style: `[spike]` log prefix, bounded waits, fail-loud exits.

Preflight logic (kill + log, per the dev-harness preference — do NOT just abort):
1. Orphan app check: `pgrep -f devtools-app` — if matches, log each PID (`[spike] preflight: killing orphan devtools-app (pid N)…`), `kill` them, then short bounded re-poll (`pgrep` again, escalate to `kill -9` after ~5s if still alive). Rationale comment in script: Tauri single-instance plugin means an orphan app blocks proper relaunch even when ports look free; worse, an orphan holding :4445 makes the nc -z poll succeed instantly so WDIO runs against the STALE app.
2. Port checks for $PORT (4445 default) and 1420 (vite): `lsof -ti tcp:$port` (note: `-ti` returns bare PIDs, one per line). For each PID still alive after step 1, log and kill it the same way.
3. After kills, poll bounded (~10s) until both ports are free; if a port is STILL held (e.g., a non-killable foreign process), exit 1 with a clear `[spike] ERROR:` message naming the port and holder PID — never proceed to launch.
4. If nothing was found, log a single `[spike] preflight: no orphans, ports ${PORT}/1420 free.` line.

Also add a dry-run escape hatch for verification: if `PREFLIGHT_ONLY=1`, run preflight then `exit 0` before launching tauri (one `if` after the preflight call; document it in the header Usage/Env comment block alongside TAURI_WEBDRIVER_PORT/MAX_WAIT).

Keep the existing trap/cleanup, nc -z poll loop, and WDIO invocation untouched. The vite port 1420 is fixed by tauri config — hardcode with a comment, or define `VITE_PORT=1420` beside PORT.

Do NOT use broad `pkill -f` patterns that could match this script's own path arguments; `pgrep -f devtools-app` is safe (verified: production app binary is TinkerDev, not devtools-app), but exclude $$ defensively if the match list could include self.
  </action>
  <verify>
    <automated>bash -n scripts/e2e-spike.sh && PREFLIGHT_ONLY=1 bash scripts/e2e-spike.sh</automated>
  </verify>
  <done>
bash -n clean. PREFLIGHT_ONLY=1 run exits 0 with `[spike] preflight:` log lines and never launches tauri. Bonus check if cheap: `nc -l 4445 &` then PREFLIGHT_ONLY=1 run shows it killing/clearing the listener (or clearly erroring on an unkillable holder), then clean up the nc. Full e2e run explicitly deferred to a later batch per constraints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add eslint to lefthook pre-commit unit gate</name>
  <files>lefthook.yml</files>
  <action>
Add a `lint` command to the existing parallel `pre-commit` block:

```yaml
    lint:
      run: pnpm lint
```

Timing verified during planning: ~2.8s — no per-commit cost concern, no flag needed.

Update the D-08 scope comment: it currently enumerates the unit gate as types + tests; eslint is part of the "mechanical half" (STATE.md's harness reminder already says "vitest + tsc + eslint green"). Adjust the wording so the comment matches reality — e.g., the gate header line mentions "broken types, lint errors, or failing tests". Do not touch the D-08 "do NOT wire codex:review / UI verification into lefthook" portion.

No `lefthook install` re-run needed for command additions (lefthook reads lefthook.yml at hook runtime), but running `pnpm lefthook install` once is harmless and confirms config parses.
  </action>
  <verify>
    <automated>pnpm lefthook validate && pnpm lint</automated>
  </verify>
  <done>
lefthook.yml has typecheck + test + lint in the parallel pre-commit block; `pnpm lefthook validate` (or `lefthook dump` if validate unavailable in 2.1.9 — fallback: `pnpm lefthook run pre-commit`) passes; comment text names lint as in-scope for the unit gate. The plan's own commit at the end exercises the hook live — all three commands must pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Hygiene — .gitignore dedupe, stale logs, doc archive</name>
  <files>.gitignore, test/e2e/__logs__/, README.md, docs/harness-and-decisions.md, docs/handoff-instructions.md, docs/archive/</files>
  <action>
(a) .gitignore: delete the duplicate block at lines 33-35 — the `# e2e UI-gate artifacts (HRN-02 spike) — generated, not source` comment plus the second `test/e2e/__screenshots__/` and `test/e2e/__logs__/` lines. Keep the first occurrence (lines 23-26) verbatim.

(b) Stale logs: delete the 9 gitignored files inside test/e2e/__logs__/ (listed in verified_facts). Plain `rm` of the listed files (or `rm -f test/e2e/__logs__/*` after confirming the dir contains only those); keep the directory itself (e2e-spike.sh mkdir -p's it anyway). These are untracked — no git impact.

(c) Doc archive (move, never delete):
1. `mkdir -p docs/archive`
2. `git mv docs/phase-0-notes.md docs/archive/phase-0-notes.md`
3. `git mv docs/phase-1-ui-review.md docs/archive/phase-1-ui-review.md`
4. `git mv docs/superpowers/specs/2026-06-02-json-xml-formatters-design.md docs/archive/superpowers-specs/2026-06-02-json-xml-formatters-design.md` (mkdir -p docs/archive/superpowers-specs first). If docs/superpowers/ is then empty, remove it (git won't track empty dirs anyway).
5. Update live references to the moved phase-0-notes.md path: README.md:34, docs/harness-and-decisions.md:113, docs/handoff-instructions.md:107 and :143 — rewrite `docs/phase-0-notes.md` → `docs/archive/phase-0-notes.md` (in handoff-instructions.md line 143's tree diagram, adjust the path text; minimal edit, don't redraw the tree).
6. Do NOT touch any reference under .planning/ — those are archived milestone history.
  </action>
  <verify>
    <automated>test $(grep -c "test/e2e/__logs__/" .gitignore) -eq 1 && test -f docs/archive/phase-0-notes.md && test -f docs/archive/phase-1-ui-review.md && test ! -f docs/phase-0-notes.md && ! grep -rn "docs/phase-0-notes" README.md docs/*.md && ls test/e2e/__logs__/ | wc -l | grep -q "^ *0$"</automated>
  </verify>
  <done>
.gitignore has each e2e artifact path exactly once; __logs__ empty; three docs live under docs/archive/ via git mv (history preserved); zero live references to old paths (grep over README.md + docs/ clean); .planning/ untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e-spike.sh → local processes | preflight kills processes by name/port match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q260611-01 | D (DoS — killing wrong process) | preflight pgrep/lsof kills | mitigate | match only `devtools-app` (dev binary; production app is `TinkerDev`) and bare PIDs from `lsof -ti tcp:4445/1420`; plain `kill` first, `-9` only after bounded wait; log every PID killed |
| T-q260611-02 | E (port hijack pre-bind) | :4445 WebDriver port | accept | existing posture — localhost-only (T-01-11), dev-machine harness, webdriver feature excluded from builds |
</threat_model>

<verification>
1. `bash -n scripts/e2e-spike.sh` clean; `PREFLIGHT_ONLY=1 bash scripts/e2e-spike.sh` exits 0 with preflight logging and no tauri launch.
2. `pnpm lint`, `pnpm tsc --noEmit`, `pnpm vitest run` all green (the commit itself proves the new three-command hook).
3. .gitignore dedupe + archive moves verified by the Task 3 automated check; `git status` shows renames (R) for the three docs, not delete+add of untracked copies.
4. No UI changes in this batch — real-webview UI gate explicitly N/A per constraints; full e2e run deferred to a later batch.
</verification>

<success_criteria>
- e2e-spike.sh can no longer silently run WDIO against a stale orphan app: preflight kills orphan devtools-app processes and clears :4445/:1420 (or fails loud) before every launch.
- A commit with an eslint error is rejected by lefthook.
- .gitignore duplicate block gone; test/e2e/__logs__/ emptied; phase-0-notes.md, phase-1-ui-review.md, superpowers spec archived under docs/archive/ with all live references updated and .planning history untouched.
- Per-task DoD honored: /simplify → /codex:review → unit gate green. UI gate N/A (no UI surface touched).
</success_criteria>

<output>
After completion, create `.planning/quick/260611-dl1-harness-e2e-spike-orphan-port-preflight-/260611-dl1-SUMMARY.md`
</output>
