---
gsd_state_version: 1.0
milestone: v1.6
milestone_name: Licensing
status: executing
last_updated: "2026-06-19T00:15:00.000Z"
last_activity: 2026-06-19 -- Phase 24 plan 03 complete
progress:
  total_phases: 16
  completed_phases: 7
  total_plans: 33
  completed_plans: 32
  percent: 97
---

# Project State

## Current Position

Milestone: **v1.6 "Licensing"** — started 2026-06-09, roadmap created 2026-06-09.
Phase: 24 (hotkeys-general-panes) — EXECUTING
Plan: 4 of 4 (Plans 01–03 COMPLETE; 01–02 2026-06-18, 03 2026-06-19)
Status: Executing Phase 24
Progress: [■■□□] · Phase 22 + 22.1 complete · **Phase 22.2 COMPLETE (2026-06-16)** — user-approved mid-Phase-23 scope change: the **⌘K command palette is now Pro-gated** (a free user's ⌘K + the header pill open a focused Unlock-Pro modal; a Pro user gets the palette unchanged) and the **contextual locked customization triggers** (pin/drag/Alt+P/Reset) open that SAME focused modal instead of the 22.1 redirect-to-Settings. Gate via `isPro` (any Pro entitlement — frontend-only, no Keygen re-issue). Restored `UpsellModal` + `upsellStore`/`useUpsell` wrapping the SAME shared `ActivationSurface` (one activation surface, two presentations — partially un-reverts D-22.1-5). `openProUpsell` routes lapsed/attention paying customers (refreshNeeded/problem) to the Settings recovery form, NEVER the pitch (D-44). DEV-only ⌘⇧K force-open escape (tree-shaken from release, confirmed absent from `dist/`). Explicit license entry points (sidebar Settings row + Unlock-Pro footer + app-menu/tray + deep link) stay free → Settings ▸ License so a free user can still buy. **SET-04 revised** (⌘K no longer a free-tier path). Gates: vitest **986/986**, tsc+eslint clean, real-WKWebView e2e **22/22 spec files** (incl. new `cmdk-pro.e2e`), fresh `tauri build` (TinkerDev.app + DMG). decoder + 19 tests untouched. Deferred (non-blocking): `useFocusTrap` extraction (UpsellModal + SettingsModal share the Tab-trap), tinkerdev.io Pro-card copy sync.
Last activity: 2026-06-19 -- Phase 24 plan 03 complete

**Phase 24 plan 03 decisions (2026-06-19, Hotkeys Settings pane — SET-08 PARTIAL, D-24-2/6, T-24-07):** the user-facing half of SET-08 landed (Wave 3, depends on 24-01/02). **(1) `src/components/HotkeyCaptureField.tsx`** — ONE reusable, binding-agnostic capture field (button semantics: click + Enter/Space record, NO mouse-only path; physical `e.code` only; `e.preventDefault()` while recording; Escape cancels with no commit; classifies invalid/reserved/same-as-other inline and only `onCommit`s a clean chord). **(2) `src/components/HotkeysSettings.tsx`** — the pane: two binding rows (Global summon via `rebindSummon` → on OS reject persist NOTHING, keep prior chord, D-24-2; Command palette pure-webview `setPaletteChord`, D-24-6), current chord in mono, per-hotkey Reset, a SINGLE polite `aria-live` region carrying every rebind/reset/reject result. **(3) `settingsPanes.tsx`** — Hotkeys entry appended after Appearance / before License; **`SettingsModal.tsx` byte-unchanged** (registry is the single control plane). **(4) `test/e2e/hotkeys.e2e.ts`** — pane render, palette-on-configured-chord, rebind-reflected (new chord opens the palette, ⌘K no longer does), Escape-cancel on the real WKWebView. **Simplify (`4d0a82c3`):** added an `otherLabel` prop (removed a leaky `label === "Global summon"` ternary → binding-agnostic), single-sourced `PALETTE_CHORD` from `DEFAULT_PREFERENCES.paletteChord`, folded summon/palette handlers → `applySummon`/`applyPalette`, de-duped a `captureFieldText()` double-eval in the e2e. **Code-review fixes (`1809b591`, +HotkeysSettings.test.tsx 4 tests):** (a) **Rule-1 bug, T-24-07** — Reset bypassed the self-collision guard: a user could rebind binding A away, rebind B onto A's freed default, then Reset A onto that same chord → native summon + webview palette collide and the palette hotkey goes DEAD; `applySummon`/`applyPalette` now reject a target equal to the OTHER binding's current chord and Reset routes through them. (b) **Rule-2 WCAG-AA** — rejects weren't announced (the polite live region kept stale success text); every reject (collision + OS-taken) now feeds the live region. **e2e regression fix (`7c9612b2`, phase-scoped):** 24-02's `matchesChord` swap made ⌘K read the physical `e.code`, but the shared e2e helpers/specs dispatched ⌘K with no `code` → 7 specs cascade-failed (`ensureProTier` "could not establish Pro tier via the ⌘K dev toggle"); added `code:"KeyK"` to the 4 dispatch sites (helpers.ts runDevToggle ⌘⇧K, cmdk-pro pressMetaK, entitlements runLicenseCommand, settings openPalette). Caught by the no-regression run (`macos-option-key-composes-letters`). Gates: **vitest 1127/1127** (+14), tsc + eslint clean (the same 2 pre-existing SidebarResetMenu warnings, out of scope), real-WKWebView e2e **24/24 spec files** (hotkeys.e2e PASSED; the 7 regressed specs re-run clean); decoder + 19 tests byte-for-byte untouched; zero new deps. Full harness gate (`/simplify` → `/codex:review` + fixes → unit → real-WKWebView e2e + no-regression) run by the orchestrator before passback. **SET-08 stays PARTIAL** — the webview-testable half is DONE + verified; **Task 4 native human walkthrough** (default ⌘⇧D summons from another app; rebind to a free chord; rebind to a taken/reserved chord shows the calm inline + keeps the prior; rebind survives quit+relaunch; Reset restores ⌘⇧D; palette rebind ⌘K→new; keyboard-only reach) is NOT WebDriver-drivable — per the no-stale-build rule and because Plan 24-04 still lands webview source, the user APPROVED deferring it to the single phase-boundary human sign-off on the final fresh build (after 24-04). Commits: `66e9c864` (HotkeyCaptureField), `a6676b72` (HotkeysSettings + SETTINGS_PANES append), `85e2179c` (hotkeys.e2e), `4d0a82c3` (simplify), `1809b591` (code-review fixes), `7c9612b2` (e2e regression). Summary: `24-03-SUMMARY.md`.

**Phase 24 plan 02 decisions (2026-06-18, summon native seam + palette matcher — SET-08/SET-09 partial, D-24-2/5/6/8/9):** the behavioral half landed (Wave 2, depends on 24-01). **(1) `src/shell/summon.ts`** — `registerSummon(chord)` now takes the PERSISTED chord arg (was arg-less, read the bare `SUMMON_CHORD`); startup stays non-fatal (catch+log, never re-throw, D-24-5). `SUMMON_CHORD` exported unchanged as the reset default. NEW `rebindSummon(old,new)`: best-effort `unregister(old)` (an "old not registered" reject is swallowed) → `register(new)`; on a register REJECT it re-registers `old` (user keeps a working summon) and RE-THROWS so the pane surfaces the inline reject + persists nothing (D-24-2, T-05-07). **(2) Native auto-reveal NEUTRALIZED — `src-tauri/src/lib.rs`:** the window-state plugin is now `Builder::default().with_state_flags(StateFlags::all() ^ StateFlags::VISIBLE).build()` (+`use tauri_plugin_window_state::StateFlags;`). `StateFlags::default()==all()` includes VISIBLE, and the plugin's `restore_state()` native-`show()`s + focuses the window on window-ready BEFORE the webview prefs load (verified vs crate 2.4.1 L262: `if flags.contains(VISIBLE) && should_show { show; set_focus }`) — the REAL normal-launch reveal. Dropping ONLY VISIBLE keeps SHL-05/D-11 geometry restore but removes the auto-show (no pre-webview flash even with start-in-tray ON, D-24-8/9, T-24-08). single-instance (L19-25) + tray (L240-275) reveals UNTOUCHED (user-triggered). `cargo check` green. **(3) `src/shell/startupReveal.ts`** — `revealOnStartup(prefs)` shows the window only when `!prefs.startInTray`; start-in-tray = never shown (no show-then-conceal pair, Pitfall 1). With the native auto-show gone this is the SOLE normal-launch reveal. **(4) `src/main.tsx`** — replaced the parked G-05-1 "summon NOT auto-registered" comment with a `getPreferencesLoaded()`-gated one-shot latch (subscribePreferences + immediate call) that, once prefs resolve, `registerSummon(prefs.summonChord)` + `revealOnStartup(prefs)` — the PERSISTED chord registers, never a default-then-real double-register (Pitfall 4, T-24-06). Promotes NAT-01/G-05-1. **(5) `src/components/CommandPalette.tsx`** — reads `preferences.paletteChord` via `usePreferences()`; the hardcoded `(meta||ctrl) && key==="k"` swapped for `matchesChord(e, paletteChord)` (D-24-6, physical e.code, T-24-04); `paletteChord` added to the keydown effect deps. Pro-gating (`openProUpsell`) + `Escape` branch unchanged. **Deviations (Rule 3 blocking):** (a) the DEV ⌘⇧K force-open escape was re-expressed as the shift-augmented palette chord (`matchesChord` is exact-modifier-set, so the base chord no longer enters the branch when Shift is held; the OLD `devForce = DEV && e.shiftKey` form would never fire) — same DEV-only semantics, tree-shaken from release; (b) the 4 CommandPalette test ⌘K dispatches needed `code:"KeyK"` added (matchesChord reads physical e.code; the fixtures predated the requirement — macos-option-key-composes-letters). **Comment-wording tweaks (Phase-23 precedent):** `startupReveal.ts` "show()-then-hide()" → "show-then-conceal pair" (grep `hide(`=0); `CommandPalette.tsx` retains the literal `import.meta.env.DEV && e.shiftKey` token in a documenting comment (DEV-escape-preserved grep). No behavior change. Gates: vitest **1113/1113** (+5), tsc + eslint clean (the same 2 pre-existing SidebarResetMenu warnings, out of scope), `cargo check` green; decoder + 19 tests byte-for-byte untouched; zero new deps. **SET-08/SET-09 stay PARTIAL** — this is the wiring; the Hotkeys + General panes (calling rebindSummon / reading the prefs) + the real-WKWebView e2e land in Plans 03/04; start-in-tray no-flash + native register-reject are human-walkthrough at the phase boundary (Plan 04 Task 4 — WebDriver can't synth the native pre-paint reveal or an OS chord collision). `/simplify` + `/codex:review` not auto-invoked — recommend `/codex:review --scope working-tree` at the phase checkpoint. Commits: `a99958b0` (registerSummon+rebindSummon), `4c2f80ae` (native auto-reveal neutralized + startup wiring), `089f0e71` (configurable palette matcher). Summary: `24-02-SUMMARY.md`.

**Phase 24 plan 01 decisions (2026-06-18, hotkeys/general/autostart FOUNDATION — SET-08/SET-09 partial, D-24-3/7/12):** the unit-testable foundation every later Phase-24 plan consumes landed (Wave 1, no deps). **(1) `src/shell/hotkeyAccelerator.ts`** — a PURE module (no native/platform import, mirrors theme.ts): `keyEventToAccelerator` builds a canonical accelerator (`CommandOrControl`/`Alt`/`Shift` + main key) from boolean modifier flags + the PHYSICAL `e.code` ONLY (macOS Option+letter composes to a glyph — `code:"KeyP"` yields `Alt+P`, never `π`; project memory `macos-option-key-composes-letters`), REQUIRES a non-shift modifier (D-24-3 → bare/shift-only/modifier-only return null); `matchesChord` exact modifier-set + e.code main-key match (`CommandOrControl` = meta OR ctrl); `isValidAccelerator` (reused by the prefs coercers); `isReservedChord` (RESEARCH §3 macOS+Edit-menu blocklist; the app's OWN defaults ⌘K/⌘⇧D are deliberately NOT reserved). 24 tests incl. the Option+P glyph case + the `matchesChord(e, keyEventToAccelerator(e))` round-trip property. **(2) Six additive Preferences fields** (after `licenseDropNoticeAck`): `summonChord`/`paletteChord` (chord coercers reuse `isValidAccelerator` → invalid coerces to the shipped default `CommandOrControl+Shift+D` / `CommandOrControl+K`, D-24-12, T-24-01), `launchAtLogin`/`startInTray` (boolean-only → false), `defaultToolId` (validated via `getToolById` against ENABLED_TOOLS, T-24-02; unknown → null = "Last used"), `showLicenseInSidebar` (default-VISIBLE — only explicit false hides). **`resolveStartupTool` gained a `defaultToolId` rung** (new signature `(target, defaultToolId, lastUsedId)`; precedence explicit > default-tool > last-used > hero; null/absent defaultToolId is backward-compatible = today's last-used); the SOLE call site `StartupRedirect.tsx` updated. Six single-writer setters added to `usePreferences` (each `update({...})` only — `savePreferences` count UNCHANGED at 3, no new direct writer; routes through the Phase-23 shared singleton). **(3) `platform.autostart` capability** (the scoped v1.7 dep, D-24-7) wired end-to-end: `{enable,disable,isEnabled}` on the `Platform` interface + proxy getter, real arm in `tauri.ts` (SOLE `@tauri-apps/plugin-autostart` importer), no-op arms in `browser.ts`/`testStore.ts` (`isEnabled→false`); `noopAutostart` added to `makeMemoryPlatform` + the 4 inline `platform.test.ts` Platform literals widened; Rust `tauri_plugin_autostart::init(MacosLauncher::LaunchAgent, None::<Vec<&str>>)` in the builder chain (no launch-arg shell-injection surface, T-24-03) + 3 scoped `autostart:allow-{enable,disable,is-enabled}` perms. Deps pinned EXACT: npm `@tauri-apps/plugin-autostart@2.5.1` (added at workspace root via `-w`, alongside the other `@tauri-apps/plugin-*`; backend `server/webhook` manifest untouched) + crate `tauri-plugin-autostart 2.5.1` in the existing target-scoped block. **Comment-wording tweaks (same Phase-23 precedent) so the literal acceptance greps pass:** `hotkeyAccelerator.ts` comments "`e.key`" → "the composed character" (grep `e.key`=0; the LOGIC never branched on it); `index.ts`/`tauri.ts` doc comments dropped the literal "@tauri-apps/plugin-autostart" → "native autostart plugin" (seam grep: tauri.ts=1, index.ts/browser.ts=0). No behavior change. Gates: vitest **1108/1108** (+57), tsc + eslint clean (the same 2 pre-existing SidebarResetMenu warnings, out of scope), `cargo build` GREEN (autostart compiles + registers, 11.13s); decoder + 19 tests byte-for-byte untouched. **SET-08/SET-09 stay PARTIAL** — this is foundation only (pure helpers + prefs + seam arms, no UI/native re-register); native wiring lands in Plan 02, the Hotkeys + General panes + real-WKWebView e2e land in Plans 03/04. `/simplify` + `/codex:review` not auto-invoked by the executor — recommend `/codex:review --scope working-tree` at the phase checkpoint. Commits: `2dd5d519` (chord helper module), `a966fad5` (6 prefs fields + coercers + default-tool seam), `587502e0` (platform.autostart end-to-end). Summary: `24-01-SUMMARY.md`.

**Phase 23 COMPLETE (2026-06-17, Appearance pane — SET-07 Validated, human-approved):** theme + accent are persisted and applied **live whole-app** with a **flash-free launch**, behind the Pro gate-on-Save. Shipped after 3 walkthrough rounds of feedback: **(1) "system" theme REMOVED** at user request — `ThemeName = "light" | "dark"`, the matchMedia OS live-flip effect deleted, copy/coercion/tests/e2e updated (a harmless dead `hint==="system"` branch remains in index.html — deferred, info-only). **(2) Theme cards redesigned** — `ThemeThumbnail` is now a mini app-window (sidebar column + a live-accent content bar + content rows) with a radio-check selection indicator (was a sidebar+dot, then a wrong full-width navbar). **(3) Unlock-Pro modal widened** 420→520px (`UpsellPanel.tsx` UpsellModal wrapper). **(4) DATA-LOSS BUG fixed — cross-writer prefs clobber:** the prefs blob had TWO independent writers — `usePreferences` (theme/accent/pins, on the Phase-23 shared singleton) AND `useRecentTools`, which held a STALE mount-era full-blob snapshot and rewrote it on every tool switch, clobbering a just-changed theme/pins. Fixed by unifying `useRecentTools` onto the SAME singleton (`usePreferences.ts` now exports `getSharedPreferences`/`updatePreferences`/`subscribePreferences`/`ensurePreferencesLoaded`; every write merges against live state, ONE writer). TDD regression test in `usePreferences.test.ts` reproduces "set theme + pin → tool switch → nothing reverts". See [[tauri-store-async-init-race]] (related store-seam pitfall family). **(5) Pro-launch dark-flash removed** — entitlements store gained a `resolved` flag (flips in a `finally`, even on resolve error); `useAppearance` gates its apply on `prefsLoaded && entsResolved` so a Pro relaunch opens straight to the saved theme. **(6) Durable writes** — tauri store `set()` now `autoSave:false` + explicit `save()` (the autoSave debounce dropped pending writes on quit). **(7) Appearance ordered BEFORE License** in `SETTINGS_PANES` (default-open pane still License via explicit `openSettings("license")`). Two direct `entitlementsOverride` disk writers (entitlements `clearEntitlementsOverride` + CommandPalette DEV toggle) still bypass the singleton — left as a deferred note (release-safe: override coerces to null / dev-toggle tree-shaken). Gates: vitest **1051/1051**, tsc + eslint clean (2 pre-existing SidebarResetMenu warnings), real-WKWebView e2e **23/23 spec files**, fresh `tauri build` (TinkerDev.app + DMG, 2026-06-17). decoder + 19 tests byte-for-byte untouched; zero new deps. Code review (`23-REVIEW.md`): 0 critical, WR-01/WR-02 fixed. **Deferred (non-blocking):** index.html dead `system`-hint branch; unify the 2 entitlements-override direct disk writers onto the singleton; a `gsd-ui-review` WCAG-AA audit pass (the verifier confirmed the contrast spec mechanizes AA in both themes).

**Phase 23 plan 04 decisions (2026-06-16, Appearance pane UI — SET-07 partial, D-23-2/3/6/7/10):** the Appearance pane shipped (Wave 2, depends on Plan 01; parallel with Plan 03 — no file overlap, Plan 03 owns App.tsx/index.html/useAppearance, this owns the pane + registry). Four new components + the SETTINGS_PANES append. **`ThemeCardGroup`** = 3 `role="radio"` cards (Dark/Light/System) in a `role="radiogroup"`, each with a tiny stylized app-preview thumbnail (two-pane box + accent dot; light card hardcodes the light surfaces it previews); roving tabindex + clamped Left/Up/Right/Down arrow-nav (no wrap, Sidebar convention); selected = `aria-checked` + `border-accent-line bg-accent-soft` + focus ring — **deliberately NOT a SegmentedControl (D-23-6)** (SegmentedControl is reused ONLY as the preview's inert sample toggle). **`AccentSwatchGrid`** = 7 round `role="radio"` swatches driven by `ACCENT_SCALE.length`, fill = `pair.dark`, `aria-label = pair.label`; **selection marked by ring + a centered white `<Check>` glyph — NOT color-alone (WCAG 1.4.1, D-23-7)**; arrow-nav clamped; `onChange(pair.dark)`. **`AppearancePreviewStrip`** = a CONTAINED bordered box (Decoder-style nav item · Activate accent button · `uint` chip mimicking FieldNode `chip-on` — accent on the SELECTED chip, neutral `#1` · inert sample `SegmentedControl`) that recolors to the pending theme+accent via inline `--color-accent`/`-soft`/`-line` **scoped to its own subtree** + an explicit `PREVIEW_SURFACE[resolved]` per-theme surface lookup — so it previews WITHOUT stamping `data-theme` on `:root` (the contained-preview invariant, D-23-3; no revert path because nothing global changes). **`AppearanceSettings`** = the pane: wrapper matches LicenseSettings (`flex flex-col gap-6 overflow-auto p-8`, no double-pad), `h3` header (one level under the dialog `h2` — preserves the Phase-22.1 heading order) + "Personalize how TinkerDev looks on this device."; Theme / Accent color (helper "Used for the active tool, selections, and focus.") / Preview sections; pending theme+accent state seeded from `preferences` — selecting updates ONLY pending + the preview, never the seam, never the DOM root. **Gate-on-Save (D-23-2):** `ents.has(ENT_THEMING)` Pro → `setTheme(pending)` + `setAccent(pending)` (App root applies live, Plan 03); free → `openProUpsell(saveRef.current)` and **persists NOTHING** (try-before-buy). The free Save button is a full-color `accent-soft` surface with a `<Lock>` glyph + "Unlock Pro to save" — **visible, not opacity-only, keyboard-reachable**. **`settingsPanes.tsx`** got ONE appended entry `{ id:"appearance", label:"Appearance", icon: Contrast }` after License — `SettingsModal.tsx` byte-unchanged (registry is the single control plane; no Notifications/Keyboard). Gates: **vitest 1046/1046** (+16), tsc + eslint clean (same 2 pre-existing SidebarResetMenu warnings, out of scope); decoder + 19 tests untouched; zero new runtime/dev deps. Minor comment-wording tweaks (same precedent as Plan 01) so the literal acceptance greps pass: "NOT a SegmentedControl" → "card radiogroup, never a segmented toggle"; the `document.documentElement` invariant comments → "the DOM root element" (grep === 0; no behavior change — neither component ever wrote the root). **SET-07 stays PARTIAL** — the live whole-app apply + persistence land in Plan 03; the real-WKWebView light/dark + Pro/free Save verification is **Plan 03's `appearance.e2e.ts` (which drives THIS pane)** + the phase-boundary `gsd-ui-review` (BOTH themes) + human walkthrough on a fresh `tauri build`. `/simplify` + `/codex:review` not auto-invoked — recommend `/codex:review --scope working-tree` at the phase checkpoint. Commits: `78889ce9` (ThemeCardGroup + AccentSwatchGrid), `f298a6af` (AppearanceSettings + AppearancePreviewStrip), `a4f88c8d` (SETTINGS_PANES append).

**Phase 23 plan 02 decisions (2026-06-16, light token block + Pitfall 5 fixes — SET-07 partial, D-23-8, Pitfall 5/6):** the FULL light token set landed as a `[data-theme="light"]` re-declaration block in `src/index.css` (CSS/className-only plan, ran parallel to Plan 01, no file overlap). The custom-property cascade flips the ENTIRE app — all 11 tools + the protobuf field tree + License/Settings panes + sidebar + ⌘K palette — with ZERO utility-class edits (utilities resolve `var(--color-*)` at use-site). Light values copied VERBATIM from Plan 01's `LIGHT_TOKENS` + `ACCENT_SCALE` light blue (`#1763d6`) — the same values `appearanceContrast.test.ts` enforces as the AA bar; no re-derivation. **Pitfall 6:** accent/warn/ok `-soft`/`-line` are re-declared EXPLICITLY under the light selector (not relying on @theme color-mix tracking the redeclared var); soft mix dropped 15%→**12%** + line 75%→70% for AA headroom on near-white (RESEARCH A4). **Pitfall 5 (hardcoded-dark survivors fixed):** borders flip `rgba(255,255,255,…)` → `rgba(0,0,0,0.08)`/`0.14` so hairlines survive on white; the base `body` `color` tokenized to `var(--color-tx)` (byte-identical dark render — dark `--color-tx` is `#e7e9ee`); a `:root[data-theme="light"] body` override swaps the dark radial-gradient for a light one (no dark gradient bleeds behind a light app). The three white-alpha hover/focus tints (invisible on near-white) → `hover:bg-input-bg` (dark `#0d0f13` / light `#f0f1f4`, visible in BOTH themes): `Sidebar` inactive nav row, `SettingsModal` inactive pane-nav button, `SidebarResetMenu` both menu items (hover+focus). `@theme` block values, `@import` lines, `.no-scrollbar` rules byte-unchanged; decoder + 19 tests untouched; zero new deps. Gates: vitest **1030/1030**, tsc + eslint clean (same 2 pre-existing SidebarResetMenu warnings, out of scope). Pure-presentational plan — real-WKWebView light-theme verification is Plan 04's `appearance.e2e` + the phase-boundary `gsd-ui-review` (BOTH themes); this plan's values pre-verified by Plan 01's contrast test. **SET-07 stays UNCHECKED** (the pane UI + live whole-app apply land in 23-03/04). `/simplify` + `/codex:review` not auto-invoked — recommend `/codex:review --scope working-tree` at the phase checkpoint. Commits: `c14c8e5c` (light token block + body gradient), `24d7c713` (theme-aware hover tints). **Carried to Plan 03:** App-root apply effect (`applyAppearance(gatePreferences(prefs, ents))`) + the no-flash pre-paint script that stamps `data-theme` BEFORE first paint so this light block applies without a dark flash.

**Phase 23 plan 01 decisions (2026-06-16, theming foundation — SET-07 partial, D-23-4/7/8/9, Pitfall 1):** the unit-testable theming foundation every later 23-plan builds on. `ThemeName` widened `"dark"` → `"light" | "dark" | "system"` (D-23-4; fresh-install default stays `dark`); `coerceTheme` rewritten untrusted-input-disciplined (`VALID_THEMES` ReadonlySet; unknown string / non-string / absent → `dark`, T-23-01) — tested THROUGH `mergePreferences` (module-private). **Pitfall 1 fixed:** `DEFAULT_PREFERENCES.accent` `#3b82f6` → `#5b9bf8` (the applied dark default-blue matching index.css `--color-accent`; the old value was dead/AA-failing and becomes live once the App root applies accent in Plan 03). New `src/shell/appearance.ts` = the single source of truth: `ACCENT_SCALE` (7 swatches, each a **dark+light hex pair** — a single hex CANNOT pass WCAG-AA in both themes, math-proven), `LIGHT_TOKENS` (full light surface/text/status set, names mirror @theme), `accentForTheme(darkHex, resolved)` (reverse-maps the persisted DARK hex → light variant; unknown hex returned UNCHANGED, fail-soft, T-23-02), `THEME_NAMES`, `DEFAULT_ACCENT`. **Persisted accent = the dark hex (RESEARCH A1)** so `DEFAULT_PREFERENCES.accent` stays a real color and `gatePreferences`' default is unchanged. New `src/shell/theme.ts` pure helpers (NO native import — only `matchMedia` + `document.documentElement`): `resolveEffectiveTheme` (`system` → matchMedia) + `applyAppearance` (**dark = ABSENCE of `data-theme`** — deletes the attr, never sets `data-theme=dark`; sets `--color-accent` via `accentForTheme`). **The WCAG-AA bar is MECHANIZED, not eyeballed:** `appearanceContrast.test.ts` ports a pure relative-luminance contrast fn (`lum`/`contrastRatio`/`softFill` with alpha-composite over the card) and asserts all 14 accent cases (7×2 both themes ≥ 4.5:1) + the tx/tx-2/tx-3 ramp + warn/ok on white + the `accentForTheme` reverse-map — every RESEARCH hex cleared on the first run, NO hand-tuning. `entitlements.test.ts` gained two assertions pinning `gatePreferences` free→`dark`+`#5b9bf8` / Pro→passthrough now that accent is live (D-23-2). Gates: vitest **1030/1030**, tsc + eslint clean (2 pre-existing SidebarResetMenu warnings, out of scope); decoder + 19 tests byte-for-byte untouched; zero new runtime/dev deps. Pure-unit plan — real-WKWebView e2e is Plans 03/04. **SET-07 stays UNCHECKED** (foundation only; the pane UI + live whole-app apply land in 23-02..04). `/simplify` + `/codex:review` not auto-invoked by the executor — recommend `/codex:review --scope working-tree` at the phase checkpoint. Commits: `8d4521eb` (widen+default), `13bf411d` (scale+tokens+contrast), `a6e801ae` (apply helpers+gate pin). **Carried to Plan 02:** re-declare the `LIGHT_TOKENS` values + light accent-soft/line (12%) under `[data-theme="light"]` in index.css + the no-flash pre-paint script. **Carried to Plan 03:** App-root apply effect calling `applyAppearance(gatePreferences(prefs, ents))` + matchMedia live-flip listener.

**Goal:** one-time-payment lifetime license — MoR checkout → webhook → Keygen (perpetual, node-locked, maxMachines=1); paste-key one-time activation (fingerprint `HMAC-SHA256(IOPlatformUUID, salt)`); offline Ed25519-verified `machine.lic` (~30-day TTL) thereafter; license key in Keychain (Rust-owned); free tier keeps all 11 tools — Pro locks customization (theming + ordering/pinning) behind a central entitlement gate (D-18 pivot; tool-gating mechanism ships dormant). Research: `docs/licensing-research.md`.

**v1.6 phase structure (ROADMAP.md):**

- **Phase 18 — Entitlements Seam & Central Gate** (ENT-01..05): pure-frontend gating seam, lock badges + upsell panel, lazy registry loaders; in-Tauri default = everything unlocked until Phase 21 flips it.
- **Phase 19 — License Activation & Offline Verification** (LIC-01/02/03/04/06): Keygen Rust core — activation, fingerprint, Ed25519 offline verify, Keychain, fail-closed. **Riskiest chunk; includes the SPIKE: confirm client-side license-key → license-token exchange against the live Keygen API.**
- **Phase 20 — Purchase Pipeline** (PAY-01..03): MoR checkout → webhook backend → Keygen license creation → key emailed. External infra; **parallel-capable with Phase 19** (depends only on 18).
- **Phase 21 — License Lifecycle & Ship Gate** (LIC-05/07/08/09): TTL refresh + grace, transfer, revocation, status UI; **flips the free-tier default live**; full 8-case ship-gate matrix on a fresh build. Depends on 19 AND 20 (end-to-end purchase→activation).

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260608-avk | Release notes via CHANGELOG.md (pure `extractChangelogSection` + wired both release drivers + multi-line updater banner) | 2026-06-08 | dcbf344f | [260608-avk-plumb-release-notes-through-changelog-md](./quick/260608-avk-plumb-release-notes-through-changelog-md/) |
| 260609-ard | `pnpm release:changelog "xxx"` edit-only driver (append to Unreleased) + `release:bump` auto-promotes Unreleased → `## [X.Y.Z] - date` (pure `appendUnreleasedEntry`/`promoteUnreleased`; CHANGELOG.md optional in bump ALLOWED_PATHS) | 2026-06-09 | e1a8e32e | [260609-ard-add-pnpm-release-changelog-command-auto-](./quick/260609-ard-add-pnpm-release-changelog-command-auto-/) |
| 260610-w61 | Protobuf example chips switch the input encoding: chip click sets raw + clears the override to auto-detect (follow-up fix — the original explicit override was sticky and broke typed decimal after a hex chip); per-example `encoding` locked by an EXAMPLES-detection contract test; unit + real-WKWebView e2e prove the flip AND type-after-chip re-detection | 2026-06-10 | 505439e9 + 966fc5d0 + b3b1676b | [260610-w61-protobuf-decoder-example-chips-switch-in](./quick/260610-w61-protobuf-decoder-example-chips-switch-in/) |
| 260611-awo | `pnpm release:changelog "xxx" --commit` opt-in flag: pure `parseChangelogArgs`/`changelogCommitMessage` grammar (typo'd flags throw, `--commit` w/o entry throws) + driver pathspec-commits ONLY CHANGELOG.md (`docs(changelog): <entry>`) so `release:bump` clean-tree preflight passes; default stays edit-only | 2026-06-11 | fc46ef67 + b51f2ed0 | [260611-awo-release-changelog-commit-flag-opt-in-own](./quick/260611-awo-release-changelog-commit-flag-opt-in-own/) |
| 260612-fast | release:publish rename-proofing (gsd-fast): lipo verify path + `.sig` glob derived from tauri.conf.json `productName` + Cargo binary name instead of hardcoded `devtools-app.app` (stale-name path "verified" old bundles, then failed after cleanup); sig glob product-pinned so stale old-name sigs can't single-match; verified vs real universal bundle (`lipo` = x86_64 arm64) + dry-run render; 36/36 publishPlan tests | 2026-06-12 | 3c4aba60 | — |
| 260611-ent | Peer-review batch 4/4: Sidebar.tsx 823→631 (drag/drop → `useSidebarDragDrop.ts`, Shift+F10 menu → `SidebarResetMenu.tsx`, pure refactor) + base64/hash/unix-time/uuid-ulid inline toggles → shared SegmentedControl (-106 lines, aria byte-identical). e2e 15/15 + vitest 816/816 + tsc/eslint green, tests/specs UNMODIFIED, decoder untouched. Human drag walkthrough **approved 2026-06-12** (pinned/unpinned reorder, end-zone, no cross-boundary, persistence + 4 toggles) | 2026-06-11 | c941dda0 + c6b2bf9a | [260611-ent-sidebar-tsx-decomposition-drag-drop-rese](./quick/260611-ent-sidebar-tsx-decomposition-drag-drop-rese/) |
| 260611-dl1 | Harness batch 1/4: e2e-spike.sh `preflight()` kills orphan devtools-app + clears :4445/:1420 LISTEN holders (TERM→KILL→fail-loud; `PREFLIGHT_ONLY=1` dry-run) so a green e2e run provably ran CURRENT code; eslint joins the lefthook unit gate (tsc+vitest+lint); .gitignore deduped, stale __logs__ purged, 3 stale docs → docs/archive/ with refs updated | 2026-06-11 | 02a76acf + fbaa2333 + 4aff0de8 | [260611-dl1-harness-e2e-spike-orphan-port-preflight-](./quick/260611-dl1-harness-e2e-spike-orphan-port-preflight-/) |
| 260611-dww | Harness batch 2/4: regex/url/cron migrated to the ONE shared CopyButton (call sites pass the bare noun — shared prepends `Copy ` so aria-labels stay byte-identical; -96 lines), dead `src/tools/_placeholder/` deleted, `docs/HARNESS.md` e2e-gate runbook (ports/env/preflight/session/WebKit quirks/DMG flake); full gate 816/816 + tsc + eslint; real-WKWebView e2e deferred to batch 3's full run | 2026-06-11 | acba7c24 + 3ff4ee97 + 94d8cf99 | [260611-dww-shared-copybutton-in-regex-url-cron-dele](./quick/260611-dww-shared-copybutton-in-regex-url-cron-dele/) |
| 260611-e5k | Harness batch 3/4: `test/e2e/helpers.ts` extracted (assert/dispatchKey/dispatchAltP/navigateToTool/saveScreenshot/readOrder/readPinnedOrder/focusRow) with the 3 WebKit lessons as helper doc comments; ALL 15 specs migrated (pure refactor, log lines + screenshots byte-identical; -336 duplicated spec lines, net -181); full real-WKWebView e2e 15/15 specs / 20 tests green — batch-2 CopyButton deferred verification DISCHARGED (regex/url/cron Copy aria-labels proven on WKWebView) | 2026-06-11 | e68a5c68 + 409d6785 | [260611-e5k-extract-test-e2e-helpers-ts-and-migrate-](./quick/260611-e5k-extract-test-e2e-helpers-ts-and-migrate-/) |
| 260614-nox | Isolate dev vs prod license storage (cfg-split): debug builds now use Keychain service `com.tinkerdev.app.dev.license` + files `machine.dev.lic[.tmp]`; release arms byte-identical (`com.tinkerdev.app.license`, `machine.lic[.tmp]`) → non-breaking for shipped installs. Mirrors config.rs D-52. e2e-spike preflight retargeted at the DEV Keychain service so the gate can never again orphan a buyer's license. codex P1 fixed: `license.e2e.ts` seed path → `machine.dev.lic` (the file the debug build reads). cargo `license::` 50/50 + vitest 889 + tsc + lint green; decoder untouched. | 2026-06-14 | d644c60c + 50efac4f + 37917aaa | [260614-nox-isolate-dev-vs-prod-license-storage-keyc](./quick/260614-nox-isolate-dev-vs-prod-license-storage-keyc/) |
| 260614-l39 | Phase 20 webhook code-review Medium findings 6–10 (harden the now-LIVE licensing pipeline): #6 idempotency guard already present (verified no-op, keygen.ts untouched); #7 opener scope `https://*`→exact `https://tinkerdev.io/*` (open-redirect surface, T-20-01); #8 root tsconfig `"types":[]` so hoisted @types/node stops masking the webview tsc gate (zero source fallout); #9 check-dev-strip.sh now FAILs on `PROD_*_PLACEHOLDER` sentinels (`grep -qaF`); #10 Buy e2e de-vacuumed — records+short-circuits `plugin:opener\|open_url`, asserts exact `https://tinkerdev.io/buy`. /simplify (shared RecorderWindow type + mailto guidance→todo) + codex review (P2: recorder install moved inside try so finally always restores). vitest 889/889 + both tsc gates + lint green; decoder untouched. **Real-WKWebView e2e = Task-4 human checkpoint (pending).** | 2026-06-14 | baaebd2a + e74c1f8f + 9194e47e + 4661958f + 62ddff9e | [260614-l39-phase-20-webhook-code-review-medium-find](./quick/260614-l39-phase-20-webhook-code-review-medium-find/) |

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-09, v1.6 started) · roadmap: .planning/ROADMAP.md · requirements: .planning/REQUIREMENTS.md · research: docs/licensing-research.md

**Core value:** Paste an unknown blob → usable, explorable interpretation in <2s, entirely offline, no mouse.
**Current focus:** Phase 24 — hotkeys-general-panes

## v1.5 — Pinned Tools (SHIPPED & ARCHIVED, 2026-06-07)

v1.5 complete — Phase 17 (2 plans), archived to `.planning/milestones/v1.5-*` (+ `v1.5-phases/`), tag `v1.5` (local-only). A distinct "Pinned" sidebar section over a persisted `pinnedToolIds` overlay (registry stays the single control plane; ⌘K palette + router pin-agnostic): left-of-grip pin toggle + **Alt+P**, two-group `partitionTools` (always a full registry partition; drop unknown / de-dupe), independent per-group drag + Alt+↑/↓ reorder (no cross-boundary), "Unpin all" in the Shift+F10 menu, persisted + reconciled on load. All 9 PIN requirements validated on the real WKWebView; full suite **694/694**; decoder + its 19 tests byte-for-byte untouched; zero new runtime/dev deps; gsd-ui-review WCAG-AA 23/24. Post-walkthrough fixes (**D-17**, see Accumulated Context): Alt+P keys off physical `KeyP` (macOS Option+P → "π"); Tab-reachable rows + pin fallback + ↑/↓ focus nav; 24×24 targets.

## Accumulated Context

### Pending Todos

- **Gate command palette as Pro feature** (`2026-06-11-gate-command-palette-as-pro-feature.md`) — add a palette entitlement to the Phase 18 seam; fold into Phase 21 planning (dev-toggle escape hatch + e2e palette-nav fallout noted in the todo).
- ~~**Shared ⌘K dev-toggle → entitlement-refresh e2e broken on the real WKWebView**~~ — **RESOLVED in Phase 21 (2026-06-15):** deterministic prefs/override + dev-Keychain reset folded into the e2e-spike preflight, and `ensureProTier`/`ensureFreeTier` hardened on the proven ⌘K toggle (retry-around-runDevToggle, re-read-and-toggle-only-when-off-target). entitlements/license/license-buy/sidebar all green; full suite 19/19 stable across many runs. See [[license-walkthrough-state-pollutes-e2e]].
- **Phase 21 ship-gate live cases 1/2/7/8 + final sign-off** (`21-HUMAN-UAT.md`, `21-SHIP-GATE-MATRIX.md`) — Phase 21 is IMPLEMENTED + goal-verified (human_needed); all automated gates green. OUTSTANDING: (a) human walkthrough of `21-HUMAN-UAT.md` tests 1–6 on the built app; (b) ship-gate live cases 1/2/7/8 (valid activation / second-device reject / deactivate-transfer / revocation) against prod CE `license.tinkerdev.io` — these need a **real prod-CE license key**, which can be **minted directly in the self-hosted Keygen CE (no Lemon Squeezy needed for the ship-gate)**; the LS-approved live store is only required for Phase 20's live-purchase pipeline (PAY-03). On completion: mark Phase 21 complete, close LIC-05/07/09 traceability, then the v1.6 milestone-complete sequence (which still gates on Phase 20 PAY-03). A corrupt `machine.lic` is currently SEEDED in `~/Library/Application Support/com.tinkerdev.app/` for the problem-state tests — delete to restore the clean free state.

### Roadmap Evolution

- **Phase 22.1 inserted after Phase 22 (2026-06-15)** — Settings follow-ups gap closure from `22-FOLLOWUP.md`: (1) BUG: app-menu product-name labels ("TinkerDev" title + About/Hide/Quit text, not the `devtools-app` bin name); (2) DESIGN: inline upsell/activation in the Settings ▸ License pane via a shared upsell-content component extracted from `UpsellPanel` (revises SET-06; standalone `UpsellModal` kept for sidebar/⌘K free-tier entries). Not planned yet → `/gsd-plan-phase 22.1`.

**Inherited binding wedge (every phase):** offline/no-network · paste-instant (<2s) · keyboard-driven · registry-driven single control plane · HashRouter only · WCAG-AA (keyboard path + `aria-live` mandatory, not optional) · layout-agnostic · **zero new runtime AND dev dependencies in the webview** · **`src/lib/protobuf/decoder.ts` + its 19 tests stay byte-for-byte untouched**. UI features add the **real-WKWebView UI gate**.

**v1.6 scoped amendments (locked, recorded in PROJECT.md + REQUIREMENTS.md):** "no network at runtime" gains a narrow licensing-only exception — one-time activation + opportunistic ~30-day TTL refresh, never per-launch checks, all tools fully functional offline. Rust crates for licensing (`ed25519-dalek`, `keyring`, HMAC) are expected and allowed; webview runtime deps stay zero.

**v1.6 architecture (locked — `docs/licensing-research.md`, do not re-litigate):** Keygen perpetual + node-locked (`maxMachines=1`); unencrypted Ed25519-signed `machine.lic` verified in Rust (embedded pubkey + fingerprint `HMAC-SHA256(IOPlatformUUID, app-salt)`); license key in macOS Keychain (Rust-owned, never readable from JS); React sees only `license_status`/`activate_license`/`refresh_license`/`deactivate_machine` returning a resolved entitlement set; MoR checkout (Lemon Squeezy default, payout-country pending) → webhook → small backend → Keygen license creation (privileged tokens server-side ONLY); webview gating = UX-gating, not DRM (accepted); OS-portable seams, macOS-only impl; locked tools stay visible with lock badge + upsell panel (never hidden, no opacity-only state).

**Phase 18 plan 01 decisions (2026-06-10):** entitlement vocabulary = `pro.theming` + `pro.ordering` (ONE arrangement entitlement covers reorder+pin+reset, D-26/D-28); reserved `premium?: boolean` DELETED from ToolDefinition (zero call sites) — `requiredEntitlements?: string[]` replaces it; entitlements store's synchronous default = `isTauriEnv() ? FULL_SET : FREE_SET` so pre/post-resolution agree without an override (no startup lock-flash); UpsellModal gained a Tab focus trap beyond plan spec (aria-modal without a trap fails WCAG-AA — codex review). `resolveEntitlements()` in `src/lib/entitlements/resolve.ts` is THE single Phase-21 flip point.

**Phase 18 plan 03 decisions (2026-06-10):** both registry surfaces gated through the ONE central seam — sidebar partition inputs swap on `ents.has(ENT_ORDERING)` (locked = registry-default render, stored toolOrder/pinnedToolIds untouched → instant restore on unlock, D-26); all five customization affordances (pin click, Alt+P physical-KeyP, Alt+↑/↓, drag, Reset order) branch FIRST on lock and open the shared UpsellModal (no write path exists locked — T-18-12 structural); free-tier-only footer "Unlock Pro" row (D-29); dormant lock badge + sr-only "— locked" suffix on sidebar AND palette rows via the same `isToolLocked` (fixture-proven under FULL_SET with an unknown entitlement — D-18; production registry zero diff); palette rows are a `tool | command` discriminated union with the DEV-only "Toggle free tier (dev)" command (module-level under `import.meta.env.DEV`, the exact string is Plan 04's dist-grep tripwire) writing the downgrade-only override then `refreshEntitlements()` — all surfaces flip live. Deferred (codex P3, `deferred-items.md`): stale-hook whole-blob prefs writes can clobber the persisted dev override (pre-existing last-writer-wins prefs trait; live snapshot unaffected).

**Phase 18 plan 04 decisions (2026-06-10, phase gate — human sign-off):** final upsell copy is a static supportive "Thank you for using TinkerDev ❤️" heading + trimmed two-paragraph body, user-approved verbatim, no pricing (D-20 held); **D-19 OVERRIDDEN by user** — the "Unlocks: {feature}" meta line dropped (lock context comes from the affordance invoked; render-unused `feature` prop deleted from UpsellPanel/UpsellModal + callers, heading/aria-labelledby intact); palette DEV commands are now searchable by typed query (same subsequence rule, always ranked AFTER tool matches — D-32 ordering; the prior empty-query-only behavior was a walkthrough-found bug whose e2e workaround had given a false positive — the rewritten e2e drives the real typed-query path); `scripts/check-dev-strip.sh` is the repeatable D-32 dist-grep artifact reused at Phase 21's flip gate (proven 0 against a fresh build); `test/e2e/entitlements.e2e.ts` proves the full locked-UX loop on the real WKWebView (toggle → D-26 default render → D-29 footer → D-28 upsell dialog → restore, T-18-15 cleanup in finally); e2e **15/15** specs, suite **795/795**; docs (REQUIREMENTS/ROADMAP/PROJECT/licensing-research) reconciled to D-18; walkthrough certified the packaged everything-unlocked default as the PRE-licensing baseline (T-18-14).

**Phase 18 plan 02 decisions (2026-06-10):** registry fully lazified (ENT-05) — all 11 entries are `component: () => import(...)`, `ToolDefinition.component` narrowed to `LazyComponent` only; tool routes render through the element-level `<ToolRoute>` gate (locked → UpsellPanel WITHOUT invoking the loader, unlocked → module-cached `React.lazy` keyed by tool.id in `Suspense fallback={null}`); route-level router `lazy` deliberately NOT used (memoized once — would defeat reactive flips AND fetch locked chunks). Build-proven: 11 per-tool Vite chunks, decoder isolated to `ProtobufDecoder-*.js` only (free-build exclusion seam is real). `lazyToolComponent(tool)` is the ONE way to materialize a tool component.

**Phase 19 plan 01 decisions (2026-06-12, D-42 SPIKE — gate passed):** key→token exchange DENIED live (403, body has NO `code` field) — **Keychain stores the RAW license key** (research-doc open item CLOSED); seat-limit error code confirmed verbatim **`MACHINE_LIMIT_EXCEEDED`** (A1) — Plan 03 maps on this exact string; validate-key codes observed live: `NO_MACHINE` (pre-activation, valid:false = EXPECTED), `VALID`, `FINGERPRINT_SCOPE_MISMATCH`; deactivate works with key auth alone (204 — LIC-07 primitive). CE host strategy: `KEYGEN_HOST=localhost` + explicit **`KEYGEN_DOMAIN=localhost`** (single-label host derives nil `Keygen::DOMAIN` → boot crash; no /etc/hosts needed); accounts#show doesn't route on single-label hosts → pubkey extracted via rails runner; CE's `meta.keys.ed25519` is base64-of-HEX, fixture normalized to base64-of-raw-32-bytes. Fixtures for Plan 02: `src-tauri/fixtures/ce-machine.lic` (synthetic fingerprint, byte-verbatim) + `ce-ed25519-pubkey.b64`. Plan 04 walkthrough must `bootstrap.sh mint_license` a FRESH license (spike seat bound to a synthetic fingerprint). Local CE: `scripts/keygen-ce/` (compose without profiles — Docker 20.10.7/Compose 2.0.0-beta.3; TLS via extracted Caddy CA, never -k; metrics: 2 tasks, 10 files, ~25 min).

**Phase 19 plan 02 decisions (2026-06-12, Rust license core):** `src-tauri/src/license/` landed pure (zero I/O in verify, zero Tauri types) with 24 cargo tests — fail-closed Ed25519 verify (`verify_strict` over literal `"machine/"+enc`, exact `"base64+ed25519"` alg gate, typed Corrupt|UnsupportedAlg|Tampered|ForeignMachine), real-CE fixture cross-validated against the real pubkey; **APP_SALT generated once and committed** (`e14f0d16…e45c75` — NEVER change post-release, orphans every activation, A5); fingerprint normalization locked to ioreg's verbatim uppercase hyphenated UUID; Keychain service = `com.tinkerdev.app.license` behind a `KeychainAccess` trait (tests never touch the real Keychain — Pitfall 5); `LicenseStatusPayload` camelCase JSON contract serde-pinned for Plans 03/04 (`{"state":"problem","problem":"foreignMachine","hasStoredKey":false}` shape; `has_stored_key` is the ONLY Keychain-derived value JS sees, lazy + fail-soft); expiry surfaced verbatim, NOT enforced (Phase 21, A6); `cargo test` joined lefthook as a pre-PUSH gate; module carries a documented temporary `#![allow(dead_code)]` — Plan 03 removes it when wiring commands; LIC-01/03/04/06 checkmarks deferred to the implementing plans 03/04 (Plan 01 precedent).

**Phase 19 plan 03 decisions (2026-06-12, online half + commands + seam):** Keygen client landed as PURE parsers (canned-JSON cargo tests, zero sockets) + thin async transport — branching ALWAYS on HTTP status + JSON:API `code`, never detail/title prose; seat-limit from BOTH paths (`FINGERPRINT_SCOPE_MISMATCH` + 422 `MACHINE_LIMIT_EXCEEDED`) → the single typed `seatLimit`; D-38 classified in Rust (NetworkUnreachable/NetworkDown/HostUnreachable→`offline`, everything else incl. ambiguous→`serviceUnreachable`, A4 residual live-proven in Plan 04). `LicenseError` serializes `{"code":"camelCase"}` (8 codes) — the webview copy layer keys on these. **Machine identifier on Keygen machine routes = the URL-safe fingerprint** (the VALID idempotent path has no machine UUID; refresh/deactivate reuse it). Activation persists ONLY after the checkout cert passes local Ed25519 verify (write-after-verify, no partial-success — T-19-15/18, test-pinned). `LicenseApi` is a generic trait slot (async-fn-in-trait is dyn-incompatible — manager<C: LicenseApi>); commands sit behind `tauri::async_runtime::Mutex`; `license_status` async but provably network-free (D-45). Startup fingerprint failure → empty-string sentinel (fail-closed Problem; activate refuses before network). reqwest 0.13 feature is `rustls` (0.12's `rustls-tls` renamed); dev CA trust (`DEVTOOLS_KEYGEN_CA`) double-gated under `cfg(debug_assertions)`. Webview: `platform.license` + `LicenseStatusPayload`/`LicenseErrorCode` TS mirror; non-Tauri arms deterministic via `createLicenseStub` (status→notActivated, mutations reject `serviceUnreachable`); 13 test Platform literals now spread `makeMemoryPlatform()` (keeps src/tools/ license-free). **LIC-04 checked off** (fully delivered, no later plan carries it); LIC-01/02 defer to Plan 04's UX halves.

**Phase 20 plan 01 decisions (2026-06-13, Buy wiring + D-52 constant split):** the app-side change is done — an `opener` capability mirrors the license-capability shape across the platform seam (`index.ts` interface + `get opener()` proxy, `tauri.ts` the SOLE `@tauri-apps/plugin-opener` importer, `browser.ts` deterministic no-op that never navigates jsdom). Installed `@tauri-apps/plugin-opener` (npm 2.5.4 + crate "2") + `tauri_plugin_opener::init()` — the FIRST new webview runtime dep, the explicit **D-67** exception; `opener:allow-open-url` scoped **https-only** (`{ url: "https://*" }`, T-20-01). Buy CTA now calls `platform.opener.openUrl("https://tinkerdev.io/buy")` — **D-68** own-domain redirect (store/MoR change never forces an app release), best-effort/calm (logs, never throws); D-21 stub tests rewritten to assert the seam call + no in-app navigation. **config.rs D-52:** `KEYGEN_HOST`/`KEYGEN_ACCOUNT_ID`/`KEYGEN_ED25519_PUBKEY_B64` cfg(debug_assertions)-split (dev=local CE, release=`license.tinkerdev.io` + `PROD_*_PLACEHOLDER` sentinels D-51); **APP_SALT stays single + byte-identical (A5)**; a `#[cfg(not(debug_assertions))]` tripwire test FAILS `cargo test --release` while placeholders remain (Plan 03's gate — `setup.sh` mints real values then re-runs green); default debug `cargo test` stays 3/3. `check-dev-strip.sh` extended with `check_prod_constants` (release-binary `license.tinkerdev.io`-present grep). Shared `noopOpener` added to `makeMemoryPlatform`/test literals (Rule-3 blocking fix). Unit suite **841/841**, tsc clean. **Carried to Plan 03:** fill the two config.rs prod placeholders, run `cargo test --release` green, run `check-dev-strip.sh` with `CHECK_PROD_BINARY`. **PAY-01 Validated (Plan 20-01).**

**Phase 20 plan 02 decisions (2026-06-13, webhook backend — PAY-02/PAY-03):** `server/webhook/` Node/TS backend landed unit-complete (30 tests, all mocked I/O, zero live calls). `mor.ts` is the ONE Lemon-Squeezy swap seam (D-61) → neutral `OrderEvent` union; `verify.ts` HMAC-SHA256 over the RAW body + length-guarded `crypto.timingSafeEqual` (D-60, T-20-09/T-20-10 — re-serialized-JSON failure pinned); `keygen.ts` searches `metadata[orderId]` before create (D-58 idempotency, Keygen as the single source of truth — no second datastore) and POSTs the JSON:API license (policy rel → D-54 inherited `pro.theming`/`pro.ordering`, `metadata.orderId`), non-2xx throws → 5xx (D-59); `email.ts` Resend plain-text key + 3 activation steps + releases link (D-66), re-throws Resend `{error}` so fulfill 5xx + alert (D-72); `fulfill.ts` orchestrates verify(401)→ignore(200)→invalid(400)→idempotent-skip(200)→create→email→200, NEVER 2xx after a side-effect failure (LS auto-retries); `index.ts` built-in-`http` server, `GET /health` + `POST /webhooks/lemonsqueezy` capturing the RAW body before any parse (Pitfall 5). **D-56 gate genuinely wired:** lefthook typecheck COMMAND now also runs `tsc --noEmit -p server/webhook/tsconfig.json` (PROVEN to catch a server-only type error the old root-only command missed) + eslint `files:["server/**/*.ts"]` node-globals block. **Structural decisions:** `server/webhook` is a pnpm WORKSPACE MEMBER (not a root dep) so `resend@6.12.4` is the webhook package's dep and never enters the app/webview manifest (zero-webview-runtime-deps wedge holds — resend is backend-only, never imported by `src/`); the optional root tsconfig project reference was DROPPED (a composite ref forces `noEmit:false` → TS6310, broke `tsc --noEmit`; the explicit `-p` gate command is the required D-56 mechanism regardless). Added `@types/node@22` (devDep) + `allowImportingTsExtensions` to the server tsconfig (Rule-3 blocking fixes). Secrets template-only: real `.env` gitignored, `.env.example` placeholders, no hardcoded token. Full suite **871/871** (+30), root+server tsc + lint clean; `decoder.ts` + its 19 tests untouched. **PAY-02/PAY-03 Done (Plan 20-02).** **Carried to Plan 03:** confirm the real LS test-mode payload field paths (`data.id`, `data.attributes.user_email`, A5) in the D-63 e2e; stand up prod CE + policy + Caddy; fill `server/webhook/.env`; live purchase→license→email round-trip.

**Phase 21 plan 01 decisions (2026-06-14, expiry-aware resolve_status — LIC-05 core, D-73):** `LicenseStatusPayload` is now 5 states — added **OfflineGrace** `{expiry, entitlements}` (Pro active, past expiry, within grace) and **RefreshNeeded** `{has_stored_key}` (Pro dropped, grace lapsed); serde shapes `offlineGrace`/`refreshNeeded` pinned in the contract test. `resolve_status` Ok-arm branches via a PURE `classify_expiry(expiry, now) -> {Active|Grace|Lapsed}` (injected `now`, no clock mocking) → Licensed/OfflineGrace/RefreshNeeded; **verify gates the expiry branch** (a tampered/foreign cert is still `Problem`, never grace). **Fail-OPEN on absent/unparseable expiry** (Active — a verified signature already proves authenticity; never downgrade a paying user on a date quirk, T-21-02/03 mitigated, test-pinned); inclusive boundaries (`now==expiry`→Active, `now==expiry+GRACE_DAYS`→Grace, strictly beyond→Lapsed). `needs_refresh(&mut self)` helper for Plan 02's scheduler: true in grace/lapsed OR Licensed-within-`RENEW_AHEAD_DAYS` (pure `within_renew_ahead`, fail-CLOSED false on absent/unparseable); both carry documented `#[allow(dead_code)]` until Plan 02 wires them. Constants landed in `config.rs` (profile-invariant, NOT cfg-split): **TTL_DAYS=30 / RENEW_AHEAD_DAYS=7 / GRACE_DAYS=7 / POLL_INTERVAL_HOURS=24** (D-74/75/76; ~37d worst-case revocation exposure). **D-45 intact** — zero network on the status path (NoNetwork client panics-on-call test green). Added **chrono 0.4** (`clock` only, src-tauri Rust dep — webview zero-dep wedge N/A). `cargo test license::` **66/66**; full webview gate 889/889 + tsc + lint green; APP_SALT byte-identical; decoder + 19 tests untouched.

**Phase 21 plan 02 decisions (2026-06-14, background refresh scheduler + 5-state TS mirror — LIC-05, D-76):** `LicenseManager::refresh_if_needed()` is the silent scheduler entry point — `needs_refresh()`-gated (ZERO network when the cert is fresh) and swallows EVERY error (offline / service down / no stored key / verify fail) → returns the prior `resolve_status()`, leaving state untouched (D-76/D-77, never propagates). **No separate connectivity probe** — the refresh call's own `LicenseError::Offline` (D-38) IS the online check. Silent `#[tauri::command] refresh_license_if_needed` (always `Ok`, scheduler-only) registered beside the existing 4 (now 5 commands); user-triggered `refresh_license` (surfaces errors) left intact for Plan 04's explicit Refresh. **lib.rs scheduler** = fire-and-forget `tauri::async_runtime::spawn` in `setup()` — never blocks first paint (2s `tokio::time::sleep` paint delay), a SINGLE `tokio::time::interval(POLL_INTERVAL_HOURS*3600)` whose immediate first tick is the launch trigger and subsequent ticks the 24h poll (no double-fire); each iteration locks `LicenseState` via `handle.state::<>()` + `refresh_if_needed()`, result discarded server-side (keeps machine.lic fresh on disk for the next `license_status`). **`license://refreshed` live-flip event deferred to Plan 04** (clear `TODO(21-04)` marker — non-blocking; Plan 04's status-open + explicit-Refresh already read fresh disk state). Added **tokio 1** (`time` feature only, src-tauri Rust dep — already in the Tauri tree; the spawned task runs on Tauri's own runtime, no second runtime). **TS union now a faithful 5-state mirror**: `offlineGrace {expiry, entitlements}` + `refreshNeeded {hasStoredKey}` added to `LicenseStatusPayload`; `payloadsEqual` gains both arms. **Rule-3 fallout fixed:** `UpsellPanel` `hasStoredKey` check made state-precise (offlineGrace carries no hasStoredKey); `TTL_DAYS` given a documented `#[allow(dead_code)]` (server-enforced). `cargo test license::` **69/69**; `cargo build` 0 warnings; tsc clean; vitest **893/893**; decoder + 19 tests untouched. **Carried to Plan 04:** wire the `license://refreshed` emit + subscribe; map offlineGrace→Pro-active / refreshNeeded→Pro-off in the status route.

**Phase 21 plan 03 decisions (2026-06-14, transfer/revocation/email/masked-key — LIC-07/08, D-89/D-81/D-82/D-79):** **D-89 email path** flows end-to-end — the webhook stamps `attributes.metadata.email` at create (`createLicense(orderId,email)`, preserved in `markEmailed`, `fulfill` passes `customerEmail`); `verify.rs` extracts it from the included `"licenses"` resource's `attributes.metadata.email` into `LicenseData.email` (None for pre-D-89 licenses, fail-soft). **Masked key (LIC-04):** `Licensed`+`OfflineGrace` payloads now carry `maskedKey` (`••••••••{last4}`, pure `mask_key()`; keys <4 chars fully masked) + `email`; the raw key is read Rust-side, masked immediately, and ONLY the masked form enters the payload (read-verified: no `get_key` usage routes the raw key into a serializable field, T-21-07). The masked-key Keychain read reuses the per-process prompt-flood cache (`masked_key_cache`, at most once — acceptable because the status route is user-initiated) and is seeded directly from the in-hand key on activation so post-activation status never re-prompts. Serde-contract test → 5-field camelCase shape; TS union + `payloadsEqual` mirror `maskedKey`/`email`. **Revocation (D-82/D-83):** `parse_checkout_response` maps revoked/suspended/expired error codes (`LICENSE_SUSPENDED`/`EXPIRED`/…) to the calm `LicenseError::Suspended`; a revoked refresh yields a typed error without panic; a garbled/revoked checkout writes no cert (write-after-verify, T-21-11) so the user keeps last-good until grace lapses to RefreshNeeded (~37d eventual consistency). **D-79 PINNED by cargo tests** (promoted from prose/walkthrough): an offline / service-unreachable `deactivate()` returns the error AND leaves the Keychain key + `machine.lic` byte-unchanged (server-delete-first, never orphan a seat, T-21-11b). **D-81 seat-release helper:** `infra/keygen/release-seat.sh` frees a seat by `--key` or `--order-id` (resolve → list machines → DELETE each), idempotent (zero machines = no-op success), `set -euo pipefail`, admin token from server-side env only (`$KEYGEN_ADMIN_TOKEN` or minted from admin creds, never argv, D-55/T-21-09); RUNBOOK "Freeing a seat" section added (D-80 fallback). `cargo test license::` **81/81**; root+webhook tsc clean; vitest **895/895**; `bash -n` clean; decoder + 19 tests untouched. **Carried to Plan 04:** render maskedKey (Key field) + email (Licensee, em-dash when null); map suspended/refreshNeeded → one calm "no longer active" (D-83); surface the calm offline-deactivate guidance. **Carried to Plan 05:** run `release-seat.sh` live vs prod CE (case-7 transfer).

**Phase 22 plan 01 decisions (2026-06-15, Settings modal shell — SET-04/05/06, D-S1..D-S6):** the v1.7 Settings surface foundation landed. `src/shell/settingsStore.ts` is a near-verbatim clone of `upsellStore` (module-singleton + `useSyncExternalStore`, synchronous invoker capture, jsdom guards) extended with `activePane` (default "license", reset to "license" on close; `openSettings(pane, invokerEl?)` no-op-if-open never overwrites pane/invoker; `setActivePane` notifies on change). `useSettings.ts` exposes `useSettingsOpen`/`useActivePane`. `SettingsModal.tsx` clones the `UpsellModal` a11y mechanics VERBATIM (mount-once invoker read with `document.activeElement` fallback, `onCloseRef`-style via `activeIndexRef`, Tab-wrap focus trap over the same focusable selector, `aria-modal`/`aria-labelledby` to a `useId()` "Settings" title, Esc + backdrop-mousedown + × close) and adds the paned layout: left nav button-list (`aria-current="page"`, **NOT tablist** — LOCKED for Phases 23-25 per the UI-SPEC reviewer note) + right pane rendering the active pane's `render()` DIRECTLY (LicenseSettings keeps its own `overflow-auto p-8 gap-12`, no double-pad — Pitfall 5), clamped Arrow/Home/End pane nav (no wrap), and an `sr-only aria-live="polite"` "{label} settings" announce (SET-05). Sizes LOCKED: dialog `w-[min(880px,92vw)] h-[min(640px,86vh)]`, nav rail `w-[200px]`, `z-[60]`. `settingsPanes.tsx` is the extensible `[{id,label,icon,render}]` registry (License pane only this phase; Phases 23-25 append, no shell change). `App.tsx` mounts `<SettingsModal/>` BEFORE `<UpsellModal/>` (Pitfall 6 — both `z-[60]`, DOM order stacks the upsell above Settings). `#/settings/license` migrated (D-S6): `src/shell/SettingsDeepLink.tsx` calls `openSettings("license", document.body)` then `<Navigate to="/" replace/>` (no duplicate in-window License surface; T-22-01 hardcodes the literal pane id). **STACKED-MODAL Esc guard (Rule-1 bug fix, real-WKWebView):** both shell modals attach document-level keydown listeners, so a single Esc closed BOTH at once and destroyed the Reactivate invoker before the upsell could return focus (E1 broke). SettingsModal now YIELDS all keyboard handling while an UpsellModal is open (detected by `aria-labelledby="upsell-heading"`) — the upsell owns Esc + its own trap and restores focus to the still-mounted Reactivate button. **e2e migration fallout (Rule-3):** `ship-gate.e2e` cases 4/5 ALSO hit Pitfall 4 (read first `<h2>` → now "Settings"/null) — migrated to open the modal + scope probes inside `[role=dialog][aria-modal]` + close-before-remount re-query + close before sidebar-focused checks; `upsellModalOpen` helper scoped to `aria-labelledby="upsell-heading"` (the Settings dialog's LicenseSettings problem copy was a false positive). Full real-WKWebView gate **20/20 spec files**; vitest **960/960**; tsc + eslint clean (2 pre-existing SidebarResetMenu warnings, out of scope); **decoder + its 19 tests AND LicenseSettings.tsx byte-for-byte untouched**; zero new runtime/dev deps. SET-04/05/06 Validated. **Carried to Plan 02:** the webview entry points (sidebar "Settings" row + ⌘K "Settings" command) + the D-88/D-S11 footer + ⌘K "License" re-point to `openSettings('license')`. **Carried to Plan 03:** native app-menu (⌘,) + tray via the `platform/` seam (`menu://open-settings` → `onOpenSettings`), manual menu/tray + Edit-menu-regression walkthrough.

**Phase 22 plan 02 decisions (2026-06-15, Settings entry points + D-88 re-point — SET-03, D-S6/D-S8/D-S9/D-S10/D-S11):** the two webview Settings entry points landed + every license-management entry consolidated onto the single Settings surface. **Sidebar:** a NEW unconditional bottom-anchored "Settings" gear row (lucide `Settings`) is the LAST aside child below the Unlock-Pro/attention affordance — opens for EVERYONE incl. unlicensed, NO lock badge → `openSettings("license")` (D-S9/D-S10/SET-04-reach); proven by a FULL_SET+notActivated unit test where the attention row is absent yet Settings is present (so the row is provably OUTSIDE the `licenseAttention || …` ternary). The D-88 footer "License needs attention" affordance re-pointed from `navigate("/settings/license")` to `openSettings("license")` (D-S11); the free-tier "Unlock Pro" branch is UNCHANGED (still opens the shared upsell). Sidebar DROPPED `useNavigate` entirely (the re-point removed `openLicenseSurface`'s sole `navigate` consumer). **CommandPalette:** a `settingsCommand` sibling to `licenseCommand` (`id:"settings"`, gear icon) → `openSettings("license", preOpenFocus)`, placed BEFORE `DEV_COMMANDS` so the dev toggle stays the last row (D-32 ordering held — the "appears at END" test still passes); the License command's manageable arm re-pointed to `openSettings("license", preOpenFocus)` (D-S11), free arm still `openUpsell(preOpenFocus)`. CommandPalette KEEPS `navigate` (still used for tool selection) — only dropped from the licenseCommand `useMemo` deps. Both ⌘K commands pass the persistent pre-palette focus as the explicit `openSettings` return target (the palette row unmounts on close — T-22-07; e2e asserts focus returns OFF `<body>`). **e2e:** `settings.e2e.ts` gained open-from-sidebar (Esc returns focus to the Settings row) + open-from-⌘K (Esc returns focus off `<body>`, ArrowUp-wrap guard to the Settings command row); `license-settings.e2e.ts` gained the D-S11 footer-routing coverage — the footer now OPENS the modal on the License pane (asserting the dialog MOUNTS, not a hash change; a corrupt machine.dev.lic seeds the problem state that surfaces the footer affordance). Real-WKWebView gate **20/20 spec files** (new screenshots from-sidebar/from-palette/footer-opens-modal); vitest **966/966**; tsc + eslint clean (the same 2 pre-existing SidebarResetMenu warnings, out of scope); **decoder + 19 tests AND LicenseSettings.tsx byte-for-byte untouched**; zero new runtime/dev deps. SET-03 Validated; SET-04/05/06 traceability rows reconciled to Validated (Plan-01 tracking lag fixed). **Carried to Plan 03 (unchanged):** native app-menu (⌘,) + tray "Settings…" via the platform seam, manual menu/tray + Edit-menu-regression walkthrough.

**Phase 22.1 plan 02 decisions (2026-06-15, inline License-pane upsell — revises SET-06, D-22.1-4/5/6/7/9):** Follow-up 2 closed. The activation surface was extracted into ONE internal `ActivationSurface(variant)` in `UpsellPanel.tsx` — it owns the activation state, the VERBATIM submit chain (`activate(trimmed||null) → clearEntitlementsOverride → refreshLicenseUi → refreshEntitlements → setValue("") → setActivated`), the `keyForm` JSX, the "Licensed — thank you" success state, the panel-mount `refreshLicenseUi` re-query, and the D-44 focus effect — with `activate(trimmed||null)` appearing EXACTLY ONCE (acceptance grep == 1). `UpsellPanel`/`UpsellModal` are now thin wrappers over the `panel` variant (output byte-for-byte unchanged — 24/24 existing tests pass, `aria-labelledby="upsell-heading"` preserved); a new `InlineActivation` export takes `variant: "upsell" | "form-only"` for the License pane. `LicenseSettings.tsx` DROPPED the `openUpsell` import (grep 0): **free/notActivated** early-returns to `<InlineActivation variant="upsell">` — its pitch heading IS the surface, so there is NO duplicated "Free" status card (the `notActivated` arms of statusLabel/statusBody became unreachable and were trimmed); **refreshNeeded/problem** keep the calm status card + Refresh and render `<InlineActivation variant="form-only">` BELOW (NO pitch — a lapsed/attention paying customer never sees it; the old modal-opening Reactivate button is gone); **licensed/offlineGrace UNCHANGED** (status + masked-key/email + confirm-first Deactivate). LIC-04 (stored-key empty-submit → `activate(null)`) + T-19-21 (key cleared on success) byte-for-byte preserved; D-22.1-5 standalone UpsellModal sidebar "Unlock Pro" + ⌘K free-tier entries UNCHANGED. No focus trap added inline (the SettingsModal owns it). **Rule-3 fallout:** `ship-gate.e2e` Cases 4/5 were coupled to the License-pane problem state — re-pinned from `routeHasButton("Reactivate")` to a new `routeHasKeyInput()` (the inline "License key" input is the calm-reactivation proof) and the free-restore cleanup now waits for the inline upsell pitch heading instead of "Free"; the fail-closed proofs (`!hasDeactivate()`, footer attention, locked Alt+P → upsell) unchanged. `license-settings.e2e.ts` inverts the two old stacked-modal tests to inline assertions (`upsellModalOpen()` FALSE throughout the License-pane flow). Full suite **968/968** (+2); tsc + eslint clean; real-WKWebView gate **20/20 spec files**; decoder + 19 tests untouched; zero new runtime/dev deps. SET-06 revised + Validated. **Slash-command gates** (`/simplify`, `/codex:review`) not auto-invoked by the executor — recommend a `/codex:review --scope working-tree` at the phase checkpoint. **Pre-existing out-of-scope:** `vite.config.ts` TS2578 under the node tsconfig (logged to `deferred-items.md`; webview `tsc` clean). **Carried to the Phase 22.1 boundary:** with Plan 01 (app-menu name, paused at its native-menu human checkpoint), the joint boundary needs the human walkthrough on a fresh `tauri build` + a `gsd-ui-review` WCAG-AA audit covering the inline License-pane states.

**v1.6 sequencing decisions:** entitlements seam FIRST (pure frontend, free-tier default = everything unlocked until Phase 21 flips it at integration); Keygen Rust integration is the riskiest chunk and carries the key→token-exchange SPIKE; PAY pipeline is external infra, parallel-capable with Phase 19; lifecycle hardening + the 8-case ship-gate matrix close the milestone.

**v1.6 open items (resolve in spike/planning):** client-side key→token exchange vs raw key in Keychain (spike, Phase 19); Lemon Squeezy seller payout-country verification (before Phase 20 commits); Keygen production tier (paid Std cloud vs self-hosted CE — free Dev tier dev-only, ~100 ALU cap); exact offline-grace behavior when TTL lapses offline (Phase 21 planning).

**v1.5 design (confirmed, do not re-litigate):** pinning is a render-time presentation overlay (`pinnedToolIds`) persisted through the existing `usePreferences`/`platform.store` seam beside `toolOrder`/`recentToolIds`; registry stays the single control plane (⌘K palette + router pin-agnostic). Reuses v1.4's `reconcileToolOrder`/`moveToolInOrder`, grip-handle drag + Alt+↑/↓ reorder, and the `aria-live` pattern. **Defaults:** no tool pinned by default (empty pinned section → no divider; hero NOT auto-pinned); pinning appends to the bottom of the pinned section; membership changes via pin/unpin only (no drag-across-boundary). Settings surface + auto-pin-hero stay deferred.

**Phase 17 context decisions (`17-CONTEXT.md`, 2026-06-05):** **D-13** pin/unpin shortcut = **Alt+P** (focused row; same family as Alt+↑/↓; plain single-key rejected per the sidebar's no-single-key model); announced "Pinned/Unpinned {tool}" via the existing `aria-live`. **D-14** pin icon sits **left of the grip handle**; **pinned rows show a persistent always-visible filled pin** (state + unpin target — no hover-only); unpinned rows show an outline pin on hover/`focus-visible` only; neutral tokens, NavLink right-padding widened for two controls. **D-15** Pinned group separated by a **bare neutral divider, no visible "PINNED" label** (compact density preserved); SR group named via `aria-label`; divider/group shown only when ≥1 pinned. **D-16** **"Unpin all" joins the existing right-click "Reset order" context menu** (reuses Shift+F10 entry; calls `setPinnedToolIds([])`), shown only when ≥1 pinned.

**D-17 (extends D-13, post-sign-off walkthrough 2026-06-07 — supersedes the planned D-05 "no roving nav"):** sidebar keyboard model reworked from live macOS feedback. **(a) Alt+P macOS fix (the real bug):** Option+P composes to the character "π", so the old `e.key === "p"` check was dead on the real WKWebView (the prior e2e gave a false positive by synthesizing `key:'p'`); now matches the PHYSICAL key `e.code === "KeyP"` (commit `cf7c566d`; e2e regression spec dispatches `key:'π'/code:'KeyP'`). **(b) Arrow nav:** plain **↑/↓** + Home/End move focus tool-to-tool across the pinned↔unpinned divider as one continuous list (clamp at ends, no wrap), via a pure `resolveRovingTarget` helper in `toolOrder.ts` (+9 unit tests). **(c) Tab model (FINAL):** every `NavLink` row is a Tab stop AND the pin button is Tab-reachable (`tabIndex={0}`; Enter/Space pins) as a keyboard fallback; the grip is pointer-only (`tabIndex={-1}`, `aria-hidden`). Alt+↑/↓ still reorder within-group (PIN-06 intact); Alt+P / Shift+F10 fire from the row. **(d)** pin+grip widened to 24×24 for WCAG 2.5.8 (`a3b0c087`). NOTE: an interim single-Tab-stop roving model (`b5ef70d3`/`6639da59`) was reverted to this Tab-friendly model (`1c6bfb8c`) per user request ("tab should also go to the pin / next tool"). Commits: `a3b0c087`, `b5ef70d3`, `6639da59`, `cf7c566d`, `1c6bfb8c`. Full suite **694/694**; real-WKWebView e2e green (macOS Option+P regression covered).

**Open carry-forwards (non-blocking):** settings surface (deferred); auto-pin/lock-hero (deferred); CI track (999.2); remaining tool wishlist (999.1); theme settings (999.3); DevTools CLI (999.4); Protobuf schema-file (999.5); FormatterView narrow-width stacking (UX-05); notarisation pending Apple enrolment (D-02); NAT-01 global summon hotkey (G-05-1); Cron advisory follow-ups (`15-REVIEW-FIX.md`); 3 minor updater a11y follow-ups.

## Harness reminder (per-task DoD, in order)

simplify → /codex:review → unit (vitest + tsc + eslint green) → real-WKWebView UI verification. Phase boundary: human sign-off on a fresh `tauri build` + a passing `gsd-ui-review` WCAG-AA audit. Never skip gates; parallelize plans but not past the gates. **Native-OS input (drag/drop, OS key chords, file drops) is manual-walkthrough coverage — the WebDriver can't synthesize it; make it an explicit human-verify item.** See [[tauri-native-dragdrop-blocks-html5-dnd]].

---

## v1.4 — Reorderable Tools (SHIPPED & ARCHIVED, 2026-06-05)

v1.4 complete — Phase 16 (2 plans), archived to `.planning/milestones/v1.4-*`, tag `v1.4` (local-only). The app's first personalization feature: a user-reorderable sidebar — `toolOrder` overlay over the registry (registry stays the single control plane), handle-initiated native HTML5 drag + neutral insertion line, Alt+↑/↓ keyboard reorder with `aria-live`, persisted + reconciled (new-tool-append / unknown-drop / de-dupe) + keyboard-reachable reset-to-default. All 7 REORD requirements validated on the real WKWebView; full suite 668/668; zero new runtime/dev deps; decoder + its 19 tests byte-for-byte untouched. Post-ship fix `1c2c7664`: Tauri `dragDropEnabled:false` so in-page HTML5 drag works.

## v1.3 — More Tools (SHIPPED & ARCHIVED, 2026-06-04)

v1.3 complete — Phases 12–15 (11 plans), archived to `.planning/milestones/v1.3-*`, tag `v1.3` (local-only). Three new tools (URL/Regex/Cron) + Protobuf decimal input; eight tools → eleven. All 25 requirements validated on the real WKWebView; full suite 650/650; zero new runtime/dev deps; decoder + its 19 tests byte-for-byte untouched.

## v1.2 — Release Tooling (SHIPPED & ARCHIVED, 2026-06-03)

v1.2 complete — Phases 9–11 (8 plans), archived to `.planning/milestones/v1.2-*`, tag `v1.2` (local-only). `pnpm release:bump` + `pnpm release:publish` over a unit-tested pure release core (`src/lib/release/`); universal-binary dual-key signature-verified cross-repo publish. All 12 REL requirements; proven live (v0.2.2 + DST-02 updater round-trip). Zero new runtime deps; decoder + its 19 tests untouched. CI track parked (999.2).

## v1.1 — Formatters (SHIPPED & ARCHIVED, 2026-06-02)

v1.1 complete — Phases 7 + 8 (4 plans), archived to `.planning/milestones/v1.1-*`, tagged `v1.1`. JSON + XML formatters behind a shared `FormatterView` (FMT-01..08, zero new runtime deps) + the opt-in `StatusBar` size readout (UIX-01). Decoder + its 19 tests untouched. **Carry-forward (non-blocking):** FormatterView narrow-width vertical stacking (UX-05, polish).

## v1.0 — Distribution (SHIPPED, signed off 2026-06-01)

v1.0 complete — all 6 phases (28/28 plans): foundation/harness (1), shell (2), Protobuf hero + Base64/Hex/Bytes + UX constraints (3), the four catalogue tools (4), native polish (5), distributable self-updating signed-DMG macOS app + verified auto-updater (6). Full archive: `.planning/milestones/v1.0-*` + `.planning/MILESTONES.md`.

**Carry-forwards (NOT v1.6 blockers):** Gatekeeper-clean notarisation deferred post-Apple-enrolment (D-02, credentials-only flip); NAT-01 configurable global summon hotkey parked (G-05-1); 3 minor a11y polish follow-ups from the updater UI review; Cron advisory follow-ups (MD-01 next-run perf, LO-02/LO-03 copy/locale — `15-REVIEW-FIX.md`); backlog 999.1 (remaining tool wishlist), 999.2 (CI track), 999.3 (theme settings), 999.4 (DevTools CLI), 999.5 (Protobuf schema-file).
