---
phase: 18-entitlements-seam-central-gate
plan: 04
type: execute
wave: 3
depends_on: ["18-02", "18-03"]
files_modified:
  - test/e2e/entitlements.e2e.ts
  - scripts/check-dev-strip.sh
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - docs/licensing-research.md
autonomous: false
requirements: [ENT-03, ENT-04, ENT-05]
must_haves:
  truths:
    - "The full existing real-WKWebView e2e suite passes unchanged — the lazified app behaves identically (phase criterion 4)"
    - "A new entitlements e2e proves on the real WKWebView: dev toggle → locked sidebar UX (default order, hidden pinned, footer row, upsell modal) → toggle back restores"
    - "'Toggle free tier (dev)' is verifiably absent from the production bundle (dist-grep check, D-32)"
    - "REQUIREMENTS.md / ROADMAP.md / PROJECT.md / docs/licensing-research.md reflect the D-18 free-tier pivot (no doc still claims the decoder is locked in free tier)"
    - "Human sign-off on a fresh `pnpm tauri build`: packaged app is everything-unlocked and visually unchanged (phase criterion 3)"
  artifacts:
    - path: "test/e2e/entitlements.e2e.ts"
      provides: "Real-WKWebView proof of the locked UX behind the dev toggle (ENT-04)"
    - path: "scripts/check-dev-strip.sh"
      provides: "Repeatable D-32 dist-grep build check (reused at the Phase 21 flip)"
  key_links:
    - from: "test/e2e/entitlements.e2e.ts"
      to: "CommandPalette DEV toggle"
      via: "palette query 'Toggle free tier' + Enter (tauri dev serves a DEV bundle)"
      pattern: "Toggle free tier"
    - from: "scripts/check-dev-strip.sh"
      to: "dist/assets"
      via: "grep -R after pnpm build"
      pattern: "dist/assets"
---

<objective>
Close the phase: D-18 documentation reconciliation, the real-WKWebView entitlements e2e + full-suite identical-behavior re-proof, the D-32 dist-grep production check, and the phase-boundary build + human walkthrough (packaged app = everything unlocked, unchanged).

Purpose: phase criteria 1, 3, and 4 are only provable on the real runtime and the real bundle; D-18 requires the superseded docs to stop claiming the decoder is locked.
Output: green full e2e, dist check script, reconciled docs, human-approved build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-entitlements-seam-central-gate/18-CONTEXT.md
@.planning/phases/18-entitlements-seam-central-gate/18-RESEARCH.md
@.planning/phases/18-entitlements-seam-central-gate/18-02-SUMMARY.md
@.planning/phases/18-entitlements-seam-central-gate/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-18 documentation reconciliation (REQUIREMENTS, ROADMAP, PROJECT, licensing-research)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, docs/licensing-research.md</files>
  <read_first>.planning/phases/18-entitlements-seam-central-gate/18-CONTEXT.md (D-18 block — the authority), .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, docs/licensing-research.md</read_first>
  <action>
    Apply the D-18 free-tier pivot to every superseded line (CONTEXT.md wins where docs disagree). Note: gsd-tools state/roadmap helpers no-op on this repo's narrative format — make these edits manually and re-read to confirm (project memory).

    1. `.planning/REQUIREMENTS.md`:
       - Milestone goal line: replace "the free tier keeps core tools but locks the Protobuf decoder (hero), theming, and tool ordering/pinning" with "all 11 tools (including the Protobuf hero) stay free; Pro locks customization — theming and tool ordering/pinning — and is the declared home for future power features (D-18)".
       - ENT-04 bullet: replace the text after the ID with: "Free tier locks **theming and tool ordering/pinning** (all 11 tools stay free — D-18 pivot). The tool-gating mechanism (lock badge in sidebar/palette + unlock/upsell panel in place of the tool UI) ships in full but **dormant**: no shipped tool carries `requiredEntitlements`. Locked features stay **visible** (not hidden; WCAG-AA, no opacity-only locked state)."
       - LIC-08 / any other line mentioning the decoder dropping to a locked state: adjust only if it claims tool locks (entitlements drop to free tier wording stays correct).
    2. `.planning/ROADMAP.md`:
       - v1.6 section intro: replace "free tier locks the Protobuf hero, theming, and ordering/pinning behind ONE central entitlement gate" with "free tier keeps all 11 tools; Pro gates customization (theming + ordering/pinning) behind ONE central entitlement gate (D-18 pivot — tool-gating mechanism ships dormant)".
       - Phase 18 success criterion 1: replace with "With a free-tier entitlement set applied via the dev/test toggle, a fixture-locked tool stays visible in the sidebar and ⌘K palette with a neutral lock badge, and opening it shows a WCAG-AA unlock/upsell panel in place of the tool UI (mechanism proven, dormant in production — D-18; never hidden, no opacity-only state)".
       - Phase 21 criterion 5: replace "an unlicensed install actually locks the decoder, theming, and ordering/pinning" with "an unlicensed install actually locks theming and ordering/pinning (all tools stay free — D-18)".
    3. `.planning/PROJECT.md`: find every free-tier line claiming the decoder/hero is locked (grep for "free tier" and "locks") and reword to the D-18 model (tools free; Pro = customization + future power features). Preserve surrounding context.
    4. `docs/licensing-research.md`: locate the "Free tier locks" row/line and amend to "Free tier locks: theming + tool ordering/pinning (D-18 pivot 2026-06-10 — all 11 tools incl. the Protobuf hero stay free; original hero-lock recommendation superseded by 18-CONTEXT.md D-18)". Do not rewrite the research narrative — annotate the locked decision row only.
  </action>
  <verify>
    <automated>! grep -rn "locks the Protobuf" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md && grep -q "D-18" .planning/REQUIREMENTS.md && grep -q "D-18" docs/licensing-research.md && echo DOCS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "locks the Protobuf"` returns nothing in .planning/REQUIREMENTS.md, ROADMAP.md, PROJECT.md
    - REQUIREMENTS.md ENT-04 contains "dormant" and "all 11 tools stay free"
    - ROADMAP.md Phase 21 criterion 5 no longer names the decoder as locked
    - docs/licensing-research.md contains "D-18" beside its free-tier row
  </acceptance_criteria>
  <done>No source-of-truth doc contradicts the D-18 pivot; future phases (esp. 21's flip) inherit correct scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entitlements e2e on the real WKWebView + full e2e re-proof + dist-grep check</name>
  <files>test/e2e/entitlements.e2e.ts, scripts/check-dev-strip.sh</files>
  <read_first>test/e2e/sidebar.e2e.ts (the harness patterns: dispatchKey for Alt/composed keys, readOrder via aria-labels, single-round-trip browser.execute, screenshot dirs), scripts/e2e-spike.sh, src/components/CommandPalette.tsx (DEV command + palette selectors), src/components/Sidebar.tsx (footer row + modal selectors)</read_first>
  <behavior>
    - e2e (real WKWebView via scripts/e2e-spike.sh): seed a custom order + ≥1 pinned tool through the UI or store; run "Toggle free tier (dev)" from the ⌘K palette; assert (a) sidebar shows registry-default order with NO pinned divider, (b) the "Unlock Pro" footer button is present, (c) clicking the pin affordance (or dispatching Alt+P with key:"π"/code:"KeyP") opens a role="dialog" containing "Tool ordering & pinning is a Pro feature", (d) Escape dismisses it, (e) toggling back restores the custom order + pinned section (prefs were never deleted — D-26)
    - All existing test/e2e/*.e2e.ts specs pass unchanged (criterion 4: lazified app behaves identically)
    - scripts/check-dev-strip.sh exits non-zero if "Toggle free tier" appears in dist/assets, zero otherwise
  </behavior>
  <action>
    1. CREATE `test/e2e/entitlements.e2e.ts` following sidebar.e2e.ts conventions exactly: auto-discovered by wdio (`test/e2e/*.e2e.ts`), single-round-trip `browser.execute` reads (WebKit stale-handle lesson), the bubbling-KeyboardEvent `dispatchKey` helper for Alt chords (dispatch `{ altKey: true, code: "KeyP", key: "π" }` — the macOS composed shape), screenshots to `test/e2e/__screenshots__/entitlements-*.png`. Open the palette by dispatching a metaKey "k" keydown on document (same in-page dispatch rationale); type "toggle free" into the palette input (set value + input event via execute); Enter to run. The dev command exists because `tauri dev` serves a DEV bundle (RESEARCH Pitfall 6 — this proves locked UX in dev/e2e; packaged free-tier proof is Phase 21's). Selectors: footer `button` by text "Unlock Pro"; dialog by `[role="dialog"]` + heading text; pinned divider by the `hr` / "Pinned tools" group absence.
    2. CREATE `scripts/check-dev-strip.sh` (executable, `chmod +x`):
       ```bash
       #!/usr/bin/env bash
       # D-32: the DEV-only palette command must be tree-shaken from production bundles.
       # Run AFTER `pnpm build`. Reused at the Phase 21 flip gate.
       set -euo pipefail
       if grep -R --include='*.js' -l "Toggle free tier" dist/assets/; then
         echo "FAIL: dev-only palette command present in production bundle" >&2
         exit 1
       fi
       echo "OK: dev toggle absent from dist/assets"
       ```
    3. RUN, in order (per project memory: write the spec, then RUN it — Chromium previews don't count; reap orphan vite/devtools-app processes if :1420/:4445 are taken):
       a. `pnpm build && ./scripts/check-dev-strip.sh` (D-32 proof)
       b. `scripts/e2e-spike.sh` — full e2e suite including the new spec; ALL specs must pass (existing specs prove identical behavior post-lazification, criterion 4).
  </action>
  <verify>
    <automated>pnpm build && ./scripts/check-dev-strip.sh && scripts/e2e-spike.sh</automated>
  </verify>
  <acceptance_criteria>
    - test/e2e/entitlements.e2e.ts exists and contains `Toggle free tier`, `Unlock Pro`, `role="dialog"`, and a `code: "KeyP"` / `key: "π"` dispatch
    - scripts/check-dev-strip.sh exists, is executable, and exits 0 against a fresh `pnpm build` output
    - e2e run output shows the entitlements spec passing AND every pre-existing *.e2e.ts spec passing (no skips of previously-green specs)
    - `git diff --stat src/lib/protobuf/decoder.ts src/lib/protobuf/decoder.test.ts` empty
  </acceptance_criteria>
  <done>Locked UX proven on the real runtime; production bundle proven clean of the dev toggle; lazified app proven behavior-identical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-boundary build + human walkthrough (everything-unlocked packaged default)</name>
  <files>(none — verification checkpoint; the agent runs the build, no source edits)</files>
  <read_first>src-tauri/tauri.conf.json (bundle identifiers), test/e2e/__screenshots__/ (evidence from Task 2)</read_first>
  <action>
    Agent: run `pnpm tauri build` (auto-build at the phase boundary — the human only launches/tests/approves). A final non-zero exit caused ONLY by the absent updater-signing key is expected — confirm success via the `.app`/`.dmg` under `src-tauri/target/release/bundle/macos/`, not the exit code. If the DMG bundling step flakes, `hdiutil detach` any mounted DMGs and retry. Then hand the built-app path to the human with the walkthrough below.
  </action>
  <verify>
    <automated>pnpm tauri build; ls src-tauri/target/release/bundle/macos/*.app</automated>
  </verify>
  <acceptance_criteria>
    - A fresh .app exists under src-tauri/target/release/bundle/macos/
    - Human walkthrough steps 1-6 below all confirmed
    - gsd-ui-review WCAG-AA audit passes
  </acceptance_criteria>
  <done>Human types "approved" after the walkthrough; Phase 18 sign-off recorded.</done>
  <what-built>
    Phase 18 complete: central entitlements seam (Tauri→everything-unlocked, browser→free), shared UpsellPanel, lock badges + locked-affordance upsell flow + footer "Unlock Pro" (all dormant/dev-only in the packaged app), and a fully lazified registry — with decoder.ts + its 19 tests byte-for-byte untouched. The agent has already run `pnpm tauri build` (auto-build at phase boundary — ignore a final non-zero exit if it is only the absent updater-signing key; if a DMG-mount flake occurs, `hdiutil detach` other DMGs and retry) and will hand off the built-app path under `src-tauri/target/release/bundle/macos/`.
  </what-built>
  <how-to-verify>
    Launch the built .app and confirm shipped behavior is UNCHANGED (phase criteria 3 + 4):
    1. All 11 tools present in the sidebar — NO lock badges anywhere, NO "Unlock Pro" footer row.
    2. ⌘K palette: search "toggle" — the dev command must NOT appear.
    3. Reorder a tool (drag + Alt+↓) and pin one (Alt+P) — works exactly as v1.5; restart the app — arrangement persists (override read path on the real store, RESEARCH Pitfall 4).
    4. Open the Protobuf decoder, paste a sample blob — interpretation appears instantly (<2s paste-to-result; lazy chunks must produce no perceptible blank — if you see one, report it: the Suspense fallback needs the neutral placeholder fallback per UI-SPEC).
    5. Switch between several tools rapidly — no flash, no spinner, inputs retained when returning.
    6. gsd-ui-review WCAG-AA audit passes (the UpsellPanel/locked surfaces are audited in dev mode since they're dormant in the packaged app).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 18, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev bundle → production bundle | DEV-only tooling must not cross into dist |
| docs → future phases | Stale free-tier claims would mis-scope Phase 21's flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-13 | Elevation of surface | "Toggle free tier (dev)" leaking into dist | mitigate | scripts/check-dev-strip.sh greps dist/assets after every production build; run at this gate and again at Phase 21's flip |
| T-18-14 | Repudiation | Pre-licensing unlocked default mistaken for licensed behavior at Phase 21 | mitigate | Docs reconciled to D-18 + the human walkthrough explicitly certifies "everything unlocked, no lock UI" as the PRE-licensing baseline; Phase 21's 8-case ship-gate matrix re-proves the flipped state |
| T-18-15 | Tampering | e2e store seeding leaving a "free" override behind on a dev machine | mitigate | e2e spec toggles back to full as its final step and asserts restoration (also proves D-26 prefs preservation) |
</threat_model>

<verification>
- Full unit suite + tsc + lint green; full e2e suite green on the real WKWebView (existing specs unchanged + new entitlements spec).
- `pnpm build && ./scripts/check-dev-strip.sh` exits 0.
- Docs grep-clean of superseded free-tier claims.
- Human sign-off recorded (criterion 3: packaged default = everything unlocked, shipped behavior unchanged).
</verification>

<success_criteria>
- Phase 18 success criteria 1–4 all demonstrated: locked UX under the dev toggle (real WKWebView), one central gate flipping all surfaces, React consuming only the resolved set with the in-Tauri unlocked default, and the lazified registry behaving identically with the decoder untouched.
- D-18 pivot reflected in every source-of-truth doc.
</success_criteria>

<output>
After completion, create `.planning/phases/18-entitlements-seam-central-gate/18-04-SUMMARY.md`
</output>
