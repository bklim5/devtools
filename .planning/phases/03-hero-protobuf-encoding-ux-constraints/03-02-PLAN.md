---
phase: 03-hero-protobuf-encoding-ux-constraints
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/tools/protobuf-decoder/detectEncoding.ts
  - src/tools/protobuf-decoder/detectEncoding.test.ts
  - src/tools/protobuf-decoder/interpretationChips.ts
  - src/tools/protobuf-decoder/interpretationChips.test.ts
  - src/tools/protobuf-decoder/useDecode.ts
  - src/tools/protobuf-decoder/useDecode.test.ts
  - src/tools/protobuf-decoder/copyAsJson.ts
  - src/tools/protobuf-decoder/copyAsJson.test.ts
autonomous: true
requirements: [PRO-01, PRO-02, PRO-03, PRO-04]
nyquist_compliant: true

must_haves:
  truths:
    - "A pasted string is classified hex vs base64 by the D-02 heuristic (purely, testably)"
    - "Decoding wraps decodeMessage so groups/truncation/oversize surface as an error STRING, never a thrown crash"
    - "Each LEN node yields chips derived ONLY from the real LenInterpretation keys present, ordered by D-04 precedence, with a smart default-selected chip"
    - "VARINT nodes yield uint64/int64/sint/bool chips from the real asUnsigned/asSigned/asZigzag/asBool fields"
    - "copy-as-JSON serializes field numbers as keys with each node's currently-selected interpretation, recursing nested messages"
  artifacts:
    - path: "src/tools/protobuf-decoder/detectEncoding.ts"
      provides: "detectEncoding(raw): 'hex' | 'base64' (D-02 heuristic)"
      exports: ["detectEncoding"]
    - path: "src/tools/protobuf-decoder/interpretationChips.ts"
      provides: "chipsForField(field) + default selection by precedence"
      exports: ["chipsForField"]
    - path: "src/tools/protobuf-decoder/useDecode.ts"
      provides: "decode orchestration: string → {bytes, encoding, fields|error, timingMs, byteCount}"
      exports: ["decodeInput"]
    - path: "src/tools/protobuf-decoder/copyAsJson.ts"
      provides: "fieldsToJson(fields, selection): pretty JSON string (D-11)"
      exports: ["fieldsToJson"]
  key_links:
    - from: "src/tools/protobuf-decoder/useDecode.ts"
      to: "src/lib/protobuf/decoder.ts"
      via: "decodeMessage wrapped in try/catch"
      pattern: "decodeMessage"
    - from: "src/tools/protobuf-decoder/useDecode.ts"
      to: "src/lib/bytes.ts"
      via: "hexToBytes/base64ToBytes via detectEncoding"
      pattern: "hexToBytes|base64ToBytes"
    - from: "src/tools/protobuf-decoder/interpretationChips.ts"
      to: "src/lib/protobuf/decoder.ts"
      via: "reads value.kind + interpretations keys (types imported)"
      pattern: "interpretations|value.kind|kind ==="
---

<objective>
Build the pure, node-unit-testable logic core of the Protobuf hero tool: the hex/base64 detection heuristic (D-02), the chip-derivation + smart-default selection from the REAL decoder output (D-04/D-06), the decode orchestration that turns errors into status strings instead of crashes (PRO-01/PRO-02), and the copy-as-JSON serializer (D-11). Keeping this logic out of React components makes it the bulk of the TDD surface; plan 03-04 renders thin components over it.

Purpose: Proves the riskiest correctness concern — mapping the UI 1:1 to the decoder's true `FieldValue`/`LenInterpretation` shape (NOT the mockup's invented keys) — under unit tests before any rendering.
Output: Four pure modules + their failing-first (RED) → passing (GREEN) test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-CONTEXT.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-RESEARCH.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-VALIDATION.md

<interfaces>
<!-- VERIFIED from src/lib/protobuf/decoder.ts and src/lib/bytes.ts (read this session). -->
<!-- THE MOCKUP'S interpretation keys (uint/u32/sint/message/float/double…) ARE INVENTED. -->
<!-- Bind ONLY to these real shapes. -->

```typescript
// decodeMessage(buf: Uint8Array, depth=0): DecodedField[]  — THROWS on invalid input,
// groups (wire 3/4: "Unsupported wire type 3 (field N); groups are not supported"),
// truncation ("Unexpected end of buffer…"), oversize ("Payload exceeds maximum size…").
interface DecodedField { fieldNumber: number; wireType: 0|1|2|5; value: FieldValue; }
type FieldValue =
  | { kind: "varint"; asUnsigned: string; asSigned: string; asZigzag: string; asBool: boolean }
  | { kind: "i64";    hex: string; asUint64: string; asInt64: string; asDouble: number }
  | { kind: "i32";    hex: string; asUint32: number; asInt32: number; asFloat: number }
  | { kind: "len";    byteLength: number; interpretations: LenInterpretation };
interface LenInterpretation {
  hex: string;                    // ALWAYS present — the bytes floor
  string?: string;                // iff valid UTF-8
  message?: DecodedField[];       // iff parses as a clean non-empty sub-message
  packedVarints?: PackedVarint[]; // iff cleanly a varint stream
  packedFixed32?: PackedFixed32[];// iff length is a positive multiple of 4
  packedFixed64?: PackedFixed64[];// iff length is a positive multiple of 8
}
// PackedVarint { asUnsigned, asSigned, asZigzag }  (strings)
// PackedFixed32 { hex, asUint32:number, asInt32:number, asFloat:number }
// PackedFixed64 { hex, asUint64:string, asInt64:string, asDouble:number }
```

```typescript
// src/lib/bytes.ts (port-unchanged)
hexToBytes(input: string): Uint8Array  // throws "Hex must have an even number of digits" / "Invalid hex characters"; strips 0x + [\s:_-]
base64ToBytes(input: string, alphabet?: "base64"|"base64url"): Uint8Array
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED→GREEN): detectEncoding heuristic (D-02)</name>
  <read_first>
    - src/lib/bytes.ts (hexToBytes already strips `0x` + `[\s:_-]`; the heuristic only PICKS the converter, never parses bytes itself)
    - src/shell/fuzzy.test.ts (a pure-node test file for conventions: `node` env, explicit `{ describe, it, expect }` imports, globals:false)
    - .planning/phases/03-hero-protobuf-encoding-ux-constraints/03-RESEARCH.md (Code Examples → detection heuristic)
  </read_first>
  <behavior>
    - detectEncoding("089601") === "hex"  (even-length hex digits)
    - detectEncoding("0x08 96 01") === "hex"  (0x prefix + whitespace separators)
    - detectEncoding("08:96:01") === "hex"  (colon separators)
    - detectEncoding("8960") === "hex"
    - detectEncoding("aGVsbG8=") === "base64"  (contains non-hex chars)
    - detectEncoding("0896011") === "base64"  (odd nibble count → not hex)
    - detectEncoding("") === "base64"  (empty string: caller treats empty as neutral, but the classifier must return a stable value, not throw)
    - detectEncoding("EiAKB...") (base64 with mixed case + letters g-z) === "base64"
  </behavior>
  <action>
RED: write detectEncoding.test.ts (node env) covering the cases above; import from "./detectEncoding" which does not yet exist (RED). GREEN: implement `export type InputEncoding = "hex" | "base64";` and `export function detectEncoding(raw: string): InputEncoding`. Algorithm exactly: `const trimmed = raw.trim().replace(/^0x/i, ""); const hexBody = trimmed.replace(/[\s:_-]/g, ""); const looksHex = hexBody.length > 0 && /^[0-9a-fA-F]+$/.test(hexBody) && hexBody.length % 2 === 0; return looksHex ? "hex" : "base64";`. Pure function, no imports from bytes.ts (it only classifies). Commit RED+GREEN together (lefthook blocks red).
  </action>
  <verify>
    <automated>npx vitest run src/tools/protobuf-decoder/detectEncoding.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/tools/protobuf-decoder/detectEncoding.test.ts exits 0
    - grep -q "hexBody.length % 2 === 0" src/tools/protobuf-decoder/detectEncoding.ts
    - detectEncoding.ts imports nothing from "@/lib/bytes" (grep -L "bytes" confirms it is self-contained classification only)
  </acceptance_criteria>
  <done>Pure detectEncoding classifies hex vs base64 per D-02 (0x prefix, separators, even-nibble rule, empty→base64), fully unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED→GREEN): interpretationChips + smart default (D-04/D-06)</name>
  <read_first>
    - src/lib/protobuf/decoder.ts (the REAL FieldValue union + LenInterpretation keys — bind to these, NOT the mockup)
    - src/tools/protobuf-decoder/detectEncoding.test.ts (test conventions from Task 1)
    - .planning/phases/03-hero-protobuf-encoding-ux-constraints/03-RESEARCH.md (Pattern 1 → chip generation per node)
  </read_first>
  <behavior>
    - For a varint field: chips in order [uint64, int64, sint, bool], values from asUnsigned/asSigned/asZigzag/String(asBool); all enabled; default-selected = "uint64".
    - For an i64 field: chips [double, uint64, int64, hex] from asDouble/asUint64/asInt64/hex; default-selected = "double".
    - For an i32 field: chips [float, uint32, int32, hex]; default-selected = "float".
    - For a len field with interpretations {hex, string, message}: chips emitted ONLY for present keys, ordered by precedence message > string > packed-varints > packed-i32 > packed-i64 > bytes(hex); here [message, string, bytes]; default-selected = "message" (first present per precedence, D-04).
    - For a len field with interpretations {hex} only: chips [bytes]; default = "bytes" (the always-present floor).
    - For a len field with {hex, packedVarints}: chips [packed-varints, bytes]; default = "packed-varints".
    - NEVER emit a chip for an absent interpretation key (no hand-curated fixed list).
  </behavior>
  <action>
RED: interpretationChips.test.ts (node env) constructing literal DecodedField objects of each kind and asserting the chip id list, order, default selection, and that absent LEN keys produce no chip. GREEN: implement interpretationChips.ts exporting:
  `export interface Chip { id: string; label: string; value: string; }` (value = the display/copy string for that reading)
  `export function chipsForField(field: DecodedField): Chip[]`
  `export function defaultChipId(field: DecodedField): string` (or return `{ chips, defaultId }` from one function — Claude's discretion, but the precedence + default rule is locked).
For len: build chips by iterating the precedence order ["message","string","packedVarints","packedFixed32","packedFixed64","hex"]; include a chip iff that key is present in `value.interpretations`; map key→chip id/label: message→"message"/"message", string→"string"/"string", packedVarints→"packed-varints"/"packed varints", packedFixed32→"packed-i32"/"packed i32", packedFixed64→"packed-i64"/"packed i64", hex→"bytes"/"bytes". `hex` is always present (floor). defaultId = first chip in that built list. For non-message LEN chip `value`: string→the string; hex→the hex string; packed*→a compact comma-joined list of the per-element selected reading (Claude's discretion on element reading — document it). For varint/i64/i32 emit the fixed full chip set (all present) with the stated default. Import the DecodedField/FieldValue TYPES only from "@/lib/protobuf/decoder" (type import; no logic copied). Commit RED+GREEN together.
  </action>
  <verify>
    <automated>npx vitest run src/tools/protobuf-decoder/interpretationChips.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/tools/protobuf-decoder/interpretationChips.test.ts exits 0
    - grep -q "interpretations" src/tools/protobuf-decoder/interpretationChips.ts (chips driven from the real LEN keys)
    - grep -qE "message.*string.*packedVarints.*packedFixed32.*packedFixed64.*hex" src/tools/protobuf-decoder/interpretationChips.ts (precedence order present, D-04)
    - interpretationChips.ts contains NO hard-coded full chip array for LEN (no fixed `["message","string","bytes","packed-varints",...]` literal emitted unconditionally) — chips are gated on key presence
  </acceptance_criteria>
  <done>chipsForField derives chips strictly from the real FieldValue/LenInterpretation keys present, in D-04 precedence order, with the correct smart default per node; VARINT exposes zigzag+signed+bool.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (RED→GREEN): useDecode orchestration + copyAsJson (PRO-01/02, D-11)</name>
  <read_first>
    - src/lib/protobuf/decoder.ts (decodeMessage throws on groups/truncation/oversize — must be caught, not propagated)
    - src/lib/bytes.ts (hexToBytes/base64ToBytes also throw on bad input — caught into the same error string)
    - src/tools/protobuf-decoder/detectEncoding.ts (from Task 1)
    - src/tools/protobuf-decoder/interpretationChips.ts (from Task 2 — copyAsJson uses the same selection model)
  </read_first>
  <behavior>
    - decodeInput("089601", undefined) → { encoding:"hex", byteCount:3, fields:[…], error:null, timingMs:number } (canonical {1:150}).
    - decodeInput("", undefined) → { encoding:"base64"|"hex", byteCount:0, fields:[], error:null } NEUTRAL empty state, NOT an error (D-02).
    - decodeInput("1c", undefined) → { error: a non-empty string containing "groups", fields:null } — a group byte; the thrown decoder error becomes a STRING, the call does NOT throw (PRO-02).
    - decodeInput of a truncated varint (e.g. "08") → { error: contains "buffer" or "end", fields:null } — caught, not thrown.
    - decodeInput respects an explicit encoding override: decodeInput(raw, "base64") forces base64 even if it looks hex (D-01 manual toggle).
    - bad bytes input (e.g. detectEncoding picks hex but hexToBytes throws) → error string, no throw.
    - fieldsToJson: for fields [{1: varint 150 default uint64}] with selection {} → JSON string parse-equals { "1": "150" }. For a nested message field selected as "message" → nested object recurse. For a len node selected as "string" → the string value; "bytes" → hex string; "packed-varints" → array of the readings. Field numbers are the keys.
  </behavior>
  <action>
RED first for both files. GREEN:
useDecode.ts — `export interface DecodeResult { encoding: InputEncoding; byteCount: number; fields: DecodedField[] | null; error: string | null; timingMs: number; }` and `export function decodeInput(raw: string, override?: InputEncoding): DecodeResult`. Logic: if raw.trim()==="" return neutral { fields:[], error:null, byteCount:0, encoding: detectEncoding(raw), timingMs:0 }. Else: `const encoding = override ?? detectEncoding(raw);` then `const t0 = performance.now();` try { `const bytes = encoding === "hex" ? hexToBytes(raw) : base64ToBytes(raw);` `const fields = decodeMessage(bytes);` return {..., bytes byteCount: bytes.length, fields, error:null, timingMs: performance.now()-t0}; } catch (e) { return {..., fields:null, error: (e as Error).message, byteCount: 0, timingMs: performance.now()-t0}; }. MUST wrap BOTH the bytes conversion and decodeMessage in the one try/catch so neither throws past the boundary (Pitfall 3). Provide a separate `decodeInput` shape only — the React state/hook wrapper lives in 03-04. (If a hook is convenient, keep the pure `decodeInput` exported and let 03-04 wrap it; this plan ships the pure function + tests.)
copyAsJson.ts — `export function fieldsToJson(fields: DecodedField[], selection: Map<string, string>): string` returning `JSON.stringify(obj, null, 2)`. Walk fields keyed by a stable path (e.g. parent path + index, matching the scheme 03-04 will use — document the path format `"<index>"` at top level, `"<parentPath>.<index>"` nested). For each field: key = String(fieldNumber); value = the currently-selected interpretation per the selection map (fall back to defaultChipId from interpretationChips). varint/i64/i32 → the selected reading string/number. len selected "message" → recurse fieldsToJson(value.interpretations.message, selection) as a nested object; "string" → the string; "bytes" → hex; "packed-*" → array of readings. Repeated same field number: collect into an array under that key (document the chosen behavior). Reuse `chipsForField`/`defaultChipId` from interpretationChips.ts — do NOT re-derive readings.
Use `platform.clipboard.writeText` ONLY in 03-04 (the component) — this module returns the string; it does NOT touch the clipboard or @tauri-apps. Commit RED+GREEN together for both files.
  </action>
  <verify>
    <automated>npx vitest run src/tools/protobuf-decoder/useDecode.test.ts src/tools/protobuf-decoder/copyAsJson.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/tools/protobuf-decoder/useDecode.test.ts src/tools/protobuf-decoder/copyAsJson.test.ts exits 0
    - grep -q "catch" src/tools/protobuf-decoder/useDecode.ts AND grep -q "decodeMessage" src/tools/protobuf-decoder/useDecode.ts (errors caught, not propagated — PRO-02)
    - A test asserts decodeInput("1c") returns a non-null error string and does NOT throw
    - A test asserts decodeInput("") returns error:null with fields:[] (neutral empty, not error)
    - grep -q "JSON.stringify" src/tools/protobuf-decoder/copyAsJson.ts
    - copyAsJson.ts contains NO import of "@tauri-apps" and NO platform.clipboard call (grep returns nothing)
    - npx vitest run src/lib/protobuf/decoder.test.ts still 19 passing
  </acceptance_criteria>
  <done>decodeInput turns every decoder/bytes error into a status string (never crashes), treats empty as neutral, honors a manual encoding override, and times the decode; fieldsToJson serializes field-numbers-as-keys with the selected interpretation and nested recursion — all under unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted string → decoder | The pasted protobuf blob is fully untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | DoS | `decodeInput` → `decodeMessage` on huge/deeply-nested paste | mitigate | `decodeMessage` already enforces `MAX_PAYLOAD_BYTES` (16 MiB) + `MAX_DEPTH` (64) and throws; `decodeInput` catches the throw and returns it as an error string so the UI stays responsive. Plans MUST NOT remove or bypass these guards. Unit-tested via oversize/group/truncation cases surfacing as strings. |
| T-03-04 | Tampering/Injection | decoded strings later rendered | mitigate | Logic modules return plain strings; 03-04 renders them as React text nodes (escaped) — never `dangerouslySetInnerHTML`. copyAsJson emits `JSON.stringify` output only (no executable content). |
| T-03-05 | Elevation of Privilege | clipboard access | mitigate | copyAsJson returns a string only; it does NOT import `@tauri-apps/*` or call `platform.clipboard` — the actual write is via the seam in 03-04. Verified by grep acceptance criterion. |
</threat_model>

<verification>
- `npx vitest run src/tools/protobuf-decoder/detectEncoding.test.ts src/tools/protobuf-decoder/interpretationChips.test.ts src/tools/protobuf-decoder/useDecode.test.ts src/tools/protobuf-decoder/copyAsJson.test.ts` exits 0.
- `npx vitest run src/lib/protobuf/decoder.test.ts` reports 19 passing.
- `npx tsc --noEmit` clean.
- Per-task harness in order: /simplify → /codex:review (--wait --scope working-tree) → vitest+tsc → real-webview UI (pure-logic plan; UI verification of these behaviors lands in 03-04 against `tauri dev`).
</verification>

<success_criteria>
- Chips/values are derived strictly from the real `FieldValue`/`LenInterpretation` keys (mockup keys never referenced).
- Group/truncation/oversize/bad-bytes inputs surface as error strings; empty is neutral.
- copy-as-JSON shape matches D-11 (field numbers as keys, selected interpretation, nested recurse).
- No port-unchanged file edited; no `@tauri-apps/*` import; decoder's 19 tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-02-SUMMARY.md`
</output>
