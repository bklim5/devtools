---
phase: 03-hero-protobuf-encoding-ux-constraints
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/tools/protobuf-decoder/ResizableSplit.tsx
  - src/tools/protobuf-decoder/ResizableSplit.test.tsx
  - src/tools/protobuf-decoder/FieldNode.tsx
  - src/tools/protobuf-decoder/FieldTree.tsx
  - src/tools/protobuf-decoder/FieldTree.test.tsx
  - src/tools/protobuf-decoder/ProtobufStatusBar.tsx
  - src/tools/protobuf-decoder/ProtobufDecoder.tsx
  - src/tools/protobuf-decoder/ProtobufDecoder.test.tsx
  - src/tools/protobuf-decoder/index.ts
autonomous: false
requirements: [PRO-01, PRO-02, PRO-03, PRO-04, PRO-05, PRO-06, PRO-07, UX-01, UX-02, UX-03, UX-04, UX-05]
nyquist_compliant: true

must_haves:
  truths:
    - "Pasting hex or base64 renders the recursive wire-format field tree instantly (no decode button)"
    - "Groups (3/4)/truncation render as an error in the status bar + an inline error state, never a crash"
    - "Each LEN node shows interpretation chips from the real LenInterpretation keys, with a smart default selected and per-node override"
    - "VARINT nodes additionally show zigzag + signed int64 readings"
    - "Nested sub-messages auto-expand on paste and are collapsible per node"
    - "Input/output panes are resizable via an in-house col-resize divider"
    - "The tree defaults to cards with a persisted rows/cards toggle (survives restart)"
    - "#N field numbers render neutral; strong accent is reserved for the selected/active state only"
    - "Every node has a visible focusable copy of its selected value; a copy-all-as-JSON action exists; no hover-only copy"
    - "A status bar shows parse state · byte count · detected encoding · errors · timing"
  artifacts:
    - path: "src/tools/protobuf-decoder/ResizableSplit.tsx"
      provides: "in-house ~30-line col-resize two-pane divider (D-09), no library"
      exports: ["ResizableSplit"]
    - path: "src/tools/protobuf-decoder/FieldTree.tsx"
      provides: "recursive DecodedField[] renderer (cards/rows), auto-expand"
      exports: ["FieldTree"]
    - path: "src/tools/protobuf-decoder/FieldNode.tsx"
      provides: "single field: #N neutral, wire, bytes, chips, value/submsg, copy"
      exports: ["FieldNode"]
    - path: "src/tools/protobuf-decoder/ProtobufDecoder.tsx"
      provides: "tool root: input + divider + tree + toggle + status bar; the real component"
      min_lines: 80
    - path: "src/tools/protobuf-decoder/index.ts"
      provides: "registry entry with component = ProtobufDecoder (no makePlaceholder)"
      contains: "ProtobufDecoder"
  key_links:
    - from: "src/tools/protobuf-decoder/ProtobufDecoder.tsx"
      to: "src/tools/protobuf-decoder/useDecode.ts"
      via: "decodeInput(raw, override) on input change"
      pattern: "decodeInput"
    - from: "src/tools/protobuf-decoder/ProtobufDecoder.tsx"
      to: "src/shell/usePreferences.ts"
      via: "preferences.protobufTreeStyle + setTreeStyle for the persisted toggle"
      pattern: "protobufTreeStyle|setTreeStyle"
    - from: "src/tools/protobuf-decoder/FieldNode.tsx"
      to: "src/tools/protobuf-decoder/interpretationChips.ts"
      via: "chipsForField + defaultChipId"
      pattern: "chipsForField"
    - from: "src/tools/protobuf-decoder/ProtobufDecoder.tsx"
      to: "src/lib/platform"
      via: "platform.clipboard.writeText for per-node copy + copy-all-as-JSON"
      pattern: "platform.clipboard"
    - from: "src/tools/protobuf-decoder/index.ts"
      to: "src/tools/protobuf-decoder/ProtobufDecoder.tsx"
      via: "component: ProtobufDecoder"
      pattern: "component: ProtobufDecoder"
---

<objective>
Ship the schema-less Protobuf hero UI into the shell's `<Outlet/>`: a resizable input/output split where pasted hex/base64 renders a recursive wire-format field tree instantly, each LEN node shows interpretation chips computed from the decoder's real `LenInterpretation` with a smart default + per-node override, VARINT nodes show zigzag+signed readings, nested messages auto-expand, the tree defaults to cards with a persisted rows/cards toggle, `#N` numbers are neutral (accent reserved for selection), every node has a visible focusable copy + a copy-all-as-JSON action, and a status bar reports parse·bytes·encoding·errors·timing. Renders thin components over the pure logic from 03-02 and persists the toggle via the 03-01 preference. Ends the phase with a human sign-off checkpoint covering both Phase-3 tools (PRO-01..07, UX-01..05).

Purpose: The product's hero. Proves explorability-at-a-glance and the ambiguity-resolution interaction on the real decoder output.
Output: ResizableSplit, FieldNode, FieldTree, ProtobufStatusBar, ProtobufDecoder, the registry swap, TDD coverage, and the phase-boundary checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-CONTEXT.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-RESEARCH.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-VALIDATION.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-02-SUMMARY.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-01-SUMMARY.md

<interfaces>
<!-- Consumed from 03-02 (pure logic) and 03-01 (prefs). Use these directly. -->
```typescript
// 03-02 — src/tools/protobuf-decoder/useDecode.ts
function decodeInput(raw: string, override?: "hex"|"base64"): {
  encoding: "hex"|"base64"; byteCount: number;
  fields: DecodedField[] | null; error: string | null; timingMs: number;
}
// 03-02 — src/tools/protobuf-decoder/interpretationChips.ts
interface Chip { id: string; label: string; value: string; }
function chipsForField(field: DecodedField): Chip[];
function defaultChipId(field: DecodedField): string;   // (or the combined-return shape 03-02 chose — check the SUMMARY)
// 03-02 — src/tools/protobuf-decoder/copyAsJson.ts
function fieldsToJson(fields: DecodedField[], selection: Map<string, string>): string;
// 03-01 — src/shell/usePreferences.ts
usePreferences(): { preferences: { protobufTreeStyle: "cards"|"rows"; ... }; prefsLoaded; setTreeStyle(s); ... }
```

```typescript
// REAL decoder shape (VERIFIED) — bind to this, NOT the mockup's invented keys.
interface DecodedField { fieldNumber: number; wireType: 0|1|2|5; value: FieldValue; }
// value.kind: "varint" | "i64" | "i32" | "len"
// len: value.interpretations.message?: DecodedField[]  ← sub-messages live HERE, not f.children
```

From src/lib/platform: `platform.clipboard.writeText(text)` (NEVER @tauri-apps/*).

Design tokens (src/index.css @theme + design/DevTools Mockup.html — VISUAL/DOM reference ONLY):
- Tree: `.tree-cards`(gap stack)/`.tree-rows`(border-divided), `.field`(card: `bg-card border border-bd rounded-[11px] p-…`; nested `.d1` uses a slightly lighter card — NOTE: `--color-card-2` is NOT yet defined in @theme; either add `--color-card-2: #1c1f26` to the `@theme` block in src/index.css or use an explicit utility — Claude's discretion).
- `.field-head` (flex row), `.fnum` (mockup uses `--accent` — OVERRIDE to `text-tx`/`text-tx-2`, NEVER text-accent — D-08/PRO-07), `.wire` (mono badge `text-tx-2`), `.field-bytes` (mono `text-tx-3`, `ml-auto`).
- `.chips`/`.chip`: chip = `font-mono text-tx-2 bg-input-bg border border-bd rounded`; `.chip.on` (selected) = `text-accent bg-accent-soft border-accent-line`; disabled chip MUST pair any opacity with `aria-disabled` + a non-opacity cue (NEVER opacity-only — D-17/UX-04).
- `.val`(mono value)/`.val.bad`(`text-bad`), `.submsg` (`pl-… border-l-2 border-accent-line` indent for nested).
- `.divider` (`cursor-col-resize`; the in-house ResizableSplit), `.workspace` grid `1fr 7px 1.05fr` (relative units only — UX-05).
- `.examples`/`.ex`(`.ex.on` = accent) — the example payload chips (D-03).
- copy: ship `.copy-btn`(always-visible) / `.copy-inline`(inline) — NEVER `.copy-btn.is-hover` / `data-copy="hover"` (D-10/UX-02).
- `.statusbar`/`.st-left`/`.st-right` (UX-03).

Example payloads (D-03 — Claude's discretion on exact bytes, here are correct ones):
- `{1:150}` canonical = hex `089601`
- nested sub-message (field 3 LEN containing {1:150}) = hex `1a03089601`
- packed varints (field 4, payload 03 8e02 9601 = [3,270,150]) = hex `2206038e029601`  ← VERIFY by decoding; adjust if needed
- UTF-8 string LEN (field 2 = "hi") = hex `120268 69` → `12026869`
Document the final example bytes you ship.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED→GREEN): ResizableSplit (in-house col-resize divider, D-09/PRO-05) + FieldNode/FieldTree (recursive tree, chips, copy)</name>
  <read_first>
    - src/tools/protobuf-decoder/interpretationChips.ts (03-02 — chip derivation + default; do NOT re-derive readings here)
    - src/tools/protobuf-decoder/useDecode.ts (03-02 — the DecodeResult shape)
    - src/components/Sidebar.tsx (accent = active-only precedent; focus-visible:ring-2 ring-accent)
    - design/DevTools Mockup.html (VISUAL only: .field/.field-head/.fnum/.wire/.chips/.chip/.val/.submsg/.divider; ship always/inline copy, never is-hover)
    - src/lib/protobuf/decoder.ts (sub-messages are value.interpretations.message: DecodedField[], NOT f.children)
  </read_first>
  <behavior>
    - ResizableSplit: renders left + right children with a draggable divider; pointer-drag (pointerdown→pointermove) updates the split ratio; uses relative grid units (no fixed px width on the panes); divider has cursor-col-resize and role/aria for the separator. jsdom test: rendering shows both children + a divider element with cursor-col-resize; a pointerdown+move updates an inline grid-template style (assert the style string changes).
    - FieldNode (varint): renders `#N` in a NEUTRAL class (assert the fnum element does NOT carry text-accent), a wire badge, and chips [uint64,int64,sint,bool]; default chip carries the `.on`/accent class; clicking another chip moves the accent (selection) to it; a visible focusable copy button copies the selected reading.
    - FieldNode (len with message): renders chips incl. "message" selected by default; the sub-message renders as an indented .submsg recursing FieldTree; the node is auto-expanded by default; a collapse control toggles it.
    - FieldNode (len bytes-only): single "bytes" chip selected; value = hex.
    - FieldTree: maps DecodedField[] → FieldNode list; supports cards vs rows style via a prop; keeps per-node selection + expansion state keyed by a STABLE path (e.g. "0", "0.2.1"), so a re-render with fresh field objects does not reset selection (Pitfall 2).
    - A disabled/unavailable chip is never signalled by opacity alone (aria-disabled / hidden).
  </behavior>
  <action>
RED: ResizableSplit.test.tsx + FieldTree.test.tsx (jsdom). Build DecodedField literals (varint, len-with-message, len-bytes-only) and assert the behaviors above, especially: `#N` is NOT accent (PRO-07), chips come from chipsForField, selection moves on click, sub-message recurses via interpretations.message, auto-expanded by default, stable-path selection survives a re-render.
GREEN:
1. ResizableSplit.tsx — `export function ResizableSplit({ left, right, initial=0.5, min=0.2 }: {...})`. A `div` with `display:grid` and inline `gridTemplateColumns` = `\`${ratio}fr 7px ${1-ratio}fr\`` (relative units, UX-05). A `<div role="separator" aria-orientation="vertical" className="cursor-col-resize ...">` between them. On pointerdown set a dragging flag + capture; on pointermove compute ratio from clientX relative to the container rect, clamp to [min, 1-min]; on pointerup release. ~30 lines, no library.
2. FieldNode.tsx — `export function FieldNode({ field, path, style, selection, onSelect, expanded, onToggleExpand }: {...})`. Head: `#${field.fieldNumber}` in `text-tx`/`text-tx-2` (NEVER text-accent — D-08); a wire-type badge (label by wireType: 0 VARINT,1 I64,2 LEN,5 I32; Claude's discretion on lucide icon); byte length for LEN (`ml-auto text-tx-3`); a visible `<button>` copy (lucide Copy, focus-visible ring) that copies the selected reading via the onCopy prop. Chips row from `chipsForField(field)`; the selected chip (selection.get(path) ?? defaultChipId(field)) gets accent classes `text-accent bg-accent-soft border-accent-line`, others neutral; clicking calls `onSelect(path, chip.id)`. Value: render the selected chip's value as a mono text node (`.val`; `text-bad` if it represents an error reading). If selected is "message": render a collapse toggle + (when expanded) `<FieldTree fields={field.value.interpretations.message!} parentPath={path} ... />` inside an indented `.submsg` (`pl-3.5 border-l-2 border-accent-line`). Default expanded=true for message nodes (D-05). Disabled-chip cue: aria-disabled + a non-opacity treatment.
3. FieldTree.tsx — `export function FieldTree({ fields, parentPath="", style, selection, onSelect, expanded, onToggleExpand, onCopyNode }: {...})`. Wrapper `className={style === "cards" ? "tree-cards flex flex-col gap-2" : "tree-rows flex flex-col"}`. Map each field to `<FieldNode key path={parentPath ? \`${parentPath}.${i}\` : String(i)} ... />`. Selection/expansion state is OWNED by ProtobufDecoder (Task 2) and threaded down; FieldTree is presentational + recursive.
Use TYPE imports from "@/lib/protobuf/decoder". NO @tauri-apps. NO hover-only copy. Commit RED+GREEN together.
  </action>
  <verify>
    <automated>npx vitest run src/tools/protobuf-decoder/ResizableSplit.test.tsx src/tools/protobuf-decoder/FieldTree.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/tools/protobuf-decoder/ResizableSplit.test.tsx src/tools/protobuf-decoder/FieldTree.test.tsx exits 0
    - grep -q "chipsForField" src/tools/protobuf-decoder/FieldNode.tsx (chips from the real keys)
    - grep -q "interpretations.message" src/tools/protobuf-decoder/FieldNode.tsx (sub-messages from the real key, not f.children)
    - grep -E "text-accent" src/tools/protobuf-decoder/FieldNode.tsx shows accent applied ONLY to chip selected/`.on` state, never to the `#N`/fnum element (manual: confirm the fnum span uses text-tx/text-tx-2)
    - grep -rE "data-copy=\"hover\"|is-hover|opacity-0" src/tools/protobuf-decoder/ returns nothing
    - grep -qE "fr 7px|gridTemplateColumns|cursor-col-resize" src/tools/protobuf-decoder/ResizableSplit.tsx (relative-unit divider, no fixed px panes)
    - A FieldTree test asserts selection persists across a re-render with fresh field objects (stable path keying)
  </acceptance_criteria>
  <done>An in-house resizable divider and a recursive cards/rows tree render the real decoder output: chips from LenInterpretation, neutral #N, accent only on the selected chip, auto-expanded sub-messages from interpretations.message, stable-path selection, visible focusable per-node copy — no hover-only, no library.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED→GREEN): ProtobufStatusBar + ProtobufDecoder root + registry swap (PRO-01/02/06, D-01/03/07/10/11, UX-01/03)</name>
  <read_first>
    - src/tools/protobuf-decoder/useDecode.ts + interpretationChips.ts + copyAsJson.ts (03-02)
    - src/tools/protobuf-decoder/FieldTree.tsx + ResizableSplit.tsx (Task 1)
    - src/shell/usePreferences.ts (03-01 — preferences.protobufTreeStyle + setTreeStyle; prefsLoaded)
    - src/tools/base64/StatusBar.tsx (03-03 — reuse the shared StatusBar if its props fit, OR co-locate a ProtobufStatusBar; document the choice. Import across tool dirs is acceptable since both are app source.)
    - src/tools/protobuf-decoder/index.ts (the makePlaceholder swap point)
    - design/DevTools Mockup.html (.examples/.ex, .statusbar, .pane-top copy-all)
  </read_first>
  <behavior>
    - Renders an input textarea (left pane) + a decoded tree (right pane) inside ResizableSplit; no decode button.
    - onChange of the input calls decodeInput(raw, override) and renders fields OR the error string; empty input = neutral empty state (no error).
    - Pasting a group byte ("1c") shows the error in the status bar + an inline error state; the tool does NOT crash (PRO-02).
    - A detected-encoding indicator + a manual hex/base64 override toggle (D-01); the status bar shows the active encoding.
    - Example payload chips (D-03): clicking one fills the input with that payload and decodes it; the active example carries accent.
    - A rows/cards toggle reads preferences.protobufTreeStyle (default cards) and calls setTreeStyle on change; the choice persists (verified on real webview, Pitfall 6).
    - A "copy all as JSON" button in the output pane header calls fieldsToJson(fields, selection) → platform.clipboard.writeText; visible + focusable, not hover-only.
    - Per-node copy (from FieldNode) writes the selected reading via platform.clipboard.
    - Status bar: parse state · byte count · detected encoding · error · timingMs.
    - Selection + expansion state owned here (Map<string,string> / Set<string> keyed by stable path), threaded into FieldTree; pruned/reset when input changes structurally.
  </behavior>
  <action>
RED: ProtobufDecoder.test.tsx (jsdom) asserting: typing "089601" renders a field tree (no button); typing "1c" shows an error and does not throw; empty input shows neutral empty state; clicking an example chip fills + decodes; toggling rows/cards calls setTreeStyle and re-renders the tree style; copy-all button calls platform.clipboard.writeText with JSON (spy via setPlatformForTest); the override toggle forces base64. Spy on platform.clipboard via makeMemoryPlatform.
GREEN:
1. ProtobufStatusBar.tsx — reuse 03-03's `StatusBar` if its props (`parseState, byteCount, encoding, error, timingMs`) fit; otherwise a thin local wrapper. Document the decision in the SUMMARY.
2. ProtobufDecoder.tsx — default-export. State: `const [raw, setRaw] = useState("")`, `const [override, setOverride] = useState<"hex"|"base64"|undefined>(undefined)`, `const result = useMemo(() => decodeInput(raw, override), [raw, override])`, `const [selection, setSelection] = useState<Map<string,string>>(new Map())`, `const [collapsed, setCollapsed] = useState<Set<string>>(new Set())` (expanded-by-default → track the collapsed set, D-05), `const { preferences, setTreeStyle } = usePreferences()`. Layout: `<ResizableSplit left={<inputPane/>} right={<outputPane/>} />` inside a `flex flex-col min-w-0 h-full` root; bottom `<ProtobufStatusBar parseState={result.error ? "error" : raw.trim()==="" ? "empty" : "ok"} byteCount={result.byteCount} encoding={result.encoding} error={result.error} timingMs={result.timingMs} />`. Input pane: examples row (.examples; each `.ex` button sets raw to a documented payload + the active one gets accent), a `<textarea value={raw} onChange={e=>setRaw(e.target.value)} className="w-full font-mono bg-input-bg ... focus-visible:ring-2 focus-visible:ring-accent">` (no fixed width), a hex/base64 override toggle (active = accent). Output pane: a header with a rows/cards toggle (reads `preferences.protobufTreeStyle`, onChange → `setTreeStyle(next)`; active = accent) + a visible focusable "copy all as JSON" `<button>` → `void platform.clipboard.writeText(fieldsToJson(result.fields ?? [], selection))`; below it either the error state (`text-bad` + message) or `<FieldTree fields={result.fields ?? []} style={preferences.protobufTreeStyle} selection={selection} onSelect={(path,id)=>setSelection(prev=>new Map(prev).set(path,id))} collapsed={collapsed} onToggleExpand={...} onCopyNode={(text)=>void platform.clipboard.writeText(text)} />`. Reset selection/collapsed when `raw` changes structurally (Claude's discretion: simplest correct approach is to keep them; prune is optional — but ensure no crash when paths disappear). NO @tauri-apps. NO hover-only copy. NO fixed widths.
3. index.ts — replace `makePlaceholder("Protobuf Decoder")` with `import ProtobufDecoder from "./ProtobufDecoder";` and `component: ProtobufDecoder`. Keep all other registry fields. Do NOT edit registry.ts/types.ts/decoder.ts/bytes.ts.
Commit RED+GREEN together.
  </action>
  <verify>
    <automated>npx vitest run src/tools/protobuf-decoder/ProtobufDecoder.test.tsx && npx tsc --noEmit && grep -L "makePlaceholder" src/tools/protobuf-decoder/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/tools/protobuf-decoder/ProtobufDecoder.test.tsx exits 0
    - grep -q "makePlaceholder" src/tools/protobuf-decoder/index.ts returns NOTHING
    - grep -q "component: ProtobufDecoder" src/tools/protobuf-decoder/index.ts
    - grep -qE "protobufTreeStyle|setTreeStyle" src/tools/protobuf-decoder/ProtobufDecoder.tsx (persisted toggle wired)
    - grep -q "decodeInput" src/tools/protobuf-decoder/ProtobufDecoder.tsx ; no `<button>` labelled decode/submit triggers decoding (decode is on change — PRO-01)
    - grep -q "fieldsToJson" src/tools/protobuf-decoder/ProtobufDecoder.tsx ; grep -q "platform.clipboard" src/tools/protobuf-decoder/ProtobufDecoder.tsx
    - grep -q "@tauri-apps" src/tools/protobuf-decoder/ProtobufDecoder.tsx returns nothing
    - grep -rE "data-copy=\"hover\"|is-hover|opacity-0|w-\[[0-9]|min-w-\[[0-9]" src/tools/protobuf-decoder/ProtobufDecoder.tsx returns nothing
    - npx vitest run src/lib/protobuf/decoder.test.ts still 19 passing
  </acceptance_criteria>
  <done>The protobuf tool decodes on paste (no button), surfaces errors without crashing, offers example chips + a manual encoding override, persists the cards/rows toggle via the prefs seam, exposes per-node copy + copy-all-as-JSON via the platform seam, and reports the status bar — registry component swapped from the placeholder; layout-agnostic; no port-unchanged file edited.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-boundary sign-off — both tools on a fresh tauri build + WCAG-AA audit</name>
  <what-built>The complete Phase-3 surface: the schema-less Protobuf hero (resizable split, instant paste→tree, LenInterpretation chips with smart default + per-node override, VARINT zigzag/signed, auto-expanded sub-messages, neutral #N, persisted cards/rows toggle, per-node + copy-all-as-JSON, status bar) and the Base64/Hex/Bytes tool — both layout-agnostic in the shell Outlet, replacing the Phase-2 placeholders.</what-built>
  <how-to-verify>
PRELUDE (automated, before sign-off): full suite green — `npm test` (vitest, incl. decoder's 19) + `npx tsc --noEmit` + `eslint .` all clean; then `bash scripts/e2e-spike.sh` passes (focusable copy present, instant paste, no hover-only copy for the real tools). Then `tauri build` produces a runnable bundle.
MANUAL on the real WKWebView (`tauri dev` and/or the built .app):
1. Open Protobuf Decoder. Paste hex `089601` → field tree renders INSTANTLY (no button), field #1 varint shows uint64=150 + sint + signed + bool chips; #N is NEUTRAL (not blue) — PRO-01/04/07.
2. Paste a nested payload (e.g. `1a03089601`) → the LEN node shows a "message" chip selected by default and the sub-message is AUTO-EXPANDED and indented — PRO-03/D-05. Select a different chip on a node → accent moves to it; the value updates — PRO-04.
3. Paste a group byte `1c` → status bar shows the "groups are not supported" error + an inline error state; the tool does NOT white-screen — PRO-02.
4. Drag the divider → both panes resize smoothly — PRO-05.
5. Toggle rows/cards → layout changes; relaunch the app → the choice PERSISTED (Pitfall 6, packaged store) — PRO-06.
6. Tab to a node copy button + the "copy all as JSON" button (visible focus ring, keyboard-reachable, NOT hover-only); copy → paste into another app → correct value / pretty JSON with field numbers as keys — UX-02/D-10/D-11.
7. Open Base64 tool → re-confirm the 03-03 checks (instant derive, alphabet toggle, per-field error, focusable copy, status bar) hold in the built app — ENC-01..03, UX-01..05.
8. `gsd-ui-review` WCAG-AA audit passes (visible focus, AA contrast on the dark palette, no opacity-only disabled chips) — UX-04/D-17.
  </how-to-verify>
  <files>src/tools/protobuf-decoder/ProtobufDecoder.tsx, src/tools/protobuf-decoder/index.ts, src/tools/base64/Base64Tool.tsx</files>
  <action>Run the prelude automated gate (npm test full suite incl. decoder 19; npx tsc --noEmit; eslint .; bash scripts/e2e-spike.sh; tauri build), then launch the built .app / `tauri dev` and walk steps 1-8 on the real WKWebView, including relaunch to confirm the rows/cards pref persisted from prefs.json (Pitfall 6). Run the gsd-ui-review WCAG-AA audit and record the result. This is the phase-boundary human checkpoint — pause for the user to sign off.</action>
  <verify>Manual: user confirms steps 1-8 + persistence-on-relaunch on the real WKWebView and gsd-ui-review passes. Prelude automated: `npm test` green (decoder 19 included), `npx tsc --noEmit` + `eslint .` clean, `bash scripts/e2e-spike.sh` passes, `tauri build` produces a runnable bundle.</verify>
  <done>User types "approved" after signing off both tools on a fresh tauri build with a passing gsd-ui-review WCAG-AA audit; Phase 3 closed.</done>
  <resume-signal>Type "approved" to close Phase 3, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted blob → decoder | Fully untrusted protobuf input |
| on-disk prefs.json → tree style | Untrusted persisted value (hand-editable) |
| tool → OS clipboard | Per-node + copy-all writes cross into the OS via the seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | DoS | paste of a huge/deeply-nested blob | mitigate | `decodeMessage`'s `MAX_PAYLOAD_BYTES`+`MAX_DEPTH` guards (untouched) throw; `decodeInput` (03-02) catches → status-bar error; the UI stays responsive and editable. Auto-expand depth is bounded by `MAX_DEPTH`. |
| T-03-11 | Tampering/Injection | decoded strings + JSON rendered/copied | mitigate | All decoded strings render as React text nodes (escaped); never `dangerouslySetInnerHTML`. copy-all uses `fieldsToJson` (JSON.stringify) → `platform.clipboard.writeText`; no executable payload. |
| T-03-12 | Tampering | protobufTreeStyle read from disk | mitigate | Coerced by `coerceTreeStyle` in `mergePreferences` (03-01) — only "rows" accepted, else "cards". |
| T-03-13 | Elevation of Privilege | clipboard capability | mitigate | All copy via `platform.clipboard`; no `@tauri-apps/*` import in the tool (grep-verified). |
</threat_model>

<verification>
- `npx vitest run src/tools/protobuf-decoder/ResizableSplit.test.tsx src/tools/protobuf-decoder/FieldTree.test.tsx src/tools/protobuf-decoder/ProtobufDecoder.test.tsx` exits 0.
- `npm test` full suite green (decoder's 19 included); `npx tsc --noEmit` + `eslint .` clean.
- `bash scripts/e2e-spike.sh` passes for the real tools; `tauri build` produces a runnable bundle.
- Per-task harness in order: /simplify → /codex:review (--wait --scope working-tree) → vitest+tsc → real-webview UI. Phase boundary: human sign-off (Task 3) + `gsd-ui-review` WCAG-AA.
</verification>

<success_criteria>
- Paste→instant recursive tree (no button); groups/truncation as errors not crashes; chips from real `LenInterpretation`; smart default + per-node override; VARINT zigzag+signed; auto-expanded sub-messages; resizable panes; cards default + persisted rows/cards toggle; neutral #N (accent = selection only); per-node + copy-all-as-JSON with visible focusable copy (no hover-only); status bar parse·bytes·encoding·errors·timing.
- Registry `component` swapped from `makePlaceholder` to `ProtobufDecoder`; layout-agnostic; no port-unchanged file edited; no `@tauri-apps/*` import; `Store` seam not widened.
- Phase signed off on a fresh `tauri build` + passing `gsd-ui-review` WCAG-AA audit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-04-SUMMARY.md`
</output>
