---
phase: 23-appearance-pane
plan: 04
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/components/AppearanceSettings.tsx
  - src/components/AppearanceSettings.test.tsx
  - src/components/ThemeCardGroup.tsx
  - src/components/ThemeCardGroup.test.tsx
  - src/components/AccentSwatchGrid.tsx
  - src/components/AccentSwatchGrid.test.tsx
  - src/components/AppearancePreviewStrip.tsx
  - src/components/settingsPanes.tsx
autonomous: true
requirements: [SET-07]
must_haves:
  truths:
    - "Settings ▸ Appearance shows three theme radio cards (Dark/Light/System) + a 7-swatch accent radiogroup + a contained PREVIEW strip"
    - "Selecting a theme/accent updates LOCAL pending state + the PREVIEW strip ONLY — the whole app does NOT change before Save"
    - "A Pro user's Save calls setTheme(pending) + setAccent(pending) (persists via the seam; the App root then applies live)"
    - "A free user's Save opens the focused Unlock-Pro path via openProUpsell(saveButtonEl) and persists NOTHING"
    - "The Save button shows a visible locked state (Unlock Pro to save + lock glyph), not opacity-only, and stays keyboard-reachable"
    - "Theme cards + accent swatches are accessible radiogroups (arrow nav, aria-checked, visible focus ring, selection not by color alone)"
  artifacts:
    - path: "src/components/AppearanceSettings.tsx"
      provides: "The Appearance pane — pending state, theme cards, swatch grid, preview strip, gate-on-Save"
      exports: ["AppearanceSettings"]
    - path: "src/components/settingsPanes.tsx"
      provides: "The appended {id:appearance,label,icon,render} entry"
      contains: "appearance"
  key_links:
    - from: "src/components/AppearanceSettings.tsx"
      to: "openProUpsell"
      via: "free-tier Save route"
      pattern: "openProUpsell"
    - from: "src/components/settingsPanes.tsx"
      to: "AppearanceSettings"
      via: "SETTINGS_PANES append (no shell change)"
      pattern: "AppearanceSettings"
---

<objective>
Build the Appearance pane (D-23-3/D-23-6/D-23-7/D-23-10): three theme radio cards, a 7-swatch
accent radiogroup, a contained PREVIEW strip reflecting the pending selection, and a Save
button that gates on entitlement — Pro persists via the prefs seam (the App root then applies
live), free routes to the focused Unlock-Pro path and persists NOTHING (try-before-buy). The
whole app never changes before Save (only the contained strip does — no revert logic). Append
ONE entry to SETTINGS_PANES (zero SettingsModal shell change).

Purpose: D-23-3 (preview strip + gate-on-Save), D-23-6 (theme cards, NOT SegmentedControl),
D-23-7 (7 AA-tuned swatches, accent reserved for selection), D-23-10 (append-only pane).
Output: 4 new components + their tests + the settingsPanes append.

Wave 2 — depends on Plan 01 (ACCENT_SCALE, ThemeName, DEFAULT_ACCENT). Parallel with Plan 03
(no file overlap: Plan 03 owns App.tsx/index.html/useAppearance, this owns the pane + registry).
Plan 03's appearance.e2e.ts drives THIS pane — the phase-boundary e2e + walkthrough live in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-appearance-pane/23-CONTEXT.md
@.planning/phases/23-appearance-pane/23-RESEARCH.md
@.planning/phases/23-appearance-pane/23-01-SUMMARY.md

<interfaces>
From src/shell/appearance.ts (Plan 01):
```ts
export interface AccentPair { key: string; label: string; dark: string; light: string; }
export const ACCENT_SCALE: readonly AccentPair[];   // 7 swatches; display = .dark hex; persist = .dark hex
export const DEFAULT_ACCENT = "#5b9bf8";
```
From src/shell/usePreferences.ts:
```ts
const { preferences, setTheme, setAccent } = usePreferences(); // preferences.theme, preferences.accent
// setTheme(t: ThemeName), setAccent(a: string) — persist via the seam (App root applies live)
```
From src/shell/useEntitlements.ts + entitlements.ts:
```ts
const ents = useEntitlements(); const entitled = ents.has(ENT_THEMING);  // import ENT_THEMING
```
From src/shell/proUpsell.ts:
```ts
export function openProUpsell(invokerEl?: HTMLElement | null): void;  // free→pitch modal, lapsed→recovery
```
From src/components/settingsPanes.tsx (CURRENT — append one entry):
```tsx
export const SETTINGS_PANES: SettingsPane[] = [
  { id: "license", label: "License", icon: Settings, render: () => <LicenseSettings /> },
];  // SettingsPane = { id; label; icon: ComponentType<{className?:string}>; render: () => ReactNode }
```
The LicenseSettings pane root is `flex flex-col gap-6 overflow-auto p-8` — match that wrapper so the
modal hosts AppearanceSettings with no double-pad (Pitfall 5 from Phase 22). The Appearance pane header
is an `<h3>` (one level under the dialog `<h2>` — preserves the Phase-22.1 heading-order a11y fix); do NOT use `<h2>`.
Reuse SegmentedControl (src/components/SegmentedControl.tsx) as the PREVIEW strip's sample
"toggle" ONLY — NOT the theme picker (D-23-6). Icons: lucide `Contrast` (pane), `Lock`
(Save lock), `Check` (selected swatch).
The protobuf `#N` field number is NEUTRAL (`text-tx`) — the preview's `uint` chip sample
must color the SELECTED chip with accent (matching FieldNode's `chip-on` = accent-soft+accent),
never the field number.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThemeCardGroup + AccentSwatchGrid accessible radiogroups (D-23-6/D-23-7)</name>
  <files>src/components/ThemeCardGroup.tsx, src/components/ThemeCardGroup.test.tsx, src/components/AccentSwatchGrid.tsx, src/components/AccentSwatchGrid.test.tsx</files>
  <read_first>
    - src/shell/appearance.ts (ACCENT_SCALE — render .dark as the swatch display color)
    - src/components/SegmentedControl.tsx (the project's radiogroup-vs-aria-pressed reference; do NOT use it for the picker)
    - src/tools/protobuf-decoder/FieldNode.tsx lines 104-129 (the existing role="radiogroup"/role="radio"/aria-checked + focus-visible:ring-accent pattern — mirror it; note: FieldNode's chips are click-only, it has NO arrow handling)
    - src/components/Sidebar.tsx (~line 290+: the ArrowUp/ArrowDown roving-focus handler — mirror THIS for the radiogroup arrow-key nav)
    - 23-RESEARCH.md "Pattern 6" (accessible radio groups; selection not by color alone) + D-23-6 (mini app-preview thumbnails) + D-23-7 (selected = ring + Check glyph)
  </read_first>
  <behavior>
    - ThemeCardGroup: renders exactly 3 cards labelled Dark / Light / System; role="radiogroup"; each card role="radio" with aria-checked reflecting the `value` prop
    - Clicking a card calls onChange with "dark"|"light"|"system"
    - Arrow keys move selection within the group (Left/Up → previous, Right/Down → next, clamp — match Sidebar's ArrowUp/ArrowDown roving convention); the selected card has aria-checked=true + a visible ring (focus-visible:ring-2 ring-accent)
    - AccentSwatchGrid: renders 7 swatches from ACCENT_SCALE; role="radiogroup"; each role="radio" aria-checked; the swatch fill = the pair.dark hex; the SELECTED swatch shows a Check glyph + ring (NOT color-alone — WCAG 1.4.1)
    - Clicking a swatch calls onChange with the pair.dark hex; each swatch has an accessible name (aria-label = the label, e.g. "Violet")
  </behavior>
  <action>
    Create `src/components/ThemeCardGroup.tsx`: `{ value: ThemeName; onChange: (t: ThemeName) => void }`. Render a `role="radiogroup"` aria-label="Theme" containing three `role="radio"` buttons (Dark/Light/System), each a layout-agnostic card (responsive Tailwind, NO fixed px widths — use flex/grid + min-w-0) with a mini app-preview thumbnail (a small stylized swatch of sidebar+pane+accent using the token classes — e.g. a tiny two-pane div with `bg-sidebar`/`bg-pane` + an `bg-accent` dot; the LIGHT card thumbnail can hardcode the light hexes for illustration since it previews the light theme). Selected card: `aria-checked`, `border-accent-line bg-accent-soft`, visible `focus-visible:ring-2 ring-accent`. Arrow-key handler mirroring Sidebar's roving ArrowUp/ArrowDown handler (track index, clamp at ends — no wrap, like the sidebar). FieldNode is the role="radio"/aria-checked structural reference only (it has no arrow handling).

    Create `src/components/AccentSwatchGrid.tsx`: `{ value: string; onChange: (darkHex: string) => void }`. `role="radiogroup"` aria-label="Accent color", a responsive grid (e.g. `flex flex-wrap gap-2` or `grid grid-cols-7`-down-to-fewer, layout-agnostic) of 7 round `role="radio"` buttons. Each: `style={{ backgroundColor: pair.dark }}`, `aria-label={pair.label}`, `aria-checked={value.toLowerCase() === pair.dark.toLowerCase()}`. Selected: a ring (`ring-2 ring-offset-2 ring-accent` or a contrasting outline) AND a centered `<Check>` glyph (so selection is not color-alone). `focus-visible:ring-2`. onChange(pair.dark).

    Create `ThemeCardGroup.test.tsx` + `AccentSwatchGrid.test.tsx` (RTL): assert 3 cards / 7 swatches, role/aria-checked, onChange fires with the right value on click, arrow-key nav moves selection, the selected swatch renders the Check glyph (query by role/test-id), each swatch has its accessible name.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/ThemeCardGroup.test.tsx src/components/AccentSwatchGrid.test.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'role="radiogroup"' src/components/ThemeCardGroup.tsx src/components/AccentSwatchGrid.tsx` matches both
    - ThemeCardGroup renders exactly 3 radios (Dark/Light/System); AccentSwatchGrid renders exactly 7 (driven by ACCENT_SCALE.length, not hardcoded)
    - The selected swatch renders a Check glyph (selection NOT by color alone — grep `Check` import + test asserts it visible when selected)
    - No fixed-width px on the card/grid containers (layout-agnostic — grep shows responsive Tailwind, no `w-[NNNpx]`)
    - `pnpm vitest run` of both test files exits 0; arrow-nav + onChange + aria-checked covered
    - SegmentedControl is NOT imported by ThemeCardGroup (D-23-6) — `grep SegmentedControl src/components/ThemeCardGroup.tsx` returns nothing
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Two accessible radiogroup components (3 theme cards, 7 accent swatches) with arrow nav, aria-checked, focus rings, and non-color-alone selection; tested; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppearanceSettings pane — pending state, preview strip, gate-on-Save (D-23-3/D-23-2)</name>
  <files>src/components/AppearanceSettings.tsx, src/components/AppearanceSettings.test.tsx, src/components/AppearancePreviewStrip.tsx</files>
  <read_first>
    - src/components/ThemeCardGroup.tsx + AccentSwatchGrid.tsx (Task 1)
    - src/components/SegmentedControl.tsx (the preview strip's sample toggle)
    - src/tools/protobuf-decoder/FieldNode.tsx lines 60-129 (the `uint`/`chip-on` accent styling + the NEUTRAL `#N` `text-tx` — the preview strip mimics this: accent only on the selected chip)
    - src/shell/usePreferences.ts (preferences/setTheme/setAccent) + src/shell/useEntitlements.ts + src/shell/proUpsell.ts (openProUpsell) + src/lib/entitlements/entitlements.ts (ENT_THEMING)
    - src/components/LicenseSettings.tsx (the pane wrapper convention: its root is `flex flex-col gap-6 overflow-auto p-8` — match it)
    - 23-RESEARCH.md "Pattern 5" (pending state + gate-on-Save) + D-23-2 (free Save → openProUpsell, no persist) + D-23-3 (lock on the Save button, visible not opacity-only; preview strip = Decoder nav item · Activate button · uint chip · a toggle)
    - 23-CONTEXT.md "Specific Ideas" (subtitle copy: "Personalize how TinkerDev looks on this device." / accent helper "Used for the active tool, selections, and focus.")
  </read_first>
  <behavior>
    - On mount, pendingTheme = preferences.theme, pendingAccent = preferences.accent
    - Selecting a theme card / accent swatch updates ONLY the pending state (not the seam, not documentElement) — and the PREVIEW strip reflects pendingTheme + pendingAccent
    - Pro (ents.has(ENT_THEMING)) Save: calls setTheme(pendingTheme) AND setAccent(pendingAccent); does NOT call openProUpsell
    - Free Save: calls openProUpsell(saveButtonEl) and does NOT call setTheme/setAccent (persists nothing)
    - Free: the Save button reads "Unlock Pro to save" + a Lock glyph (visible locked state, NOT opacity-only); it is type=button, keyboard-reachable, fires the upsell on Enter/Space/click
    - Pro: the Save button reads "Save" (no lock); disabled/no-op only when pending === persisted (optional — your discretion, but it must be enabled whenever there is a pending change)
    - The component never writes to document.documentElement (the contained-preview invariant, D-23-3)
  </behavior>
  <action>
    Create `src/components/AppearancePreviewStrip.tsx`: `{ theme: ThemeName; accent: string }`. A CONTAINED sample box (its own bordered region) showing the D-23-3 sample set: a Decoder-style nav item, an "Activate"-style accent button, a `uint` chip (mimic FieldNode `chip-on`: `border-accent-line bg-accent-soft text-accent`), and a sample `SegmentedControl` toggle. The strip must reflect the PENDING theme+accent WITHOUT touching documentElement — apply the pending look by scoping: wrap the strip in a div that sets `data-theme` locally via a wrapper attribute is NOT enough (CSS [data-theme] is rooted at :root). Simplest correct approach: drive the strip's sample colors from inline style vars on the strip's root element — set `style={{ ["--color-accent" as string]: accentForTheme(accent, resolveEffectiveTheme(theme)) }}` on the strip wrapper so the accent-dependent samples recolor, and for the light/dark surface preview render the sample using explicit light vs dark token values chosen by `resolveEffectiveTheme(theme)` (a small local lookup of the surface hexes for the preview only). Keep it contained — NEVER set anything on document.documentElement. Document this clearly (the strip is a localized preview, the whole-app apply is App-root only).

    Create `src/components/AppearanceSettings.tsx`: the pane. Wrapper matching LicenseSettings (`flex flex-col gap-6 overflow-auto p-8`). Header: an `<h3>` "Appearance" (one level under the dialog `<h2>` — preserves the Phase-22.1 heading-order fix; never `<h2>`) + subtitle "Personalize how TinkerDev looks on this device." A "Theme" section → `<ThemeCardGroup value={pendingTheme} onChange={setPendingTheme} />`. An "Accent color" section + helper "Used for the active tool, selections, and focus." → `<AccentSwatchGrid value={pendingAccent} onChange={setPendingAccent} />`. A "Preview" section → `<AppearancePreviewStrip theme={pendingTheme} accent={pendingAccent} />`. A Save row:
    ```tsx
    const entitled = ents.has(ENT_THEMING);
    const saveRef = useRef<HTMLButtonElement>(null);
    function onSave() {
      if (!entitled) { openProUpsell(saveRef.current); return; }   // D-23-2: no persist
      setTheme(pendingTheme); setAccent(pendingAccent);            // App root applies live
    }
    ```
    Save button: when `!entitled` render the lock affordance — `<Lock/>` glyph + label "Unlock Pro to save" (visible, full-color, keyboard-reachable, focus-visible:ring-2 ring-accent — NOT opacity-only). When entitled, label "Save". `type="button"`, `ref={saveRef}`, `onClick={onSave}`.

    Create `AppearanceSettings.test.tsx` (RTL, mock usePreferences/useEntitlements/openProUpsell via vi.mock): Pro Save → setTheme+setAccent called with pending values, openProUpsell NOT called. Free Save → openProUpsell called with the button el, setTheme/setAccent NOT called. Free button shows "Unlock Pro to save" + is enabled/keyboard-reachable. Selecting a card/swatch updates the preview without calling the setters (assert setters not called on selection).
  </action>
  <verify>
    <automated>pnpm vitest run src/components/AppearanceSettings.test.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'openProUpsell' src/components/AppearanceSettings.tsx` matches (free Save route) and is called ONLY in the `!entitled` branch
    - `grep -c 'document.documentElement' src/components/AppearanceSettings.tsx src/components/AppearancePreviewStrip.tsx` === 0 (contained preview, never the root — D-23-3)
    - The free Save button label is "Unlock Pro to save" with a `Lock` glyph (grep both); it is not disabled/opacity-gated
    - `pnpm vitest run src/components/AppearanceSettings.test.tsx` exits 0: Pro→setters, free→upsell-no-persist, selection-does-not-persist all green
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AppearanceSettings holds pending state, drives the contained preview strip, and gates Save (Pro persists, free → openProUpsell no-persist) with a visible lock; never touches documentElement; tested; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Append the Appearance entry to SETTINGS_PANES (D-23-10)</name>
  <files>src/components/settingsPanes.tsx</files>
  <read_first>
    - src/components/settingsPanes.tsx (the SettingsPane interface + the array — append one entry, no shell change)
    - src/components/AppearanceSettings.tsx (Task 2 — the render target)
    - src/components/SettingsModal.tsx lines 186-224 (confirm the nav + content derive 1:1 from SETTINGS_PANES — no edit needed here)
    - 23-RESEARCH.md "Append the pane (D-23-10)" code example + D-23-10 (icon = a contrast/half-circle glyph; nav shows only real panes — no Notifications/Keyboard)
  </read_first>
  <action>
    In `src/components/settingsPanes.tsx`: import `{ Contrast }` from lucide-react and `{ AppearanceSettings }` from `./AppearanceSettings`. Append a second entry to `SETTINGS_PANES` AFTER the license entry:
    ```tsx
    { id: "appearance", label: "Appearance", icon: Contrast, render: () => <AppearanceSettings /> },
    ```
    Do NOT add any "Notifications"/"Keyboard" entries (those mockup labels are illustrative — out of scope, D-23-10). Do NOT change SettingsModal.tsx (its nav + content derive from the array). Verify the modal now lists License + Appearance.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm lint && grep -q '"appearance"' src/components/settingsPanes.tsx && grep -q 'AppearanceSettings' src/components/settingsPanes.tsx && grep -q 'Contrast' src/components/settingsPanes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"appearance"' src/components/settingsPanes.tsx` matches (the appended entry id)
    - `grep 'AppearanceSettings' src/components/settingsPanes.tsx` matches (import + render)
    - `grep -c 'Notifications\|Keyboard' src/components/settingsPanes.tsx` === 0 (no out-of-scope panes)
    - SettingsModal.tsx is byte-unchanged (the registry is the single control plane — `git diff --stat src/components/SettingsModal.tsx` shows no change)
    - `pnpm exec tsc --noEmit` + `pnpm lint` exit 0
  </acceptance_criteria>
  <done>SETTINGS_PANES has the Appearance entry (License + Appearance only); SettingsModal unchanged; tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| free user → Save | A non-entitled user must not be able to persist a Pro theme/accent through the pane. |
| pending state → whole app | The contained preview must not leak into the global app appearance before Save. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Elevation of Privilege | AppearanceSettings Save | mitigate | A free Save routes to `openProUpsell` and never calls setTheme/setAccent — persists NOTHING (D-23-2). Even if a free user forced a persist (hand-edited prefs.json), the App-root `gatePreferences` (Plan 03) re-forces defaults — defense in depth. Pinned by the Task-2 free-Save test. Client-side UX gate by design (v1.6 model — accepted, not a server secret). |
| T-23-08 | Tampering | contained preview strip | mitigate | The preview strip + pending state NEVER write to document.documentElement (asserted: grep === 0) — the global app appearance is changed ONLY by the App-root effect on a persisted (gated) value. This prevents a preview from leaking Pro appearance to a free user's whole app pre-Save and removes any revert path (D-23-3). |
</threat_model>

<verification>
- `pnpm vitest run src/components/ThemeCardGroup.test.tsx src/components/AccentSwatchGrid.test.tsx src/components/AppearanceSettings.test.tsx` green.
- Full `pnpm vitest run` + `tsc --noEmit` + `pnpm lint` clean.
- `grep document.documentElement` in the pane + preview components returns nothing (contained preview).
- SETTINGS_PANES lists License + Appearance only; SettingsModal.tsx unchanged.
- The live whole-app apply + persistence are proven by Plan 03's appearance.e2e.ts (which drives THIS pane) on the real WKWebView.
- Decoder + its 19 tests byte-for-byte untouched.
- Harness order per task: /simplify → /codex:review --wait --scope working-tree → unit (vitest+tsc+eslint) → real-WKWebView e2e (Plan 03's spec covers this pane) → phase boundary (Plan 03 checkpoint).
</verification>

<success_criteria>
- The Appearance pane renders 3 theme cards + 7 accent swatches + a contained preview strip, all accessible radiogroups.
- Selecting updates pending state + the preview ONLY; the whole app does not change pre-Save.
- Pro Save persists via setTheme/setAccent; free Save opens openProUpsell and persists nothing, with a visible (non-opacity) lock on the Save button.
- One Appearance entry appended to SETTINGS_PANES; SettingsModal shell unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/23-appearance-pane/23-04-SUMMARY.md`
</output>
