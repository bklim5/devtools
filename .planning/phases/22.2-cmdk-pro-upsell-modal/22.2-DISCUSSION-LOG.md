# Phase 22.2: Pro-gate ⌘K + focused upsell modal - Discussion Log

> **Audit trail only.** Do not use as input to planning/execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-16
**Phase:** 22.2-cmdk-pro-upsell-modal
**Origin:** mid-Phase-23 discussion; user reconsidered the 22.1 "redirect every locked trigger to Settings" decision.

---

## Contextual locked-trigger UX

| Option | Description | Selected |
|--------|-------------|----------|
| Focused upsell modal | Light modal reusing the shared ActivationSurface; sidebar-local state; explicit entries still → Settings | ✓ (refined) |
| Keep current (→ Settings ▸ License) | 22.1 status quo | |
| Inline popover near control | Lightest, new pattern | |

**User's choice:** Focused modal — but asked to verify sidebar-local state feasibility, noting the command palette "doesn't seem to be in sidebar".

**Finding:** all contextual locked triggers (pin/drag/Alt+P/Reset) are in the Sidebar; the ⌘K palette has NO locked customization commands today. Sidebar-local WOULD suffice — until the ⌘K-gating decision below made a shared trigger necessary.

---

## ⌘K command palette scope

| Option | Description | Selected |
|--------|-------------|----------|
| Keep ⌘K free | Core keyboard wedge stays free | |
| Gate whole ⌘K behind Pro | Free users can't open it | ✓ |
| Add Pro-only ⌘K commands later | Keep palette free, gate future commands | |

**User's choice:** Gate the whole ⌘K behind Pro. Confirmed knowingly after Claude flagged that ⌘K is the primary fast tool-switcher for the 11 free tools and that this reverses SET-04.

---

## Free-user ⌘K behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Focused Unlock-Pro modal | ⌘K → same focused modal as contextual triggers | ✓ |
| Palette opens, locked | Palette UI with locked rows | |
| No-op / silent | ⌘K does nothing | |

**User's choice:** Focused Unlock-Pro modal. → Implies ONE shared trigger (the ⌘K handler is not in the Sidebar), confirming the user's instinct; sidebar-local state is insufficient → a minimal shared `upsellStore`.

---

## Timing

| Option | Description | Selected |
|--------|-------------|----------|
| Now, before Phase 23 | Small follow-up first | ✓ |
| After Phase 23 | Note + continue Appearance | |

**User's choice:** Now, before Phase 23.

## Claude's Discretion
- Entitlement mechanism decided by Claude (feasibility): `isPro(ents)` predicate (any Pro entitlement), NOT a new cert code — protects existing customers, frontend-only. Rationale in CONTEXT D-22.2-2.
- upsellStore shape, header-pill lock affordance, pitch wording, e2e structure.

## Deferred Ideas
- Dedicated `pro.palette` cert entitlement (needs Keygen re-issue) — avoided.
- tinkerdev.io Pro-card copy sync (command palette as Pro feature) — separate repo, flagged.
