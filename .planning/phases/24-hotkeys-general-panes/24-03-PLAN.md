---
phase: 24-hotkeys-general-panes
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - src/components/HotkeysSettings.tsx
  - src/components/HotkeyCaptureField.tsx
  - src/components/HotkeyCaptureField.test.tsx
  - src/components/settingsPanes.tsx
  - test/e2e/hotkeys.e2e.ts
autonomous: false
requirements: [SET-08]
tags: [hotkeys, settings-pane, ui, wcag-aa]

must_haves:
  truths:
    - "The Hotkeys pane shows two binding rows (Global summon, Command palette), each: label, current chord (mono), live-capture field, per-hotkey Reset"
    - "Clicking/activating the capture field enters a 'recording' state; pressing a chord captures it via e.code; Escape cancels (no change)"
    - "An invalid/reserved/taken chord shows a calm inline message, keeps the prior binding, persists nothing (D-24-2)"
    - "Per-hotkey Reset restores the shipped default (summon CommandOrControl+Shift+D, palette CommandOrControl+K)"
    - "The Hotkeys pane is keyboard-reachable, WCAG-AA, with an aria-live region for rebind/reset feedback"
  artifacts:
    - path: "src/components/HotkeysSettings.tsx"
      provides: "The Hotkeys pane: two binding rows + aria-live feedback"
      min_lines: 40
    - path: "src/components/HotkeyCaptureField.tsx"
      provides: "Reusable capture field (record/cancel/reject/reset) for both bindings"
    - path: "src/components/settingsPanes.tsx"
      provides: "Hotkeys SETTINGS_PANES entry (append-only)"
      contains: "hotkeys"
  key_links:
    - from: "src/components/HotkeyCaptureField.tsx"
      to: "src/shell/hotkeyAccelerator.ts"
      via: "keyEventToAccelerator + isReservedChord on capture"
      pattern: "keyEventToAccelerator"
    - from: "src/components/HotkeysSettings.tsx"
      to: "src/shell/summon.ts"
      via: "rebindSummon on a valid summon capture"
      pattern: "rebindSummon"
    - from: "src/components/settingsPanes.tsx"
      to: "src/components/HotkeysSettings.tsx"
      via: "SETTINGS_PANES append-only entry"
      pattern: "HotkeysSettings"
---

<objective>
Build the **Hotkeys** Settings pane (SET-08): two binding rows (Global summon, Command palette) each driven by ONE reusable `HotkeyCaptureField` (live capture → validate → reject-inline/persist → reset). Summon rebinds go through `rebindSummon` (native re-register, D-24-5); palette rebinds persist the chord (in-webview, D-24-6). Append the pane to `SETTINGS_PANES` with zero `SettingsModal` change.

Purpose: The user-facing Hotkeys surface, the visible half of SET-08.
Output: HotkeysSettings pane + reusable HotkeyCaptureField + pane registry entry + e2e + human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-hotkeys-general-panes/24-CONTEXT.md
@.planning/phases/24-hotkeys-general-panes/24-UI-SPEC.md
@.planning/phases/24-hotkeys-general-panes/24-01-SUMMARY.md
@.planning/phases/24-hotkeys-general-panes/24-02-SUMMARY.md
@src/components/AppearanceSettings.tsx
@src/components/settingsPanes.tsx

<interfaces>
<!-- From Plans 01/02 + the existing pane blueprint. -->

Plan 01 (src/shell/hotkeyAccelerator.ts):
```typescript
export function keyEventToAccelerator(e: ChordEvent): string | null   // null = no valid chord yet
export function isReservedChord(accelerator: string): boolean
```
Plan 02 (src/shell/summon.ts):
```typescript
export async function rebindSummon(oldChord: string, newChord: string): Promise<void>  // throws on OS reject (restores old)
export const SUMMON_CHORD = "CommandOrControl+Shift+D";   // summon reset default
```
Plan 01 (src/shell/usePreferences.ts):
```typescript
const { preferences, setSummonChord, setPaletteChord } = usePreferences();
// preferences.summonChord, preferences.paletteChord
```
Palette reset default = "CommandOrControl+K".

Pane blueprint (src/components/AppearanceSettings.tsx — clone its wrapper):
- Wrapper: `flex flex-col gap-6 overflow-auto p-8`
- Header: `<header className="flex flex-col gap-1">` → `<h3 className="text-[15px] font-semibold text-tx">` + `<p className="text-[13px] text-tx-2">`
- Sections: `flex flex-col gap-2` with `<h4 className="text-[13px] font-medium text-tx">`

settingsPanes.tsx (append-only registry — D-23-10):
```typescript
export const SETTINGS_PANES: SettingsPane[] = [
  { id: "appearance", label: "Appearance", icon: Contrast, render: () => <AppearanceSettings /> },
  { id: "license", label: "License", icon: Settings, render: () => <LicenseSettings /> },
];
```

UI-SPEC copy (verbatim): inline reject taken = `That shortcut is already in use — try another.`; invalid = `Add Cmd, Ctrl, or Alt to set a shortcut.`; reserved = `That shortcut is reserved by macOS — try another.`; same-as-other = `That shortcut is already used by {the other binding}.`; recording prompt = `Press a shortcut…`; cancel hint = `Esc to cancel`. Pane header `Hotkeys`, sub-text `Rebind the app's keyboard shortcuts.`. Summon label `Global summon`; palette label `Command palette`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reusable HotkeyCaptureField (record / cancel / reject / reset)</name>
  <files>src/components/HotkeyCaptureField.tsx, src/components/HotkeyCaptureField.test.tsx</files>
  <read_first>
    - src/components/AppearanceSettings.tsx (control + focus-ring + section conventions, the AccentSwatchGrid/ThemeCardGroup a11y patterns)
    - src/components/SegmentedControl.tsx (focus-visible:ring-accent + keyboard-operable precedent)
    - src/shell/hotkeyAccelerator.ts (keyEventToAccelerator, isReservedChord — Plan 01)
    - .planning/phases/24-hotkeys-general-panes/24-UI-SPEC.md (Hotkeys pane structure + Copywriting + Keyboard & A11y model)
    - project memory: macos-option-key-composes-letters
  </read_first>
  <behavior>
    - idle: renders a button-semantics control showing the current chord in font-mono; accessible name `Rebind {label}`
    - activate (click OR Enter/Space): enters recording; text swaps to `Press a shortcut…`; a window-level keydown listener is attached
    - recording + Escape: cancels, returns to idle, no onCommit call (D-24-1)
    - recording + a valid chord (keyEventToAccelerator non-null, not reserved, not same-as-other): calls onCommit(accel); the parent decides persist/reject
    - recording + a chord with no non-shift modifier (keyEventToAccelerator null): keeps recording, shows the invalid hint
    - recording + a reserved chord (isReservedChord true): shows the reserved message, stays/returns idle, no onCommit
    - reject (parent passes rejectMessage prop): renders the calm inline `text-[12px] text-tx-2` message; prior chord still displayed
    - Reset button: keyboard-reachable RotateCcw button, accessible name `Reset {label} to default`, calls onReset()
    - e.preventDefault() during recording so the captured chord doesn't trigger app shortcuts
  </behavior>
  <action>
    Create `src/components/HotkeyCaptureField.tsx` — a layout-agnostic, responsive control (no fixed widths) cloning the AppearanceSettings token vocabulary. Props:
    ```typescript
    interface HotkeyCaptureFieldProps {
      label: string;               // "Global summon" | "Command palette"
      helper: string;              // the UI-SPEC helper line
      chord: string;               // current persisted accelerator (display)
      otherChord: string;          // the OTHER binding (prevent summon === palette)
      rejectMessage: string | null; // parent-supplied calm inline message (null = none)
      onCommit: (accel: string) => void;   // a validated, non-reserved, non-duplicate chord
      onReset: () => void;
      onRecordingClearReject: () => void;  // clear the reject when a new capture starts
    }
    ```
    Implement:
    - A `recording` local state. The capture field is a real `<button type="button">` (button semantics; click + Enter/Space enter recording — no mouse-only path, WCAG-AA).
    - Idle: show `chord` in `font-mono text-[12px]`/`text-[13px]`, `bg-input-bg border border-bd rounded-[7px]`, ≥44px tall, `focus-visible:ring-2 focus-visible:ring-accent`. accessible name `Rebind {label}`.
    - Recording: `bg-accent-soft border-accent-line ring-accent`; text = `Press a shortcut…`; below it the `Esc to cancel` hint (`text-[12px] text-tx-3`). Attach a `window` keydown listener while recording; on each key `e.preventDefault()`:
      - `Escape` → cancel (exit recording, no commit).
      - else `const accel = keyEventToAccelerator(e);` — if null, keep recording (the parent's invalid hint, via rejectMessage, conveys "add a modifier").
      - if `isReservedChord(accel)` → exit recording, surface the reserved message (call a `onCommit`-adjacent reject path — simplest: pass the result up and let the parent classify; OR expose the classification here). Keep it SIMPLE: do reserved + same-as-other classification HERE and only `onCommit` a clean chord; surface the others via an internal message state OR the parent rejectMessage. (Reserved/same-as-other never call onCommit.)
      - if `accel === otherChord` → surface the same-as-other message, no commit.
      - else `onRecordingClearReject(); onCommit(accel);` exit recording.
    - The Reset control: a small `RotateCcw` icon button (`text-tx-2 hover:text-tx focus-visible:ring-accent`, ≥24×24px), accessible name `Reset {label} to default`, calls `onReset()`.
    - Use ONLY theme tokens (no hardcoded dark-only hex — Phase-23 Pitfall 5).

    Create `src/components/HotkeyCaptureField.test.tsx` (jsdom + Testing Library): dispatch keydown events to assert recording start, Escape-cancel (no onCommit), a valid Cmd+Shift+J commits, a reserved Cmd+Space does NOT commit, an Option+P composed-glyph event (key:"π", code:"KeyP") commits the e.code-based chord. Use `@testing-library/react` (already a dep).
  </action>
  <verify>
    <automated>pnpm vitest run src/components/HotkeyCaptureField.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/components/HotkeyCaptureField.test.tsx` exits 0
    - grep "keyEventToAccelerator" src/components/HotkeyCaptureField.tsx matches
    - grep "isReservedChord" src/components/HotkeyCaptureField.tsx matches
    - grep "Press a shortcut" src/components/HotkeyCaptureField.tsx matches
    - grep "e.preventDefault" src/components/HotkeyCaptureField.tsx matches
    - grep "focus-visible:ring-accent" src/components/HotkeyCaptureField.tsx matches
    - grep -c "e.key" src/components/HotkeyCaptureField.tsx === 0 (capture via e.code only; helper handles it)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The reusable capture field records via e.code, cancels on Escape, rejects reserved/duplicate without committing, and exposes a keyboard-reachable Reset — all on theme tokens.</done>
</task>

<task type="auto">
  <name>Task 2: HotkeysSettings pane + SETTINGS_PANES append</name>
  <files>src/components/HotkeysSettings.tsx, src/components/settingsPanes.tsx</files>
  <read_first>
    - src/components/AppearanceSettings.tsx (the pane wrapper/header/section blueprint to clone)
    - src/components/HotkeyCaptureField.tsx (Task 1)
    - src/components/settingsPanes.tsx (the append-only registry)
    - src/shell/summon.ts (rebindSummon, SUMMON_CHORD — Plan 02)
    - src/shell/usePreferences.ts (preferences.summonChord/paletteChord, setSummonChord/setPaletteChord — Plan 01)
    - .planning/phases/24-hotkeys-general-panes/24-UI-SPEC.md (Layout + Copywriting + A11y)
  </read_first>
  <action>
    Create `src/components/HotkeysSettings.tsx` — the pane. Clone the AppearanceSettings wrapper EXACTLY: `<div className="flex flex-col gap-6 overflow-auto p-8">`, header `<h3>Hotkeys</h3>` + `<p className="text-[13px] text-tx-2">Rebind the app's keyboard shortcuts.</p>`.

    Read prefs once: `const { preferences, setSummonChord, setPaletteChord } = usePreferences();`. Local `rejectSummon`/`rejectPalette` message state + a single polite `aria-live` region (`<div role="status" aria-live="polite" className="sr-only">`) carrying the last rebind/reset result.

    Render TWO `HotkeyCaptureField` rows:
    - **Global summon** (`label="Global summon"`, helper `Show TinkerDev from anywhere.`, chord=`preferences.summonChord`, otherChord=`preferences.paletteChord`):
      - `onCommit(accel)`: `try { await rebindSummon(preferences.summonChord, accel); setSummonChord(accel); announce(`Global summon set to ${accel}`); setRejectSummon(null); } catch { setRejectSummon("That shortcut is already in use — try another."); }` — on the OS-register reject, persist NOTHING and keep the prior binding (rebindSummon already restored it) (D-24-2).
      - `onReset()`: rebind to `SUMMON_CHORD`, then `setSummonChord(SUMMON_CHORD)`, announce.
    - **Command palette** (`label="Command palette"`, helper `Open the ⌘K command palette.`, chord=`preferences.paletteChord`, otherChord=`preferences.summonChord`):
      - `onCommit(accel)`: pure-webview — `setPaletteChord(accel); announce(...); setRejectPalette(null);` (no native register; D-24-6). The coercer + matchesChord already agree, so there is no OS reject path.
      - `onReset()`: `setPaletteChord("CommandOrControl+K"); announce(...);`
    - Pass the verbatim invalid/reserved/same-as-other reject strings from the UI-SPEC for the non-OS classifications (the field surfaces invalid/reserved/duplicate; the parent surfaces the OS-taken case).

    Append the pane to `SETTINGS_PANES` in `settingsPanes.tsx` (import `Keyboard` from lucide-react + `HotkeysSettings`). Insert the entry AFTER appearance and BEFORE license (keep license last, matching the Phase-23 ordering):
    ```typescript
    { id: "hotkeys", label: "Hotkeys", icon: Keyboard, render: () => <HotkeysSettings /> },
    ```
    Do NOT modify `SettingsModal.tsx` (registry is the single control plane — nav + content derive 1:1).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run src/components</automated>
  </verify>
  <acceptance_criteria>
    - grep "rebindSummon" src/components/HotkeysSettings.tsx matches
    - grep "setSummonChord\|setPaletteChord" src/components/HotkeysSettings.tsx matches
    - grep "aria-live=\"polite\"" src/components/HotkeysSettings.tsx matches
    - grep "That shortcut is already in use — try another." src/components/HotkeysSettings.tsx matches (verbatim UI-SPEC copy)
    - grep "id: \"hotkeys\"" src/components/settingsPanes.tsx matches AND grep "HotkeysSettings" src/components/settingsPanes.tsx matches
    - git diff --quiet src/components/SettingsModal.tsx (shell byte-unchanged) — verify no diff
    - `pnpm exec tsc --noEmit` exits 0 ; `pnpm vitest run src/components` exits 0
  </acceptance_criteria>
  <done>The Hotkeys pane renders both binding rows wired to rebindSummon / setPaletteChord with calm inline rejects + aria-live; the pane is an append-only SETTINGS_PANES entry, SettingsModal untouched.</done>
</task>

<task type="auto">
  <name>Task 3: hotkeys.e2e.ts — palette-on-configured-chord on the real WKWebView</name>
  <files>test/e2e/hotkeys.e2e.ts</files>
  <read_first>
    - test/e2e/helpers.ts (dispatchKey/navigateToTool/saveScreenshot/assert + the WebKit quirks)
    - test/e2e/cmdk-pro.e2e.ts (palette + Pro-gating e2e precedent)
    - test/e2e/settings.e2e.ts (open Settings modal + pane nav precedent)
    - project memory: verify-gate-builds-real-app, macos-option-key-composes-letters, e2e-spike-port-and-single-instance
  </read_first>
  <action>
    Create `test/e2e/hotkeys.e2e.ts` (the Wave-0 e2e gap) covering the webview-testable parts of SET-08 on the real WKWebView (native register is NOT WebDriver-drivable — that is human-walkthrough, Task 4):
    - **Pane reachable:** open Settings (sidebar gear / ⌘,), navigate the pane nav to "Hotkeys", assert both binding rows render (`Global summon`, `Command palette`) and the current chords display.
    - **Palette opens on the configured chord:** with the default `CommandOrControl+K`, dispatch the composed ⌘K key shape (via the `dispatchKey` helper) and assert the palette opens (reuse the cmdk-pro Pro-tier setup so the palette — not the upsell — opens).
    - **Rebind reflected in capture field:** activate the palette capture field, dispatch a valid chord (e.g. Cmd+Shift+J with code "KeyJ"), assert the displayed chord updates AND the palette now opens on the new chord (dispatch the new shape) and no longer on ⌘K. Use the helper that dispatches the composed key shape (code + key + modifiers) per memory macos-option-key-composes-letters.
    - **Escape cancels capture:** activate capture, press Escape, assert the chord is unchanged.
    Follow the helper conventions (scoped `[role=dialog][aria-modal]` probes, screenshot on key steps, finally-cleanup that resets prefs override + closes the modal). Do NOT assert native summon register (Task 4 manual).
  </action>
  <verify>
    <automated>MISSING — run via scripts/e2e-spike.sh at the wave merge (real WKWebView); pnpm exec tsc --noEmit must pass on the spec first</automated>
  </verify>
  <acceptance_criteria>
    - File test/e2e/hotkeys.e2e.ts exists and `pnpm exec tsc --noEmit` includes it cleanly (exits 0)
    - grep "Global summon" test/e2e/hotkeys.e2e.ts AND grep "Command palette" test/e2e/hotkeys.e2e.ts match
    - grep "dispatchKey" test/e2e/hotkeys.e2e.ts matches (composed key shape)
    - Running `scripts/e2e-spike.sh` (wave merge) drives hotkeys.e2e.ts green on the real WKWebView (recorded in SUMMARY; preflight reaps orphan vite/app per memory)
  </acceptance_criteria>
  <done>The webview-testable SET-08 behavior (pane render, palette-on-configured-chord, rebind-reflected, Escape-cancel) passes on the real WKWebView.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human walkthrough — native summon re-register + rebind survives restart</name>
  <action>Auto-run `pnpm tauri build` as the LAST step, confirm the bundle (.app/.dmg under src-tauri/target/release/bundle/macos/ with mtime newer than the last source commit — NOT the exit code), hand off the built-app path, then PAUSE for the human walkthrough below. See <what-built>/<how-to-verify> for the exact steps.</action>
  <verify>Human confirms the native summon register, taken-chord reject, restart persistence, and keyboard reachability on the built `.app` per <how-to-verify>.</verify>
  <done>The human types "approved" after the native summon re-register, reject-inline, restart-persistence, and keyboard walkthrough pass on a fresh build.</done>
  <what-built>
    The Hotkeys pane with live capture, calm inline rejects, and per-hotkey reset. registerSummon now reads the persisted summon chord at startup (promoting NAT-01/G-05-1); rebindSummon re-registers the OS global shortcut. These native paths are NOT WebDriver-drivable (RESEARCH Pitfall 6) — they need a human on the built app. The agent auto-runs `pnpm tauri build` FIRST (confirm the bundle by the `.app`/`.dmg` under src-tauri/target/release/bundle/macos/ with mtime newer than the last source commit — NOT the exit code; the only non-zero is the absent updater-signing key) and hands off the built-app path.
  </what-built>
  <how-to-verify>
    1. Launch the built `.app`. Press the default summon chord (Cmd+Shift+D) from another app → TinkerDev should reveal/focus.
    2. Open Settings → Hotkeys. Rebind Global summon to a free chord (e.g. Cmd+Shift+J) → expect the field to show the new chord + a polite announcement. Press the new chord from another app → TinkerDev reveals.
    3. Rebind Global summon to a TAKEN/reserved chord (e.g. Cmd+Space) → expect the calm inline message "That shortcut is already in use — try another.", NO change, and the previous chord STILL summons.
    4. Quit and relaunch → the rebound chord (Cmd+Shift+J) still summons (persistence survived restart).
    5. Reset Global summon → expect it returns to Cmd+Shift+D and that chord summons.
    6. Rebind the Command palette chord (e.g. to Cmd+Shift+P) → expect ⌘P-shape opens the palette and ⌘K no longer does; Reset restores ⌘K.
    7. Keyboard-only pass: Tab to each capture field + Reset, activate via Enter/Space (no mouse) — confirm WCAG-AA reachability.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured KeyboardEvent → persisted chord | live capture in the pane |
| pane → OS (summon re-register) | rebindSummon via the native seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | DoS (dead summon) | HotkeysSettings onCommit | mitigate | On rebindSummon reject: catch, show the calm inline message, persist NOTHING (rebindSummon already restored the prior OS binding) — D-24-2. |
| T-24-04 | Injection (glyph) | HotkeyCaptureField capture | mitigate | keyEventToAccelerator reads physical e.code (never e.key) — Pitfall 2. |
| T-24-07 | Tampering (self-collision) | capture same-as-other | mitigate | The field rejects a chord equal to the OTHER binding (otherChord prop) before committing — no two bindings collide. |
| T-24-08 | A11y regression | capture affordance | accept→mitigate | Button semantics + Enter/Space activation + visible focus ring + aria-live feedback; no mouse-only path (WCAG-AA, human keyboard pass in Task 4 + gsd-ui-review at the phase boundary). |
</threat_model>

<verification>
- `pnpm vitest run` full suite green; `pnpm exec tsc --noEmit` + `pnpm lint` clean; decoder's 19 tests untouched.
- SettingsModal.tsx byte-unchanged (append-only registry).
- Real-WKWebView e2e: hotkeys.e2e.ts green via scripts/e2e-spike.sh at the wave merge.
- Native summon register + rebind-survives-restart: human walkthrough (Task 4) on a fresh `pnpm tauri build`.
- Harness order per task: `/simplify` → `/codex:review --wait --scope working-tree` → unit+tsc green → e2e → human checkpoint.
</verification>

<success_criteria>
- The Hotkeys pane lets the user view + rebind both chords; a taken/invalid/reserved chord is rejected with calm inline messaging, prior binding preserved (SET-08 criterion 1).
- Both chords persist and survive restart (criterion 2 — verified in Task 4).
- The pane is fully keyboard-reachable + WCAG-AA (criterion 5).
</success_criteria>

<output>
After completion, create `.planning/phases/24-hotkeys-general-panes/24-03-SUMMARY.md`.
</output>
