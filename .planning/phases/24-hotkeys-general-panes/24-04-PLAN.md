---
phase: 24-hotkeys-general-panes
plan: 04
type: execute
wave: 4
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - src/components/GeneralSettings.tsx
  - src/components/SettingToggle.tsx
  - src/components/SettingToggle.test.tsx
  - src/components/settingsPanes.tsx
  - src/components/Sidebar.tsx
  - src/components/Sidebar.test.tsx
  - test/e2e/hotkeys.e2e.ts
autonomous: false
requirements: [SET-09]
tags: [general-pane, toggles, sidebar, autostart, default-tool, wcag-aa]

must_haves:
  truths:
    - "The General pane exposes four controls: Launch at login, Start in the menu bar, Open to (default tool), Show license status in sidebar"
    - "Launch-at-login seeds from platform.autostart.isEnabled() AND persists intent; on flip it calls enable/disable AND updatePreferences"
    - "Open-to dropdown first option is literal 'Last used', then each ENABLED_TOOLS name; selection persists as defaultToolId"
    - "Show-license-status toggle hides/shows the sidebar license/upgrade affordance"
    - "Each toggle is keyboard-operable (role=switch + aria-checked), visible focus ring, no opacity-only state"
  artifacts:
    - path: "src/components/GeneralSettings.tsx"
      provides: "The General pane: four app-behavior controls"
      min_lines: 50
    - path: "src/components/SettingToggle.tsx"
      provides: "Reusable accessible boolean switch (role=switch)"
    - path: "src/components/settingsPanes.tsx"
      provides: "General SETTINGS_PANES entry (append-only)"
      contains: "general"
  key_links:
    - from: "src/components/GeneralSettings.tsx"
      to: "src/lib/platform/index.ts"
      via: "platform.autostart.enable/disable/isEnabled"
      pattern: "platform.autostart"
    - from: "src/components/GeneralSettings.tsx"
      to: "src/lib/tools/registry.ts"
      via: "ENABLED_TOOLS drives the default-tool options"
      pattern: "ENABLED_TOOLS"
    - from: "src/components/Sidebar.tsx"
      to: "src/shell/usePreferences.ts"
      via: "preferences.showLicenseInSidebar gates the affordance"
      pattern: "showLicenseInSidebar"
---

<objective>
Build the **General** Settings pane (SET-09): four app-behavior controls — Launch at login (the autostart capability, D-24-7), Start in the menu bar (D-24-8/9), Open to / default tool (D-24-10), Show license status in sidebar (D-24-11). Each persists via the single-writer prefs seam and takes effect. Gate the sidebar license affordance on the new toggle. Append the pane to `SETTINGS_PANES` with zero `SettingsModal` change.

Purpose: The user-facing General surface, all of SET-09.
Output: GeneralSettings pane + reusable accessible SettingToggle + sidebar gate + pane registry entry + e2e + human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-hotkeys-general-panes/24-CONTEXT.md
@.planning/phases/24-hotkeys-general-panes/24-UI-SPEC.md
@.planning/phases/24-hotkeys-general-panes/24-01-SUMMARY.md
@src/components/AppearanceSettings.tsx
@src/components/settingsPanes.tsx

<interfaces>
<!-- From Plan 01 + the existing codebase. -->

Plan 01 (src/shell/usePreferences.ts):
```typescript
const { preferences, setLaunchAtLogin, setStartInTray, setDefaultToolId, setShowLicenseInSidebar } = usePreferences();
// preferences.launchAtLogin / .startInTray / .defaultToolId (string|null) / .showLicenseInSidebar
```
Plan 01 (src/lib/platform/index.ts):
```typescript
platform.autostart.enable(): Promise<void>
platform.autostart.disable(): Promise<void>
platform.autostart.isEnabled(): Promise<boolean>   // OS truth; browser/test arm -> false
```
registry (src/lib/tools/registry.ts):
```typescript
export const ENABLED_TOOLS: ToolDefinition[]   // { id, name }
```

Sidebar.tsx (src/components/Sidebar.tsx) — the affordance to gate is the `licenseAttention || !ents.has(ENT_ORDERING) || !ents.has(ENT_THEMING)` block at ~L660 rendering "Unlock Pro" / "License needs attention". The bottom "Settings" gear row at ~L679 is UNCONDITIONAL (SET-04) and MUST stay (do NOT gate it).

Pane blueprint (AppearanceSettings.tsx): wrapper `flex flex-col gap-6 overflow-auto p-8`; header `<h3 className="text-[15px] font-semibold text-tx">` + `<p className="text-[13px] text-tx-2">`; sections `flex flex-col gap-2` with `<h4 className="text-[13px] font-medium text-tx">`.

UI-SPEC copy (verbatim): pane header `General`, sub-text `App behavior and startup.`; `Launch at login` (helper `Start TinkerDev automatically when you log in.`); `Start in the menu bar` (helper `Launch hidden; summon or click the tray icon to show.`); `Open to` (first option literal `Last used`); `Show license status in sidebar`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reusable accessible SettingToggle (role=switch)</name>
  <files>src/components/SettingToggle.tsx, src/components/SettingToggle.test.tsx</files>
  <read_first>
    - src/components/SegmentedControl.tsx (keyboard-operable + focus-ring + selected-state token precedent)
    - src/components/AppearanceSettings.tsx (accent-soft selected vs neutral, no-opacity-only-state precedent)
    - .planning/phases/24-hotkeys-general-panes/24-UI-SPEC.md (Toggle visual contract + Keyboard & A11y model)
  </read_first>
  <behavior>
    - renders a button with role="switch" and aria-checked reflecting the `checked` prop
    - clicking OR pressing Space/Enter toggles → calls onChange(!checked)
    - the on state uses accent fill (bg-accent-soft / accent), the off state is neutral — distinguished by fill + position/label, NOT opacity
    - has a visible focus-visible:ring-accent
    - exposes an accessible label (aria-label or aria-labelledby) tied to the control's text
  </behavior>
  <action>
    Create `src/components/SettingToggle.tsx` — a labeled accessible switch:
    ```typescript
    interface SettingToggleProps {
      label: string; helper?: string; checked: boolean;
      onChange: (next: boolean) => void; id?: string;
    }
    ```
    Render a row: the `label` (`text-[13px] font-medium text-tx`) + optional `helper` (`text-[12px] text-tx-3`) on the left, and a `<button type="button" role="switch" aria-checked={checked}>` switch on the right. Keyboard-operable (native button handles Enter/Space → onClick → `onChange(!checked)`). On state = `bg-accent-soft border-accent-line` + a translated knob/dot to the right; off = neutral `bg-input-bg border-bd` + knob left. NO opacity-only distinction. `focus-visible:ring-2 focus-visible:ring-accent`. Track ≥24px tall. Use ONLY theme tokens (works in both themes — Pitfall 5). Give the switch `aria-label={label}` (or wire `aria-labelledby` to the label element).

    Create `src/components/SettingToggle.test.tsx` (jsdom + Testing Library): assert role="switch" + aria-checked reflects checked; click toggles via onChange; Space keypress toggles; the off→on class changes are not opacity-only (assert the accent class appears when checked).
  </action>
  <verify>
    <automated>pnpm vitest run src/components/SettingToggle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/components/SettingToggle.test.tsx` exits 0
    - grep "role=\"switch\"" src/components/SettingToggle.tsx matches
    - grep "aria-checked" src/components/SettingToggle.tsx matches
    - grep "focus-visible:ring-accent" src/components/SettingToggle.tsx matches
    - grep -i "opacity-" src/components/SettingToggle.tsx returns 0 for the on/off state distinction (no opacity-only state)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A reusable role=switch toggle with aria-checked, keyboard operation, visible focus ring, and accent-fill (not opacity) state.</done>
</task>

<task type="auto">
  <name>Task 2: GeneralSettings pane + sidebar license-affordance gate + SETTINGS_PANES append</name>
  <files>src/components/GeneralSettings.tsx, src/components/settingsPanes.tsx, src/components/Sidebar.tsx, src/components/Sidebar.test.tsx</files>
  <read_first>
    - src/components/AppearanceSettings.tsx (pane wrapper/header/section blueprint)
    - src/components/SettingToggle.tsx (Task 1)
    - src/components/settingsPanes.tsx (append-only registry)
    - src/components/Sidebar.tsx (~L660 license affordance block; ~L679 unconditional Settings row)
    - src/components/Sidebar.test.tsx (extend)
    - src/shell/usePreferences.ts (the new setters — Plan 01)
    - src/lib/tools/registry.ts (ENABLED_TOOLS)
    - .planning/phases/24-hotkeys-general-panes/24-UI-SPEC.md (General pane structure + Copywriting)
    - project memory: tauri-store-async-init-race (gate autostart seed on prefsLoaded)
  </read_first>
  <action>
    Create `src/components/GeneralSettings.tsx` — the pane. Clone the AppearanceSettings wrapper: `<div className="flex flex-col gap-6 overflow-auto p-8">`, header `<h3>General</h3>` + `<p className="text-[13px] text-tx-2">App behavior and startup.</p>`.
    `const { preferences, prefsLoaded, setLaunchAtLogin, setStartInTray, setDefaultToolId, setShowLicenseInSidebar } = usePreferences();`

    Four controls (top → bottom):
    1. **Launch at login** — `<SettingToggle label="Launch at login" helper="Start TinkerDev automatically when you log in." checked={preferences.launchAtLogin} onChange={...} />`. On mount (gated on `prefsLoaded`), reconcile the OS truth: `void platform.autostart.isEnabled().then((on) => { if (on !== preferences.launchAtLogin) setLaunchAtLogin(on); });` (the OS is the source of truth — RESEARCH §7). On flip: `async (next) => { if (next) await platform.autostart.enable(); else await platform.autostart.disable(); setLaunchAtLogin(next); }` — call the seam AND persist the intent (so the UI is correct pre-async-read; D-24-12). Reach the OS ONLY via `platform.autostart` (FND-04 — no @tauri-apps import).
    2. **Start in the menu bar** — `<SettingToggle label="Start in the menu bar" helper="Launch hidden; summon or click the tray icon to show." checked={preferences.startInTray} onChange={setStartInTray} />`. Pure persist (revealOnStartup reads it next launch — Plan 02).
    3. **Open to** (default tool) — a keyboard-operable native `<select>` (best fit for 11 tools per UI-SPEC; `focus-visible:ring-accent`, theme-token styled). First `<option value="last-used">Last used</option>`, then `ENABLED_TOOLS.map((t) => <option value={t.id}>{t.name}</option>)`. Value = `preferences.defaultToolId ?? "last-used"`. onChange: `setDefaultToolId(v === "last-used" ? null : v)`. Label it via an associated `<label>` ("Open to").
    4. **Show license status in sidebar** — `<SettingToggle label="Show license status in sidebar" checked={preferences.showLicenseInSidebar} onChange={setShowLicenseInSidebar} />`.

    In `Sidebar.tsx`, gate the license/upgrade affordance on the new toggle. Read `preferences.showLicenseInSidebar` (the Sidebar already calls usePreferences). Wrap the existing `licenseAttention || !ents.has(ENT_ORDERING) || !ents.has(ENT_THEMING)` render condition (the "Unlock Pro" / "License needs attention" block) so it ALSO requires `preferences.showLicenseInSidebar` (i.e. `preferences.showLicenseInSidebar && (licenseAttention || !ents.has(ENT_ORDERING) || !ents.has(ENT_THEMING))`). Do NOT gate the unconditional bottom "Settings" gear row (SET-04 — it must always show, even unlicensed).

    Append the pane to `SETTINGS_PANES` (import `SlidersHorizontal` from lucide-react + `GeneralSettings`). Insert AFTER hotkeys and BEFORE license:
    ```typescript
    { id: "general", label: "General", icon: SlidersHorizontal, render: () => <GeneralSettings /> },
    ```
    Do NOT modify `SettingsModal.tsx`.

    Extend `Sidebar.test.tsx`: a `showLicenseInSidebar:false` case hides the Unlock-Pro/attention affordance while the bottom Settings row STILL renders (proving the gate is on the affordance, not the Settings row).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run src/components/Sidebar.test.tsx src/components</automated>
  </verify>
  <acceptance_criteria>
    - grep "platform.autostart" src/components/GeneralSettings.tsx matches AND grep -c "@tauri-apps" src/components/GeneralSettings.tsx === 0 (FND-04 seam discipline)
    - grep "ENABLED_TOOLS" src/components/GeneralSettings.tsx matches AND grep "Last used" src/components/GeneralSettings.tsx matches
    - grep "App behavior and startup." src/components/GeneralSettings.tsx matches (verbatim UI-SPEC copy)
    - grep "showLicenseInSidebar" src/components/Sidebar.tsx matches (affordance gated)
    - grep "id: \"general\"" src/components/settingsPanes.tsx matches AND grep "GeneralSettings" src/components/settingsPanes.tsx matches
    - git diff --quiet src/components/SettingsModal.tsx (shell byte-unchanged)
    - `pnpm vitest run src/components/Sidebar.test.tsx` exits 0 (incl. the showLicenseInSidebar:false case)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The General pane renders four working controls; launch-at-login reconciles the OS state + persists intent; the sidebar license affordance is gated on the toggle (Settings row unaffected); pane is an append-only SETTINGS_PANES entry, SettingsModal untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend hotkeys.e2e.ts — General pane: show-license-in-sidebar toggle</name>
  <files>test/e2e/hotkeys.e2e.ts</files>
  <read_first>
    - test/e2e/hotkeys.e2e.ts (Plan 03 Task 3 — extend it)
    - test/e2e/helpers.ts (assert/dispatchKey/saveScreenshot, scoped dialog probes)
    - test/e2e/settings.e2e.ts (pane-nav precedent)
    - project memory: verify-gate-builds-real-app, license-walkthrough-state-pollutes-e2e
  </read_first>
  <action>
    Extend `test/e2e/hotkeys.e2e.ts` with the webview-testable SET-09 part (autostart/start-in-tray/default-tool-on-open are native/launch-time → human-walkthrough Task 4, NOT here):
    - Open Settings, navigate to the "General" pane, assert the four controls render (`Launch at login`, `Start in the menu bar`, `Open to`, `Show license status in sidebar`).
    - **Show-license-in-sidebar:** in a state where the sidebar shows the "Unlock Pro" / "License needs attention" affordance (reuse the free-tier / attention setup from cmdk-pro/sidebar e2e), toggle "Show license status in sidebar" OFF → assert the sidebar affordance disappears; toggle ON → it reappears. Assert the bottom unconditional "Settings" gear row stays visible in BOTH states (SET-04 invariant).
    - Verify the toggle's `aria-checked` flips. Follow helper conventions (scoped probes, screenshots, finally-cleanup that restores prefs override + closes the modal — license-walkthrough-state-pollutes-e2e).
  </action>
  <verify>
    <automated>MISSING — run via scripts/e2e-spike.sh at the wave merge (real WKWebView); pnpm exec tsc --noEmit must pass on the spec first</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0 with the extended spec
    - grep "Show license status in sidebar" test/e2e/hotkeys.e2e.ts matches
    - grep "Launch at login" test/e2e/hotkeys.e2e.ts AND grep "Open to" test/e2e/hotkeys.e2e.ts match
    - Running `scripts/e2e-spike.sh` (wave merge) drives the extended hotkeys.e2e.ts green on the real WKWebView (recorded in SUMMARY)
  </acceptance_criteria>
  <done>The webview-testable SET-09 behavior (General pane render + show-license-in-sidebar toggle effect, Settings row invariant) passes on the real WKWebView.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human walkthrough — launch-at-login, start-in-tray (no flash), default-tool on open + phase build sign-off</name>
  <action>This is the phase-boundary sign-off: all Phase-24 source (Plans 01-04) has landed. Auto-run `pnpm tauri build` as the LAST step, confirm the bundle (.app/.dmg under src-tauri/target/release/bundle/macos/ with mtime newer than the last source commit — NOT the exit code), hand off the built-app path, then PAUSE for the human walkthrough + gsd-ui-review below. See <what-built>/<how-to-verify> for the exact steps.</action>
  <verify>Human confirms launch-at-login plist round-trip, start-in-tray no-flash, default-tool-on-open, and the show-license-in-sidebar effect on the built `.app`; gsd-ui-review WCAG-AA passes on both panes in both themes.</verify>
  <done>The human types "approved" after the General-pane native walkthrough + gsd-ui-review WCAG-AA pass on a fresh build.</done>
  <what-built>
    The General pane (launch-at-login via the autostart plugin, start-in-tray, default-tool dropdown, show-license-in-sidebar). The autostart LaunchAgent plist round-trip, the start-in-tray launch-hidden + reveal + no-flash, and the default-tool-on-open behavior are launch-time/native and NOT WebDriver-drivable (RESEARCH Pitfall 6) — human-walkthrough on the built app. This is ALSO the phase-boundary sign-off: ALL Phase-24 source has landed (Plans 01-04), so the agent auto-runs `pnpm tauri build` as the LAST step (confirm the bundle by the `.app`/`.dmg` under src-tauri/target/release/bundle/macos/ with mtime newer than the last source commit — NOT the exit code) and hands off the built-app path. Sign-off requires this walkthrough + a passing `gsd-ui-review` WCAG-AA audit (both panes, both themes).
  </what-built>
  <how-to-verify>
    1. Launch the built `.app`. Open Settings → General.
    2. **Launch at login:** toggle ON → confirm a login item / LaunchAgent registered for TinkerDev (System Settings ▸ General ▸ Login Items, or `ls ~/Library/LaunchAgents/ | grep -i tinkerdev`). Toggle OFF → confirm it is removed.
    3. **Start in the menu bar:** toggle ON → quit → relaunch → the app should launch HIDDEN to the tray with NO window flash (no window appears then disappears). Click the tray icon (or press the summon chord) → the window reveals. Toggle OFF → relaunch → the window opens normally.
    4. **Combined (D-24-9):** with BOTH launch-at-login + start-in-tray ON, simulate a login launch (relaunch) → app stays hidden in the tray, no focus steal; summon/tray-click reveals.
    5. **Open to (default tool):** set "Open to" to a specific tool (not "Last used") → quit → relaunch → the app opens directly to that tool. Set back to "Last used" → relaunch → it opens to the last-used tool (today's behavior preserved).
    6. **Show license status in sidebar:** toggle OFF → the sidebar Unlock-Pro/attention affordance disappears (the bottom Settings gear row stays). Toggle ON → it returns.
    7. **WCAG-AA keyboard pass:** Tab to each control + operate via keyboard only (Space on toggles, arrow/typeahead on the select) — no mouse-only path. Confirm both Hotkeys + General panes pass `gsd-ui-review` in both light and dark themes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → OS (autostart) | launch-at-login LaunchAgent registration via platform.autostart |
| hand-edited prefs.json → startup tool | defaultToolId used to pick the open tool |
| pane → sidebar render | showLicenseInSidebar gates an affordance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Elevation | platform.autostart enable/disable | mitigate | Reached ONLY through the seam (FND-04, grep-asserted); capability scoped to the three autostart perms; `None` launch args (Plan 01). The pane never imports @tauri-apps. |
| T-24-02 | Tampering | defaultToolId → startup nav | mitigate | Validated via getToolById in the coercer (Plan 01) AND in resolveStartupTool; the select only ever offers ENABLED_TOOLS ids; unknown → "Last used". |
| T-24-06 | Race | autostart isEnabled seed | mitigate | Seed the launch-at-login toggle from `platform.autostart.isEnabled()` gated on `prefsLoaded` so a pre-load read doesn't seed a wrong value (tauri-store-async-init-race). |
| T-24-09 | Tampering (single writer) | GeneralSettings setters | mitigate | All four controls write ONLY through the usePreferences single-writer setters (updatePreferences) — never a second prefs writer (prefs-blob-single-writer, the Phase-23 data-loss bug). |
| T-24-08 | A11y regression | SettingToggle / select | mitigate | role=switch + aria-checked + keyboard operation + visible focus ring; no opacity-only state; human keyboard pass + gsd-ui-review (Task 4). |
</threat_model>

<verification>
- `pnpm vitest run` full suite green; `pnpm exec tsc --noEmit` + `pnpm lint` clean; decoder's 19 tests untouched.
- SettingsModal.tsx byte-unchanged (append-only registry); the unconditional Settings sidebar row preserved.
- Real-WKWebView e2e: extended hotkeys.e2e.ts green via scripts/e2e-spike.sh at the wave merge.
- Launch-at-login / start-in-tray-no-flash / default-tool-on-open: human walkthrough (Task 4) on a fresh `pnpm tauri build` + gsd-ui-review WCAG-AA (both panes, both themes).
- Harness order per task: `/simplify` → `/codex:review --wait --scope working-tree` → unit+tsc green → e2e → human checkpoint.
</verification>

<success_criteria>
- The General pane exposes four toggles/controls (launch-at-login, start-in-tray, default tool, show-license-in-sidebar); each persists + takes effect (SET-09 criterion 3).
- Launch-at-login works via the autostart plugin — the single recorded v1.7 dep (criterion 4).
- The pane is fully keyboard-reachable + WCAG-AA (criterion 5).
</success_criteria>

<output>
After completion, create `.planning/phases/24-hotkeys-general-panes/24-04-SUMMARY.md`.
</output>
