---
phase: 06-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - src-tauri/tauri.conf.json
autonomous: true
requirements: [DST-01, DST-02]
user_setup: []

must_haves:
  truths:
    - "A committed .env / .envrc / *.key / *.p8 is impossible — git ignores them before any secret exists"
    - "tauri.conf.json version and package.json version are identical and bumped past 0.1.0"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rules for secret material (.env/.envrc/*.key/*.p8)"
      contains: ".env"
    - path: "package.json"
      provides: "Bumped app version in lockstep with tauri.conf.json"
      contains: "\"version\": \"0.2.0\""
    - path: "src-tauri/tauri.conf.json"
      provides: "Bumped app version in lockstep with package.json"
      contains: "\"version\": \"0.2.0\""
  key_links:
    - from: "package.json version"
      to: "src-tauri/tauri.conf.json version"
      via: "manual lockstep bump (D-16)"
      pattern: "\"version\": \"0.2.0\""
---

<objective>
Close the two repo-hygiene gaps research flagged BEFORE any secret or release artifact can touch the tree: (1) `.gitignore` does not yet ignore `.env`/`.envrc`/`*.key`/`*.p8` — the minisign private key + Apple credentials this phase introduces (D-05/D-15) must be un-committable from the start; (2) `package.json` and `src-tauri/tauri.conf.json` are both `0.1.0` and must move in lockstep (D-16) — the updater compares the app version against `latest.json`, so a divergent version breaks the round-trip.

Purpose: a safe foundation for every later Phase-6 plan. This MUST land first (it is the only thing other plans implicitly depend on for secret-safety; nothing here can break the build).
Output: hardened `.gitignore`, version bumped to `0.2.0` in both manifests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution/06-CONTEXT.md
@.planning/phases/06-distribution/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden .gitignore against secret material (D-05, Pitfall 6)</name>
  <read_first>
    - `.gitignore` (the file being modified — current rules ignore node_modules/, dist/, src-tauri/target/, *.local, .claude/, .agents/, but NOT secrets)
    - `06-RESEARCH.md` § "Minisign Updater Signing" `.env gitignore status` note + § "Common Pitfalls" Pitfall 6
    - `06-CONTEXT.md` D-05 (local gitignored env vars only; nothing secret enters git history)
  </read_first>
  <action>
    Append a new section to `.gitignore` (do NOT remove or reorder existing rules) that ignores all secret material this phase introduces. Add exactly these patterns under a clear comment header:

    ```
    # Secrets (D-05) — minisign updater private key + password, Apple notary
    # credentials. These are LOCAL gitignored env only; never committed. The
    # minisign PUBLIC key lives in tauri.conf.json (it is public by design).
    .env
    .env.*
    .envrc
    *.key
    *.p8
    ```

    Note: `*.key` would also match `~/.tauri/devtools.key` if a key were ever placed in-repo, but the canonical key location is `~/.tauri/` (outside the repo) per D-15 — this is defense-in-depth. The committed `tauri.conf.json` pubkey is a string field, not a `*.key`/`*.pub` file, so it is unaffected.
  </action>
  <verify>
    <automated>grep -qx '.env' .gitignore && grep -qx '*.key' .gitignore && grep -qx '*.p8' .gitignore && grep -qx '.envrc' .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qx '.env' .gitignore` exits 0
    - `grep -qx '.env.*' .gitignore` exits 0
    - `grep -qx '.envrc' .gitignore` exits 0
    - `grep -qx '*.key' .gitignore` exits 0
    - `grep -qx '*.p8' .gitignore` exits 0
    - All pre-existing `.gitignore` lines (`node_modules/`, `src-tauri/target/`, `*.local`, `.claude/`) still present: `grep -qx 'node_modules/' .gitignore && grep -qx 'src-tauri/target/' .gitignore` exits 0
    - A scratch `git check-ignore .env secret.key apple.p8 .envrc` lists all four as ignored
  </acceptance_criteria>
  <done>`.env`, `.env.*`, `.envrc`, `*.key`, `*.p8` are git-ignored; no existing ignore rule removed; `git check-ignore` confirms all are ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Lockstep version bump to 0.2.0 (D-16)</name>
  <read_first>
    - `package.json` line 4 (`"version": "0.1.0"`) — the field being modified
    - `src-tauri/tauri.conf.json` line 4 (`"version": "0.1.0"`) — the field being modified
    - `06-RESEARCH.md` § "RELEASE.md runbook" step 1 (bump BOTH in sync) + § Key Findings #6 (both at 0.1.0 now)
    - `06-CONTEXT.md` D-16 (manual version bump now in tauri.conf.json + package.json)
  </read_first>
  <action>
    Change the `version` field from `"0.1.0"` to `"0.2.0"` in BOTH files, and ONLY the version field:
    - `package.json`: line 4 `"version": "0.1.0"` → `"version": "0.2.0"`
    - `src-tauri/tauri.conf.json`: line 4 `"version": "0.1.0"` → `"version": "0.2.0"`

    `0.2.0` (minor bump) marks the first distribution-capable build (the first version that ships the updater). Do NOT touch `src-tauri/Cargo.toml`'s `version = "0.1.0"` — Tauri derives the bundle version from `tauri.conf.json`, and the Cargo crate version is internal (leave it to avoid an unrelated diff). Do NOT change `productName`, `identifier`, or any other field.
  </action>
  <verify>
    <automated>grep -q '"version": "0.2.0"' package.json && grep -q '"version": "0.2.0"' src-tauri/tauri.conf.json && ! grep -q '"version": "0.1.0"' package.json && ! grep -q '"version": "0.1.0"' src-tauri/tauri.conf.json && pnpm test && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"version": "0.2.0"' package.json` exits 0
    - `grep -q '"version": "0.2.0"' src-tauri/tauri.conf.json` exits 0
    - Neither manifest still contains `"version": "0.1.0"`
    - `package.json` `identifier`/`productName` untouched; `grep -q 'com.boonkhailim.devtools-app' src-tauri/tauri.conf.json` still exits 0
    - `pnpm test` exits 0 (decoder 19 + all suites green — version bump touches no code)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both manifests report `0.2.0`; no `0.1.0` remains in either; all unit tests + tsc stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → git history | Secret env files / keys could be accidentally staged and committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | `.env`/`*.key`/`*.p8` (minisign private key, Apple notary creds) | mitigate | Add `.env`/`.env.*`/`.envrc`/`*.key`/`*.p8` to `.gitignore` BEFORE any secret exists (Task 1, D-05/Pitfall 6); `git check-ignore` confirms |
| T-06-02 | Tampering | App version field divergence between manifests | mitigate | Lockstep bump enforced + verified by grep (Task 2); divergence would let a forged/stale `latest.json` mis-compare versions |
</threat_model>

<verification>
- `git check-ignore .env secret.key apple.p8 .envrc` lists all four as ignored.
- Both manifests at `0.2.0`; no `0.1.0` remains.
- `pnpm test && pnpm exec tsc --noEmit` green (no code changed).
</verification>

<success_criteria>
Secret material is un-committable (gitignore hardened) and the app version is `0.2.0` in lockstep across both manifests — the safe foundation every later Phase-6 plan builds on.
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-01-SUMMARY.md`.
</output>
