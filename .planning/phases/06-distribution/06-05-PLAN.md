---
phase: 06-distribution
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - docs/RELEASE.md
  - .planning/phases/06-distribution/06-HUMAN-UAT.md
autonomous: false
requirements: [DST-01, DST-02]
user_setup:
  - service: GitHub public repo + Release
    why: "Host the DMG + .app.tar.gz + .sig + latest.json so the updater endpoint resolves (D-07/D-08)"
    note: "NO git remote exists yet. The owner/repo pinned in tauri.conf.json (boonkhailim/devtools) MUST be confirmed/created before the first real release; correct the endpoint if the actual repo differs."
  - service: minisign signing env (D-15)
    why: "pnpm tauri build signs the .app.tar.gz with the private key"
    env_vars:
      - name: TAURI_SIGNING_PRIVATE_KEY
        source: "~/.tauri/devtools.key (generated in Plan 03 Task 1) — gitignored env, never committed"
      - name: TAURI_SIGNING_PRIVATE_KEY_PASSWORD
        source: "the password chosen at key generation"

must_haves:
  truths:
    - "A RELEASE.md runbook documents the full manual release: confirm repo → bump → build → publish → latest.json → verify round-trip"
    - "pnpm tauri build produces a DMG AND a .app.tar.gz + .app.tar.gz.sig with the signing env set"
    - "The full automated suite is green: vitest (decoder 19) + tsc + eslint + real-WKWebView e2e"
    - "A human verifies the packaged build + a real updater round-trip (build N → publish → build N+1 → update verifies & relaunches); Gatekeeper-clean is DEFERRED post-enrolment"
  artifacts:
    - path: "docs/RELEASE.md"
      provides: "manual release runbook (D-16) incl. per-arch caveat, hdiutil unmount workaround, post-enrolment notarisation flip"
      contains: "createUpdaterArtifacts"
    - path: ".planning/phases/06-distribution/06-HUMAN-UAT.md"
      provides: "human sign-off checklist for the packaged build + updater round-trip"
      contains: "DST-02"
  key_links:
    - from: "docs/RELEASE.md"
      to: ".app.tar.gz.sig → latest.json signature"
      via: "documented copy-the-fresh-sig step (Pitfall 2)"
      pattern: "signature"
---

<objective>
Close the phase: write the `RELEASE.md` runbook (D-16) capturing the full manual release process, run the complete automated gate (vitest + tsc + eslint + real-WKWebView e2e), perform a fresh `pnpm tauri build` producing the DMG + updater artifacts, run `gsd-ui-review` (WCAG-AA, focused on the new banner/opt-in), and obtain the HUMAN sign-off on the packaged build INCLUDING a real updater round-trip (build N → publish a GitHub Release → build N+1 → confirm the update verifies and relaunches). The Gatekeeper-clean clause of DST-01 is DEFERRED to post-enrolment (D-02) and is NOT a blocker here.

This plan is `autonomous: false`: the real `tauri build` (signing env + DMG hdiutil step), creating a GitHub Release, and the two-build updater round-trip require human action and the signing secrets; the minisign verify + bundle swap are plugin internals exercisable only by this manual round-trip.

Purpose: deliver the "release-ready, pending cert" acceptance shape for DST-01 and the verify-before-apply proof for DST-02, with a runbook so the user can cut releases repeatably.
Output: `docs/RELEASE.md`, `06-HUMAN-UAT.md`, a verified packaged build + updater round-trip, phase marked complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-distribution/06-CONTEXT.md
@.planning/phases/06-distribution/06-RESEARCH.md
@.planning/phases/06-distribution/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the RELEASE.md runbook (D-16)</name>
  <files>docs/RELEASE.md</files>
  <read_first>
    - `06-RESEARCH.md` § "latest.json Manifest + Release Runbook" (the 7 numbered steps, the latest.json schema, the per-arch pitfall, the hdiutil unmount workaround) + § "macOS Signing" (the post-enrolment flip table)
    - `06-CONTEXT.md` D-05/D-07/D-08/D-14/D-15/D-16
    - `src-tauri/tauri.conf.json` (the pinned `boonkhailim/devtools` endpoint — RELEASE.md must flag confirming the real repo)
    - STATE.md "Build note (2026-06-01)" + user memory `tauri-dmg-bundle-flake` (hdiutil detach + retry)
  </read_first>
  <action>
    Create `docs/RELEASE.md` — a concrete, copy-pasteable runbook covering:
    0. **PRE-RELEASE (one-time):** confirm/create the public GitHub repo; verify the `plugins.updater.endpoints` owner/repo in `tauri.conf.json` matches it (currently the placeholder `boonkhailim/devtools`) — correct it if the real repo differs, AND update the `url`s in `latest.json` accordingly.
    1. Bump version in BOTH `src-tauri/tauri.conf.json` and `package.json` in lockstep (D-16).
    2. Export the signing env (gitignored, never committed): `TAURI_SIGNING_PRIVATE_KEY`, `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` (and, post-enrolment, the `APPLE_*` notary vars).
    3. `pnpm tauri build` → note the three outputs (`bundle/dmg/*.dmg`, `bundle/macos/*.app.tar.gz`, `*.app.tar.gz.sig`). Include the **hdiutil unmount workaround** (`hdiutil info` → `hdiutil detach <node>` for stray DMGs, then retry — Pitfall 5).
    4. Create a GitHub Release tagged `vX.Y.Z`; upload the DMG + `.app.tar.gz`.
    5. Build `latest.json` (include the schema): copy the FRESH `.app.tar.gz.sig` contents into `platforms.<key>.signature` (NEVER reuse a stale `.sig` — Pitfall 2); set `url` to the uploaded asset's `releases/download/...` URL; set `version`/`pub_date`/`notes`. Note the platform key is `darwin-aarch64` (or `darwin-x86_64`) for the build-host arch.
    6. Upload `latest.json` to the same release so `releases/latest/download/latest.json` resolves.
    7. Verify the round-trip: install the OLD DMG, run it, trigger "Check for Updates…", confirm prompt → install → relaunch into the new version.
    Plus three callout sections:
    - **Per-arch caveat (Pitfall 7 / A4):** a local Apple-Silicon build serves only `darwin-aarch64`; Intel users are not served until an `x86_64` (or universal) build is published — deferred to the CI phase.
    - **Post-enrolment notarisation flip (D-02/D-03):** export `APPLE_API_KEY`/`APPLE_API_ISSUER`/`APPLE_API_KEY_PATH` + `APPLE_SIGNING_IDENTITY`, add `providerShortName: "<TeamID>"` to `bundle.macOS`, rebuild — Tauri notarises automatically when the env is present. THEN re-verify Gatekeeper-clean install.
    - **Secrets reminder (D-05):** the minisign private key + password + Apple creds are local gitignored env only; the `.gitignore` already ignores `.env`/`*.key`/`*.p8` (Plan 01).
    - **CSP round-trip note (A2):** the updater download is Rust-side and may bypass the webview CSP; the GitHub hosts were added to `connect-src` defensively — note to confirm the real round-trip works (step 7 IS that confirmation).
  </action>
  <verify>
    <automated>test -f docs/RELEASE.md && grep -q 'createUpdaterArtifacts\|\.app\.tar\.gz' docs/RELEASE.md && grep -q 'hdiutil detach' docs/RELEASE.md && grep -q 'darwin-aarch64' docs/RELEASE.md && grep -q 'providerShortName' docs/RELEASE.md && grep -qi 'latest.json' docs/RELEASE.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RELEASE.md` exists
    - Documents the lockstep version bump, signing env export, `pnpm tauri build`, GitHub Release upload, `latest.json` construction (with schema + fresh-`.sig` warning), and the verify round-trip (all 7 steps + pre-release repo-confirm step 0)
    - `grep -q 'hdiutil detach' docs/RELEASE.md` exits 0 (DMG flake workaround)
    - `grep -q 'darwin-aarch64' docs/RELEASE.md` exits 0 (per-arch caveat)
    - `grep -q 'providerShortName' docs/RELEASE.md` exits 0 (post-enrolment notarisation flip)
    - Mentions confirming the `boonkhailim/devtools` endpoint matches the real repo before first release
  </acceptance_criteria>
  <done>`docs/RELEASE.md` is a complete, copy-pasteable runbook: pre-release repo-confirm, bump, signed build, publish, latest.json (fresh sig), round-trip verify, plus per-arch / notarisation-flip / secrets / CSP callouts.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full automated gate</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - `06-VALIDATION.md` § "Full suite command" + "Sampling Rate" (phase-gate row)
    - STATE.md harness reminder (simplify → /codex:review → vitest+tsc → real-webview UI; phase boundary build + ui-review)
  </read_first>
  <action>
    Run the full automated suite and confirm green BEFORE the human build sign-off:
    `pnpm test && pnpm exec tsc --noEmit && pnpm lint && bash scripts/e2e-spike.sh`
    Confirm: decoder's 19 tests still green; the new updater/banner/prefs tests pass; tsc clean; eslint 0; all real-WKWebView e2e specs pass (incl. `update.e2e.ts`). Also run the seam grep audit:
    `grep -rn "@tauri-apps/" src --include=*.ts --include=*.tsx | grep -v 'src/lib/platform/tauri.ts'` — only `src/lib/platform/index.ts`'s dynamic `import("./tauri")` may match; nothing else.
    If anything is red, STOP and fix (or report) before the human sign-off — the gate must be green first.
  </action>
  <verify>
    <automated>pnpm test && pnpm exec tsc --noEmit && pnpm lint && bash scripts/e2e-spike.sh</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0; the decoder 19-test suite is present and green
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm lint` exits 0
    - `bash scripts/e2e-spike.sh` exits 0 (all `test/e2e/*.e2e.ts` green, incl. `update.e2e.ts`)
    - Seam grep audit clean: no `@tauri-apps` import outside `src/lib/platform/tauri.ts` (and the index.ts dynamic import)
  </acceptance_criteria>
  <done>Full automated gate green: vitest (decoder 19 intact) + tsc + eslint + real-WKWebView e2e; seam audit clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Packaged build + updater round-trip human sign-off + gsd-ui-review</name>
  <what-built>The complete distribution path: a signed (ad-hoc) DMG + updater artifacts, the in-app updater UX (opt-in, tray check, dismissible banner), and a verifying auto-updater. This checkpoint verifies what automation cannot: the real `tauri build`, the packaged UX, and a real signature-verified update round-trip.</what-built>
  <read_first>
    - `docs/RELEASE.md` (the runbook — follow it for the build + round-trip)
    - `06-RESEARCH.md` § "Validation Architecture" Manual-Only rows + § "Updater UX Flow"
    - `06-VALIDATION.md` § "Manual-Only Verifications"
    - `04-HUMAN-UAT.md` / `05-04` sign-off precedent (deferred Phase-4 UAT was batched at Phase-5 — confirm it is closed or note carry-forward)
  </read_first>
  <how-to-verify>
    First create `.planning/phases/06-distribution/06-HUMAN-UAT.md` with this checklist, then perform it:

    A. BUILD (DST-01 "release-ready, pending cert"):
      1. Export `TAURI_SIGNING_PRIVATE_KEY` + `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` (from Plan 03 key gen).
      2. `pnpm tauri build` exits 0 (unmount stray DMGs first if the hdiutil step flakes — RELEASE.md).
      3. Confirm THREE artifacts exist under `src-tauri/target/release/bundle/`: a `*.dmg`, a `*.app.tar.gz`, and a `*.app.tar.gz.sig`.
      4. Install the DMG; the app launches (ad-hoc Gatekeeper friction is EXPECTED + acceptable this phase — D-02; right-click→Open to bypass).

    B. UPDATER UX (DST-02):
      5. First launch: the one-time opt-in prompt appears ("Enable automatic update checks?"); choose and confirm it persists (relaunch → not re-asked).
      6. Tray "Check for Updates…" runs a manual check regardless of the toggle; with no newer release it shows a quiet "up to date".
      7. The in-app banner is keyboard-reachable: Tab to Install/dismiss, dismiss with the keyboard; it re-appears on a subsequent detection.

    C. REAL ROUND-TRIP (DST-02 verify-before-apply — the load-bearing proof):
      8. Publish the current build as a GitHub Release + `latest.json` per RELEASE.md (build N).
      9. Bump to N+1, build + publish again.
      10. Run build N, trigger "Check for Updates…", confirm: prompt → Install → signature verified → relaunches into N+1. (A signature mismatch MUST refuse to install — that is DST-02 working.)

    D. AUDIT:
      11. Run `gsd-ui-review` (WCAG-AA) on the packaged build, focused on the UpdateBanner + opt-in prompt; record results in `06-UI-REVIEW.md`. Fix any AA blocker.

    DEFERRED (NOT a blocker — D-02): Gatekeeper-clean install on a clean machine (needs Apple Developer ID cert + notarisation) — re-verified post-enrolment.

    Record PASS/FAIL per item in `06-HUMAN-UAT.md`. On full pass, mark DST-01 (release-ready, pending cert) + DST-02 Complete in REQUIREMENTS.md + Traceability, mark Phase 6 `[x]` in ROADMAP.md, and update STATE.md.
  </how-to-verify>
  <acceptance_criteria>
    - `06-HUMAN-UAT.md` exists with the A–D checklist and recorded PASS/FAIL
    - `pnpm tauri build` produced a DMG + `.app.tar.gz` + `.sig` (DST-01 release-ready)
    - The opt-in prompt, tray manual check, and dismissible re-appearing banner all verified on the packaged build (DST-02 UX)
    - A real round-trip applied a signature-verified update and relaunched into the new version (DST-02 verify-before-apply)
    - `gsd-ui-review` WCAG-AA run; any AA blocker fixed; result in `06-UI-REVIEW.md`
    - Gatekeeper-clean explicitly recorded as DEFERRED post-enrolment (not failed)
  </acceptance_criteria>
  <action>
    HUMAN VERIFICATION (requires the real `tauri build`, the signing secrets, a published GitHub Release,
    and a two-build updater round-trip — not autonomous). Create `06-HUMAN-UAT.md` with the A–D checklist
    below, then perform every item, recording PASS/FAIL. See <how-to-verify> for the full A–D steps:
    A) build the signed DMG + updater artifacts; B) verify the opt-in/tray/banner UX on the packaged app;
    C) the real signature-verified round-trip (build N → publish → build N+1 → update applies + relaunches);
    D) run `gsd-ui-review` (WCAG-AA) on the banner/opt-in. Gatekeeper-clean is DEFERRED post-enrolment (D-02).
    On full pass, mark DST-01/DST-02 Complete + Phase 6 `[x]` and update STATE.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-distribution/06-HUMAN-UAT.md && grep -q 'DST-02' .planning/phases/06-distribution/06-HUMAN-UAT.md</automated>
  </verify>
  <done>`06-HUMAN-UAT.md` records PASS for the signed build (DMG + `.app.tar.gz` + `.sig`), the packaged updater UX, and a real signature-verified round-trip; `gsd-ui-review` AA pass; Gatekeeper-clean noted DEFERRED; requirements/roadmap/state updated.</done>
  <resume-signal>Type "approved" (with the round-trip + build confirmed), or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build host secrets → artifact | The minisign private key (env) signs the released `.app.tar.gz` |
| published GitHub Release → installed app | The update payload + `latest.json` are fetched and verified before apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Tampering / Elevation | the real update round-trip applying an unverified bundle | mitigate | The human round-trip (Task 3 C) confirms the plugin's minisign verify gates the apply — a mismatched `.sig` must refuse to install (DST-02 proof) |
| T-06-16 | Information disclosure | signing secret leaking via the build process | mitigate | Signing env is local gitignored (`.env`/`*.key` — Plan 01); RELEASE.md reiterates never-commit; only the public key is in the repo |
| T-06-17 | Spoofing | Gatekeeper friction misread as a build failure | accept | Ad-hoc signing shows expected Gatekeeper friction (D-02); documented as expected + deferred, not a phase blocker. Re-verified post-enrolment |
</threat_model>

<verification>
- `docs/RELEASE.md` complete (runbook + caveats).
- Full automated gate green (vitest/tsc/eslint/e2e), seam audit clean.
- Human: signed DMG + updater artifacts built; opt-in/tray/banner verified; real signature-verified round-trip relaunches into the new version; `gsd-ui-review` AA pass.
- Gatekeeper-clean explicitly deferred (D-02), not failed.
</verification>

<success_criteria>
DST-01 is "release-ready, pending cert" (signed DMG + updater artifacts; Developer-ID/notarisation a credentials-only flip) and DST-02 is proven (a real, signature-verified update applied and relaunched). RELEASE.md makes releases repeatable. Phase 6 is signed off (Gatekeeper-clean the sole deferred clause).
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-05-SUMMARY.md` and update REQUIREMENTS.md / ROADMAP.md / STATE.md to reflect Phase 6 completion (with the Gatekeeper-clean carry-forward noted).
</output>
