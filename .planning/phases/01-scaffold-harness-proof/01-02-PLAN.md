---
phase: 01-scaffold-harness-proof
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/router.tsx
  - src/main.tsx
  - src/App.tsx
  - src/lib/tools/registry.ts
  - src/lib/platform/index.ts
  - src/lib/platform/tauri.ts
  - src/lib/platform/stub.ts
  - src/lib/platform/platform.test.ts
  - src/tools/_skeleton/index.tsx
  - src/tools/_skeleton/transform.ts
  - src/tools/_skeleton/transform.test.ts
  - src/tools/_skeleton/ByteInspector.test.tsx
  - package.json
autonomous: true
requirements: [FND-02, FND-04, HRN-01]
user_setup: []

must_haves:
  truths:
    - "HashRouter is wired via createHashRouter; an unknown route redirects to the first tool (no BrowserRouter)"
    - "ENABLED_TOOLS is non-empty because the skeleton is registered enabled:true — so router.tsx's `firstTool = ENABLED_TOOLS[0]` resolves at module load and the app boots (no startup crash)"
    - "The throwaway byte-inspector skeleton transforms input instantly on paste (Cmd+V), with no decode button"
    - "The skeleton's copy affordance is always-visible and keyboard-focusable (reachable in ≤1 keystroke), never hover-only"
    - "The skeleton shows a status bar: parse state · byte count · timing"
    - "The skeleton copies via src/lib/platform — no tool imports @tauri-apps/* directly"
  artifacts:
    - path: "src/router.tsx"
      provides: "HashRouter ported verbatim from scaffold; unknown route → first tool"
      contains: "createHashRouter"
    - path: "src/lib/tools/registry.ts"
      provides: "Control plane — skeleton added as the first enabled:true entry so ENABLED_TOOLS is non-empty (registry is intentionally modified here; NOT byte-frozen)"
      contains: "_skeleton"
    - path: "src/lib/platform/index.ts"
      provides: "Platform capability interface + impl picker (clipboard real, store stub)"
      exports: ["platform", "Platform"]
    - path: "src/lib/platform/tauri.ts"
      provides: "Real clipboard impl wrapping @tauri-apps/plugin-clipboard-manager (the ONLY file allowed to import it)"
      contains: "clipboard-manager"
    - path: "src/tools/_skeleton/transform.ts"
      provides: "Throwaway trivial transform (length + uppercase/hex) — NOT the real protobuf/base64 lib"
      contains: "PHASE 1 THROWAWAY"
    - path: "src/tools/_skeleton/transform.test.ts"
      provides: "TDD unit tests for the skeleton transform logic"
      contains: "describe"
  key_links:
    - from: "src/router.tsx"
      to: "src/lib/tools/registry.ts ENABLED_TOOLS"
      via: "createHashRouter mapping ENABLED_TOOLS + Navigate fallback; skeleton makes ENABLED_TOOLS[0] resolve"
      pattern: "createHashRouter"
    - from: "src/lib/tools/registry.ts TOOLS"
      to: "src/tools/_skeleton (skeletonTool, enabled:true)"
      via: "registry import + TOOLS array entry → ENABLED_TOOLS non-empty → firstTool resolves"
      pattern: "_skeleton"
    - from: "src/tools/_skeleton (copy button)"
      to: "src/lib/platform clipboard.writeText"
      via: "platform.clipboard.writeText (NOT @tauri-apps/*)"
      pattern: "platform\\.clipboard\\.writeText"
---

<objective>
Wire the HashRouter (verbatim from scaffold), stand up the `src/lib/platform/` capability seam (real Tauri clipboard impl + thin stubs), and build the throwaway walking-skeleton "byte inspector" — a paste→transform→copy demo that exercises the exact UX-constraint surface the per-task UI gate must verify. Critically, register the skeleton as the first `enabled:true` tool in `registry.ts` so `ENABLED_TOOLS` is non-empty and the verbatim `router.tsx` (which reads `ENABLED_TOOLS[0]` at module load) boots without crashing. This is the realistic shape the harness will be driven through in Plan 04.

Purpose: Prove FND-02 (HashRouter, no BrowserRouter), FND-04 (tools reach OS capabilities ONLY through the platform seam), and build the HRN-01 skeleton with TDD coverage. The skeleton is explicitly throwaway (D-05) — deleted before Phase 2, at which point its registry entry is removed too — and must NOT reuse the real Protobuf/Base64 tools.
Output: A routed dark app whose unknown routes redirect to the first tool, a tested platform seam, and a tested byte-inspector skeleton with instant-paste, a visible+focusable copy affordance, and a status bar.

Implements: FND-02 (HashRouter), FND-04 (platform seam, no direct @tauri-apps/* in tools), HRN-01 (walking-skeleton feature).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-harness-proof/01-CONTEXT.md
@.planning/phases/01-scaffold-harness-proof/01-RESEARCH.md
@CLAUDE.md
@docs/harness-and-decisions.md
@scaffold/src/main.tsx
@scaffold/src/router.tsx
@scaffold/src/lib/tools/types.ts
@scaffold/src/lib/tools/registry.ts
@design/DevTools Mockup.html

<interfaces>
<!-- Contracts the executor builds against — no codebase exploration needed. -->

HashRouter (scaffold/src/router.tsx — port VERBATIM, FND-02):
  import { createHashRouter, Navigate } from "react-router-dom";
  const firstTool = ENABLED_TOOLS[0];                       // ← evaluated AT MODULE LOAD; firstTool.id used below
  createHashRouter([{ path: "/", element: <App/>, children: [
    { index: true, element: <Navigate to={`/tools/${firstTool.id}`} replace/> },
    ...ENABLED_TOOLS.map(t => ({ path: `tools/${t.id}`, element: <t.component/> })),
    { path: "*", element: <Navigate to={`/tools/${firstTool.id}`} replace/> },  // unknown → first
  ]}]);
  CRITICAL (Warning 2): if ENABLED_TOOLS is empty, `firstTool` is undefined and `firstTool.id` THROWS
  at module load — the app never boots. Plan 01 left all three registry tools `enabled:false` (stubs),
  so ENABLED_TOOLS is currently []. THIS plan fixes that by registering the skeleton enabled:true in
  registry.ts (Task 2 + Task 3), making it ENABLED_TOOLS[0]. There is NO verbatim path that injects the
  skeleton — registry.ts MUST be edited. router.tsx itself stays verbatim.

Entry stylesheet (Warning 1): scaffold/src/main.tsx imports `./index.css`. Plan 01 named the entry
  stylesheet `src/index.css` (matching this import). When porting main.tsx verbatim in Task 3, the
  `import "./index.css"` line therefore resolves a real file — do NOT change it to styles.css.

registry.ts edit (Warning 2 — registry is the explicit control plane, NOT byte-frozen; only
  decoder.ts/bytes.ts/types.ts are byte-frozen). Add the skeleton as the FIRST entry, enabled:true:
    import { skeletonTool } from "@/tools/_skeleton";   // skeletonTool: ToolDefinition, id "_skeleton", enabled: true
    export const TOOLS: ToolDefinition[] = [skeletonTool, unixTimeTool, base64Tool, protobufDecoderTool];
  The three Phase-3 stubs remain enabled:false, so ENABLED_TOOLS === [skeletonTool].
  Mark the skeleton import + array entry with `// PHASE 1 THROWAWAY — remove with the skeleton before Phase 2`.

Platform seam (RESEARCH Pattern 2, harness-and-decisions §2):
  export interface Platform {
    clipboard: { writeText(t: string): Promise<void>; readText(): Promise<string> };
    store: { get(k: string): Promise<unknown>; set(k: string, v: unknown): Promise<void> }; // STUB in P1
  }
  // tauri.ts is the ONLY file allowed to import @tauri-apps/plugin-clipboard-manager:
  import { writeText, readText } from "@tauri-apps/plugin-clipboard-manager";
  // index.ts picks impl: export const platform: Platform = tauriPlatform;

Skeleton spec (D-04, D-05): a "byte inspector" — on paste show input length + an uppercase/hex transform.
  Constraints it MUST exercise: paste-transforms-instantly (Cmd+V, no decode button); a visible + focusable
  copy affordance reachable in ≤1 keystroke (NO hover-only); a status bar (parse state · byte count · timing).
  It must NOT call the real bytes.ts/decoder.ts (D-05) — keep the transform trivial.
  The skeleton's tool module ALSO exports a `skeletonTool: ToolDefinition` (id "_skeleton", enabled:true,
  component = ByteInspector) so registry.ts can register it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the platform capability seam (real Tauri clipboard + thin stubs) with a mock-seam test</name>
  <read_first>
    - .planning/phases/01-scaffold-harness-proof/01-RESEARCH.md (Pattern 2, FND-04 row)
    - docs/harness-and-decisions.md (§2 platform seam rule: tools import lib/platform, never @tauri-apps/*)
    - src-tauri/src/lib.rs (clipboard plugin registered in Plan 01 — confirm)
  </read_first>
  <behavior>
    - Test 1: `platform.clipboard.writeText(s)` delegates to the injected clipboard impl (assert it's called with `s`) via a mockable seam point
    - Test 2: `platform.store.get`/`set` are callable no-op/in-memory stubs (return without throwing) — full store lands Phase 2
    - Test 3: a consumer using `platform.clipboard` needs NO `@tauri-apps/*` import
  </behavior>
  <action>
    1. Create `src/lib/platform/index.ts`: export the `Platform` interface (clipboard{writeText,readText} + store{get,set}) and `export const platform: Platform` that picks the impl. Structure the impl pick so a test can inject a mock (e.g. export a `setPlatform()` test seam or import the impl from `./tauri`).
    2. Create `src/lib/platform/tauri.ts`: the ONLY file importing `@tauri-apps/plugin-clipboard-manager` — `import { writeText, readText } from "@tauri-apps/plugin-clipboard-manager"` and expose `{ clipboard: { writeText, readText }, store: <stub from ./stub> }`.
    3. Create `src/lib/platform/stub.ts`: an in-memory `store` stub (`get`/`set` over a `Map`) and any shortcuts/window no-op stubs. Comment: "Phase 2 (SHL-05) replaces store with @tauri-apps/plugin-store".
    4. Create `src/lib/platform/platform.test.ts` (environment: node, or jsdom if needed): mock the clipboard impl, assert writeText delegates; assert store stub round-trips in memory.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/platform && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/lib/platform` → all pass
    - `grep -rn "@tauri-apps" src/lib/platform` shows the import ONLY in `tauri.ts` (no other platform file imports it)
    - `grep -q 'writeText' src/lib/platform/index.ts` (interface exposes clipboard.writeText)
    - `grep -q 'store' src/lib/platform/stub.ts`
    - `pnpm tsc --noEmit` clean
  </acceptance_criteria>
  <done>Platform seam exists with a real Tauri clipboard impl isolated in tauri.ts, in-memory store stub, and a passing mock-seam test proving clipboard delegation. No tool/consumer needs @tauri-apps/* directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the throwaway byte-inspector skeleton (transform + UI + ToolDefinition export) with TDD coverage</name>
  <read_first>
    - .planning/phases/01-scaffold-harness-proof/01-CONTEXT.md (D-04, D-05 skeleton spec; "specifics" — copy must be always-visible, hover forbidden)
    - .planning/phases/01-scaffold-harness-proof/01-RESEARCH.md (Don't Hand-Roll row on skeleton; Pitfall 2 jsdom; Pitfall 3 registry approach)
    - scaffold/src/lib/tools/types.ts (the ToolDefinition contract the skeletonTool must satisfy)
    - src/lib/platform/index.ts (the seam to copy through — built in Task 1)
    - design/DevTools Mockup.html (status-bar + copy-affordance patterns, --ok/--bad colors)
  </read_first>
  <behavior>
    - transform.test.ts (env node): `inspect("abc")` returns byteLength 3 + uppercase "ABC" + hex "616263"; empty input → byteLength 0, parse state "empty"; multibyte/unicode byte count is correct (UTF-8 byte length, not char length)
    - ByteInspector.test.tsx (jsdom): pasting into the input updates the output WITHOUT clicking any button (instant transform); the copy button is in the tab order (focusable) and visible by default (NOT hover-gated); the status bar renders byte count + a timing value
  </behavior>
  <action>
    1. Create `src/tools/_skeleton/transform.ts`: a TRIVIAL transform (D-05 — must NOT import bytes.ts/decoder.ts). Compute UTF-8 byte length (`new TextEncoder().encode(s).length`), an uppercase form, and a hex form using inline trivial code (NOT the ported lib). Add header comment `// PHASE 1 THROWAWAY — delete before Phase 2. Does NOT use the real protobuf/base64 lib.`
    2. Create `src/tools/_skeleton/transform.test.ts` (env node) implementing the behaviors above (write tests first — RED — then implement transform.ts — GREEN).
    3. Create `src/tools/_skeleton/index.tsx` — the `ByteInspector` component AND a `skeletonTool: ToolDefinition` export. Component: a textarea/input that transforms INSTANTLY on change/paste (Cmd+V, no decode button, satisfies UX-01 shape); an output region with an ALWAYS-visible, keyboard-focusable copy button (a real `<button>` in tab order, NOT `opacity-0 group-hover` — D-04/specifics; this is the hover-only regression the gate must catch); the copy button calls `platform.clipboard.writeText(...)` from the seam (NEVER @tauri-apps/*); a status bar showing parse state · byte count · timing (ms to transform). Use Tailwind utilities, layout-agnostic (no fixed widths). Also export `export const skeletonTool: ToolDefinition = { id: "_skeleton", name: "Byte Inspector (skeleton)", enabled: true, component: ByteInspector, ... }` satisfying `types.ts` (fill required fields: description/keywords/etc.). Mark the file `// PHASE 1 THROWAWAY` so it is obvious for deletion before Phase 2 (D-05).
    4. Create `src/tools/_skeleton/ByteInspector.test.tsx` (`// @vitest-environment jsdom`, @testing-library/react) implementing the component behaviors above. (Install `pnpm add -D @testing-library/react jsdom` if Plan 01 didn't.)
  </action>
  <verify>
    <automated>pnpm vitest run src/tools/_skeleton && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/tools/_skeleton` → all pass (transform + component)
    - `grep -q 'PHASE 1 THROWAWAY' src/tools/_skeleton/transform.ts` AND `grep -q 'PHASE 1 THROWAWAY' src/tools/_skeleton/index.tsx`
    - `grep -q 'skeletonTool' src/tools/_skeleton/index.tsx` AND the export satisfies `ToolDefinition` with `enabled: true` (tsc proves the type)
    - `grep -rn "@tauri-apps" src/tools/_skeleton` → NO matches (copies through the seam)
    - `grep -rn "decoder\|bytes" src/tools/_skeleton/transform.ts` → NO import of the real lib (D-05)
    - `grep -q 'platform.clipboard.writeText' src/tools/_skeleton/index.tsx`
    - `! grep -qE 'group-hover.*copy|opacity-0.*copy' src/tools/_skeleton/index.tsx` (copy is NOT hover-only)
    - Component test asserts the copy `<button>` is focusable (in tab order) and the output transforms without a button click
    - `pnpm tsc --noEmit` clean
  </acceptance_criteria>
  <done>Throwaway byte-inspector skeleton with a trivial (non-lib) transform, instant-paste, an always-visible focusable copy button routed through the platform seam, a status bar, AND a `skeletonTool` ToolDefinition export (enabled:true) ready to register — all TDD-covered (node transform tests + jsdom component tests).</done>
</task>

<task type="auto">
  <name>Task 3: Register the skeleton in registry.ts (enabled:true), wire the HashRouter verbatim, prove firstTool resolves + unknown→skeleton redirect</name>
  <read_first>
    - scaffold/src/router.tsx (port VERBATIM — FND-02)
    - scaffold/src/main.tsx (port VERBATIM — imports `./index.css`, which Plan 01 created; do NOT change to styles.css)
    - scaffold/src/lib/tools/registry.ts (the control plane to edit — NOT byte-frozen)
    - CLAUDE.md (HashRouter only; BrowserRouter forbidden; registry is the single control plane)
    - src/tools/_skeleton/index.tsx (exports skeletonTool — built in Task 2)
  </read_first>
  <action>
    1. Edit `src/lib/tools/registry.ts` (Warning 2 — registry is the explicit control plane and is intentionally MODIFIED here, NOT byte-verbatim; only decoder.ts/bytes.ts/types.ts stay byte-frozen). Add `import { skeletonTool } from "@/tools/_skeleton";` and prepend it to the `TOOLS` array: `export const TOOLS: ToolDefinition[] = [skeletonTool, unixTimeTool, base64Tool, protobufDecoderTool];`. Since the three Phase-3 stubs are `enabled:false` and the skeleton is `enabled:true`, `ENABLED_TOOLS === [skeletonTool]` and `ENABLED_TOOLS[0]` resolves. Mark the import + array entry `// PHASE 1 THROWAWAY — remove with the skeleton before Phase 2` (D-05).
    2. Port `scaffold/src/router.tsx` to `src/router.tsx` VERBATIM — `createHashRouter` mapping `ENABLED_TOOLS` to `tools/<id>` routes, an index `Navigate` to the first tool, and a `path: "*"` `Navigate` to the first tool (unknown → first). Do NOT introduce BrowserRouter. Because Task-2/step-1 made the skeleton ENABLED_TOOLS[0], `firstTool = ENABLED_TOOLS[0]` resolves at module load and `firstTool.id` no longer throws.
    3. Port `scaffold/src/main.tsx` to `src/main.tsx` VERBATIM — including its `import "./index.css"` (Plan 01 named the entry stylesheet `index.css`, so this resolves) — wiring `<RouterProvider router={router} />`. Ensure `src/App.tsx` is the layout shell (the dark window from Plan 01 stays; the routed tool renders inside via `<Outlet/>`). Keep it minimal — the real shell/sidebar is Phase 2.
    4. Confirm the dark window + skeleton render under `tauri dev` (manual UI gate happens in Plan 04). Verify in build that no startup crash occurs (firstTool resolved).
  </action>
  <verify>
    <automated>grep -q "createHashRouter" src/router.tsx && ! grep -rn "BrowserRouter" src && grep -q "skeletonTool" src/lib/tools/registry.ts && pnpm tsc --noEmit && pnpm vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'createHashRouter' src/router.tsx`
    - `grep -rn 'BrowserRouter' src` → NO matches (FND-02 invariant)
    - `grep -q 'path: "\*"' src/router.tsx` AND `grep -q 'Navigate' src/router.tsx` (unknown→first redirect present)
    - `grep -q 'skeletonTool' src/lib/tools/registry.ts` (skeleton registered enabled:true → ENABLED_TOOLS non-empty)
    - `grep -q 'index.css' src/main.tsx` (entry stylesheet import resolves — Warning 1)
    - `grep -q 'RouterProvider' src/main.tsx`
    - `pnpm vite build` exits 0 — this PROVES `firstTool = ENABLED_TOOLS[0]` resolves at module load (an empty ENABLED_TOOLS would throw `firstTool.id` and fail the build/render)
    - Navigating to a bad hash route (e.g. `#/tools/does-not-exist`) redirects to the skeleton route `#/tools/_skeleton` (verified manually in Plan 04's UI gate; the `path: "*"` Navigate-to-firstTool wiring is present in router.tsx)
    - `pnpm tsc --noEmit` clean
  </acceptance_criteria>
  <done>Skeleton registered enabled:true in registry.ts (control-plane edit, NOT byte-frozen); HashRouter + main.tsx ported verbatim and mounted; `firstTool` resolves at module load (build green proves no startup crash); unknown routes redirect to the skeleton; no BrowserRouter anywhere; entry stylesheet links via index.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool code ↔ OS capability | Tools must reach clipboard ONLY through `src/lib/platform`; a direct `@tauri-apps/*` import in a tool bypasses the audited seam |
| pasted input ↔ skeleton transform | User-pasted bytes enter the throwaway transform |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of privilege | A tool importing `@tauri-apps/*` directly bypasses the capability seam | mitigate | Acceptance criterion `grep -rn "@tauri-apps" src/tools` → no matches; the import lives ONLY in `src/lib/platform/tauri.ts` |
| T-01-05 | Denial of service | Pathological paste into the skeleton transform causes a hang | accept | Skeleton transform is trivial O(n) string work (TextEncoder + uppercase + hex), not recursive; it is throwaway and deleted before Phase 2; no untrusted parsing |
| T-01-06 | Tampering | BrowserRouter (forbidden) would 404 on reload and break deep-links | mitigate | HashRouter ported verbatim; acceptance criterion `grep -rn "BrowserRouter" src` → none |
| T-01-13 | Denial of service | Empty ENABLED_TOOLS makes `firstTool.id` throw at module load → app never boots | mitigate | Skeleton registered enabled:true so ENABLED_TOOLS[0] resolves; acceptance criterion `pnpm vite build` exits 0 proves module-load evaluation succeeds |
</threat_model>

<verification>
- `pnpm vitest run src/lib/platform src/tools/_skeleton` → all pass
- `pnpm tsc --noEmit` → clean
- `grep -rn "@tauri-apps" src/tools` → no matches (FND-04)
- `grep -rn "BrowserRouter" src` → no matches (FND-02)
- `grep -q "skeletonTool" src/lib/tools/registry.ts` → skeleton registered (ENABLED_TOOLS non-empty)
- `pnpm vite build` → succeeds (proves firstTool resolves at module load)
- Manual (deferred to Plan 04 real-webview gate): paste transforms instantly, copy button focusable+visible, status bar shows byte count + timing, bad hash route → skeleton
</verification>

<success_criteria>
- HashRouter wired verbatim; unknown routes redirect to the first tool; no BrowserRouter (FND-02)
- Skeleton registered enabled:true in registry.ts so ENABLED_TOOLS[0] resolves — no empty-array startup crash (Warning 2 closed)
- main.tsx ported verbatim with `./index.css` resolving to Plan 01's entry stylesheet (Warning 1 closed)
- Platform seam exists; tools reach clipboard only through it; no direct @tauri-apps/* in tools (FND-04)
- Throwaway byte-inspector skeleton built with instant-paste, always-visible focusable copy via the seam, and a status bar (HRN-01)
- Skeleton transform is trivial (does NOT reuse decoder/bytes) and clearly marked throwaway (D-05); its registry entry is marked for removal before Phase 2
- All new code TDD-covered; tsc clean; build green
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-harness-proof/01-02-SUMMARY.md` documenting: the platform seam shape, the skeleton's in-registry registration (enabled:true, the explicit Warning-2 fix — registry.ts edited, NOT byte-frozen), confirmation main.tsx's `./index.css` resolves (Warning 1), and confirmation the skeleton + its registry entry are marked throwaway for Phase-2 deletion.
</output>
</output>
