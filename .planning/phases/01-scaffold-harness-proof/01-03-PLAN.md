---
phase: 01-scaffold-harness-proof
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lefthook.yml
  - package.json
  - docs/phase-0-notes.md
autonomous: true
requirements: [HRN-03, HRN-04]
user_setup: []

must_haves:
  truths:
    - "A lefthook pre-commit hook runs `pnpm tsc --noEmit` + `pnpm vitest run` and BLOCKS a commit with a type error or failing test"
    - "It is documented that the UI gate and /codex:review remain manual per-task steps (lefthook covers the unit gate only)"
    - "`pnpm tauri build` produces a runnable unsigned macOS bundle"
    - "Build findings (signing/Gatekeeper surprises, bundle size, toolchain quirks) are recorded in docs/phase-0-notes.md"
  artifacts:
    - path: "lefthook.yml"
      provides: "Pre-commit unit gate (tsc --noEmit + vitest run), parallel"
      contains: "pre-commit"
    - path: "docs/phase-0-notes.md"
      provides: "HRN-04 build findings + gate-enforcement notes (automation path appended in Plan 04)"
      contains: "tauri build"
  key_links:
    - from: "lefthook.yml pre-commit"
      to: "pnpm tsc --noEmit + pnpm vitest run"
      via: "lefthook install hook → git pre-commit"
      pattern: "vitest run"
    - from: "pnpm tauri build"
      to: "src-tauri/target/release/bundle/macos/*.app"
      via: "tauri CLI macOS bundler"
      pattern: "tauri build"
---

<objective>
Install the mechanical unit gate (lefthook pre-commit running `tsc --noEmit` + `vitest run`, D-07) and prove it blocks bad commits, then run `pnpm tauri build` to produce a runnable unsigned macOS bundle (HRN-04) and capture findings in `docs/phase-0-notes.md`. This plan touches build/tooling files only — it runs in parallel with Plan 02 (no file overlap).

Purpose: HRN-03 demands the per-task DoD be enforced for every subsequent phase; lefthook makes the unit gate mechanical (not just agent discipline). HRN-04 demands a verified `tauri build` at each phase boundary; running it now surfaces signing/toolchain surprises early (the documented reason this build happens in Phase 1).
Output: A working blocking pre-commit hook, a runnable `.app`/`.dmg` bundle, and a `docs/phase-0-notes.md` recording build findings + the manual-gate boundary.

Implements: HRN-03 (gate enforcement, parallel plans never bypass), HRN-04 (runnable macOS bundle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-harness-proof/01-CONTEXT.md
@.planning/phases/01-scaffold-harness-proof/01-RESEARCH.md
@CLAUDE.md
@docs/harness-and-decisions.md

<interfaces>
<!-- Exact config the executor writes — from RESEARCH.md. -->

lefthook.yml (RESEARCH Code Examples; D-07):
  pre-commit:
    parallel: true
    commands:
      typecheck:
        run: pnpm tsc --noEmit
      test:
        run: pnpm vitest run
  # Install: `pnpm lefthook install` (or a package.json "prepare": "lefthook install")
  # D-08: lefthook covers the UNIT gate only. /codex:review (disable-model-invocation)
  #       and the real-webview UI check stay MANUAL per-task DoD steps — a git hook cannot run them.

tauri build (RESEARCH Pitfall 6; HRN-04):
  `pnpm tauri build`  → unsigned artifacts in:
    src-tauri/target/release/bundle/macos/*.app
    src-tauri/target/release/bundle/dmg/*.dmg
  Unsigned → Gatekeeper "unidentified developer"/"damaged" is EXPECTED in Phase 1 (signing = Phase 6).
  To launch locally: right-click → Open, or `xattr -dr com.apple.quarantine <App>.app`.
  First cargo build is slow (compiles all Tauri crates). cargo 1.83 present; if MSRV error → `rustup update stable`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lefthook pre-commit unit gate and prove it BLOCKS bad commits</name>
  <read_first>
    - .planning/phases/01-scaffold-harness-proof/01-RESEARCH.md (lefthook.yml Code Example; Don't Hand-Roll lefthook row; D-07/D-08)
    - docs/harness-and-decisions.md (§3 three-gate DoD — which gates are mechanical vs manual)
    - package.json (add lefthook + prepare script)
  </read_first>
  <action>
    1. `pnpm add -D lefthook@2.1.9`. Add a `"prepare": "lefthook install"` script to package.json (so the hook installs on `pnpm install`), then run `pnpm lefthook install` now.
    2. Create `lefthook.yml` exactly per the interface above: `pre-commit` with `parallel: true` and two commands — `typecheck: pnpm tsc --noEmit` and `test: pnpm vitest run`.
    3. PROVE the gate blocks (acceptance is behavioral, not just "file exists"): create a temporary throwaway file with a deliberate type error (e.g. `const x: number = "oops";`), `git add` it, attempt `git commit` → the commit MUST be rejected by lefthook. Then revert/delete the bad file. Repeat the proof conceptually for a failing test (or note the typecheck block is sufficient evidence the hook runs). Record the blocked-commit output in the SUMMARY.
    4. Add a clear note (in lefthook.yml comments and the SUMMARY) per D-08: the UI gate and `/codex:review` remain MANUAL per-task steps — lefthook enforces the unit gate only.
  </action>
  <verify>
    <automated>test -f lefthook.yml && grep -q "pnpm vitest run" lefthook.yml && grep -q "tsc --noEmit" lefthook.yml && test -f .git/hooks/pre-commit</automated>
  </verify>
  <acceptance_criteria>
    - `lefthook.yml` exists with `pre-commit` running both `pnpm tsc --noEmit` and `pnpm vitest run`
    - `.git/hooks/pre-commit` exists (lefthook installed the hook)
    - A commit attempted with a deliberate type error is REJECTED (capture the output); after reverting, a clean commit succeeds
    - `package.json` has a `"prepare": "lefthook install"` script
    - lefthook.yml comment documents D-08 (UI gate + /codex:review stay manual)
  </acceptance_criteria>
  <done>lefthook pre-commit unit gate installed and demonstrated to block a commit with a type error; the manual-gate boundary (UI + /codex:review) is documented. HRN-03 mechanical enforcement in place.</done>
</task>

<task type="auto">
  <name>Task 2: Run tauri build, produce a runnable unsigned macOS bundle, capture findings in phase-0-notes.md</name>
  <read_first>
    - .planning/phases/01-scaffold-harness-proof/01-RESEARCH.md (Pitfall 6; Environment Availability — cargo 1.83/Xcode/macOS present; A6 unsigned-build assumption)
    - .planning/phases/01-scaffold-harness-proof/01-CONTEXT.md (deliverable: docs/phase-0-notes.md is the HRN-02/HRN-04 record)
    - src-tauri/tauri.conf.json (bundle config from Plan 01)
  </read_first>
  <action>
    1. Run `pnpm tauri build`. The first cargo build compiles all Tauri crates (slow — allow time). If a Tauri/crate MSRV error appears, run `rustup update stable` and retry (note it).
    2. Locate the artifacts: `src-tauri/target/release/bundle/macos/*.app` and `src-tauri/target/release/bundle/dmg/*.dmg`. Confirm the `.app` is runnable locally — `xattr -dr com.apple.quarantine <App>.app` then launch it (unsigned/Gatekeeper warnings are EXPECTED and in-scope-as-noted; signing is Phase 6, do NOT attempt to fix).
    3. Create `docs/phase-0-notes.md` (the phase deliverable) and record under an "HRN-04: tauri build" section: the exact artifact paths, bundle size of the `.app`/`.dmg`, build duration, cargo/rustc version used, any MSRV/toolchain quirks, and the unsigned-Gatekeeper behavior observed. Leave a clearly-marked placeholder section "HRN-02: macOS webview automation path" for Plan 04 to fill (so both deliverables live in one file).
    4. Re-confirm no webdriver/debug-only surface leaked into this release bundle (the webdriver plugin is added in Plan 04 behind `debug_assertions`; this build predates it — note that the release build must never expose port 4445).
  </action>
  <verify>
    <automated>ls src-tauri/target/release/bundle/macos/*.app >/dev/null 2>&1 && test -f docs/phase-0-notes.md && grep -qi "tauri build" docs/phase-0-notes.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls src-tauri/target/release/bundle/macos/*.app` shows a built `.app`
    - The `.app` launches locally after clearing quarantine (manual confirmation; record in notes)
    - `docs/phase-0-notes.md` exists with an "HRN-04" section recording artifact paths, bundle size, build duration, toolchain version, and unsigned-Gatekeeper behavior
    - `docs/phase-0-notes.md` contains a placeholder "HRN-02" section for Plan 04
    - No code-signing/notarisation was attempted (out of scope — Phase 6)
  </acceptance_criteria>
  <done>`pnpm tauri build` produced a runnable unsigned macOS `.app`/`.dmg`; findings (paths, size, duration, toolchain, Gatekeeper behavior) recorded in docs/phase-0-notes.md with an HRN-02 placeholder for Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit ↔ repo | Untested/broken code attempting to enter the repo; the pre-commit gate is the control |
| release bundle ↔ end user | The shipped artifact must not expose dev-only surfaces (e.g. webdriver server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Broken/untyped code lands in the repo, eroding the decoder's green bar | mitigate | lefthook pre-commit runs tsc+vitest and BLOCKS the commit; proven by a deliberate-failure test (acceptance criterion) |
| T-01-08 | Information disclosure | Dev-only WebDriver server (port 4445) compiled into a release build → remote control surface | mitigate | This release build predates the webdriver plugin; Plan 04 gates it strictly under `[target.'cfg(debug_assertions)'.dependencies]` + `#[cfg(debug_assertions)]` (RESEARCH Pitfall 4); noted here |
| T-01-09 | Spoofing | Unsigned bundle is trivially impersonable/altered | accept | Phase 1 ships an UNSIGNED dev bundle by design to surface toolchain surprises early; signing/notarisation is Phase 6 (DST-01) — explicitly out of scope here |
</threat_model>

<verification>
- `lefthook.yml` present; `.git/hooks/pre-commit` installed; a deliberate-type-error commit is rejected
- `pnpm tauri build` produces `src-tauri/target/release/bundle/macos/*.app`
- `docs/phase-0-notes.md` records HRN-04 findings + an HRN-02 placeholder
- Manual: the unsigned `.app` launches after `xattr -dr com.apple.quarantine`
</verification>

<success_criteria>
- lefthook unit gate installed and DEMONSTRATED to block bad commits (HRN-03)
- Manual-gate boundary (UI + /codex:review) documented per D-08
- `pnpm tauri build` produces a runnable unsigned macOS bundle (HRN-04)
- Build findings recorded in docs/phase-0-notes.md
- No signing attempted (Phase 6); no webdriver surface in this release build
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-harness-proof/01-03-SUMMARY.md` documenting: the blocked-commit proof output, the bundle artifact paths + size + build duration, and any toolchain quirks (cargo MSRV / rustup update).
</output>
