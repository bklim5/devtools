---
phase: 22.1-settings-followups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/lib.rs
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "The macOS app menu shows the product name TinkerDev everywhere: the bold app-menu title plus About TinkerDev / Hide TinkerDev / Quit TinkerDev (not the devtools-app bin name)"
    - "The Edit submenu still carries Undo/Redo/Cut/Copy/Paste/Select-All and the Window submenu its standard items (no regression — Pitfall 1)"
    - "The ⌘, Settings item still lives under the App submenu and still emits menu://open-settings"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "set_menu() App submenu with explicit PredefinedMenuItem about/hide/quit text"
      contains: "PredefinedMenuItem::about"
  key_links:
    - from: "src-tauri/src/lib.rs App submenu"
      to: "macOS application menu labels"
      via: "explicit PredefinedMenuItem text Some(\"About/Hide/Quit TinkerDev\")"
      pattern: "Some\\(\"About TinkerDev\"\\)"
---

<objective>
Close Follow-up 1 (BUG) from Phase 22's walkthrough: the macOS app menu reads the Cargo bin name `devtools-app` ("About devtools-app" / "Hide devtools-app" / "Quit devtools-app") instead of the product name **TinkerDev**.

The App `SubmenuBuilder` title is ALREADY `"TinkerDev"` (lib.rs ~line 146). The wrong labels come from the no-text chainable predefined items `.about(None)` / `.hide()` / `.quit()`, which derive their label from the executable name. Fix: build explicit `PredefinedMenuItem`s with custom text and `.item()` them.

Purpose: a paying/free user sees the product name, not a build artifact name, in the macOS menu bar.
Output: a rebuilt `.app` whose App menu reads About/Hide/Quit **TinkerDev**, with Edit + Window submenus intact, manually re-verified at the human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-settings-followups/22.1-CONTEXT.md
@.planning/phases/22-settings-modal-shell/22-FOLLOWUP.md
@src-tauri/src/lib.rs

<interfaces>
<!-- Verified against the installed crate tauri-2.11.2/src/menu/predefined.rs (per CONTEXT D-22.1-2). -->
<!-- All three are associated functions taking the manager + Option<&str> text. Use the explicit form, NOT the SubmenuBuilder no-text chainable methods. -->

PredefinedMenuItem::about<M: Manager<R>>(manager: &M, text: Option<&str>, metadata: Option<AboutMetadata>) -> Result<PredefinedMenuItem<R>>
PredefinedMenuItem::hide<M: Manager<R>>(manager: &M, text: Option<&str>) -> Result<PredefinedMenuItem<R>>
PredefinedMenuItem::quit<M: Manager<R>>(manager: &M, text: Option<&str>) -> Result<PredefinedMenuItem<R>>

// SubmenuBuilder.item(&self, item: &dyn IsMenuItem<R>) -> Self  — adds a pre-built item.
// The existing builder already uses .item(&settings_app_i) for the ⌘, Settings MenuItem, so the .item() pattern is in-file.
// The predefined `services`/`hide_others`/`show_all`/`separator` chainable methods stay AS-IS (no text-name leak — they are localized OS labels).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set explicit TinkerDev text on the App submenu's About/Hide/Quit predefined items</name>
  <files>src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/lib.rs (the `set_menu()` setup block, App submenu at ~line 146 — read the Pitfall-1 comment block ~line 128-141 before editing; a partial menu strips Copy/Paste/Undo/Select-All/Quit)
    - .planning/phases/22.1-settings-followups/22.1-CONTEXT.md (D-22.1-1/D-22.1-2 — the verified API + exact label strings)
    - The PredefinedMenuItem signatures in this plan's <interfaces> block
  </read_first>
  <action>
    In `src-tauri/src/lib.rs`, inside the `.setup(|app| { ... })` closure, fix the App submenu so the macOS application menu reads the product name.

    1. Add `PredefinedMenuItem` to the `tauri::menu` import on lines 4 — change:
       `use tauri::{ menu::{Menu, MenuBuilder, MenuItem, SubmenuBuilder}, ... };`
       to include `PredefinedMenuItem`:
       `use tauri::{ menu::{Menu, MenuBuilder, MenuItem, PredefinedMenuItem, SubmenuBuilder}, ... };`

    2. Just BEFORE the `let app_menu = SubmenuBuilder::new(app, "TinkerDev")` line (~146), build the three explicit predefined items with custom text (the `?` propagates the `tauri::Error` like the surrounding setup code):
       ```rust
       // Follow-up 1 (BUG, 22.1): the no-text chainable .about(None)/.hide()/.quit()
       // derive their label from the Cargo bin name (devtools-app). Build explicit
       // PredefinedMenuItems with the product name so the menu reads "TinkerDev"
       // (D-22.1-1/D-22.1-2; API verified vs tauri-2.11.2 predefined.rs). about
       // metadata stays None (unchanged behavior).
       let about_i = PredefinedMenuItem::about(app, Some("About TinkerDev"), None)?;
       let hide_i = PredefinedMenuItem::hide(app, Some("Hide TinkerDev"))?;
       let quit_i = PredefinedMenuItem::quit(app, Some("Quit TinkerDev"))?;
       ```

    3. Rewrite the `app_menu` builder chain to `.item()` the three explicit items in place of the no-text chainable forms, keeping the title, the ⌘, Settings item, services/hide_others/show_all, and every separator EXACTLY as-is:
       ```rust
       let app_menu = SubmenuBuilder::new(app, "TinkerDev")
           .item(&about_i)
           .separator()
           .item(&settings_app_i)
           .separator()
           .services()
           .separator()
           .item(&hide_i)
           .hide_others()
           .show_all()
           .separator()
           .item(&quit_i)
           .build()?;
       ```

    Do NOT touch the `edit_menu`, `window_menu`, the `app_menu_bar` assembly, `app.set_menu(...)`, the `on_menu_event` open_settings handler, or anything in the tray block. The tray "Show TinkerDev" / "Settings…" / "Quit" labels are already correct and out of scope.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | tail -5; test -n "$(grep -F 'PredefinedMenuItem::about(app, Some("About TinkerDev"), None)' src/lib.rs)" && echo ABOUT_OK; test -n "$(grep -F 'PredefinedMenuItem::hide(app, Some("Hide TinkerDev"))' src/lib.rs)" && echo HIDE_OK; test -n "$(grep -F 'PredefinedMenuItem::quit(app, Some("Quit TinkerDev"))' src/lib.rs)" && echo QUIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd src-tauri && cargo build` exits 0 (no unused-import warning for PredefinedMenuItem — all three are used)
    - `grep -F 'PredefinedMenuItem::about(app, Some("About TinkerDev"), None)' src-tauri/src/lib.rs` matches (1 line)
    - `grep -F 'PredefinedMenuItem::hide(app, Some("Hide TinkerDev"))' src-tauri/src/lib.rs` matches (1 line)
    - `grep -F 'PredefinedMenuItem::quit(app, Some("Quit TinkerDev"))' src-tauri/src/lib.rs` matches (1 line)
    - The App submenu no longer contains the bare `.about(None)` / `.hide()` / `.quit()` chainable forms: `grep -E '\.about\(None\)|^\s*\.hide\(\)|^\s*\.quit\(\)' src-tauri/src/lib.rs` returns NOTHING (note: `.hide_others()`/`.show_all()` must REMAIN — the regex anchors `.hide()`/`.quit()` with no args)
    - The Edit submenu is untouched: `grep -E '\.copy\(\)' src-tauri/src/lib.rs` and `grep -E '\.paste\(\)' src-tauri/src/lib.rs` and `grep -E '\.select_all\(\)' src-tauri/src/lib.rs` all still match (Pitfall 1 regression guard)
    - The ⌘, Settings wiring is intact: `grep -F 'menu://open-settings' src-tauri/src/lib.rs` still matches and `grep -F '.item(&settings_app_i)' src-tauri/src/lib.rs` matches
    - `cargo test` exits 0 (license:: suite + any others unchanged; the menu code is setup-only, no unit tests)
    - decoder untouched: `git diff --quiet -- src/lib/protobuf/decoder.ts` exits 0 (this plan touches no webview files — vacuously true; assert it anyway)
  </acceptance_criteria>
  <done>App submenu builds with explicit "About TinkerDev" / "Hide TinkerDev" / "Quit TinkerDev" predefined items; Edit/Window submenus + ⌘, Settings wiring intact; cargo build + cargo test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual menu re-verify on the rebuilt .app (native chrome — not WebDriver-drivable)</name>
  <files>src-tauri/target/release/bundle/macos/ (the rebuilt .app — verification target, not edited)</files>
  <read_first>
    - .planning/phases/22.1-settings-followups/22.1-CONTEXT.md (D-22.1-3 — native change → rebuild + manual re-verify)
    - Memory: [[auto-build-at-phase-boundary]], [[verify-gate-builds-real-app]], [[tauri-dmg-bundle-flake]]
  </read_first>
  <action>
    AGENT (before pausing for the human): auto-run `pnpm tauri build` (Memory: auto-build at phase boundary). Its final non-zero exit is ONLY the absent updater-signing key — confirm the bundle exists under `src-tauri/target/release/bundle/macos/` (the `.app`/`.dmg`), NOT via the exit code. If the DMG step flakes because another DMG is mounted, `hdiutil detach` the stale mount and retry (Memory: Tauri DMG bundle flake). Then hand off the built-app path and pause for the human menu walkthrough below — do NOT self-approve native menu labels (WebDriver cannot assert native chrome).
  </action>
  <what-built>
    The macOS App submenu now sets explicit product-name text on About/Hide/Quit. Native menu chrome cannot be asserted by the WebDriver e2e harness (Memory: native-OS input is manual-walkthrough coverage), so this requires a human re-check on a rebuilt bundle.
  </what-built>
  <how-to-verify>
    1. Launch the freshly-built TinkerDev.app from `src-tauri/target/release/bundle/macos/`.
    2. Click the **bold app-menu title** (leftmost, after the Apple menu): it must read **TinkerDev** (not `devtools-app`).
    3. Open that App menu and confirm the items read **About TinkerDev**, **Hide TinkerDev**, and **Quit TinkerDev**.
    4. Confirm **Settings…** is still in the App menu with the **⌘,** accelerator and opens the Settings modal.
    5. Open the **Edit** menu and confirm **Undo / Redo / Cut / Copy / Paste / Select All** are all present and Copy/Paste work in a text field (Pitfall 1 regression backstop).
    6. Open the **Window** menu and confirm Minimize / Enter Full Screen / Bring All to Front / Close are present.
  </how-to-verify>
  <verify>
    <automated>MISSING — native menu chrome is not WebDriver-drivable; verification is the human walkthrough below. The agent auto-runs `pnpm tauri build` and confirms the bundle under src-tauri/target/release/bundle/macos/ (ignore the updater-key exit code).</automated>
  </verify>
  <resume-signal>Type "approved" if the App menu reads TinkerDev everywhere and Edit/Window/Settings are intact, or describe any wrong label / missing item.</resume-signal>
  <done>Human confirms the rebuilt .app's App menu reads About/Hide/Quit TinkerDev and Edit/Window/Settings are intact (no Pitfall-1 regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change edits native menu label STRINGS only — no input crosses any boundary, no IPC/network/storage touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.1-01 | Tampering | App submenu (set_menu) | accept | Static label strings only; no untrusted data flows into the menu. No new attack surface vs Phase 22's already-approved menu (22-03 walkthrough). |
| T-22.1-02 | DoS | Edit/Window submenu regression | mitigate | Pitfall-1 regression guard: acceptance asserts `.copy()/.paste()/.select_all()` remain; human checkpoint re-verifies the full Edit/Window menus on the rebuilt app. |

No high/medium security threats introduced. LIC-04 (raw license key never round-trips through JS) is irrelevant to this plan — no webview/license code is touched.
</threat_model>

<verification>
Per-task DoD (binding harness, in order):
1. `/simplify` on the just-written Rust change (reuse/clarity only).
2. `/codex:review --wait --scope working-tree` — address findings.
3. Unit gate: `cd src-tauri && cargo build` exits 0, `cargo test` exits 0. (No webview change → vitest/tsc unaffected; run `pnpm exec tsc --noEmit` only if any TS touched — it is not.)
4. Real-WKWebView UI verification: N/A for native menu chrome (WebDriver cannot assert it) — replaced by the Task-2 manual menu re-verify on the rebuilt `.app`.

Phase-boundary: the human menu walkthrough above + the auto `pnpm tauri build` is the sign-off for this plan. `gsd-ui-review` (webview WCAG-AA audit) is unchanged by a native-menu-only edit; run it at the phase boundary together with Plan 02's webview changes.
</verification>

<success_criteria>
- macOS App menu title + About/Hide/Quit all read **TinkerDev** on the rebuilt `.app` (human-verified).
- Edit submenu (Undo/Redo/Cut/Copy/Paste/Select-All) and Window submenu intact; ⌘, Settings opens the modal.
- `cargo build` + `cargo test` green; decoder + its 19 tests byte-for-byte untouched (vacuous — no webview files touched).
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-settings-followups/22.1-01-SUMMARY.md`.
</output>
