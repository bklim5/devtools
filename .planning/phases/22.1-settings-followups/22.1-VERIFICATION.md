---
phase: 22.1-settings-followups
verified: 2026-06-16T13:05:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  note: "Initial verification (no prior VERIFICATION.md)."
---

# Phase 22.1: Settings Follow-ups Verification Report

**Phase Goal:** The two non-blocking follow-ups from Phase 22's walkthrough are closed — the macOS app menu reads "TinkerDev" everywhere, and the Settings ▸ License pane shows the upsell/activation inline (no modal-on-modal). The phase grew (22.1-03/04) to a full License-pane redesign ($9 pricing reversing D-20, warn/ok token triads) and to REMOVE the standalone Unlock-Pro UpsellModal entirely, routing every former opener to `openSettings("license", invoker)`. Plus post-review fix batch (commit 316eaf33).
**Verified:** 2026-06-16
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | macOS app menu reads "TinkerDev" everywhere (title + About/Hide/Quit), not the `devtools-app` bin name | ✓ VERIFIED | `lib.rs:150-152` explicit `PredefinedMenuItem::about/hide/quit(app, Some("About/Hide/Quit TinkerDev"))`; `:155` `SubmenuBuilder::new(app, "TinkerDev")`. No bare `.about(None)/.hide()/.quit()` (only a comment matches). Built binary contains literal "About/Hide/Quit TinkerDev". Edit submenu `.copy()/.paste()/.select_all()` (`:177-179`) + ⌘, Settings (`menu://open-settings`, `:206`) intact — no Pitfall-1 regression. Human menu walkthrough APPROVED (handoff). |
| 2 | Settings ▸ License pane renders upsell/activation INLINE for not-Pro states — no stacked UpsellModal | ✓ VERIFIED | `LicenseSettings.tsx:277` `<InlineActivation variant="upsell">` (free/notActivated full pitch), `:453` `<InlineActivation variant="form-only">` (problem/refreshNeeded form-only). `openUpsell` grep == 0 in the pane. e2e asserts `stackedUpsellModalPresent()` stays false throughout (`license-settings.e2e.ts:232/280/359/379/444`). |
| 3 | Standalone Unlock-Pro UpsellModal removed; upsellStore/useUpsell gone; every opener routes to openSettings("license", invoker) | ✓ VERIFIED | `src/shell/upsellStore.ts` + `useUpsell.ts` DELETED (file-not-found). No `UpsellModal` JSX usage anywhere (only comments/test refs). Openers: Sidebar footer `:678 openSettings("license", e.currentTarget)`, locked pin/reorder/reset thread invoker (`Sidebar.tsx:103/114/335/546`, `useSidebarDragDrop.ts:51 openOrderingUpsell(getRowEl(id))`), ⌘K `CommandPalette.tsx:217/232 openSettings("license", preOpenFocus)`, App `:127`. NOTE: this REVERSES D-22.1-5 (which originally kept the standalone modal); the final 22.1-04 decision removed it. Goal ("no modal-on-modal, one surface") still met. |
| 4 | Activation logic in ONE shared piece (ActivationSurface), no duplication; LIC-04 + T-19-21 preserved | ✓ VERIFIED | `UpsellPanel.tsx:190 ActivationSurface(variant)`; `UpsellPanel`/`InlineActivation` are thin wrappers (`:568/:592`). `platform.license.activate(trimmed \|\| null)` exact-chain grep == 1 (`:262`). Chain verbatim: `clearEntitlementsOverride → refreshLicenseUiDetailed → refreshEntitlements → setValue("")` (`:267-279`). T-19-21 `setValue("")` clears key on success. |
| 5 | Heading hierarchy: dialog h2 → pane h3 → status h4 (variant-specific) | ✓ VERIFIED | `LicenseSettings.tsx:298 <h3>` pane title; status headings `<h4>` (`:247/:327/:380/:475`). Shared `ActivationSurface` `StatusHeading = variant === "panel" ? "h2" : "h4"` (`:205`) — inline→h4, standalone panel placement→h2 (Codex P2 fix: a lone inline h4 with no h3 above would itself skip in the route-placement panel). |
| 6 | Post-review fixes 1-4 present | ✓ VERIFIED | (1) masked-key: submit success calls `refreshLicenseUiDetailed()` (`UpsellPanel.tsx:277`). (2) heading order: variant-specific h2/h4 (`:205`). (3) locked-affordance invoker: `togglePin(id, invokerEl)` (`Sidebar.tsx:227-228`), `getRowEl` stable resolver useCallback (`:146`), threaded through `useSidebarDragDrop.ts:51`. (4) dead Done removed: `{onDismiss ? <button>Done</button> : null}` (`UpsellPanel.tsx:401`). All on committed `316eaf33`. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src-tauri/src/lib.rs` | Explicit TinkerDev menu labels | ✓ VERIFIED | About/Hide/Quit predefined items + title; Edit/Window/Settings intact |
| `src/components/UpsellPanel.tsx` | Shared ActivationSurface + InlineActivation, single activate chain | ✓ VERIFIED | One ActivationSurface; UpsellModal removed; variant-specific heading |
| `src/components/LicenseSettings.tsx` | Inline upsell/form, no openUpsell, h3 pane + h4 status | ✓ VERIFIED | InlineActivation upsell/form-only; openUpsell==0; licensed/offlineGrace unchanged (`maskedKey`, Deactivate) |
| `src/components/Sidebar.tsx` + `useSidebarDragDrop.ts` | Invoker-threaded openSettings | ✓ VERIFIED | togglePin(id, invokerEl), getRowEl resolver |
| `src/index.css` | warn/ok token triads | ✓ VERIFIED | `--color-warn`/`-soft`/`-line` (`:63-65`), `--color-ok`/`-soft`/`-line` (`:31/76-77`) via color-mix off accent |
| `src/shell/upsellStore.ts`, `useUpsell.ts` | DELETED | ✓ VERIFIED | Both absent on disk |

### Key Link Verification

| From | To | Status | Details |
|------|----|--------|---------|
| LicenseSettings | shared surface (`./UpsellPanel`) | ✓ WIRED | `import { InlineActivation } from "./UpsellPanel"` (`:63`), rendered inline |
| ActivationSurface | platform.license.activate | ✓ WIRED | Exact chain, count 1 |
| Sidebar/CmdPalette/App openers | openSettings("license", invoker) | ✓ WIRED | All call sites thread the invoker element |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TS types | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| Unit suite | `npx vitest run` | 966 passed / 77 files | ✓ PASS |
| Real-WKWebView e2e | `e2e-22.1-fix.log` | 21 passed, 21 total, exit 0 | ✓ PASS (run 10:39, against post-fix tree — log references h4/getRowEl probes) |
| Menu strings in release binary | grep binary | About/Hide/Quit TinkerDev present | ✓ PASS |
| Dev seam stripped from release | grep binary | `dev_set_license_state`=0, `test@tinkerdev.io`=0; `license.tinkerdev.io` present | ✓ PASS |
| Bundle freshness | dist/binary mtime vs post-fix code | dist `index-BfKETYKe.js` (10:40) contains pricing + "Your Pro features turned off" + getRowEl markers; binary 10:40:39 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|-------------|-------------|--------|----------|
| SET-06 (revised) | 22.1-02 | ✓ SATISFIED | License pane all 5 states; inline upsell/activation; no modal-on-modal. (Doc drift: REQUIREMENTS.md SET-06 text still says "standalone UpsellModal still opens from sidebar" — superseded by 22.1-04 removal; non-blocking.) |

### Anti-Patterns Found

None blocking. SUMMARY known-stubs check clean (only legitimate input placeholder + AA-contrast comment). No TODO/FIXME/empty-return stubs in changed source.

### Gaps Summary

No goal-level gaps. All 4 plans (01 app-menu, 02 inline upsell, 03 redesign, 04 modal-removal) plus the post-review fix batch (316eaf33) are present and wired in the actual codebase. Gates green: tsc clean, vitest 966/966, real-WKWebView e2e 21/21, fresh bundle (TinkerDev.app) built from the post-fix tree and containing both the Rust menu fix and the webview fixes.

**Bundle freshness note (resolved):** The post-review fix commit `316eaf33` is timestamped 13:56, later than the 10:40 build — but the handoff documents the fixes were applied uncommitted-on-master, e2e'd (10:39) and built (10:40) from that working tree, then committed at 13:56. Verified the built `dist/` and binary contain the post-fix markers; no source `.ts/.tsx/.rs` changed after the build except docs/SUMMARYs. Bundle is genuinely fresh.

**Non-blocking observations (carry, not gaps):**
- REQUIREMENTS.md SET-06 prose and stale stacking comments in `SettingsModal.tsx`/`App.tsx` still describe the removed standalone-modal path — doc drift to refresh at phase close.
- Open advisories in `deferred-items.md`: disabled-Refresh neutral tokens on amber/green banners (transient cosmetic); redundant mount re-query for inline variants; axe re-measure of the 3 color-mix tinted surfaces on the live app.
- 22.1-01/03/04 SUMMARYs are untracked (present on disk per handoff plan); 02 has a SUMMARY.

---

_Verified: 2026-06-16_
_Verifier: Claude (gsd-verifier)_
