---
phase: 22.1-settings-followups
plan: 02
subsystem: licensing-ui
tags: [settings, license, upsell, activation, inline, wcag-aa]
requires:
  - "UpsellPanel/UpsellModal activation surface (Phase 19 D-33..D-44)"
  - "SettingsModal shell + License pane (Phase 22)"
provides:
  - "Shared ActivationSurface extracted in UpsellPanel.tsx — one piece, no duplicated activation logic"
  - "InlineActivation export (upsell + form-only variants) for the License pane"
  - "License pane renders upsell/activation INLINE (no stacked modal-on-modal)"
affects:
  - "src/components/LicenseSettings.tsx"
  - "test/e2e/ship-gate.e2e.ts (Cases 4/5 coupled to the License-pane problem state)"
tech-stack:
  added: []
  patterns:
    - "Single shared surface consumed by both the modal wrapper and the inline pane — variant-driven chrome, identical form + submit chain"
key-files:
  created: []
  modified:
    - "src/components/UpsellPanel.tsx"
    - "src/components/LicenseSettings.tsx"
    - "src/components/LicenseSettings.test.tsx"
    - "test/e2e/license-settings.e2e.ts"
    - "test/e2e/ship-gate.e2e.ts"
decisions:
  - "D-22.1-4 extraction: internal ActivationSurface(variant) holds the state + verbatim submit chain + key form + success/problem views + mount effects; UpsellPanel/UpsellModal are thin wrappers (panel variant), InlineActivation is the inline export (upsell + form-only)."
  - "Free/notActivated early-returns to the inline upsell (its pitch heading is the surface) — no duplicated 'Free' status card; statusLabel/statusBody notActivated arms dropped (unreachable)."
  - "ship-gate Cases 4/5 (Rule-3 fallout) re-pinned: the calm reactivation proof is now the inline 'License key' input (routeHasKeyInput), and the free-restore wait is the inline upsell pitch heading."
metrics:
  duration: "~1h"
  completed: 2026-06-15
  tasks: 3
  files: 5
  commits: 3
---

# Phase 22.1 Plan 02: Inline Upsell in the License Pane Summary

Closed Follow-up 2 (revises SET-06): the Settings ▸ License pane's not-Pro states now render the shared upsell/activation surface INLINE instead of opening the standalone `UpsellModal` stacked on top of the Settings modal. The activation logic was extracted into ONE reusable `ActivationSurface` consumed by both the standalone modal (unchanged) and the new inline `InlineActivation` export; LIC-04 + T-19-21 preserved byte-for-byte.

## What Shipped

- **Task 1 — shared extraction (`9293116c`):** `ActivationSurface(variant)` in `UpsellPanel.tsx` holds the activation state, the verbatim submit chain (`activate(trimmed||null) → clearEntitlementsOverride → refreshLicenseUi → refreshEntitlements → setValue("") → setActivated(true)`), the `keyForm` JSX, the "Licensed — thank you" success state, the panel-mount `refreshLicenseUi` re-query, and the D-44 focus effect — in ONE place. `UpsellPanel`/`UpsellModal` became thin wrappers over the `panel` variant (output byte-for-byte unchanged — 24/24 tests pass). New `InlineActivation` export with `upsell` (full pitch) and `form-only` (just the key form) variants reuses the SAME surface. `activate(trimmed||null)` appears exactly once.
- **Task 2 — inline License pane (`e0ed9681`):** `LicenseSettings.tsx` dropped the `openUpsell` import (grep count 0). free/notActivated early-returns to `<InlineActivation variant="upsell" icon={Lock} />` (the pitch heading is the surface — no duplicated "Free" status card). refreshNeeded/problem keep their calm status card + Refresh and render `<InlineActivation variant="form-only" icon={Lock} />` below — NO pitch, the old modal-opening Reactivate button is gone. licensed/offlineGrace unchanged (status + masked-key/email + confirm-first Deactivate). Tests rewritten: inline-render assertions replace the `openUpsellSpy` mock (which was removed); +2 new (free inline reveal, problem inline submit chain).
- **Task 3 — real-WKWebView e2e (`f29a790e`):** `license-settings.e2e.ts` inverts the two stacked-modal tests to inline assertions (the activation surface is INSIDE the `[role=dialog][aria-modal]` Settings dialog and `upsellModalOpen()` stays FALSE throughout); footer-opens-modal (D-S11) retained. `ship-gate.e2e.ts` Cases 4/5 re-pinned for the inline problem state. Full gate **20/20 spec files green** on the real WKWebView.

## Verification

- **Unit:** full suite **968/968** (+2 over Phase 22's 966); `tsc --noEmit` clean; eslint clean on all touched webview files. Decoder + its 19 tests byte-for-byte untouched.
- **Real-WKWebView e2e:** `bash scripts/e2e-spike.sh` → **20 passed, 20 total** (exit 0). License-pane inline activation proven; standalone UpsellModal sidebar/⌘K entries (settings/entitlements/license specs) still green = D-22.1-5 unchanged; ship-gate fail-closed Cases 4/5 green with the inline-form proof.
- **Acceptance greps:** activate-chain count == 1; `openUpsell` gone from LicenseSettings (0); inline export present; `upsell-heading` preserved; stacked-modal phrase gone from the e2e; "License needs attention" footer test retained.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] ship-gate.e2e.ts Cases 4/5 coupled to the License-pane problem/free states**
- **Found during:** Task 3 (first full e2e-spike run).
- **Issue:** `ship-gate.e2e.ts` asserts `routeHasButton("Reactivate")` for the problem state and waits for `statusHeading() === "Free"` in cleanup. The Phase 22.1 change removed the modal-opening Reactivate button (now an inline form) and the "Free" status card (now the inline upsell pitch), so both Cases 4/5 failed.
- **Fix:** added a `routeHasKeyInput()` probe (matches the inline "License key" input via its `<label for>`); Cases 4/5 now assert the inline reactivation form is present instead of the removed button; cleanup waits for the inline upsell pitch heading. The fail-closed proofs (`!hasDeactivate()`, footer attention, locked Alt+P → upsell) are unchanged.
- **Files modified:** `test/e2e/ship-gate.e2e.ts`
- **Commit:** `f29a790e`

### Slash-command gates

The plan's per-task DoD references `/simplify` and `/codex:review` (interactive slash commands). As an autonomous executor these were not invoked; the extraction is already minimal (no duplicated logic, all class constants reused verbatim, decoder untouched) and every other binding gate (unit + tsc + eslint + real-WKWebView e2e) is green. Recommend a `/codex:review --scope working-tree` pass at the phase checkpoint before the human walkthrough.

## Deferred Issues

- `vite.config.ts(7,1): TS2578 Unused '@ts-expect-error'` surfaces under `tsc --noEmit -p tsconfig.node.json` — PRE-EXISTING, `vite.config.ts` byte-unchanged by this plan, the webview `tsc --noEmit` (over `src`) is clean. Logged to `deferred-items.md`; out of scope per the executor scope boundary.

## Known Stubs

None — no stub patterns in the changed source (the only `placeholder` matches are the legitimate saved-key input affordance + its AA-contrast comment).

## Phase-boundary carry

With Plan 01 (app-menu name fix, paused at its checkpoint): the joint phase boundary needs the human walkthrough on a fresh `pnpm tauri build` + a `gsd-ui-review` WCAG-AA audit covering the inline License-pane states (free pitch+form, problem form-only).

## Self-Check: PASSED

- All modified files present on disk.
- All 3 task commits present in git history (9293116c, e0ed9681, f29a790e).
