---
phase: 22.1-settings-followups
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/UpsellPanel.tsx
  - src/components/LicenseSettings.tsx
  - src/components/UpsellPanel.test.tsx
  - src/components/LicenseSettings.test.tsx
  - test/e2e/settings.e2e.ts
  - test/e2e/license-settings.e2e.ts
autonomous: true
requirements:
  - SET-06
user_setup: []

must_haves:
  truths:
    - "The Settings ▸ License pane renders the upsell/activation content INLINE for the not-Pro states (free/notActivated/problem/refreshNeeded) — no stacked UpsellModal opens on top of the Settings modal"
    - "free/notActivated shows the full pitch inline: 'Thank you for using TinkerDev ❤️' + Buy license + the 'I have a license key' reveal → key input + Activate (identical content to the standalone modal)"
    - "refreshNeeded/problem keep the calm status card + Refresh and render the key-input + Activate form INLINE below — NO sales pitch (the modal-opening Reactivate button is gone)"
    - "licensed/offlineGrace are UNCHANGED (status + masked-key/email fields + confirm-first Deactivate)"
    - "The standalone UpsellModal keeps identical behavior + entry points (Sidebar Unlock Pro / ⌘K free-tier License) — only the License pane no longer opens it stacked"
    - "The activation logic (submit chain, stored-key reactivation, error copy, aria-live, success state) lives in ONE shared piece with NO duplication; LIC-04 + T-19-21 preserved byte-for-byte"
  artifacts:
    - path: "src/components/UpsellPanel.tsx"
      provides: "Shared activation surface extracted so both UpsellModal and the License pane consume it without logic duplication"
      contains: "export function"
    - path: "src/components/LicenseSettings.tsx"
      provides: "License pane that renders the shared upsell/activation surface inline for not-Pro states (no openUpsell)"
      contains: "notActivated"
  key_links:
    - from: "src/components/LicenseSettings.tsx"
      to: "the shared activation surface from UpsellPanel.tsx"
      via: "inline render for free/notActivated/problem/refreshNeeded (not openUpsell)"
      pattern: "from \"./UpsellPanel\""
    - from: "the shared activation surface"
      to: "platform.license.activate"
      via: "the exact submit chain activate(trimmed||null) → clearEntitlementsOverride → refreshLicenseUi → refreshEntitlements → clear key"
      pattern: "platform\\.license\\.activate"
---

<objective>
Close Follow-up 2 (DESIGN, revises SET-06): the Settings ▸ License pane's not-Pro states currently open the standalone `UpsellModal` STACKED on top of the Settings modal (modal-on-modal). Replace with the upsell/activation surface rendered INLINE in the License pane.

Architecture (D-22.1-4): extract the activation surface from `UpsellPanel.tsx` into ONE reusable piece consumed BOTH by the standalone `UpsellModal` (unchanged) AND inline in the License pane. No activation logic duplicated. The activation submit chain, stored-key reactivation (LIC-04), transient-key handling (T-19-21), inline error copy, aria-live, and success state are behavior-unchanged.

Purpose: a user managing their license in Settings activates/buys inline — no jarring second modal.
Output: a License pane that renders inline upsell (free/notActivated) and inline Activate form (problem/refreshNeeded), a standalone modal that still works for the non-Settings entries, and green unit + real-WKWebView e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-settings-followups/22.1-CONTEXT.md
@.planning/phases/22-settings-modal-shell/22-FOLLOWUP.md
@.planning/phases/22-settings-modal-shell/22-UI-SPEC.md
@src/components/UpsellPanel.tsx
@src/components/LicenseSettings.tsx
@src/components/settingsPanes.tsx
@src/components/UpsellPanel.test.tsx
@src/components/LicenseSettings.test.tsx
@test/e2e/settings.e2e.ts
@test/e2e/license-settings.e2e.ts

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. No exploration required. -->

// src/components/UpsellPanel.tsx CURRENT exports (the source of the activation surface):
export const BUY_LICENSE_URL = "https://tinkerdev.io/buy";
export interface UpsellPanelProps { icon: ComponentType<{ className?: string }>; headingId?: string; onDismiss?: () => void; }
export function UpsellPanel(props: UpsellPanelProps): JSX.Element;   // sales pitch + reveal-form + problem + success
export interface UpsellModalProps { icon: ComponentType<{ className?: string }>; onClose: () => void; }
export function UpsellModal(props: UpsellModalProps): JSX.Element;   // dialog wrapper around UpsellPanel

// The EXACT activation submit chain to preserve verbatim (UpsellPanel.tsx submit(), lines ~147-180):
//   const trimmed = value.trim();
//   if (!trimmed && !hasStoredKey) return;
//   await platform.license.activate(trimmed || null);
//   await clearEntitlementsOverride();
//   await refreshLicenseUi();
//   await refreshEntitlements();
//   setValue("");            // T-19-21: drop the key on success
//   setActivated(true);      // success state
//   catch → setError(toErrorCode(err)); inputRef.current?.focus();   // D-37 keep value + refocus

// Class constants are DEFINED IDENTICALLY in BOTH files already (CARD_CLASS/HEADING_CLASS/BODY_CLASS/
// PRIMARY_BTN_CLASS/SECONDARY_BTN_CLASS) — the 21-UI-SPEC reuse mandate. Reuse verbatim; do NOT invent new tokens.

// src/components/LicenseSettings.tsx CURRENT shape:
export function LicenseSettings(): JSX.Element;
//   ui = useLicenseUi();  states: licensed | offlineGrace | refreshNeeded | problem | notActivated
//   isProActive = licensed | offlineGrace   → status card + masked-key/email + confirm-first Deactivate (KEEP)
//   canReactivate = refreshNeeded | problem → currently renders <button onClick={openUpsell}>Reactivate</button>  (REPLACE)
//   notActivated  → currently renders <button onClick={openUpsell}>Activate a license</button>  (REPLACE)
//   Imports `openUpsell` from "@/shell/upsellStore"  ← to be removed if no inline path keeps it

// Platform license seams reused as-is (no change):
//   refreshLicenseUi, refreshLicenseUiDetailed from "@/lib/license/licenseUi"
//   refreshEntitlements, clearEntitlementsOverride from "@/lib/entitlements/store"
//   useLicenseUi from "@/shell/useLicenseUi"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared activation surface in UpsellPanel.tsx (no duplicated logic; UpsellModal behavior identical)</name>
  <files>src/components/UpsellPanel.tsx</files>
  <read_first>
    - src/components/UpsellPanel.tsx (the FULL file — the submit chain, the problem/success/sales views, the keyForm JSX, the panel-mount refreshLicenseUi effect, the D-44 focus effect)
    - src/components/UpsellPanel.test.tsx (the existing UpsellPanel + UpsellModal + activation describe blocks that must keep passing unchanged)
    - .planning/phases/22.1-settings-followups/22.1-CONTEXT.md (D-22.1-4 / D-22.1-5 / D-22.1-9 + the "Specific Ideas" submit chain)
  </read_first>
  <behavior>
    - The extracted shared surface, given a not-Pro state, renders the SAME activation form (label "License key", input, "Activate" button, single aria-live status line) and the SAME submit behavior as today's UpsellPanel.
    - `platform.license.activate(trimmed || null)` is called exactly once per submit; whitespace-only input with no stored key is a no-op (does NOT call activate).
    - A padded key is trimmed before send: `activate("KEY-TRIM")` for input `"  KEY-TRIM  "`.
    - With a stored key, an empty-field submit calls `activate(null)` (stored-key reactivation, LIC-04).
    - On success: clears the key from state (T-19-21), refreshes entitlements live, swaps to the "Licensed — thank you" success state.
    - On error: renders the calm ERROR_COPY line in the SAME aria-live region, keeps the field value, refocuses the input.
    - UpsellModal is UNCHANGED externally: still role=dialog + aria-modal + aria-labelledby="upsell-heading", Esc/scrim dismiss, focus-trap, focus return to invoker; the sales pitch + Buy CTA + "I have a license key" reveal still render for free/notActivated.
  </behavior>
  <action>
    Refactor `src/components/UpsellPanel.tsx` so the activation logic + form is a SINGLE reusable piece, with NO logic duplicated, and add a thin variant the License pane can render inline. Claude's discretion on the exact split (CONTEXT D-22.1-4) — the cleanest extraction that removes duplication. A recommended shape:

    1. Extract the activation state + submit chain + `keyForm` JSX + the success ("Licensed — thank you") state + the panel-mount `refreshLicenseUi` effect + the D-44 focus effect into a new internal component, e.g. `ActivationSurface`, that takes the props it needs (at minimum: `icon`, optional `headingId`, optional `onDismiss`, and a `variant` controlling which heading/body/pitch wraps the form). Move the EXACT submit chain verbatim — do NOT re-author it:
       ```
       const trimmed = value.trim();
       if (!trimmed && !hasStoredKey) return;
       await platform.license.activate(trimmed || null);
       await clearEntitlementsOverride();
       await refreshLicenseUi();
       await refreshEntitlements();
       setValue("");
       setActivated(true);
       ```
       Keep `toErrorCode`/`ERROR_COPY`, the `hasStoredKey` derivation, the readOnly-not-disabled input, the stored-key hint line + aria-describedby, and the single `aria-live="polite"` status line BYTE-FOR-BYTE.

    2. Keep `UpsellPanel(props)` rendering the SALES variant (sales pitch heading + body + Buy CTA + "I have a license key" reveal → form), unchanged in output. It now composes the shared surface internally rather than holding its own copy of the submit logic.

    3. Export a new component for inline License-pane use — Claude's discretion on naming, e.g. `export function InlineActivation({ variant }: { variant: "upsell" | "form-only" })`:
       - `variant="upsell"` → the FULL pitch inline (free/notActivated): "Thank you for using TinkerDev ❤️" + Buy + the "I have a license key" reveal → form. (D-22.1-6)
       - `variant="form-only"` → ONLY the key-input + Activate form, NO pitch heading/body (problem/refreshNeeded render their OWN calm status card above it; D-22.1-7). For consistency with today's `problem` behavior (D-44/D-22.1-9), pre-reveal + focus the field in this variant.
       Both variants reuse the SAME `ActivationSurface`/`keyForm` — no second copy of activate().

    4. `UpsellModal` is UNCHANGED — it still wraps `UpsellPanel` (the sales variant), keeps `aria-labelledby="upsell-heading"`, the focus trap, the store-captured invoker return. Do NOT change its props or behavior (D-22.1-5).

    5. WCAG-AA (D-22.1-9): real `<label>` for the key input, visible focus-visible rings, the single aria-live region, neutral tokens with accent reserved for the primary CTA + focus rings. The inline form needs NO focus trap (SettingsModal owns the trap). Reuse the class constants verbatim.

    Run `/simplify` after to fold any reuse the extraction exposes (CONTEXT notes the /simplify gate covers this), but the result MUST keep `UpsellPanel`/`UpsellModal` output identical.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/UpsellPanel.test.tsx 2>&1 | tail -20; pnpm exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The activation submit chain exists EXACTLY ONCE in UpsellPanel.tsx: `grep -c 'platform.license.activate(trimmed || null)' src/components/UpsellPanel.tsx` returns `1` (no duplicated copy)
    - `grep -F 'clearEntitlementsOverride()' src/components/UpsellPanel.tsx` and `grep -F 'refreshEntitlements()' src/components/UpsellPanel.tsx` both still match (chain preserved)
    - The new inline export exists: `grep -E 'export function (InlineActivation|ActivationSurface|UpsellContent|LicenseActivation)' src/components/UpsellPanel.tsx` matches at least one
    - `UpsellPanel` and `UpsellModal` are still exported: `grep -E 'export function UpsellPanel' src/components/UpsellPanel.tsx` and `grep -E 'export function UpsellModal' src/components/UpsellPanel.tsx` both match
    - `aria-labelledby="upsell-heading"` (MODAL_HEADING_ID) is preserved so the SettingsModal's Esc-yield detection still works: `grep -F 'upsell-heading' src/components/UpsellPanel.tsx` matches
    - T-19-21 preserved: `grep -F 'setValue("")' src/components/UpsellPanel.tsx` matches (key cleared on success)
    - `pnpm exec vitest run src/components/UpsellPanel.test.tsx` exits 0 — ALL existing UpsellPanel + UpsellModal + activation tests pass UNCHANGED (no regression to the standalone modal)
    - `pnpm exec tsc --noEmit` exits 0
    - decoder untouched: `git diff --quiet -- src/lib/protobuf/decoder.ts` exits 0
  </acceptance_criteria>
  <done>The activation logic lives in one shared piece; a new inline export ("upsell" + "form-only" variants) reuses it; UpsellPanel/UpsellModal output + all their existing tests are unchanged; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the shared upsell/activation surface INLINE in the License pane (remove openUpsell from the not-Pro paths)</name>
  <files>src/components/LicenseSettings.tsx, src/components/LicenseSettings.test.tsx</files>
  <read_first>
    - src/components/LicenseSettings.tsx (the FULL file — the state machine, isProActive/canReactivate, the status card, the Refresh/Reactivate/Activate buttons block, the confirm-first Deactivate)
    - src/components/UpsellPanel.tsx AS UPDATED IN TASK 1 (the new inline export + its variants)
    - src/components/LicenseSettings.test.tsx (the existing tests that assert openUpsell is called for free/refreshNeeded/problem — these MUST be rewritten to assert inline render instead)
    - .planning/phases/22.1-settings-followups/22.1-CONTEXT.md (D-22.1-6 / D-22.1-7 / D-22.1-8 — what each state renders)
  </read_first>
  <behavior>
    - free/notActivated: the pane renders the FULL inline upsell ("Thank you for using TinkerDev ❤️" + Buy license + "I have a license key" → key input + Activate) and does NOT call openUpsell.
    - refreshNeeded: the pane keeps the calm "Pro is no longer active" status card + Refresh, AND renders the inline key-input + Activate form below; NO "Thank you ❤️" pitch; no Reactivate button that opens a modal.
    - problem: the pane keeps the calm "License needs attention" status card + Refresh, AND renders the inline key-input + Activate form below (field pre-revealed + focused); NO pitch.
    - licensed/offlineGrace: UNCHANGED — status card + masked-key/email fields + confirm-first Deactivate. NO inline upsell.
    - No path in LicenseSettings opens the standalone UpsellModal anymore.
  </behavior>
  <action>
    Edit `src/components/LicenseSettings.tsx`:

    1. Import the inline export from Task 1: `import { <InlineActivation> } from "./UpsellPanel";` (use the actual name chosen in Task 1). Remove the `import { openUpsell } from "@/shell/upsellStore";` line — it is no longer used (the standalone modal is opened only from the sidebar/⌘K, not from this pane). Keep the `Lock` icon import for the InlineActivation `icon` prop and the status glyph.

    2. free/notActivated branch (D-22.1-6): REMOVE the `Activate a license` button that calls `openUpsell()`. In its place render the inline FULL upsell variant, e.g. `<InlineActivation variant="upsell" icon={Lock} />`. Keep the Free status card heading/body as-is above it (the existing `statusLabel`/`statusBody` "Free" copy) OR let the inline upsell's own pitch heading serve — Claude's discretion to avoid a duplicated/awkward double heading, but the free state MUST end up showing the pitch + Buy + key-input + Activate inline and MUST NOT render a button that opens a modal.

    3. refreshNeeded + problem branch (D-22.1-7): KEEP the existing calm status card (`statusLabel`/`statusBody` for "Pro is no longer active" / "License needs attention") AND the Refresh button. REMOVE the `Reactivate` button that calls `openUpsell()`. Render the inline `form-only` variant below the status/Refresh, e.g. `<InlineActivation variant="form-only" icon={Lock} />`. Do NOT show the sales pitch here (D-22.1-7 / D-44 principle).

    4. licensed/offlineGrace branch (D-22.1-8): UNCHANGED. Status card + masked-key/email `<dl>` fields + the confirm-first inline Deactivate stay exactly as they are.

    5. a11y (D-22.1-9): no focus trap added (SettingsModal owns it). The inline form's own aria-live + label come from the shared surface. Ensure focus is not stolen on mount beyond the existing problem-state field focus the shared surface already does.

    Run `/simplify` after — e.g. collapse the now-dead `canReactivate` branching if it becomes redundant; remove any now-unused helpers.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/LicenseSettings.test.tsx 2>&1 | tail -20; pnpm exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - openUpsell is GONE from the License pane: `grep -c 'openUpsell' src/components/LicenseSettings.tsx` returns `0` (the not-Pro paths render inline, never open the standalone modal)
    - The pane imports the inline surface: `grep -E 'from "./UpsellPanel"' src/components/LicenseSettings.tsx` matches
    - The free path no longer renders a modal-opening button: `grep -F 'Activate a license' src/components/LicenseSettings.tsx` returns nothing (the button is replaced by inline render), OR if the label is reused as a heading it is NOT wired to openUpsell (grep openUpsell already proves 0)
    - The "Thank you for using TinkerDev" pitch reaches the free/notActivated path inline (assert via the rewritten unit test, see below — the pitch comes from the shared surface, so it need not be a literal in LicenseSettings.tsx)
    - licensed/offlineGrace unchanged: `grep -F 'Deactivate this device' src/components/LicenseSettings.tsx` still matches and `grep -F 'maskedKey' src/components/LicenseSettings.tsx` still matches
    - Refresh kept for managed states: `grep -F '"Refresh"' src/components/LicenseSettings.tsx` or the Refresh button JSX still present (`grep -F 'Refresh' src/components/LicenseSettings.tsx` matches)
    - LicenseSettings.test.tsx rewritten: the old `openUpsellSpy` assertions for notActivated/refreshNeeded/problem are REPLACED with inline-render assertions — `grep -c 'openUpsellSpy' src/components/LicenseSettings.test.tsx` should reflect ONLY any remaining valid use (expected 0 after the rewrite; the `vi.mock("@/shell/upsellStore")` block may be removed)
    - `pnpm exec vitest run src/components/LicenseSettings.test.tsx` exits 0 with NEW tests proving: (a) free/notActivated renders the inline pitch + key input (no modal), (b) problem/refreshNeeded render the inline key input + Activate BELOW the status card with NO pitch, (c) licensed still shows masked-key + Deactivate
    - `pnpm exec tsc --noEmit` exits 0
    - decoder untouched: `git diff --quiet -- src/lib/protobuf/decoder.ts` exits 0
  </acceptance_criteria>
  <done>The License pane renders inline upsell (free) / inline Activate form (problem, refreshNeeded) and never opens the standalone modal; licensed/offlineGrace unchanged; rewritten unit tests + tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the real-WKWebView e2e — inline activation in the License pane, no modal-on-modal; standalone modal still works for sidebar/⌘K</name>
  <files>test/e2e/license-settings.e2e.ts, test/e2e/settings.e2e.ts</files>
  <read_first>
    - test/e2e/license-settings.e2e.ts (the FULL file — the deep-link open, statusHeading scoping, the now-OBSOLETE "Activate a license opens the shared Unlock Pro modal stacked above Settings" test and the "problem-state Reactivate opens the shared modal" test that this plan INVERTS)
    - test/e2e/settings.e2e.ts (the sidebar/⌘K open-from + focus-return coverage that MUST stay green — the standalone modal entries are unchanged)
    - test/e2e/helpers.ts (assert, navigateToTool, saveScreenshot, upsellModalOpen — reuse; do not re-author)
    - The Memory note [[verify-gate-builds-real-app]]: the UI gate = update test/e2e/*.e2e.ts then RUN scripts/e2e-spike.sh on the real WKWebView (Chromium screenshots are only a preview)
  </read_first>
  <action>
    Update the e2e to reflect the inline behavior. The KEY inversion: the License pane must NOT open the standalone upsell modal anymore — the activation surface is inline inside the Settings dialog.

    1. In `test/e2e/license-settings.e2e.ts`:
       - REPLACE the test `'the free state "Activate a license" opens the shared Unlock Pro modal stacked above Settings (D-88)'` with one asserting the free state renders the upsell/activation INLINE inside the `[role="dialog"][aria-modal="true"]` Settings dialog and NO second `upsellModalOpen()` dialog stacks: after opening the deep-link on the Free state, assert the dialog contains the inline pitch (e.g. a heading/text matching `Thank you for using TinkerDev`) and a "License key" labelled input + "Activate" button, and assert `!(await upsellModalOpen())` throughout (no `aria-labelledby="upsell-heading"` dialog appears).
       - REPLACE the test `'the problem state "Reactivate" opens the shared Unlock Pro modal and returns focus to the invoker on dismiss'` with one asserting the problem state (seeded corrupt machine.dev.lic) renders the calm "License needs attention" status heading + Refresh AND an INLINE "License key" input + Activate form inside the Settings dialog, with NO `upsellModalOpen()` second dialog. Keep the corrupt-file seed/restore + cleanup discipline (T-18-15) verbatim from the existing test.
       - Keep the existing first test (deep-link opens modal on License pane, Free baseline, corrupt → problem, Refresh aria-live) — but update its assertions if they reference the removed "Reactivate"/"Activate a license" modal-opening buttons (the buttons are gone; assert the inline form instead). Keep the footer-opens-modal test (D-S11) — the footer still OPENS the Settings modal on the License pane; only the IN-pane behavior changed.
       - Add `saveScreenshot` calls for the new inline states (e.g. `license-settings-free-inline.png`, `license-settings-problem-inline.png`).

    2. In `test/e2e/settings.e2e.ts`: NO behavioral change to the sidebar/⌘K standalone-modal coverage is required (those entries still open the standalone modal — D-22.1-5). Re-run it to confirm no regression. If any assertion incidentally depended on the License pane opening a stacked modal, fix it to the inline expectation.

    3. RUN the real-WKWebView gate (this is the binding UI verification, not Chromium): `bash scripts/e2e-spike.sh` (it reaps orphans + resets the DEV prefs/Keychain + machine.dev.lic baseline per the preflight). All spec files must pass.
  </action>
  <verify>
    <automated>bash scripts/e2e-spike.sh 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `test/e2e/license-settings.e2e.ts` no longer asserts a stacked upsell modal opens FROM the License pane: `grep -F 'opens the shared Unlock Pro modal stacked above Settings' test/e2e/license-settings.e2e.ts` returns nothing (replaced by the inline assertion)
    - The inline assertion exists: the spec asserts the activation form is inside the Settings dialog AND `upsellModalOpen()` is false during the License-pane flow — `grep -F 'upsellModalOpen' test/e2e/license-settings.e2e.ts` matches in a NOT-open assertion context, and the spec references the inline "License key" input/"Activate" inside `[role="dialog"][aria-modal="true"]`
    - The footer-opens-modal (D-S11) test is retained: `grep -F 'License needs attention' test/e2e/license-settings.e2e.ts` still matches
    - `bash scripts/e2e-spike.sh` exits 0 — ALL e2e spec files pass on the real WKWebView (settings.e2e.ts sidebar/⌘K standalone-modal coverage still green = D-22.1-5 unchanged; license-settings.e2e.ts inline coverage green)
    - new screenshots written under the e2e screenshot dir for the inline states
    - decoder untouched: `git diff --quiet -- src/lib/protobuf/decoder.ts` exits 0
  </acceptance_criteria>
  <done>The real-WKWebView e2e proves: License pane activates INLINE with no modal-on-modal; the standalone modal still opens from sidebar/⌘K; full e2e-spike green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview JS ↔ Rust license commands | The activation form submits a pasted key to `platform.license.activate` (Rust-owned). UNCHANGED — this plan re-homes the same form, it does not alter the seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.1-03 | Information Disclosure | License key handling in the extracted activation surface | mitigate | LIC-04 preserved verbatim: the RAW key never round-trips through JS — stored-key reactivation calls `activate(null)`; JS sees only `hasStoredKey: boolean`. Acceptance asserts the submit chain is unchanged and exists exactly once. |
| T-22.1-04 | Information Disclosure | Transient pasted key in component state | mitigate | T-19-21 preserved: the pasted key lives ONLY in transient state for the one activate call and is cleared on success (`setValue("")`); acceptance greps for it. The extraction moves the state but does not persist/log it. |
| T-22.1-05 | Tampering | Refactor duplicating/diverging the activate chain | mitigate | The chain must exist EXACTLY ONCE (acceptance `grep -c ... == 1`); both the modal and the inline surface consume the SAME piece — no second authority that could drift. |

No NEW attack surface: Follow-up 2 re-homes an existing UI surface and reuses the activation logic byte-for-byte (no network/IPC/storage changes). The invariant to NOT break is LIC-04.
</threat_model>

<verification>
Per-task DoD (binding harness, in order) for EACH task:
1. `/simplify` on the just-written change (reuse/clarity only — explicitly expected here to fold the extraction's duplication).
2. `/codex:review --wait --scope working-tree` — address findings.
3. Unit gate: `pnpm exec vitest run` (full suite, the decoder's 19 tests are the immovable bar) + `pnpm exec tsc --noEmit` + eslint clean.
4. Real-WKWebView UI verification: `bash scripts/e2e-spike.sh` green on the real WKWebView (Task 3 is the gate for the whole plan; the Chromium screenshots are only a preview — the spike run is binding).

Phase-boundary (with Plan 01): human walkthrough on a fresh `pnpm tauri build` (the agent auto-builds at the checkpoint) + a passing `gsd-ui-review` WCAG-AA audit covering the inline License-pane states.
</verification>

<success_criteria>
- Settings ▸ License pane renders upsell/activation INLINE for free/notActivated/problem/refreshNeeded — no stacked UpsellModal (verified on the real WKWebView).
- Standalone UpsellModal unchanged: still opens from Sidebar "Unlock Pro" + ⌘K free-tier "License".
- Activation logic in ONE shared piece, no duplication; LIC-04 + T-19-21 byte-for-byte preserved.
- licensed/offlineGrace unchanged; full vitest + tsc + eslint green; decoder + its 19 tests untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-settings-followups/22.1-02-SUMMARY.md`.
</output>
