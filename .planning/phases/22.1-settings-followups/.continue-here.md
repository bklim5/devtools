# Phase 22.1 — Continue Here (handoff after /clear, 2026-06-16)

## Status: green up to here, NOT yet complete
Phase grew 2 planned plans → 4 (22.1-01 app-menu, 02 inline upsell, 03 License-pane
redesign, 04 modal-removal + dev e2e harness). All committed on `master`.

**Gates passing as of handoff:** vitest **963** · cargo `license::` **85** · tsc + eslint clean ·
real-WKWebView e2e **21/21 spec files** · dev-strip validated on the shipped binary
(`dev_set_license_state`/`••••••••TEST`/`test@tinkerdev.io` ABSENT, prod host present) ·
fresh `tauri build` at 09:02 · gsd-ui-review **22/24** (`22.1-UI-REVIEW.md`) · Codex
ultrathink review: **licensing security + webhook CLEAN, LIC-04 holds**.

## DO NEXT — apply review fixes (each through the FULL gate: simplify→/codex:review→vitest+tsc+eslint→real-WKWebView e2e (scripts/e2e-spike.sh)→rebuild), in priority order:

1. **[P2 Codex] Inline activation blanks the masked key** — `src/components/UpsellPanel.tsx`
   `submit()` success path calls `refreshLicenseUi()` (non-detailed → maskedKey null). After a
   notActivated/problem→licensed TRANSITION, `carryForwardKeyEmail` (licenseUi.ts:116) does NOT
   apply, so the License pane shows "License key —" until the route reopens. FIX: call
   `refreshLicenseUiDetailed()` after a successful activate (the masked_key_cache is already seeded
   in verify_then_persist, so NO extra Keychain prompt). Add a unit test.
2. **[both reviews] Heading hierarchy** — inside SettingsModal (dialog title = `h2`), the pane
   title is `h3` but the inner STATUS headings (LicenseSettings green/amber/confirm + UpsellPanel
   pitch) are `h2` → SR hears a level increase. FIX: demote status headings `h2`→`h4`; KEEP pane
   title `h3`. Then switch the e2e `statusHeading()` probe (license-states.e2e.ts + license-settings
   /ship-gate) from "first non-Settings h2" to a role+name lookup, since no status h2 will remain.
3. **[P3 Codex] Locked pin/drag lose the explicit invoker** — `Sidebar.tsx:223` locked `togglePin`
   calls `openOrderingUpsell()` with no invoker; same in `useSidebarDragDrop.ts`. They fall back to
   `document.activeElement` → unreliable focus return after closing Settings. FIX: thread an explicit
   invoker (`e.currentTarget` for pin; a stable row el for drag) into openOrderingUpsell→openSettings.
4. **[gsd-ui #3] Dead `Done` no-op** — `UpsellPanel.tsx:381` success card's Done calls `onDismiss?.()`
   but `InlineActivation` is rendered without `onDismiss`. Conditionally render Done only when
   `onDismiss` is passed (or drop it for the inline variant).
5. **Minor (record or fix):** disabled-Refresh neutral tokens on the amber/green banners (transient,
   own bg-input-bg — cosmetic); redundant mount re-query (see deferred-items.md); an **axe re-measure**
   of the 3 color-mix tinted surfaces (warn-soft / ok-soft / bad-tinted) on the live app — they clear
   AA on opaque math but composite over the gradient body; stale stacking comments in SettingsModal.tsx
   /App.tsx (post-modal-removal).

## THEN close the phase:
- Write `22.1-01-SUMMARY.md` (app-menu: `lib.rs` sets explicit TinkerDev on About/Hide/Quit, commit
  d9dba1f1; human menu walkthrough APPROVED). 22.1-02 has a SUMMARY; 03/04 do not (executor was told
  not to) — write brief SUMMARYs or fold into verification.
- Run gsd-verifier (goal-verification) → mark Phase 22.1 complete → evolve PROJECT.md.

## Durable context (decisions made this phase):
- **D-20 REVERSED:** in-app pricing now SHOWN — "$9 · once · lifetime license" + feature list +
  "One-time payment · Free updates forever · 14-day refund". "Lost your key?" → "Check your purchase
  email" (NO account portal). Price $9 must stay in sync with Lemon Squeezy + tinkerdev.io (already
  added there: `../tinkerdev-io` components/site/pro.tsx, commit a0a92b8, NOT pushed).
- **D-22.1-5 / D-28 / D-29 REVERSED:** standalone Unlock-Pro UpsellModal REMOVED; every opener
  (sidebar footer, locked pin/reorder/reset, ⌘K free "License") → `openSettings("license", invoker)`.
  upsellStore.ts + useUpsell.ts DELETED. The ONE upsell surface = inline in Settings ▸ License.
- **New design tokens:** amber `warn` triad + green `ok` triad in `src/index.css` (color-mix off the
  accent triad). Dark-theme only.
- **Dev-only e2e seam:** `dev_set_license_state` (cfg(debug_assertions), stripped from release;
  check-dev-strip.sh guards it). `license-states.e2e.ts` drives free/licensed/problem.
- **Keychain note** added to the activation form (pre-empts the macOS prompt).
- **maskedKey/email stickiness** fix in `licenseUi.ts` (`carryForwardKeyEmail` — same-state only).

## Out-of-band (DONE this phase, prod):
- License `f56c9a77…` (key `83B59C-…-V3`) email **backfilled** to bkbklim@gmail.com (run-Refresh-in-app
  to pull it into a fresh cert).
- **Webhook redeployed** on tinkerdev-box (`deploy.sh webhook`) → now stamps metadata.email; health 200.
- **C7 (deactivate/transfer) + C8 (revoke→suspend→reactivate-blocked) VERIFIED** against prod CE. The
  user's license was suspended then **REINSTATED (ACTIVE)**; machine.lic was deleted during C8 — user
  should reactivate in-app to restore Pro. Suspend/reinstate via `ssh tinkerdev-box` admin (license id
  f56c9a77-66ae-425c-b9f1-5826cbe277d7; actions/suspend|reinstate; admin token server-side in
  /home/claude/devtools/infra/keygen/.env).
- Ship-gate Cases 1/2/3/4/5/6/8 effectively covered; Case 7 (transfer) is the deactivate path (C7).

## Pointers: full advisories in `deferred-items.md`; UI scores in `22.1-UI-REVIEW.md`.
