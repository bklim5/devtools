---
phase: 22.1-settings-followups
plan: 99 (post-review fixes)
subsystem: licensing-ui
tags: [settings, license, a11y, heading-order, focus-return, review-fixes, wcag-aa]
requires:
  - "22.1-UI-REVIEW.md (6-pillar audit, 22/24)"
  - "Codex working-tree review"
provides:
  - "Masked key/email populate the License pane immediately on a fresh inline activation"
  - "Heading order: dialog h2 → pane h3 → status h4 (variant-specific; standalone panel stays h2)"
  - "Locked pin/drag affordances thread an explicit focus-return invoker into openSettings"
  - "Dead inline 'Done' button removed (renders only when onDismiss is passed)"
key-files:
  created: []
  modified:
    - "src/components/UpsellPanel.tsx"
    - "src/components/LicenseSettings.tsx"
    - "src/components/Sidebar.tsx"
    - "src/components/useSidebarDragDrop.ts"
    - "test/e2e/helpers.ts (statusHeading consolidated)"
    - "test/e2e/{license-settings,license-states,ship-gate}.e2e.ts"
decisions:
  - "Fix 1: submit() success calls refreshLicenseUiDetailed() — a notActivated/problem→licensed TRANSITION is not carried forward by carryForwardKeyEmail, so the keychain-free refresh left maskedKey/email null. statusDetail reads the masked_key_cache seeded by verify_then_persist (no extra Keychain prompt)."
  - "Fix 2: status headings demoted h2→h4 — VARIANT-SPECIFIC in the shared ActivationSurface (inline→h4 under the h3 pane title; standalone ToolRoute panel→h2, since a lone h4 there would itself skip — Codex P2). e2e statusHeading() probe reads first h4 in the dialog, consolidated into helpers.ts (was triplicated)."
  - "Fix 3: togglePin(id, invokerEl) + locked Alt chords thread e.currentTarget; useSidebarDragDrop gets a stable getRowEl resolver (the grip hides off-hover) so openSettings restores focus to the row (MED-22-02)."
  - "Fix 4: the success-card Done renders only when onDismiss is passed (inline variants render without it — the live entitlement flip re-renders behind the surface)."
metrics:
  duration: "~1h"
  completed: 2026-06-16
  tasks: 4
  files: 11
  commits: 1
---

# Phase 22.1 Post-Review Fixes Summary

Addressed the four prioritized findings from the 22.1 UI audit (22/24) + the Codex
working-tree review, each through the full per-task gate.

## What Shipped (commit `316eaf33`)

1. **Masked key blanked on inline activation** — `UpsellPanel` submit() success now
   calls `refreshLicenseUiDetailed()`; the License pane shows the masked key/email
   immediately instead of "License key —" until the route reopened. Unit test added.
2. **Heading order** — status headings demoted h2→h4 across LicenseSettings + the
   shared ActivationSurface; level made variant-specific after Codex flagged the
   standalone ToolRoute panel (no dialog/pane above) would skip to h4. e2e probe
   migrated to first-h4 + consolidated into `helpers.ts`. (Closes UI-REVIEW fix #1.)
3. **Locked pin/drag focus-return** — explicit invoker threaded through
   `togglePin`/locked-Alt/drag (stable `getRowEl`) so focus returns to the tool row
   after Settings closes. Sidebar.test asserts the threaded element.
4. **Dead Done no-op** — removed for the inline variants. (Closes UI-REVIEW fix #3.)

## Gate Evidence

- `/simplify`: 2 cleanups (statusHeading→helpers.ts, getRowEl→useCallback).
- `/codex:review --scope working-tree`: 1 P2 → fixed (variant-specific heading level).
- `tsc --noEmit` clean · eslint clean · **vitest 966** (963 + 3 new tests).
- Real-WKWebView e2e: **21/21 spec files** (`test/e2e/__logs__/e2e-22.1-fix.log`).
- Rebuild: fresh `TinkerDev.app` + `TinkerDev_0.3.1_aarch64.dmg` (only the expected
  absent-updater-signing-key error).

## Not Addressed (deferred — see deferred-items.md)

- UI-REVIEW fix #2 (form-only mount-focus lands SR users below the status card) —
  documented intentional per D-22.1-7; deferred, not in the prioritized batch.
- Item-5 minors: disabled-Refresh tokens, redundant mount re-query, live-app axe
  re-measure of the warn-soft/ok-soft/bad-tinted surfaces, stale stacking comments.

## Self-Check: PASSED

- All four fixes present in the source; commit `316eaf33` in git history.
