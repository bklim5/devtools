---
phase: 25-updates-pane-milestone-ship
plan: 05
type: execute
wave: 4
depends_on: [25-04]
files_modified: []
autonomous: false
requirements: [SET-10]
tags: [updates, milestone-signoff, wcag-audit, ship-gate]

must_haves:
  truths:
    - "The full five-pane Settings surface (General, Hotkeys, Appearance, License, Updates) passes a gsd-ui-review WCAG-AA audit in both themes"
    - "lastUpdateCheck + the auto-check toggle persist across a real restart of the packaged app"
    - "A human signs off on a fresh tauri build with the Updates pane working from every entry point"
    - "decoder.ts + its 19 tests are byte-for-byte untouched across the whole phase"
  artifacts:
    - path: ".planning/phases/25-updates-pane-milestone-ship/25-UI-REVIEW.md"
      provides: "the gsd-ui-review WCAG-AA audit over all five panes"
    - path: ".planning/phases/25-updates-pane-milestone-ship/25-05-SUMMARY.md"
      provides: "the milestone sign-off record"
  key_links:
    - from: "fresh tauri build (.app/.dmg)"
      to: "human walkthrough"
      via: "built LAST after all source lands; mtime newer than the last source commit"
      pattern: "tauri build"
---

<objective>
The phase's final gate (D-25-11) — the v1.7 milestone-close sign-off. NOT a separate milestone-audit workflow artifact; this IS the phase's standard final verification checkpoint. Three parts:
(a) a `gsd-ui-review` WCAG-AA audit over the **full five-pane Settings surface** (General, Hotkeys, Appearance, License, Updates) in BOTH themes + every entry point;
(b) a **human sign-off on a fresh `tauri build`** (.app/.dmg under `src-tauri/target/release/bundle/macos/`), built as the LAST step after every Plan 01-04 source change has landed — verify the bundle binary mtime is newer than the last source commit (never a stale `.app`, memory `auto-build-at-phase-boundary` / the project harness rebuild-last rule);
(c) verification that **`decoder.ts` + its 19 tests are byte-for-byte untouched** across the whole phase.

Purpose: prove SET-10 + the whole Settings milestone on real hardware. The lastUpdateCheck + toggle persistence-across-restart that unit/WebDriver gates can't cover (memory `tauri-store-async-init-race`) is verified here by the human on the real WKWebView.
Output: `25-UI-REVIEW.md` (the audit) + the human-approved sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-updates-pane-milestone-ship/25-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-build gates + decoder-untouched proof + gsd-ui-review WCAG-AA audit</name>
  <read_first>
    - .planning/phases/25-updates-pane-milestone-ship/25-CONTEXT.md (D-25-11 — the exact gate definition)
    - docs/harness-and-decisions.md (the binding build+verify harness order)
    - .planning/phases/24-hotkeys-general-panes/24-04-SUMMARY.md (the prior phase-boundary gate precedent — what the walkthrough + audit covered)
  </read_first>
  <action>
    Run the harness gate BEFORE the build (memory `always-run-harness-gate-before-passback`):
    1. `pnpm exec vitest run` (full suite green) + `pnpm exec tsc --noEmit` + `pnpm exec eslint` clean.
    2. Real-WKWebView e2e via `scripts/e2e-spike.sh` (all spec files, incl. the extended `settings.e2e.ts` from Plan 04 + a no-regression run of the updater/appearance/hotkeys/license specs).
    3. **Decoder-untouched proof:** `git diff --stat <phase-base>..HEAD -- src/lib/protobuf/decoder.ts` and the decoder test file show ZERO changes; `pnpm exec vitest run src/lib/protobuf` shows the 19 tests green.
    4. **gsd-ui-review WCAG-AA audit** over the full five-pane Settings surface in BOTH light + dark themes + every entry point (app menu ⌘, · tray · sidebar row · ⌘K [Pro] · deep link). Record findings in `.planning/phases/25-updates-pane-milestone-ship/25-UI-REVIEW.md` with a pass/score and any deductions. Address any blocking (non-AA) finding before the build; record non-blocking deferrals.
  </action>
  <verify>
    <automated>pnpm exec vitest run && pnpm exec tsc --noEmit && git diff --stat -- src/lib/protobuf/decoder.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run` exits 0 (full suite)
    - `pnpm exec tsc --noEmit` exits 0
    - `git diff --stat -- src/lib/protobuf/decoder.ts` shows 0 changes (and the decoder test file likewise)
    - `.planning/phases/25-updates-pane-milestone-ship/25-UI-REVIEW.md` exists with a WCAG-AA pass over all five panes; no unaddressed blocking finding
    - The real-WKWebView e2e suite passed under `scripts/e2e-spike.sh` (recorded in the SUMMARY)
  </acceptance_criteria>
  <done>All automated gates green, decoder proven untouched, and the five-pane WCAG-AA audit recorded with no blocking findings.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh tauri build as the LAST step (after all source lands)</name>
  <read_first>
    - .planning/phases/25-updates-pane-milestone-ship/25-CONTEXT.md (D-25-11 — build LAST, verify mtime)
    - CLAUDE.md (the build harness: the agent auto-runs `pnpm tauri build` at the checkpoint; the final non-zero exit is only the absent updater-signing key — confirm via the .app/.dmg, not the exit code)
    - project memory `tauri-dmg-bundle-flake` (unmount other DMGs + retry if the DMG step flakes)
  </read_first>
  <action>
    Confirm every Plan 01-04 source change is committed FIRST (no pending webview source). Then run `pnpm tauri build`. Per the harness: its final non-zero exit is only the absent updater-signing key — confirm success via the produced `.app`/`.dmg` under `src-tauri/target/release/bundle/macos/`, NOT the exit code. **Verify the bundle binary mtime is newer than the last source commit** (never hand off a stale `.app`). If the DMG step flakes on a mounted DMG (memory `tauri-dmg-bundle-flake`), `hdiutil detach` the stray mount and retry. Capture the built `.app` path for the human handoff.
  </action>
  <verify>
    <automated>ls -la src-tauri/target/release/bundle/macos/*.app && ls -la src-tauri/target/release/bundle/dmg/*.dmg 2>/dev/null || ls -la src-tauri/target/release/bundle/macos/*.dmg 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - A fresh `.app` (and `.dmg`) exists under `src-tauri/target/release/bundle/macos/`
    - The `.app` binary mtime is newer than the last source commit timestamp (verified, recorded in the SUMMARY)
    - All Plan 01-04 changes are committed before the build (clean tree except planning docs)
  </acceptance_criteria>
  <done>A fresh, non-stale TinkerDev.app + DMG are built after all source landed; the path is ready for the human walkthrough.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on the fresh build — five-pane Settings + Updates pane</name>
  <what-built>
    The Updates pane (ungated): app version, last-checked (relative + "Never"), Check-for-updates with inline result, auto-check toggle — appended append-only to a now-five-pane Settings surface. App.tsx's updater state was lifted to a shared `useUpdater` singleton (tray + pane share one check; lastUpdateCheck stamped on every check). The fresh `.app`/`.dmg` is built and mtime-verified non-stale.
  </what-built>
  <how-to-verify>
    Launch the built app from `src-tauri/target/release/bundle/macos/TinkerDev.app` (path in the SUMMARY), then:
    1. Open Settings (app menu TinkerDev ▸ Settings… ⌘, · tray Settings… · sidebar Settings row · — and for a Pro user, ⌘K "Settings"). Confirm the modal opens and lands on General.
    2. Navigate to the **Updates** pane by keyboard only (Tab/arrow pane-nav). Confirm: a version line matching the build (0.4.0), "Last checked: Never" on first run, and the "Automatically check for updates on launch" toggle is keyboard-operable (Space/Enter) with a visible non-opacity state.
    3. Click/Enter "Check for updates". Confirm an inline result appears (up-to-date, OR — if the live endpoint has a newer version — "Version X available" AND the existing UpdateBanner appears for install; the pane shows NO separate Install button).
    4. Confirm "Last checked" now shows a relative time ("just now"/"x seconds ago"), with the absolute timestamp on hover.
    5. **Persistence:** Quit and relaunch the app. Re-open Settings ▸ Updates. Confirm "Last checked" still shows the prior time (not "Never") AND the auto-check toggle retains its state — proving the prefs.json round-trip on the real WKWebView (memory `tauri-store-async-init-race`).
    6. Spot-check the other four panes (General, Hotkeys, Appearance, License) still render + work (no regression from the App.tsx refactor).
    7. Confirm WCAG-AA feel: visible focus rings, no opacity-only states, readable contrast in both light and dark.
  </how-to-verify>
  <resume-signal>Type "approved" to mark SET-10 + the v1.7 Settings milestone signed off, or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged app → updater endpoint | The only live network call (manual check) — through the existing verified seam |
| prefs.json (real store) → app | lastUpdateCheck/autoUpdateCheck round-trip on the real WKWebView, verified by the human |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Tampering | stale-build handoff | mitigate | Build LAST + mtime-verify newer than the last source commit; never hand off a stale `.app` (the multi-plan stale-bundle rule). |
| T-25-15 | Tampering | decoder drift | mitigate | `git diff --stat` proves decoder.ts + its 19 tests byte-for-byte untouched across the phase (Task 1). |
| T-25-16 | Information disclosure / a11y | WCAG-AA regression | mitigate | gsd-ui-review audit over all five panes in both themes blocks on any non-AA finding before sign-off. |

No high-severity threats: this is a verification/sign-off gate; the only live surface (the updater check) reuses the unchanged verify-before-apply seam.
</threat_model>

<verification>
- Full unit suite + tsc + eslint green; real-WKWebView e2e suite green
- `git diff --stat -- src/lib/protobuf/decoder.ts` (+ the decoder test file) shows 0 changes
- `25-UI-REVIEW.md` records a five-pane WCAG-AA pass with no blocking findings
- A fresh, mtime-verified non-stale `.app`/`.dmg` exists
- Human typed "approved" after the walkthrough (incl. the quit/relaunch persistence check)
</verification>

<success_criteria>
SET-10 is Validated and the v1.7 Settings milestone is signed off: the five-pane Settings surface passes WCAG-AA in both themes, the Updates pane works from every entry point with last-checked + toggle surviving a real restart, the build is fresh and non-stale, and decoder.ts + its 19 tests are byte-for-byte untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/25-updates-pane-milestone-ship/25-05-SUMMARY.md` recording the gate results, the build path + mtime proof, the UI-review score, and the human approval.
</output>
