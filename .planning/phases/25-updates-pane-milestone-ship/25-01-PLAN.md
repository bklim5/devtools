---
phase: 25-updates-pane-milestone-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/platform/index.ts
  - src/lib/platform/tauri.ts
  - src/lib/platform/browser.ts
  - src/lib/platform/stub.ts
  - src/lib/platform/platform.test.ts
autonomous: true
requirements: [SET-10]
tags: [updates, platform-seam, version]

must_haves:
  truths:
    - "The Updates pane can read the running app version through the platform seam, never @tauri-apps/* directly"
    - "Outside Tauri (jsdom/vite preview) getVersion resolves a safe fallback with no native call"
  artifacts:
    - path: "src/lib/platform/index.ts"
      provides: "app.getVersion() on the Platform interface + the `get app()` delegate"
      contains: "getVersion"
    - path: "src/lib/platform/tauri.ts"
      provides: "real getVersion arm reading Tauri's app getVersion()"
      contains: "getVersion"
    - path: "src/lib/platform/browser.ts"
      provides: "fallback app.getVersion arm (no native)"
      contains: "getVersion"
  key_links:
    - from: "src/lib/platform/index.ts"
      to: "active.app"
      via: "get app() delegate getter (mirrors get updater())"
      pattern: "get app"
    - from: "src/lib/platform/tauri.ts"
      to: "@tauri-apps/api/app getVersion"
      via: "real arm only in tauri.ts"
      pattern: "getVersion"
---

<objective>
Add an `app.getVersion()` capability to the `src/lib/platform/` seam (D-25-2) so the Updates pane can display the running app version while keeping seam discipline — the pane never imports `@tauri-apps/*` directly. Real impl in `tauri.ts` reads Tauri's app `getVersion()` (single source of truth = `tauri.conf.json` `version`, currently `0.4.0`); the browser/stub arms return a safe fallback so unit tests + `vite preview` never touch native.

Purpose: foundational seam surface the Updates pane (Plan 04) reads version from. Independent of the prefs work (Plan 02) — no file overlap, both wave 1.
Output: a new `app: { getVersion(): Promise<string> }` capability across all four seam files + the `platform` delegate + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-updates-pane-milestone-ship/25-CONTEXT.md

<interfaces>
The seam shape to extend (from src/lib/platform/index.ts). Add `app` ALONGSIDE the existing capabilities, mirroring `updater`/`opener`/`autostart`:

```typescript
// On the Platform interface (index.ts), add after `autostart`:
/** App metadata (SET-10/D-25-2). getVersion() returns the packaged app version
 *  (tauri.conf.json `version`). Reached ONLY through the seam (FND-04) — the pane
 *  never imports @tauri-apps/*. The real arm (tauri.ts) reads Tauri's app
 *  getVersion(); the browser/test arms return a safe fallback (no native call). */
app: {
  getVersion(): Promise<string>;
};
```

The `platform` const (index.ts ~L187) delegates per capability via getters:
```typescript
get autostart() { return active.autostart; },
get app() { return active.app; },   // ADD THIS
```

tauri.ts already imports from @tauri-apps. Add the app import + arm:
```typescript
import { getVersion } from "@tauri-apps/api/app";  // ADD near the other imports
// ...in tauriPlatform, after `autostart: {...}`:
app: {
  getVersion: () => getVersion(),
},
```

browser.ts fallback (must NOT import @tauri-apps/*). The build-time fallback value:
```typescript
// after `autostart: {...}`:
app: {
  async getVersion(): Promise<string> {
    return import.meta.env.VITE_APP_VERSION ?? "0.0.0-dev";
  },
},
```
(`import.meta.env.VITE_APP_VERSION` is undefined unless set; the `?? "0.0.0-dev"` constant is the safe fallback. Vite exposes package.json version as no built-in, so the literal fallback is fine — D-25-2 says "a constant or build-time inject", constant chosen.)

stub.ts — the makeMemoryPlatform test factory must also gain an `app` arm (otherwise the 4 inline Platform literals in platform.test.ts fail tsc). Mirror browser.ts: `app: { async getVersion() { return "0.0.0-test"; } }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add app.getVersion() to the seam interface + all four arms</name>
  <read_first>
    - src/lib/platform/index.ts (the Platform interface + the `platform` delegate const ~L187-215; mirror the `updater`/`autostart` getter pattern)
    - src/lib/platform/tauri.ts (the ONLY file allowed to import @tauri-apps/*; the import block L13-26 + tauriPlatform arms; add the app arm after `autostart`)
    - src/lib/platform/browser.ts (the no-@tauri-apps fallback; the autostart arm ~L115 is the last capability — add `app` after it)
    - src/lib/platform/stub.ts (makeMemoryPlatform / the test Platform factory — find how it builds a Platform literal and add the `app` arm so test fixtures stay type-complete)
  </read_first>
  <behavior>
    - platform.app.getVersion() resolves a non-empty string in the browser/test environment (the fallback constant), never throwing, never a network/native call.
    - The seam delegate `platform.app` reflects the active impl (getter, not a snapshot) — same pattern as `platform.updater`.
    - tsc stays clean: every Platform literal in the codebase (browser.ts, stub.ts factory, the 4 inline literals in platform.test.ts) carries the `app` arm.
  </behavior>
  <action>
    1. **index.ts**: add the `app: { getVersion(): Promise<string> }` capability to the `Platform` interface (after `autostart`, with the doc comment from `<interfaces>`). Add `get app() { return active.app; }` to the `platform` const delegate (mirror `get autostart()`).
    2. **tauri.ts**: add `import { getVersion } from "@tauri-apps/api/app";` to the import block; add `app: { getVersion: () => getVersion() }` to `tauriPlatform` after the `autostart` arm.
    3. **browser.ts**: add `app: { async getVersion(): Promise<string> { return import.meta.env.VITE_APP_VERSION ?? "0.0.0-dev"; } }` after the `autostart` arm. Add a one-line comment per the file's house style ("App version is read from the build-time env fallback outside Tauri; this file must NOT import @tauri-apps/*").
    4. **stub.ts**: find `makeMemoryPlatform` (or the inline Platform factory) and add an `app: { async getVersion() { return "0.0.0-test"; } }` arm so every test-constructed Platform is type-complete.
    5. If `src/lib/platform/platform.test.ts` has inline `Platform` literals (the Phase-24 precedent widened 4 of them for `autostart`), add the `app` arm to each.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec vitest run src/lib/platform/platform.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/platform/index.ts` contains `getVersion` AND `get app`
    - `src/lib/platform/tauri.ts` contains `@tauri-apps/api/app` AND `getVersion`
    - `src/lib/platform/browser.ts` contains `getVersion` AND does NOT contain `@tauri-apps` (grep `@tauri-apps` in browser.ts returns 0)
    - `pnpm exec tsc --noEmit` exits 0
    - `git diff --stat src/lib/protobuf/decoder.ts` shows 0 changes
  </acceptance_criteria>
  <done>The `app.getVersion()` capability exists on the Platform interface and in all four arms; tsc clean; the browser/stub arms return a fallback string with no native call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test the seam getVersion fallback + delegate</name>
  <read_first>
    - src/lib/platform/platform.test.ts (existing seam tests — the pattern for asserting the browser fallback + setPlatformForTest/resetPlatformForTest)
    - src/lib/platform/index.ts (setPlatformForTest, resetPlatformForTest, the `platform` delegate)
    - src/lib/platform/stub.ts (the test stub factory used by these tests)
  </read_first>
  <behavior>
    - Test 1: `await platform.app.getVersion()` in the default (browser) environment resolves a non-empty string and does not throw.
    - Test 2: after `setPlatformForTest(stub)` where the stub's getVersion returns a known sentinel (e.g. "9.9.9-fixture"), `platform.app.getVersion()` resolves that sentinel — proving the delegate reads the active impl, not a snapshot.
  </behavior>
  <action>
    Add a `describe("app.getVersion", ...)` block to `platform.test.ts` with the two tests above. Reuse the existing `setPlatformForTest`/`resetPlatformForTest` harness (afterEach reset). Construct the stub via the same factory the other tests use; if you need a custom getVersion, spread the factory result and override `app`. Assert `typeof v === "string" && v.length > 0` for the fallback, and `toBe("9.9.9-fixture")` for the injected stub.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/platform/platform.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/platform/platform.test.ts` contains `app.getVersion`
    - `pnpm exec vitest run src/lib/platform/platform.test.ts` exits 0 with the 2 new tests passing
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Two tests pin the getVersion fallback string and the active-impl delegate; both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native runtime → webview | Tauri's `getVersion()` returns `tauri.conf.json` version into JS (read-only metadata, no user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information disclosure | app.getVersion() | accept | Version string is already public (shown in About/updater banner, embedded in the bundle). No new secret crosses the seam. |
| T-25-02 | Tampering | browser.ts fallback constant | accept | The fallback only renders in non-packaged (jsdom/vite preview) where the real version is irrelevant; the packaged app always uses the real Tauri arm. No DRM dependence on this value. |
| T-25-03 | Spoofing (seam discipline) | pane importing @tauri-apps directly | mitigate | The seam is the ONLY path; acceptance grep asserts browser.ts has zero `@tauri-apps` imports and the real import lives only in tauri.ts. |

No high-severity threats: read-only public metadata, no auth/network surface added.
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` clean
- `pnpm exec vitest run src/lib/platform/platform.test.ts` green (incl. 2 new tests)
- `grep -c "@tauri-apps" src/lib/platform/browser.ts` == 0
- `grep -c "@tauri-apps/api/app" src/lib/platform/tauri.ts` == 1
- decoder.ts + its 19 tests byte-for-byte untouched
</verification>

<success_criteria>
The Updates pane (Plan 04) can call `platform.app.getVersion()` and receive the real version in the packaged app / a safe fallback in tests, with no `@tauri-apps/*` import outside `tauri.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/25-updates-pane-milestone-ship/25-01-SUMMARY.md`
</output>
