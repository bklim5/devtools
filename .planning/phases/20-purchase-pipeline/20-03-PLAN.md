---
phase: 20-purchase-pipeline
plan: 03
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - infra/keygen/compose.yaml
  - infra/keygen/Caddyfile
  - infra/keygen/.env.example
  - infra/keygen/webhook.Dockerfile
  - infra/keygen/swap.sh
  - infra/keygen/setup.sh
  - infra/keygen/deploy.sh
  - infra/keygen/README.md
  - infra/keygen/RUNBOOK.md
  - src-tauri/src/license/config.rs
  - .gitignore
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03]
user_setup:
  - service: hetzner
    why: "Production Keygen CE host (D-47 CX23, EU)"
    dashboard_config:
      - task: "Provision a Hetzner CX23 (Cost-Optimized, x86, 2 vCPU / 4 GB / 40 GB, EU); enable provider snapshots (D-49); open ONLY ports 22/80/443; add SSH key"
        location: "Hetzner Cloud Console"
  - service: dns-tinkerdev-io
    why: "license.tinkerdev.io A-record (ACME HTTP-01), email SPF/DKIM/DMARC, tinkerdev.io/buy redirect, alerts@ forwarding"
    dashboard_config:
      - task: "A record license.tinkerdev.io → VPS IP; SPF/DKIM/DMARC for Resend; tinkerdev.io/buy redirect → live LS checkout; alerts@tinkerdev.io email routing → user inbox"
        location: "DNS provider (Cloudflare) for tinkerdev.io"
  - service: lemonsqueezy
    why: "MoR checkout (D-61 resolved — SG payout confirmed) + order_created webhook"
    env_vars:
      - name: LS_WEBHOOK_SECRET
        source: "Lemon Squeezy Dashboard → Settings → Webhooks (signing secret)"
    dashboard_config:
      - task: "Create store + USD-9 one-time product (D-62); set success page 'check email' copy (D-70); add order_created webhook → https://license.tinkerdev.io/webhooks/lemonsqueezy"
        location: "Lemon Squeezy Dashboard"
  - service: resend
    why: "License key email delivery (D-64)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys"
    dashboard_config:
      - task: "Verify tinkerdev.io domain (add Resend DKIM/SPF DNS records)"
        location: "Resend Dashboard → Domains"
  - service: uptimerobot
    why: "Uptime monitoring of CE + webhook health endpoints (D-72)"
    dashboard_config:
      - task: "Add HTTP monitors for the CE health route + https://license.tinkerdev.io/health (webhook)"
        location: "UptimeRobot (free)"

must_haves:
  truths:
    - "A production Keygen CE is live at https://license.tinkerdev.io behind real ACME TLS (NOT tls internal) with a fresh account + new Ed25519 keypair (D-51)"
    - "The CE policy is perpetual / node-locked / maxMachines=1 with pro.theming + pro.ordering entitlement codes attached (D-53/D-54), created idempotently"
    - "The webhook backend runs as a container on the same VPS, reaching CE over localhost; the admin token never crosses the public internet (D-55)"
    - "config.rs release constants now hold the REAL prod account id + base64-raw-32-byte pubkey; cargo test --release passes the prod tripwire"
    - "A full Lemon Squeezy test-mode purchase mints a license + emails the key + activates via the Phase-19 flow; then one live USD-9 purchase proves it end-to-end, refunded after"
    - "No privileged Keygen token / SECRET_KEY_BASE / Resend key / LS webhook secret appears in the repo or the packaged app bundle (criterion 4)"
  artifacts:
    - path: "infra/keygen/compose.yaml"
      provides: "Prod stack: CE web+worker + postgres + redis + webhook + Caddy (real ACME)"
      contains: "webhook"
    - path: "infra/keygen/Caddyfile"
      provides: "license.tinkerdev.io reverse_proxy with real ACME (no tls internal)"
      contains: "license.tinkerdev.io"
    - path: "infra/keygen/swap.sh"
      provides: "2 GB swap + vm.swappiness=10 before CE bring-up (D-48)"
      contains: "swapon"
    - path: "infra/keygen/setup.sh"
      provides: "idempotent account/keypair + product + policy + entitlement codes + metadata-filter validation (D-51/D-53/D-54)"
      contains: "pro.theming"
    - path: "infra/keygen/RUNBOOK.md"
      provides: "human checklist: provision → DNS → bring-up → LS store → Resend → secrets → prod constants → D-63 live purchase + refund → grep-clean"
      contains: "D-63"
    - path: "src-tauri/src/license/config.rs"
      provides: "real prod account id + pubkey replacing the Plan-01 placeholders"
      contains: "license.tinkerdev.io"
  key_links:
    - from: "Caddy"
      to: "webhook + keygen-web"
      via: "reverse_proxy on license.tinkerdev.io (real ACME)"
      pattern: "reverse_proxy"
    - from: "infra/keygen/setup.sh"
      to: "CE policy"
      via: "attach pro.theming + pro.ordering entitlement codes (D-54)"
      pattern: "pro\\.(theming|ordering)"
    - from: "config.rs release consts"
      to: "production CE identity"
      via: "real account id + pubkey from setup.sh (D-51/D-52)"
      pattern: "license.tinkerdev.io"
---

<objective>
Stand up the PRODUCTION licensing infrastructure (D-46 "full prod now") and prove the whole purchase pipeline end-to-end. Commit the reproducible `infra/keygen/` stack (compose: CE web+worker + Postgres + Redis + the Plan-02 webhook + Caddy with REAL ACME; swap; idempotent CE setup creating the fresh account/keypair + policy + entitlement codes; deploy script), write the human RUNBOOK for the manual steps Claude cannot do (provision the VPS, set DNS, create the LS store + Resend domain, register secrets, run the live D-63 purchase + refund), capture the real prod account id + pubkey into `config.rs`'s release constants, and grep-clean the bundle/repo of every privileged secret (criterion 4).

This plan is NOT autonomous: VPS provisioning, SSH deploys, DNS, the LS/Resend dashboards, and the live USD-9 purchase + refund are human steps. The committed `infra/` scripts ARE autonomous artifacts; the bring-up + purchase are checkpoint/runbook steps.

Purpose: PAY-01/PAY-02/PAY-03 proven against real infra (criterion 1-4) — a buyer pays once and receives a working key by email that activates via the unchanged Phase-19 flow.
Output: committed `infra/keygen/` + RUNBOOK.md, real prod constants in config.rs, and a human-verified live purchase walkthrough + grep-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-purchase-pipeline/20-CONTEXT.md
@.planning/phases/20-purchase-pipeline/20-RESEARCH.md
@.planning/phases/20-purchase-pipeline/20-VALIDATION.md

<interfaces>
<!-- Reuse the proven Phase-19 local CE shape; the deltas are: real ACME, two-label host,
     fresh keypair, the webhook container, entitlement codes, license metadata. -->

Phase-19 local CE scaffolding to ADAPT (same shape, prod values):
- scripts/keygen-ce/compose.yaml — CE web+worker + postgres:17.5 + redis + caddy; NO compose
  profile keys (Pitfall 8: run setup via `docker compose run --rm setup`, never list in `up`).
  Only caddy publishes host ports. ADD a `webhook` service (the Plan-02 server/webhook image,
  built from the repo) reaching `web:3000` over the compose network (localhost-equivalent, D-55).
- scripts/keygen-ce/Caddyfile — currently `tls {$CADDY_ACME_EMAIL:internal}` (LOCAL). PROD must
  use REAL ACME: `license.tinkerdev.io { reverse_proxy /webhooks/* webhook:PORT ; reverse_proxy web:3000 }`
  — NO `tls internal` (Pitfall 3: release builds compile out the custom-CA path, so a self-signed
  prod cert fails every activation). Set CADDY_ACME_EMAIL to a real address.
- scripts/keygen-ce/bootstrap.sh — the idempotent lookup-then-create pattern (product, policy with
  authenticationStrategy="LICENSE" + maxMachines=1 — Pitfall 1) + the pubkey extraction
  (`rails runner 'Base64.strict_encode64([Account.first.ed25519_public_key].pack("H*"))'`,
  base64-of-raw-32-bytes — the SPIKE encoding gotcha). setup.sh EXTENDS this with:
  (a) entitlement create+attach (pro.theming, pro.ordering — D-54), (b) a metadata-filter
  validation (create a throwaway license with metadata.orderId, GET ?metadata[orderId]= , assert
  it returns, then delete it — A2 verify-on-live).

Prod CE env (.env, gitignored — Pitfall 4):
  KEYGEN_EDITION=CE, KEYGEN_MODE=singleplayer, KEYGEN_HOST=license.tinkerdev.io,
  KEYGEN_DOMAIN=license.tinkerdev.io, fresh KEYGEN_ACCOUNT_ID (from setup), SECRET_KEY_BASE +
  ENCRYPTION_* via `openssl rand` (NEVER committed). Redis maxmemory-policy noeviction (Pitfall 7).

config.rs release constants (Plan 01 left placeholders): replace
  `KEYGEN_ACCOUNT_ID = "PROD_ACCOUNT_ID_PLACEHOLDER"` and
  `KEYGEN_ED25519_PUBKEY_B64 = "PROD_PUBKEY_PLACEHOLDER"` with the real values setup.sh prints.
  The `#[cfg(not(debug_assertions))]` tripwire test (Plan 01) FAILS until these are real — so
  `cargo test --release --manifest-path src-tauri/Cargo.toml` is THIS plan's gate that the prod
  consts are valid (32-byte pubkey, license.tinkerdev.io host).

Webhook env (server/webhook/.env on the box, gitignored): KEYGEN_HOST=web (compose service name,
localhost-equivalent over the compose network), KEYGEN_ACCOUNT_ID, KEYGEN_ADMIN_TOKEN,
KEYGEN_POLICY_ID (from setup), LS_WEBHOOK_SECRET, RESEND_API_KEY, EMAIL_FROM, PORT.

tinkerdev.io/buy redirect (D-68, Open Question 2): the app constant already ships pointing at it
(Plan 01). The redirect target (live LS checkout URL) only exists AFTER the LS product is created —
sequence the redirect-config step AFTER store creation in the RUNBOOK; until then /buy may placeholder.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the production infra/keygen/ stack (compose + Caddy + swap + setup + deploy)</name>
  <files>infra/keygen/compose.yaml, infra/keygen/Caddyfile, infra/keygen/.env.example, infra/keygen/webhook.Dockerfile, infra/keygen/swap.sh, infra/keygen/setup.sh, infra/keygen/deploy.sh, infra/keygen/README.md, .gitignore</files>
  <read_first>
    - scripts/keygen-ce/compose.yaml (the proven local stack to adapt — services, no-profiles, volumes)
    - scripts/keygen-ce/Caddyfile (the local `tls internal` to REPLACE with real ACME)
    - scripts/keygen-ce/bootstrap.sh (the idempotent lookup-then-create + pubkey extraction to EXTEND)
    - scripts/keygen-ce/.env.example (the env template shape)
    - server/webhook/ (the Plan-02 backend the webhook service containerizes)
    - 20-RESEARCH.md "Recommended Production Topology" + "Recommended Repo Layout" + Pitfalls 3/4/6/7/8 + the entitlement/metadata/Resend code examples
    - 20-CONTEXT.md D-47/D-48/D-50/D-51/D-53/D-54/D-55/D-72
    - .gitignore (the keygen-ce gitignore pattern at :42-48 to mirror for infra/keygen)
  </read_first>
  <action>
Create the committed, reproducible `infra/keygen/` stack (NO real secrets — templates only, D-41/D-50). All scripts must be idempotent / re-runnable on a box rebuild (D-53).

1. `compose.yaml`: adapt scripts/keygen-ce/compose.yaml. Keep: setup (one-shot, NOT in `up` — Pitfall 8), postgres:17.5, redis (set `command: redis-server --maxmemory-policy noeviction` — Pitfall 7), web (`command: web`), worker (`command: worker`), caddy (publishes 80/443). ADD a `webhook` service built from `infra/keygen/webhook.Dockerfile` (build context `../../server/webhook`), `env_file: server/webhook/.env` (on the box), on the compose network only — NO published ports (Caddy fronts it, D-55). Named volumes for postgres/redis/keygen/caddy_data/caddy_config.

2. `webhook.Dockerfile`: a small Node image building `server/webhook/` (install, build, run `node dist/index.js` or `tsx src/index.ts`); EXPOSE the PORT; no secrets baked in (env at runtime).

3. `Caddyfile`: REAL ACME (Pitfall 3) — `license.tinkerdev.io { reverse_proxy /webhooks/* webhook:{PORT} ; reverse_proxy web:3000 }` with NO `tls internal` block (automatic Let's Encrypt; requires ports 80+443 open + the A-record live, noted in RUNBOOK). Route `/webhooks/*` to the webhook container and everything else to keygen-web. ACME email from `CADDY_ACME_EMAIL` env.

4. `.env.example`: prod CE template (Pitfall 4) — `KEYGEN_EDITION=CE`, `KEYGEN_MODE=singleplayer`, `KEYGEN_HOST=license.tinkerdev.io`, `KEYGEN_DOMAIN=license.tinkerdev.io`, `KEYGEN_ACCOUNT_ID=<from setup>`, `SECRET_KEY_BASE=<openssl rand -hex 64>`, `ENCRYPTION_DETERMINISTIC_KEY=`, `ENCRYPTION_KEY_DERIVATION_SALT=`, `ENCRYPTION_PRIMARY_KEY=`, `CADDY_ACME_EMAIL=`, `POSTGRES_*`, `REDIS_URL`. Placeholders only; comment which come from `openssl rand` and which from setup.

5. `swap.sh` (D-48): `fallocate -l 2G /swapfile` (guard if exists) → `chmod 600` → `mkswap` → `swapon` → append to `/etc/fstab` (idempotent — grep before append) → `sysctl vm.swappiness=10` + persist in `/etc/sysctl.d/`. Run BEFORE CE bring-up (Pitfall 6 ordering — stated in RUNBOOK).

6. `setup.sh` (D-51/D-53/D-54): extend bootstrap.sh's pattern. (a) get admin token; (b) ensure product; (c) ensure policy (perpetual, `maxMachines:1`, `authenticationStrategy:"LICENSE"`, `expirationStrategy:"RESTRICT_ACCESS"`); (d) NEW: ensure two entitlements by code `pro.theming` + `pro.ordering` (lookup-by-code then create) and attach BOTH to the policy via `POST /policies/{id}/relationships/entitlements`; (e) extract+normalize the Ed25519 pubkey (base64-of-raw-32-bytes — the SPIKE gotcha) and PRINT it + the account id + policy id for the config.rs paste (Task 3) and the webhook .env; (f) NEW (A2 validation): create a throwaway license with `metadata.orderId=__setupcheck__`, `GET ?metadata[orderId]=__setupcheck__`, assert it returns, then delete it — fail loudly if CE ignores metadata. Idempotent throughout (re-runnable, D-53).

7. `deploy.sh` (D-72/Claude's discretion): rsync `infra/keygen/` + `server/webhook/` to the box, build the webhook image, `docker compose up -d` over SSH; restart strategy documented. Script-over-SSH (origin remote is release-tags-only — not CI).

8. `README.md`: how the stack fits together (mirror scripts/keygen-ce/README.md). Update `.gitignore`: add `infra/keygen/.env`, `infra/keygen/*.crt`, `!infra/keygen/.env.example`.

NOTE: these scripts are committed but EXECUTED by the human during bring-up (Task 2 runbook) — Claude cannot provision the VPS. The autonomous deliverable here is the committed, parse-clean scripts + templates.
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && test -f infra/keygen/compose.yaml && test -x infra/keygen/swap.sh && test -x infra/keygen/setup.sh && grep -q "license.tinkerdev.io" infra/keygen/Caddyfile && ! grep -q "tls internal" infra/keygen/Caddyfile && grep -q "pro.theming" infra/keygen/setup.sh && grep -q "pro.ordering" infra/keygen/setup.sh && grep -q "noeviction" infra/keygen/compose.yaml && grep -q "webhook" infra/keygen/compose.yaml && grep -q "infra/keygen/.env" .gitignore && bash -n infra/keygen/swap.sh && bash -n infra/keygen/setup.sh && bash -n infra/keygen/deploy.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `infra/keygen/Caddyfile` contains `license.tinkerdev.io` and does NOT contain `tls internal`
    - `infra/keygen/setup.sh` contains `pro.theming` AND `pro.ordering`, attaches them to the policy, and has a metadata-filter validation step
    - `infra/keygen/compose.yaml` contains a `webhook` service AND `noeviction` for redis AND has NO `profiles:` key
    - `infra/keygen/swap.sh` contains `swapon` and `vm.swappiness`
    - `infra/keygen/.env.example` is placeholders only — `grep -nE "re_[A-Za-z0-9]{16}|whsec_[A-Za-z0-9]|SECRET_KEY_BASE=[A-Za-z0-9]{16}" infra/keygen/.env.example` returns nothing
    - `.gitignore` contains `infra/keygen/.env` and `!infra/keygen/.env.example`
    - `bash -n` parses every shell script without error
  </acceptance_criteria>
  <done>The reproducible prod stack is committed (real-ACME Caddy, webhook container, swap, idempotent setup with entitlements + metadata validation, deploy), secrets template-only and gitignored.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Bring up production CE + provision MoR/email/DNS (human runbook)</name>
  <files>infra/keygen/RUNBOOK.md</files>
  <read_first>
    - infra/keygen/* (the committed stack from Task 1 the human will run)
    - 20-RESEARCH.md "Environment Availability" (the blocking long-poles: Hetzner VPS, MoR KYC, Resend domain verify, ports 80/443) + Pitfalls 3/4/6
    - 20-CONTEXT.md D-46..D-72 (the locked infra decisions)
    - 20-VALIDATION.md "Manual-Only Verifications"
  </read_first>
  <action>
First, Claude WRITES `infra/keygen/RUNBOOK.md` — an ordered, copy-pasteable human checklist (this part is autonomous). Then the HUMAN executes it (this is why the task is non-autonomous). The runbook MUST sequence (long-poles first):

1. **Start the slow accounts first:** create the Lemon Squeezy seller account (KYC review can take days; D-61 SG payout already user-confirmed) and the Resend account; begin Resend `tinkerdev.io` domain verification (DNS propagation).
2. **Provision the VPS:** Hetzner CX23 (Cost-Optimized, x86, EU, D-47); enable provider snapshots (D-49); firewall to ports 22/80/443 ONLY; install Docker + Compose; add SSH key.
3. **DNS:** A-record `license.tinkerdev.io` → VPS IP (BEFORE first Caddy boot — ACME HTTP-01 needs it, Pitfall 3); Resend SPF/DKIM/DMARC records on tinkerdev.io (D-65); `alerts@tinkerdev.io` email routing → user inbox (Cloudflare Email Routing).
4. **Bring-up (in order — Pitfall 6):** `swap.sh` FIRST → generate `.env` (`openssl rand` secrets, NEVER commit) → `docker compose run --rm setup` (Pitfall 8) → record the printed account id + pubkey + admin token → `setup.sh` (creates account/keypair/product/policy/entitlements + runs the metadata-filter validation, A2) → `docker compose up -d` → verify `https://license.tinkerdev.io/<health>` over real TLS (NOT -k — Pitfall 3 proof).
5. **LS store:** create store + USD-9 one-time product (D-62); configure the success page "check email" copy (D-70); add the `order_created` webhook → `https://license.tinkerdev.io/webhooks/lemonsqueezy`; copy the signing secret into the webhook `.env` (`LS_WEBHOOK_SECRET`).
6. **Redirect (AFTER the store exists — Open Question 2):** point `tinkerdev.io/buy` at the live LS checkout URL (Cloudflare rule or Caddy, D-68).
7. **Webhook `.env`** on the box: admin token + account id + policy id (from setup) + LS secret + Resend key + EMAIL_FROM; redeploy the webhook container (`deploy.sh`).
8. **UptimeRobot:** monitors on the CE health route + `https://license.tinkerdev.io/health` (D-72).

The runbook lists exactly which secret goes where and reminds: secrets stay in gitignored `.env` on the box ONLY (D-41/D-55) — never committed, never in the app. End the runbook with a pointer to the D-63 live-purchase ship-gate (Task 4).
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && test -f infra/keygen/RUNBOOK.md && grep -q "license.tinkerdev.io/webhooks/lemonsqueezy" infra/keygen/RUNBOOK.md && grep -q "swap.sh" infra/keygen/RUNBOOK.md && grep -qi "snapshot" infra/keygen/RUNBOOK.md && grep -q "D-63" infra/keygen/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `infra/keygen/RUNBOOK.md` exists and contains `license.tinkerdev.io/webhooks/lemonsqueezy`, `swap.sh`, a snapshot/backup step, and a `D-63` purchase step
    - The runbook sequences swap BEFORE CE bring-up (Pitfall 6) and the /buy redirect AFTER store creation
    - The runbook names which secret goes into which gitignored `.env` (criterion-4 discipline)
    - (Human) `https://license.tinkerdev.io/<health>` responds over REAL TLS without `-k`; setup.sh metadata-filter validation passed
  </acceptance_criteria>
  <what-built>
Claude wrote `infra/keygen/RUNBOOK.md`, the ordered human bring-up checklist (LS/Resend accounts → VPS → DNS → swap-first CE bring-up → LS store + webhook → /buy redirect → webhook secrets → UptimeRobot).
  </what-built>
  <how-to-verify>
Execute the runbook end to end:
  1. Confirm `https://license.tinkerdev.io/<health>` responds over REAL TLS without `-k` (Pitfall 3 proof — release builds need a publicly-trusted cert).
  2. Confirm `setup.sh` printed a fresh account id + a base64-raw-32-byte pubkey + policy id, and its metadata-filter validation step PASSED (A2).
  3. Confirm the LS `order_created` webhook points at `…/webhooks/lemonsqueezy` and `tinkerdev.io/buy` redirects to the live LS checkout.
  4. Confirm UptimeRobot monitors are green on both health endpoints.
Then provide the printed account id + pubkey to Task 3.
  </how-to-verify>
  <resume-signal>Type "infra up" with the prod account id + pubkey from setup.sh, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Capture real prod constants into config.rs + verify release tripwire</name>
  <files>src-tauri/src/license/config.rs</files>
  <read_first>
    - src-tauri/src/license/config.rs (the Plan-01 cfg-split placeholders + the `#[cfg(not(debug_assertions))]` tripwire test)
    - 20-RESEARCH.md "Pattern 1" (pubkey encoding gotcha: base64-of-raw-32-bytes, NOT CE's base64-of-hex) + A5 (APP_SALT must NOT change)
    - The Task-2 resume-signal values (prod account id + pubkey from setup.sh)
  </read_first>
  <action>
Replace the Plan-01 placeholders in the `#[cfg(not(debug_assertions))]` arms with the REAL production values from Task 2's `setup.sh` output:
  - `KEYGEN_ACCOUNT_ID` prod arm = the fresh prod account UUID.
  - `KEYGEN_ED25519_PUBKEY_B64` prod arm = the base64-of-raw-32-bytes pubkey setup.sh printed (verify it is 32 bytes when decoded — NOT CE's base64-of-hex form; the SPIKE gotcha).
  - Leave `KEYGEN_HOST` prod arm = `license.tinkerdev.io` (already set in Plan 01).
  - Do NOT touch `APP_SALT` (A5 — a change orphans every activation).
Then prove the prod constants are valid: `cargo test --release --manifest-path src-tauri/Cargo.toml` — the Plan-01 `#[cfg(not(debug_assertions))]` tripwire test (host == license.tinkerdev.io, pubkey decodes to 32 bytes) now compiles into the release test build and MUST pass. The debug `cargo test` also stays green (local CE consts).
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && cargo test --release --quiet --manifest-path src-tauri/Cargo.toml license::config 2>&1 | tail -5 && ! grep -q "PROD_ACCOUNT_ID_PLACEHOLDER\|PROD_PUBKEY_PLACEHOLDER" src-tauri/src/license/config.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/license/config.rs` no longer contains `PROD_ACCOUNT_ID_PLACEHOLDER` or `PROD_PUBKEY_PLACEHOLDER`
    - `cargo test --release --manifest-path src-tauri/Cargo.toml` exits 0 (the prod tripwire test passes — pubkey decodes to 32 bytes, host is license.tinkerdev.io)
    - `cargo test --manifest-path src-tauri/Cargo.toml` (debug) still exits 0
    - `APP_SALT` is byte-identical to its prior value (unchanged)
  </acceptance_criteria>
  <done>config.rs release constants hold the real prod account id + 32-byte pubkey; release + debug cargo tests both green; APP_SALT untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live purchase walkthrough (D-63) + build + grep-clean + UI audit</name>
  <files></files>
  <read_first>
    - docs/HARNESS.md (the phase-boundary build + walkthrough gate; auto-build note: agent runs `pnpm tauri build`, human launches/tests/approves)
    - 20-VALIDATION.md "Manual-Only Verifications" + "Phase gate" sampling row
    - 20-CONTEXT.md D-63 (test mode → one live purchase, refunded) + criterion 4 (grep-clean)
    - scripts/check-dev-strip.sh (the dist-grep gate extended in Plan 01 — run it against the release bundle)
  </read_first>
  <action>
Claude AUTONOMOUSLY (before the human step): run the full unit gate (`pnpm test && pnpm exec tsc --noEmit -p server/webhook/tsconfig.json && pnpm lint && cargo test --release --manifest-path src-tauri/Cargo.toml`), then `pnpm tauri build` (per project memory: the agent runs the build; its final non-zero exit is only the absent updater-signing key — confirm the `.app`/`.dmg` under `src-tauri/target/release/bundle/macos/`, not the exit code). Then run the grep-clean (criterion 4): grep the packaged `.app` AND the repo for the CE admin token, `SECRET_KEY_BASE`, the Resend key (`re_…`), and the LS webhook secret (`whsec_…`) — ALL must be ABSENT — and run `scripts/check-dev-strip.sh` plus the D-52 prod-constant grep (`license.tinkerdev.io` present in the binary, localhost Keygen host absent). Hand the built-app path + grep results to the human.

The HUMAN then runs the D-63 ship-gate:
  1. **LS test mode:** Buy (from the built app → browser → `tinkerdev.io/buy` → LS test checkout) → complete a test purchase → confirm the `order_created` webhook fired → CE minted a license (perpetual/node-locked/max=1, entitlements pro.theming/pro.ordering, metadata.orderId) → Resend emailed the plain-text key → paste the key in Unlock Pro → it activates via the unchanged Phase-19 flow.
  2. **One live purchase:** one real USD-9 purchase, full chain, **refunded afterward** (D-63 — the strongest criterion-3 proof).
  3. **gsd-ui-review** WCAG-AA audit on the Buy affordance.
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && bash scripts/check-dev-strip.sh && APP=$(ls -d src-tauri/target/release/bundle/macos/*.app 2>/dev/null | head -1); test -n "$APP" && ! grep -rqI "SECRET_KEY_BASE\|whsec_\|re_[A-Za-z0-9]\{16\}" "$APP" && echo "BUNDLE CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-dev-strip.sh` exits 0 and the D-52 prod-constant check passes (license.tinkerdev.io present in the release binary, localhost Keygen host absent)
    - The packaged `.app` contains NO `SECRET_KEY_BASE`, `whsec_`, `re_…` Resend key, or CE admin token (grep returns nothing)
    - (Human) LS test-mode purchase mints + emails + activates a key via the Phase-19 flow
    - (Human) one live USD-9 purchase completes the same chain end-to-end and is refunded after (D-63)
    - (Human) gsd-ui-review WCAG-AA audit on the Buy affordance passes
  </acceptance_criteria>
  <what-built>
Claude ran the full unit + cargo-release gate, `pnpm tauri build` (built `.app`/`.dmg` under src-tauri/target/release/bundle/macos/), the dist-grep prod-constant check, and the criterion-4 grep-clean (CE admin token / SECRET_KEY_BASE / Resend key / LS webhook secret absent from the bundle AND repo).
  </what-built>
  <how-to-verify>
  1. Launch the built app. Open Unlock Pro → Buy → confirm the default browser opens `tinkerdev.io/buy` → the LS **test-mode** checkout. Complete a test purchase.
  2. Confirm the `order_created` webhook fired, CE minted a perpetual/node-locked/max=1 license carrying `pro.theming`+`pro.ordering` + `metadata.orderId`, and Resend emailed the plain-text key.
  3. Paste the key in Unlock Pro → it activates via the unchanged Phase-19 flow and unlocks Pro.
  4. Do ONE real USD-9 live purchase, confirm the same chain end-to-end, then **refund it**.
  5. Confirm the gsd-ui-review WCAG-AA audit on the Buy affordance passes.
  6. Confirm "BUNDLE CLEAN" from the automated grep-clean (no privileged secret in the `.app`).
  </how-to-verify>
  <resume-signal>Type "approved" once the test-mode + live purchase both minted+emailed+activated a key (live one refunded) and the UI audit + grep-clean passed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → license.tinkerdev.io | First public surface; Caddy terminates TLS, routes /webhooks/* → webhook, else → keygen-web |
| Caddy → webhook → CE | Privileged admin-token calls stay on the compose network / localhost (D-55), never public |
| release app → prod CE TLS | Release builds trust ONLY the OS root store (custom-CA path compiled out) — prod cert must be real ACME |
| repo / app bundle → secrets | Privileged tokens/keys must never appear in the repo or the shipped `.app` (criterion 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | Tampering/availability (self-signed prod TLS ⇒ release activation fails closed) | prod Caddyfile | mitigate | REAL ACME (Let's Encrypt) for license.tinkerdev.io — NO `tls internal` (Pitfall 3); A-record live + ports 80/443 open before first boot; auto-renew with a persisted caddy_data volume; RUNBOOK proves the cert is trusted WITHOUT `-k` |
| T-20-12 | Information Disclosure (admin token / SECRET_KEY_BASE / Resend key / LS secret leaks to repo or bundle) | infra/.env, webhook/.env, packaged app | mitigate | All secrets in gitignored `.env` ON THE BOX only (D-41/D-50/D-55); `.env.example` placeholders committed; Task-4 grep-clean asserts absence from the `.app` AND repo (criterion 4) |
| T-20-13 | Elevation/Access Control (admin token reachable over the public Caddy) | Caddy routing | mitigate | Caddy exposes only keygen-web + the signed `/webhooks/*` route; the webhook reaches CE over the compose network (localhost-equivalent); admin-token endpoints are NOT proxied publicly (D-55) |
| T-20-14 | DoS / data-loss (license DB lost ⇒ all buyers' refresh/transfer/revocation break) | postgres | mitigate | Provider snapshots enabled NOW (D-49); 2 GB swap prevents OOM-killing Postgres on the 4 GB box (D-48, Pitfall 6); offsite pg_dump is the documented deferred follow-up |
| T-20-15 | Spoofing (forged webhook to the public endpoint mints a free license) | public /webhooks route | mitigate | The Plan-02 signature verify (D-60) runs on every POST before any CE call; Caddy HTTPS only; LS signing secret server-side only |
| T-20-16 | DoS / availability (VPS or webhook down ⇒ buyer pays, no key, unnoticed) | whole stack | mitigate | UptimeRobot on CE + webhook /health (D-72); D-59 5xx→LS-retry covers transient downtime; failure-alert email to alerts@tinkerdev.io |
</threat_model>

<verification>
- Committed `infra/keygen/` scripts parse (`bash -n`) and contain NO real secrets (templates only).
- `cargo test --release --manifest-path src-tauri/Cargo.toml` green (prod constants valid — pubkey 32 bytes, host license.tinkerdev.io); debug `cargo test` stays green.
- `scripts/check-dev-strip.sh` green + prod-constant grep (license.tinkerdev.io present, localhost Keygen host absent) on the release binary.
- Grep-clean: no CE admin token / SECRET_KEY_BASE / Resend key / LS webhook secret in the repo or the packaged `.app` (criterion 4).
- Human D-63 walkthrough: LS test-mode purchase + one live USD-9 purchase (refunded) each mint→email→activate a working key via the unchanged Phase-19 flow; gsd-ui-review WCAG-AA on the Buy affordance.
- Phase-boundary harness honored: agent runs `pnpm tauri build`; human launches/tests/approves.
</verification>

<success_criteria>
- Production CE live at https://license.tinkerdev.io behind real ACME TLS, fresh account + new Ed25519 keypair (D-51), perpetual/node-locked/max=1 policy carrying pro.theming + pro.ordering (criterion 1/2).
- Webhook container co-located, reaching CE over localhost; admin token never public (PAY-02, criterion 4).
- config.rs release constants = real prod account id + 32-byte pubkey; release tripwire test passes (D-52).
- A completed purchase auto-mints the license + auto-emails the key, which activates via Phase-19 (criterion 1-3, PAY-01/02/03).
- No privileged token in the repo or app bundle (criterion 4).
</success_criteria>

<output>
After completion, create `.planning/phases/20-purchase-pipeline/20-03-SUMMARY.md`.
</output>
