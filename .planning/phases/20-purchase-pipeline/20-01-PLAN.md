---
phase: 20-purchase-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/platform/index.ts
  - src/lib/platform/tauri.ts
  - src/lib/platform/browser.ts
  - src/lib/platform/stub.ts
  - src/lib/platform/platform.test.ts
  - src/components/UpsellPanel.tsx
  - src/components/UpsellPanel.test.tsx
  - src-tauri/capabilities/default.json
  - src-tauri/Cargo.toml
  - src-tauri/src/license/config.rs
  - test/e2e/license-buy.e2e.ts
  - scripts/check-dev-strip.sh
autonomous: true
requirements: [PAY-01]
user_setup: []

must_haves:
  truths:
    - "Clicking 'Buy license' in the upsell panel opens https://tinkerdev.io/buy in the OS default browser (Tauri), and is an inert no-op under jsdom/vite-preview (never navigates the in-app document)"
    - "The opener capability is reached ONLY through src/lib/platform/ — no component imports @tauri-apps/* directly, and only tauri.ts imports @tauri-apps/plugin-opener"
    - "The opener capability is scoped to https URLs only in the Tauri capability config"
    - "config.rs embeds the prod CE host/account/pubkey in a release build and the local CE values in a dev build, via cfg(debug_assertions), with ONE unchanged APP_SALT across both arms"
    - "A release-build dist-grep proves the prod host string is present and the localhost Keygen host string is absent"
  artifacts:
    - path: "src/lib/platform/tauri.ts"
      provides: "opener.openUrl wired to @tauri-apps/plugin-opener (the ONLY @tauri-apps/* opener import)"
      contains: "@tauri-apps/plugin-opener"
    - path: "src/lib/platform/index.ts"
      provides: "opener capability on the Platform interface + platform proxy getter"
      contains: "opener"
    - path: "src/components/UpsellPanel.tsx"
      provides: "Buy CTA calling platform.opener.openUrl(BUY_LICENSE_URL) with BUY_LICENSE_URL = https://tinkerdev.io/buy"
      contains: "https://tinkerdev.io/buy"
    - path: "src-tauri/src/license/config.rs"
      provides: "cfg(debug_assertions)-split per-env constants; prod arm = license.tinkerdev.io"
      contains: "license.tinkerdev.io"
    - path: "src-tauri/capabilities/default.json"
      provides: "opener:allow-open-url permission scoped https-only"
      contains: "opener"
    - path: "test/e2e/license-buy.e2e.ts"
      provides: "real-WKWebView proof the Buy click invokes the seam and does not navigate the route"
  key_links:
    - from: "src/components/UpsellPanel.tsx"
      to: "src/lib/platform opener seam"
      via: "platform.opener.openUrl(BUY_LICENSE_URL)"
      pattern: "platform\\.opener\\.openUrl"
    - from: "src/lib/platform/tauri.ts"
      to: "@tauri-apps/plugin-opener"
      via: "openUrl import"
      pattern: "plugin-opener"
---

<objective>
Wire the in-app "Buy license" affordance (PAY-01) to open the real checkout in the OS default browser through a new platform-seam opener capability, and flip `config.rs` to embed production CE constants in release builds (D-52). This is the only app-side code change of the phase — small and surgical: replace the D-21 `BUY_LICENSE_URL` stub no-op with a `platform.opener.openUrl("https://tinkerdev.io/buy")` call behind a new https-scoped opener seam, and split the licensing constants by build profile.

Purpose: PAY-01 ("an in-app Buy affordance opens the purchase page in the default browser") + D-52 (shipped binary embeds ONLY prod licensing constants; local dev keeps working unchanged).
Output: opener seam (index/tauri/browser/stub), https-only opener capability, rewired UpsellPanel Buy CTA + updated tests, `cfg(debug_assertions)`-split config.rs, a Buy-wiring e2e, and an extended dist-grep build check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-purchase-pipeline/20-CONTEXT.md
@.planning/phases/20-purchase-pipeline/20-RESEARCH.md
@.planning/phases/20-purchase-pipeline/20-VALIDATION.md

<interfaces>
<!-- Contracts extracted from the codebase. Use directly — no exploration needed. -->

The platform seam (src/lib/platform/) is a Tauri-vs-browser split with FOUR files:
- index.ts: the `Platform` interface + a `platform` proxy whose getters delegate to `active`.
  `tauriPlatform` is loaded LAZILY via `import("./tauri")` ONLY inside the WKWebView
  (`__TAURI_INTERNALS__` present). browser.ts/stub.ts are the deterministic non-Tauri arms.
- tauri.ts: "The ONLY file allowed to import @tauri-apps/*." Each capability is one object literal.
- browser.ts + stub.ts: must NOT import @tauri-apps/*; deterministic no-ops under jsdom/preview.

To add a capability you touch exactly: the `Platform` interface (index.ts), the `platform`
proxy getter (index.ts), `tauriPlatform` (tauri.ts), `browserPlatform` (browser.ts).
The license capability (index.ts:94-104, tauri.ts:120-125, stub.ts createLicenseStub) is the
exact pattern to mirror for `opener`.

config.rs current constants (the D-52 switch targets):
```rust
pub const KEYGEN_HOST: &str = "localhost";
pub const KEYGEN_ACCOUNT_ID: &str = "23c88309-2584-4771-81df-1d351672ff91";
pub const KEYGEN_ED25519_PUBKEY_B64: &str = "ZBd2u102TCpivzVAisQZi7h5YUqhmtT6DA1Ej0YPes4=";
pub const APP_SALT: &[u8] = b"e14f0d1630565bf022fe7d40de2aeceefb254a01151d3df397489335b4e45c75";
// verifying_key() decodes KEYGEN_ED25519_PUBKEY_B64; 3 unit tests guard it.
```
keygen_client.rs:304 already uses `#[cfg(debug_assertions)]` for the dev-only `DEVTOOLS_KEYGEN_CA`
custom-CA path — release builds compile it OUT, so prod MUST use a real OS-trusted cert.

UpsellPanel.tsx: `BUY_LICENSE_URL` constant at line 51 (currently `"https://example.invalid/devtools/buy"`),
Buy handler at lines 313-317 (`void BUY_LICENSE_URL;` — the no-op to replace).
UpsellPanel.test.tsx asserts the stub: line 68 ("stub no-op, D-21"), line 75
(`expect(window.location.href).not.toContain(BUY_LICENSE_URL)`), line 78
("exports BUY_LICENSE_URL as a single stub constant"), line 88 ("inert stub — must not throw").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the https-only opener capability to the platform seam</name>
  <files>src/lib/platform/index.ts, src/lib/platform/tauri.ts, src/lib/platform/browser.ts, src-tauri/capabilities/default.json, src-tauri/Cargo.toml, src-tauri/src/lib.rs</files>
  <read_first>
    - src/lib/platform/index.ts (the Platform interface + platform proxy getters + the license capability pattern at :94-104, :152-154)
    - src/lib/platform/tauri.ts (the @tauri-apps/* import block + license capability at :120-125)
    - src/lib/platform/browser.ts (the deterministic no-op arms + license at :99)
    - src/lib/platform/stub.ts (createLicenseStub at :33-45 — the deterministic-reject pattern)
    - src/lib/platform/platform.test.ts (existing seam tests to extend)
    - src-tauri/capabilities/default.json (the permissions array)
    - src-tauri/src/lib.rs (the existing `.plugin(...)` registration chain)
    - 20-RESEARCH.md "Pattern 2: Opener seam wrapper" + the tauri-plugin-opener capability/scope examples (A3)
  </read_first>
  <action>
Add an `opener` capability to the platform seam mirroring the `license` capability shape, and install the Tauri opener plugin.

1. Add the dependency: `pnpm add @tauri-apps/plugin-opener` (npm 2.5.4) AND add the Rust crate to `src-tauri/Cargo.toml`: `tauri-plugin-opener = "2"`. Register the plugin in the Rust builder (in `src-tauri/src/lib.rs` where other `.plugin(...)` calls are — add `.plugin(tauri_plugin_opener::init())`). NOTE: this is the FIRST new webview runtime dep allowed this milestone — it is the explicit D-67 exception (the seam wrapper); the rest of the webview stays zero-new-dep.

2. In `src/lib/platform/index.ts`, add to the `Platform` interface (after `license`):
```ts
  /** Open an external URL in the OS default browser (PAY-01, D-67). https-only
   *  (capability-scoped). No-op in the browser/test fallback — NEVER navigates
   *  jsdom/vite-preview. Only tauri.ts reaches @tauri-apps/plugin-opener. */
  opener: {
    openUrl(url: string): Promise<void>;
  };
```
   and add a proxy getter alongside the others:
```ts
  get opener() {
    return active.opener;
  },
```

3. In `src/lib/platform/tauri.ts`, add the import at the top (this stays the ONLY file importing @tauri-apps/*):
```ts
import { openUrl } from "@tauri-apps/plugin-opener";
```
   and add to `tauriPlatform`:
```ts
  opener: {
    openUrl: (url: string) => openUrl(url),
  },
```

4. In `src/lib/platform/browser.ts` add to `browserPlatform` a deterministic no-op (never navigate jsdom):
```ts
  // Opening external URLs is Tauri-only (PAY-01, D-67): outside Tauri this is a
  // no-op so jsdom/vite-preview NEVER navigate the document. This file must NOT
  // import @tauri-apps/*.
  opener: {
    async openUrl(): Promise<void> {},
  },
```

5. In `src-tauri/capabilities/default.json` add the permission `"opener:allow-open-url"` to the `permissions` array, scoped to https only. Per 20-RESEARCH A3, verify the exact scope placement against the installed plugin 2.5.4 — the opener scope is a regex and the default permits mailto/tel/http(s); restrict to https. Express it as the scoped permission object form:
```json
{ "identifier": "opener:allow-open-url", "allow": [{ "url": "https://*" }] }
```
   If plugin 2.5.4 expects the `{ "open": "^https://.+" }` regex shape instead, use that exact form (read the generated schema at `src-tauri/gen/schemas/desktop-schema.json` for the opener scope key). The acceptance criterion is grep-checkable regardless of which shape the plugin version takes: the capability file must contain `https` constraining the opener permission.
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && grep -q "opener" src/lib/platform/index.ts && grep -q "@tauri-apps/plugin-opener" src/lib/platform/tauri.ts && grep -q "openUrl" src/lib/platform/browser.ts && grep -q "opener:allow-open-url" src-tauri/capabilities/default.json && grep -q "https" src-tauri/capabilities/default.json && ! grep -rq "@tauri-apps/plugin-opener" src/components src/shell 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/platform/index.ts` contains `opener` on the Platform interface AND a `get opener()` proxy getter
    - `src/lib/platform/tauri.ts` contains `@tauri-apps/plugin-opener` (the only file with this import)
    - `grep -rn "@tauri-apps/plugin-opener" src/ | grep -v "platform/tauri.ts"` returns nothing
    - `src/lib/platform/browser.ts` contains an `opener` with an `async openUrl` no-op and no `@tauri-apps` import
    - `src-tauri/capabilities/default.json` contains `opener:allow-open-url` and `https`
    - `src-tauri/Cargo.toml` contains `tauri-plugin-opener` and `src-tauri/src/lib.rs` contains `tauri_plugin_opener::init`
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The opener capability exists end-to-end behind the seam, https-scoped, with @tauri-apps imports confined to tauri.ts; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the Buy CTA to the opener seam + update tests</name>
  <files>src/components/UpsellPanel.tsx, src/components/UpsellPanel.test.tsx, src/lib/platform/platform.test.ts</files>
  <read_first>
    - src/components/UpsellPanel.tsx (BUY_LICENSE_URL at :51, the Buy handler at :311-321)
    - src/components/UpsellPanel.test.tsx (the stub-asserting tests at :68, :75, :78, :88 that MUST be rewritten)
    - src/lib/platform/platform.test.ts (extend with the opener no-op arm assertion)
    - 20-VALIDATION.md Per-Task Verification Map (the two PAY-01 unit rows)
  </read_first>
  <behavior>
    - UpsellPanel test: clicking "Buy license" calls `platform.opener.openUrl` exactly once with `"https://tinkerdev.io/buy"` (use a stub Platform via setPlatformForTest whose opener.openUrl is a spy); the in-app route/location does NOT change.
    - UpsellPanel test: `BUY_LICENSE_URL` exported and equals `"https://tinkerdev.io/buy"`.
    - platform.test.ts: the browser/stub opener arm's `openUrl` resolves without throwing and does not navigate (jsdom `window.location.href` unchanged).
  </behavior>
  <action>
1. In `src/components/UpsellPanel.tsx` line 51, change the constant to the redirect URL (D-68 — the compiled constant points at the own-domain redirect, NOT the raw LS link):
```ts
/** D-68: the own-domain redirect the user controls (Cloudflare/Caddy) forwards
 *  to the live Lemon Squeezy checkout. ONE constant; MoR/store changes never
 *  require an app release. */
export const BUY_LICENSE_URL = "https://tinkerdev.io/buy";
```

2. Replace the no-op Buy handler (lines ~313-317) so the onClick opens the URL through the seam. Import `platform` is already imported (line 41). Make the handler:
```ts
            onClick={() => {
              // PAY-01/D-67: open the checkout in the OS default browser via the
              // platform opener seam (https-only, capability-scoped). Best-effort:
              // a failed open is calm — log, never throw at the user.
              void platform.opener
                .openUrl(BUY_LICENSE_URL)
                .catch((err) => console.error("[buy] open failed:", err));
            }}
```

3. Rewrite the stub-asserting tests in `src/components/UpsellPanel.test.tsx`:
   - Line ~68 test ("renders the Buy CTA as a Tab-reachable button (stub no-op, D-21)"): keep the Tab-reachability assertion but DROP the "stub no-op" framing. Add a spy: install a test Platform via `setPlatformForTest` (mirror how the file already stubs the license arm — read the existing setup) whose `opener.openUrl` is a `vi.fn().mockResolvedValue(undefined)`. Click "Buy license" and assert `openUrl` was called once with `"https://tinkerdev.io/buy"`, and assert `window.location.href` did NOT change.
   - Line ~75: replace `expect(window.location.href).not.toContain(BUY_LICENSE_URL)` with the no-navigation assertion above (the seam, not location, is what fires).
   - Line ~78 ("exports BUY_LICENSE_URL as a single stub constant"): change to assert `BUY_LICENSE_URL === "https://tinkerdev.io/buy"` and that it is https.
   - Line ~88 ("inert stub — must not throw"): clicking still must not throw — keep, but it now also drives the spy.

4. Extend `src/lib/platform/platform.test.ts`: add a test that `browserPlatform.opener.openUrl("https://x")` resolves and does not set `window.location.href` (or, under node env where window is absent, simply resolves). Mirror the existing license-arm test in that file.

Per project memory (TDD note): land tests GREEN with the impl in the same commit — lefthook rejects failing vitest/tsc, so do not split a RED-only commit.
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && pnpm vitest run src/components/UpsellPanel.test.tsx src/lib/platform/platform.test.ts && grep -q 'https://tinkerdev.io/buy' src/components/UpsellPanel.tsx && grep -q 'platform.opener.openUrl' src/components/UpsellPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/UpsellPanel.tsx` contains `https://tinkerdev.io/buy` and `platform.opener.openUrl(BUY_LICENSE_URL)`
    - `src/components/UpsellPanel.tsx` no longer contains the bare `void BUY_LICENSE_URL;` no-op line
    - `grep -c "https://example.invalid" src/components/UpsellPanel.tsx` returns 0
    - `pnpm vitest run src/components/UpsellPanel.test.tsx src/lib/platform/platform.test.ts` exits 0
    - The UpsellPanel test asserts `openUrl` is called once with `"https://tinkerdev.io/buy"` and that the document does not navigate
    - `pnpm exec tsc --noEmit && pnpm lint` exits 0
  </acceptance_criteria>
  <done>Buy CTA opens https://tinkerdev.io/buy via the seam; tests assert the seam call + no navigation; full unit gate green.</done>
</task>

<task type="auto">
  <name>Task 3: Build-time prod/dev constant switch in config.rs + extended dist-grep</name>
  <files>src-tauri/src/license/config.rs, scripts/check-dev-strip.sh</files>
  <read_first>
    - src-tauri/src/license/config.rs (ALL of it — the 4 constants, verifying_key(), the 3 unit tests at :60-83)
    - src-tauri/src/license/keygen_client.rs:295-320 (the existing `#[cfg(debug_assertions)]` idiom to mirror — A4)
    - scripts/check-dev-strip.sh (the dist-grep gate to extend)
    - 20-RESEARCH.md "Pattern 1" (the cfg(debug_assertions) split + APP_SALT-must-not-change caveat + pubkey-encoding gotcha)
  </read_first>
  <action>
Split the per-env constants in `config.rs` by build profile so a release build embeds ONLY prod values, mirroring the `#[cfg(debug_assertions)]` idiom already in keygen_client.rs:304. The PROD account-id + pubkey are produced by the production CE in Plan 03's `setup.sh`; until that runs, embed clearly-marked placeholder sentinels that this plan's config.rs unit tests will REJECT if left in a release build — Plan 03's runbook fills the real values and re-runs `cargo test`.

1. Replace the three env-varying constants (KEYGEN_HOST, KEYGEN_ACCOUNT_ID, KEYGEN_ED25519_PUBKEY_B64) with cfg-split pairs. KEEP `APP_SALT` as a SINGLE unsplit constant — it MUST NOT differ between dev and prod and must NEVER change after first release (a new salt orphans every activation, A5). Example:
```rust
/// Keygen host. Dev = local CE; release = production CE at license.tinkerdev.io (D-52).
#[cfg(debug_assertions)]
pub const KEYGEN_HOST: &str = "localhost";
#[cfg(not(debug_assertions))]
pub const KEYGEN_HOST: &str = "license.tinkerdev.io";

/// Keygen account id (present in every API URL; not a secret — D-41).
#[cfg(debug_assertions)]
pub const KEYGEN_ACCOUNT_ID: &str = "23c88309-2584-4771-81df-1d351672ff91"; // local CE
#[cfg(not(debug_assertions))]
pub const KEYGEN_ACCOUNT_ID: &str = "PROD_ACCOUNT_ID_PLACEHOLDER"; // ← Plan 03 setup.sh (D-51)

/// Account Ed25519 pubkey, base64 of the RAW 32 bytes (NOT CE's base64-of-hex — SPIKE).
#[cfg(debug_assertions)]
pub const KEYGEN_ED25519_PUBKEY_B64: &str = "ZBd2u102TCpivzVAisQZi7h5YUqhmtT6DA1Ej0YPes4="; // local CE
#[cfg(not(debug_assertions))]
pub const KEYGEN_ED25519_PUBKEY_B64: &str = "PROD_PUBKEY_PLACEHOLDER"; // ← Plan 03 setup.sh (D-51)
```
   Keep `APP_SALT` exactly as-is (single value, byte-for-byte). Keep `verifying_key()` unchanged — it reads whichever `KEYGEN_ED25519_PUBKEY_B64` the active profile selects.

2. Update the unit tests so they run against BOTH profiles' active constant. The existing 3 tests already decode the active `KEYGEN_ED25519_PUBKEY_B64` and assert 32 bytes — under `cargo test` (a dev/debug build) they validate the local const. Add a guard that the prod placeholder can never silently ship: a `#[cfg(not(debug_assertions))]` test that asserts `KEYGEN_HOST == "license.tinkerdev.io"` and that `KEYGEN_ED25519_PUBKEY_B64` decodes to exactly 32 bytes (this FAILS while the placeholder is in place, which is the intended tripwire — Plan 03 makes it pass via `cargo test --release`). Document this in a comment so Plan 03 knows `cargo test --release` is its gate.

3. Extend `scripts/check-dev-strip.sh` (or add a sibling assertion block) to also verify the D-52 prod-constant build check: after a release `vite build` + `tauri build`, grep the packaged binary/bundle so that `license.tinkerdev.io` is PRESENT and the localhost Keygen host is ABSENT. Since the Rust binary is the artifact (not dist/assets), add a clearly-named function that takes the bundle path and:
   - `grep -q "license.tinkerdev.io" <binary>` must succeed (present)
   - `grep -c "localhost" <binary>` for the Keygen host must be 0 in the licensing constant region — guard pragmatically: assert the prod host present is the load-bearing check; document that the localhost-absent grep runs against the release binary at the phase-boundary build (Plan 03), not the dev binary. Keep the existing dist/assets dev-toggle check intact.
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && cargo test --quiet --manifest-path src-tauri/Cargo.toml license::config 2>&1 | tail -5 && grep -q 'license.tinkerdev.io' src-tauri/src/license/config.rs && grep -q 'cfg(debug_assertions)' src-tauri/src/license/config.rs && grep -c 'APP_SALT' src-tauri/src/license/config.rs</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/license/config.rs` contains `#[cfg(debug_assertions)]` and `#[cfg(not(debug_assertions))]` for KEYGEN_HOST, KEYGEN_ACCOUNT_ID, KEYGEN_ED25519_PUBKEY_B64
    - `src-tauri/src/license/config.rs` contains exactly ONE `APP_SALT` declaration (not cfg-split), value byte-identical to the prior `e14f0d16...e45c75`
    - `src-tauri/src/license/config.rs` contains `license.tinkerdev.io`
    - `cargo test --manifest-path src-tauri/Cargo.toml` (debug profile) exits 0 (local consts validate; the prod-only tripwire test is `#[cfg(not(debug_assertions))]` so it does NOT run under the default debug `cargo test`)
    - `scripts/check-dev-strip.sh` contains a prod-constant assertion referencing `license.tinkerdev.io`
    - The dev `cargo test` still validates the local CE pubkey (32 bytes)
  </acceptance_criteria>
  <done>config.rs is cfg(debug_assertions)-split with prod placeholders + a release-only tripwire test; APP_SALT unchanged; dist-grep extended for the prod-constant check.</done>
</task>

<task type="auto">
  <name>Task 4: Real-WKWebView Buy-wiring e2e</name>
  <files>test/e2e/license-buy.e2e.ts</files>
  <read_first>
    - test/e2e/helpers.ts (assert/dispatchKey/navigateToTool/saveScreenshot helpers — reuse these)
    - test/e2e/entitlements.e2e.ts (the existing upsell-flow e2e — how it opens the Unlock Pro panel; mirror its setup)
    - docs/HARNESS.md (e2e gate runbook: ports, preflight, WebKit quirks, native-input note)
    - src/components/UpsellPanel.tsx (the Buy button label "Buy license" + the panel reveal path)
    - 20-VALIDATION.md "Manual-Only Verifications" (the native browser-open is a MANUAL walkthrough item — e2e asserts the seam call + no route change only)
  </read_first>
  <action>
Add `test/e2e/license-buy.e2e.ts` proving on the real WKWebView that clicking "Buy license" invokes the opener seam and does NOT navigate the in-app route. Per HARNESS native-input note + 20-VALIDATION, the OS browser actually launching is non-observable inside WebDriver — so the e2e asserts the observable in-app contract, and the real browser-open stays a manual walkthrough item.

1. Open the upsell surface (mirror entitlements.e2e.ts: toggle free tier via the DEV palette command if needed, open "Unlock Pro" footer → the UpsellPanel renders), locate the "Buy license" button by its accessible name.
2. Record the current location/route (e.g. `window.location.hash` — HashRouter). Click "Buy license".
3. Assert: the in-app route did NOT change (hash unchanged), the panel did not crash, and no new in-page navigation occurred. Because the native open is non-observable, assert the seam was reached by stubbing/observing it IF the harness exposes a hook; otherwise assert the negative contract (no navigation, no thrown error, panel still mounted) and leave the positive "browser opened" assertion to the documented manual walkthrough.
4. Add a screenshot via `saveScreenshot` for the gsd-ui-review audit of the Buy affordance.
5. Follow the helpers.ts WebKit lessons (physical key codes, etc.) documented in that file.
  </action>
  <verify>
    <automated>cd /Users/boonkhailim/Documents/projects/bk/playground/devtools && test -f test/e2e/license-buy.e2e.ts && grep -q "Buy license" test/e2e/license-buy.e2e.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test/e2e/license-buy.e2e.ts` exists and references the "Buy license" button by accessible name
    - The spec asserts the in-app route/hash does NOT change after the click (no in-page navigation)
    - The spec imports and uses `test/e2e/helpers.ts`
    - `pnpm exec tsc --noEmit` exits 0
    - Documented in-spec comment that the native browser-open itself is a manual walkthrough item (HARNESS native-input note)
    - (Wave-merge gate) `bash scripts/e2e-spike.sh` runs this spec green on the real WKWebView
  </acceptance_criteria>
  <done>A real-WKWebView spec proves the Buy click reaches the seam without navigating the in-app route; native open documented as manual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-app UI → OS browser | The Buy CTA hands a URL to the OS via the opener plugin; the URL must be a fixed, https, app-controlled value |
| release build → embedded constants | The shipped binary must embed ONLY prod CE constants; a dev-constant leak points buyers at localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering (open-redirect/SSRF via opener) | UpsellPanel Buy → opener.openUrl | mitigate | URL is the fixed compile-time constant `https://tinkerdev.io/buy` (no user/remote input); opener capability scoped to `^https://` in `default.json` (D-67) so the app cannot be coerced to open arbitrary schemes (file:, tel:, mailto:) |
| T-20-02 | Information Disclosure (dev constants ship) | config.rs release build | mitigate | `cfg(debug_assertions)` split (A4); a `#[cfg(not(debug_assertions))]` tripwire test fails the release build while the prod placeholder remains; dist/binary grep asserts `license.tinkerdev.io` present + localhost Keygen host absent (criterion-adjacent, D-52) |
| T-20-03 | Tampering (self-signed prod TLS breaks release activation) | release TLS trust | mitigate | Release compiles OUT the `DEVTOOLS_KEYGEN_CA` custom-CA path (keygen_client.rs:304, `#[cfg(debug_assertions)]`); prod MUST use real ACME (enforced in Plan 03 Caddyfile). Recorded here because it is the cross-plan consequence of the config split |
| T-20-04 | Spoofing (seam bypass — component imports @tauri-apps directly) | platform seam discipline | mitigate | grep gate: `@tauri-apps/plugin-opener` appears ONLY in tauri.ts; CLAUDE.md "tools import src/lib/platform/, never @tauri-apps/* directly" enforced by the Task-1 acceptance grep |
</threat_model>

<verification>
- `pnpm vitest run` (full suite) green; the decoder's 19 tests byte-for-byte untouched and passing.
- `pnpm exec tsc --noEmit` + `pnpm lint` clean.
- `cargo test --manifest-path src-tauri/Cargo.toml` green (debug profile; local CE consts validate).
- `bash scripts/e2e-spike.sh` green including the new `license-buy.e2e.ts` (wave-merge gate).
- `@tauri-apps/plugin-opener` imported ONLY in `src/lib/platform/tauri.ts`.
- Per-task harness DoD honored: /simplify → /codex:review → unit green → real-WKWebView verify.
</verification>

<success_criteria>
- Clicking "Buy license" opens `https://tinkerdev.io/buy` in the OS default browser inside Tauri; no-op (no navigation) under jsdom/vite-preview (PAY-01).
- Opener reached only through the platform seam; opener capability https-scoped.
- `config.rs` release build embeds prod CE host/account/pubkey via `cfg(debug_assertions)`; APP_SALT unchanged across both arms (D-52, A5).
- dist-grep build check extended to assert the prod constant (`license.tinkerdev.io`) present / localhost Keygen host absent in the release artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/20-purchase-pipeline/20-01-SUMMARY.md`.
</output>
