---
phase: 04-catalogue
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/tools/hash/hashes.ts
  - src/tools/hash/hashes.test.ts
  - src/tools/hash/HashTool.tsx
  - src/tools/hash/HashTool.test.tsx
  - src/tools/hash/index.ts
  - test/e2e/hash.e2e.ts
autonomous: true
requirements: [HASH-01]
user_setup: []

must_haves:
  truths:
    - "Inputting text/hex/base64 (via an input-encoding toggle, single internal Uint8Array) produces MD5 + SHA-1/256/384/512 digests, all five shown at once, stacked."
    - "MD5 uses js-md5 (sync); SHA-1/256/384/512 use Web Crypto crypto.subtle.digest (async); digests compute reactively on input change within the <2s budget."
    - "Each digest row has its own visible focusable ≤1-keystroke copy; there is a lowercase-default hex output with an uppercase toggle."
    - "Async SHA results never show stale (out-of-order resolution guarded); the registry renders the real HashTool."
  artifacts:
    - path: "src/tools/hash/hashes.ts"
      provides: "md5 (sync) + sha (async) → hex digest functions"
      exports: ["md5Hex", "shaHex", "ALGORITHMS"]
    - path: "src/tools/hash/HashTool.tsx"
      provides: "Hash tool UI (input toggle + 5 stacked digests + casing toggle)"
      min_lines: 60
    - path: "src/tools/hash/index.ts"
      provides: "Registry entry component swapped to HashTool"
      contains: "component: HashTool"
  key_links:
    - from: "src/tools/hash/hashes.ts"
      to: "js-md5 + crypto.subtle.digest"
      via: "md5(bytes) sync + subtle.digest async"
      pattern: "subtle.digest"
    - from: "src/tools/hash/HashTool.tsx"
      to: "src/lib/bytes.ts"
      via: "utf8ToBytes/hexToBytes/base64ToBytes → single Uint8Array"
      pattern: "@/lib/bytes"
---

<objective>
Ship the Hash tool (HASH-01) into the registry. Input text/hex/base64 via an input-encoding toggle (single internal `Uint8Array`, reusing `bytes.ts` like Base64, D-11) → MD5 + SHA-1/256/384/512 digests all shown at once, stacked (D-12). MD5 = `js-md5` (sync, Plan 01 dep); SHA = Web Crypto `crypto.subtle.digest` (async, D-14). Lowercase hex by default + uppercase toggle (D-13). Each row has its own ≤1-keystroke visible focusable copy. Out-of-order async resolution guarded (Pitfall 3). Debounce/large-input UX is Claude's discretion within the <2s budget (D-19).

Purpose: complete HASH-01; the ONLY plan that consumes `js-md5` + Web Crypto.
Output: pure `hashes.ts` (digest functions) + HashTool UI + component swap + real-webview e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-catalogue/04-CONTEXT.md
@.planning/phases/04-catalogue/04-RESEARCH.md
@.planning/phases/04-catalogue/04-VALIDATION.md

<interfaces>
<!-- bytes.ts (PORT-UNCHANGED — consume only). From src/lib/bytes.ts: -->
```typescript
export type Base64Alphabet = "base64" | "base64url";
export function utf8ToBytes(s: string): Uint8Array;
export function hexToBytes(input: string): Uint8Array;          // throws on odd/invalid
export function base64ToBytes(input: string, alphabet?: Base64Alphabet): Uint8Array;
export function bytesToHex(bytes: Uint8Array, opts?: { upper?: boolean; sep?: string }): string;
```
<!-- js-md5 (Plan 01 dep). Accepts string | number[] | ArrayBuffer | Uint8Array → hex string: -->
```typescript
import { md5 } from "js-md5";  // md5(bytes: Uint8Array): string (lowercase hex)
```
<!-- Web Crypto (native): crypto.subtle.digest("SHA-256", bytes) → Promise<ArrayBuffer> -->
<!-- StatusBar + CopyButton (Plan 01, src/components/). For Hash, encoding = the selected input encoding (optional). -->
<!-- Reference: src/tools/base64/Base64Tool.tsx (input-encoding toggle, single Uint8Array, timed() wrapper, accent=selected). -->
<!-- src/tools/hash/index.ts created in Plan 01 with component: makePlaceholder("Hash") — swap to HashTool here. Do NOT edit registry.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hashes.ts — md5 (sync) + sha (async) → hex, against known vectors (D-12/D-14)</name>
  <read_first>
    - .planning/phases/04-catalogue/04-RESEARCH.md §"Pattern 3: Reactive async digest" + §"Known-good digest vectors"
    - .planning/phases/04-catalogue/04-VALIDATION.md §"Known-good digest vectors" (the exact MD5/SHA strings)
    - src/lib/bytes.ts (bytesToHex for SHA output formatting; do NOT re-implement hex)
    - .planning/phases/04-catalogue/04-CONTEXT.md D-01, D-12, D-13, D-14
  </read_first>
  <behavior>
    hashes.test.ts:
    - md5Hex(utf8ToBytes("")) === "d41d8cd98f00b204e9800998ecf8427e"
    - md5Hex(utf8ToBytes("abc")) === "900150983cd24fb0d6963f7d28e17f72"
    - await shaHex("SHA-1", utf8ToBytes("abc")) === "a9993e364706816aba3e25717850c26c9cd0d89d"
    - await shaHex("SHA-256", utf8ToBytes("")) === "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
    - await shaHex("SHA-256", utf8ToBytes("abc")) starts with "ba7816bf8f01cfea"
    - await shaHex("SHA-384", utf8ToBytes("abc")) starts with "cb00753f45a35e8b"
    - await shaHex("SHA-512", utf8ToBytes("abc")) starts with "ddaf35a193617aba"
    - all output is lowercase hex by default; the casing is applied by the caller via bytesToHex({upper})
    (Note: crypto.subtle is available in the vitest jsdom/node env; if a SHA test cannot run in jsdom, mark it and rely on the e2e gate — but Node 22 has crypto.subtle, so it should run.)
  </behavior>
  <action>
    Create `src/tools/hash/hashes.ts` (imports `js-md5` + `@/lib/bytes` bytesToHex):
    - `export const ALGORITHMS = ["MD5","SHA-1","SHA-256","SHA-384","SHA-512"] as const;`
    - `md5Hex(bytes: Uint8Array): string` — `md5(bytes)` from js-md5 (already lowercase hex).
    - `shaHex(algo: "SHA-1"|"SHA-256"|"SHA-384"|"SHA-512", bytes: Uint8Array): Promise<string>` — `const buf = await crypto.subtle.digest(algo, bytes); return bytesToHex(new Uint8Array(buf));` (lowercase default; do NOT hand-roll hex — use bytesToHex).
    - `digestAll(bytes: Uint8Array): Promise<{ algo: string; hex: string }[]>` — compute MD5 (sync) + all four SHA (await `Promise.all`), returning rows in ALGORITHMS order. Lowercase. (Casing toggle applied in the UI via `bytesToHex({upper})` or `.toUpperCase()` on display — keep hashes.ts lowercase-canonical.)
    Do NOT hand-roll MD5 or SHA (D-01 + Don't-Hand-Roll); use js-md5 + Web Crypto. Randomness/crypto correctness via vetted primitives only.
    Create `src/tools/hash/hashes.test.ts` with the EXACT vectors in <behavior>.
  </action>
  <verify>
    <automated>npx vitest run src/tools/hash/hashes.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/tools/hash/hashes.ts` exports `md5Hex`, `shaHex`, `ALGORITHMS` (grep each) and `grep -q 'subtle.digest' src/tools/hash/hashes.ts` succeeds.
    - `grep -q 'js-md5' src/tools/hash/hashes.ts` succeeds (no hand-rolled MD5).
    - `npx vitest run src/tools/hash/hashes.test.ts` exits 0; the suite asserts `md5Hex(utf8ToBytes("")) === "d41d8cd98f00b204e9800998ecf8427e"` and `await shaHex("SHA-256", utf8ToBytes("")) === "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>hashes.ts produces all five digests against the known vectors (MD5 via js-md5, SHA via Web Crypto); lowercase-canonical; suite + tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HashTool UI (input toggle + 5 stacked digests + casing) + swap + e2e (HASH-01/D-11/D-13/D-19)</name>
  <read_first>
    - src/tools/hash/hashes.ts (Task 1 output)
    - src/tools/base64/Base64Tool.tsx + src/tools/base64/useBytesConvert.ts (input-encoding toggle + single Uint8Array source; reuse the IDEA, not the 3-way mutual derive — Hash is one-directional, per RESEARCH anti-pattern note)
    - src/components/StatusBar.tsx + src/components/CopyButton.tsx (Plan 01)
    - src/lib/bytes.ts (utf8ToBytes/hexToBytes/base64ToBytes input parsing + bytesToHex upper for casing toggle)
    - src/tools/hash/index.ts (created in Plan 01 — swap component to HashTool)
    - .planning/phases/04-catalogue/04-CONTEXT.md D-11, D-12, D-13, D-14, D-19
    - test/e2e/base64.e2e.ts (e2e template)
  </read_first>
  <behavior>
    HashTool.test.tsx (// @vitest-environment jsdom):
    - typing "abc" with the UTF-8 input mode renders MD5 row "900150983cd24fb0d6963f7d28e17f72" and (after async resolve) the SHA-256 row "ba7816bf8f01cfea..." — use waitFor for the async SHA rows
    - the input-encoding toggle (text/hex/base64) is present (aria-pressed); switching to hex and typing "616263" yields the SAME digests as text "abc" (single Uint8Array path)
    - an invalid input for the chosen encoding (e.g. odd-length hex) shows a field-scoped error, status parseState "error", no crash, and does not render stale digests
    - empty input → status parseState "empty", no digests, no error
    - the casing toggle flips hex output to uppercase (e.g. "900150983CD24FB0D6963F7D28E17F72") and back
    - each of the 5 digest rows has a button[aria-label^="Copy"]
  </behavior>
  <action>
    1. Create `src/tools/hash/HashTool.tsx` — layout-agnostic default export:
       - One input `<textarea>` + an **input-encoding toggle** (text / hex / base64) mirroring Base64's `AlphabetToggle` (aria-pressed, accent=selected only). On every change (paste-instant, UX-01) parse the raw input through `utf8ToBytes` | `hexToBytes` | `base64ToBytes` per the selected mode into a single `Uint8Array`; wrap in try/catch → field-scoped error node (`aria-invalid`+`text-bad`, never opacity-only) on bad input; empty → neutral (no digests).
       - **Reactive digests (D-14, Pitfall 3 guard)**: in a `useEffect` keyed on the bytes (+ a stable identity), compute `digestAll(bytes)` and `setRows` only if a `let live = true` cleanup hasn't invalidated it — so fast typing never shows a stale SHA. Optionally a transient "computing…" affordance (D-19 discretion). Stay within <2s (instant for normal dev inputs); debounce is discretion (D-19).
       - **Five stacked rows** (D-12), MD5 + SHA-1/256/384/512 in order, each a labelled row showing the hex digest + a `<CopyButton value={hex} label={algo} />`. No algorithm picker.
       - **Casing toggle (D-13)**: a lowercase/uppercase control; default lowercase. Apply on DISPLAY (`hex.toUpperCase()` when uppercase) and to the copied value. Keep the canonical digest lowercase.
       - **Status bar**: `<StatusBar parseState byteCount={bytes.length} encoding={selectedEncoding} error={firstError} timingMs={...} />` — here `encoding` IS meaningful (the chosen input mode), so pass it. Measure timing with a `timed()` wrapper.
       Do NOT import `@tauri-apps/*`; copy via CopyButton→platform seam.
    2. In `src/tools/hash/index.ts` (Plan 01): change `component: makePlaceholder("Hash")` → `component: HashTool`, add `import HashTool from "./HashTool";`, remove the unused makePlaceholder import. Do NOT touch registry.ts.
    3. Create `test/e2e/hash.e2e.ts` mirroring base64.e2e.ts: navigate to `#/tools/hash`; type "abc"; assert the MD5 digest "900150983cd24fb0d6963f7d28e17f72" appears AND (waitUntil) the SHA-256 "ba7816bf8f01cfea..." appears — this is the load-bearing Web-Crypto-secure-context check on the REAL WKWebView (Assumption A1: if crypto.subtle is undefined the gate fails loudly). Assert a `button[aria-label^="Copy"]` is displayed; screenshot to `hash-wkwebview.png`. Then RUN `bash scripts/e2e-spike.sh`.
    Create `src/tools/hash/HashTool.test.tsx` (jsdom) covering <behavior>; stub the platform store as Base64Tool.test.tsx does.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'component: HashTool' src/tools/hash/index.ts` succeeds; `grep -q 'makePlaceholder' src/tools/hash/index.ts` finds NOTHING.
    - `grep -q '@/lib/bytes' src/tools/hash/HashTool.tsx` succeeds (single-Uint8Array input via bytes.ts).
    - `grep -q '@tauri-apps' src/tools/hash/HashTool.tsx` finds NOTHING.
    - `npx vitest run src/tools/hash/HashTool.test.tsx` exits 0; the suite asserts hex→"616263" and text→"abc" produce the SAME MD5, and the uppercase toggle yields "900150983CD24FB0D6963F7D28E17F72".
    - `test/e2e/hash.e2e.ts` exists, references `#/tools/hash`, asserts the SHA-256 "ba7816bf8f01cfea" digest renders on the real WKWebView (secure-context A1 check) and a displayed copy button; `bash scripts/e2e-spike.sh` exits 0.
    - `npm test` green (decoder 19 untouched), `npx tsc --noEmit` exit 0, `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>Hash tool renders 5 digests from a single Uint8Array (text/hex/base64 input) with casing toggle + focusable per-row copy, async-guarded; component swapped; real-WKWebView gate (incl. secure-context SHA check) green; full suite + tsc + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted input → byte parser → hashers | Untrusted text/hex/base64 crosses into bytes.ts parsing then digest functions. |
| Web Crypto secure-context | crypto.subtle requires a secure context — gated to the real WKWebView (tauri://). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Denial of Service | large-input / fast-typing hashing | mitigate | Reactive compute with a stale-result guard (Pitfall 3); digests stay within the <2s budget for normal inputs; debounce/large-input handling is in-scope discretion (D-19) but MUST not hang the UI. Bad-encoding input → bounded field-scoped error, never a crash. |
| T-04-11 | Spoofing | hand-rolled / incorrect crypto | mitigate | MD5 = vetted js-md5; SHA = native Web Crypto. NO hand-rolled crypto primitives. Correctness pinned to known digest vectors (md5("")/sha256("")) in unit + e2e. |
| T-04-12 | Tampering | Web Crypto unavailable in packaged webview (A1) | mitigate | The `hash.e2e.ts` real-WKWebView gate asserts a known SHA-256 digest renders; if `crypto.subtle` is undefined the gate fails loudly (Phase-3 precedent: production-only bugs only surface on the real webview). |
| T-04-13 | Information Disclosure | logging/sending hashed input | accept | Offline, display-only; no network; no logging of input; clipboard write only on explicit user copy. |
</threat_model>

<verification>
- Hash renders in the shell at `#/tools/hash` (component swapped; entry pre-registered in Plan 01).
- Five digests from a single Uint8Array across text/hex/base64 input; casing toggle; per-row focusable copy.
- Web Crypto SHA confirmed on the REAL WKWebView (A1 secure-context check in e2e).
- `npm test` green (decoder 19), tsc clean, lint 0; real-WKWebView e2e passes.
</verification>

<success_criteria>
- HASH-01 satisfied: text/bytes → MD5 + SHA-1/256/384/512 (Web Crypto for SHA, js-md5 for MD5).
- D-11 (input modes), D-12 (all five stacked), D-13 (lowercase default + uppercase toggle), D-14 (async SHA reactive) honored.
- UX-01..05 honored; real component swapped + real-webview gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalogue/04-04-SUMMARY.md`.
</output>
