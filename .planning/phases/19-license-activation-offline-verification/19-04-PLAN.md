---
phase: 19-license-activation-offline-verification
plan: 04
type: execute
wave: 4
depends_on: ["19-02", "19-03"]
files_modified:
  - src/lib/license/licenseUi.ts
  - src/lib/license/licenseUi.test.ts
  - src/components/UpsellPanel.tsx
  - src/components/UpsellPanel.test.tsx
  - src/components/Sidebar.tsx
  - src/main.tsx
  - test/e2e/license.e2e.ts
autonomous: false
requirements: [LIC-01, LIC-02, LIC-03, LIC-06]
must_haves:
  truths:
    - "Clicking 'I have a license key' reveals an inline key form in the shared upsell panel — no new modal/route (D-33)"
    - "During activation the submit is disabled with a calm aria-live='polite' status line under the field (D-34); success swaps to a dismissible 'Licensed — thank you' state and entitlements refresh live (D-35)"
    - "Every activation error renders inline below the field, calm red-tint, aria-live, field keeps its value (D-37); seat-limit names the resolution path (D-36); offline vs unreachable have distinct messages (D-38)"
    - "A corrupt/tampered/foreign machine.lic launches silently to free tier with the footer swapped to a 'license needs attention' state (D-43); opening the panel shows the distinct license-problem state — never the sales pitch (D-44)"
    - "The phase's entire network surface is the user-initiated activate call — no launch-time network (D-45)"
  artifacts:
    - path: "src/components/UpsellPanel.tsx"
      provides: "Inline activation form + activating/licensed/error/problem states (D-33..D-39, D-44)"
      contains: "aria-live"
      min_lines: 150
    - path: "src/lib/license/licenseUi.ts"
      provides: "Snapshot store over platform.license.status() (footer + panel share one source)"
      contains: "refreshLicenseUi"
    - path: "src/components/Sidebar.tsx"
      provides: "D-43 footer attention state on the D-29 row"
      contains: "needs attention"
    - path: "test/e2e/license.e2e.ts"
      provides: "Real-WKWebView proof: form reveal, error rendering, footer attention + panel problem-state via seeded bad machine.lic"
      contains: "machine.lic"
  key_links:
    - from: "src/components/UpsellPanel.tsx"
      to: "src/lib/platform/index.ts"
      via: "platform.license.activate(...)"
      pattern: "platform\\.license\\.activate"
    - from: "src/components/UpsellPanel.tsx"
      to: "src/lib/entitlements/store.ts"
      via: "refreshEntitlements() on success (D-35 live unlock)"
      pattern: "refreshEntitlements"
    - from: "src/components/Sidebar.tsx"
      to: "src/lib/license/licenseUi.ts"
      via: "license attention snapshot drives the footer state"
      pattern: "licenseUi|useLicenseUi"
    - from: "src/main.tsx"
      to: "src/lib/license/licenseUi.ts"
      via: "one startup refresh (pure-local status, D-45-safe)"
      pattern: "refreshLicenseUi"
---

<objective>
Build the activation UX inside the existing shared upsell panel (D-33..D-39), the fail-closed surfacing (D-43 footer attention + D-44 panel problem state), prove the offline-reachable UI states on the real WKWebView, then run the phase boundary: `pnpm tauri build` + human walkthrough against the live local CE + gsd-ui-review WCAG-AA audit.

Purpose: this is where LIC-01/02/06 become user-visible. The webview consumes only `platform.license` + typed error codes — all copy lives here (calm tone, locked decisions).

Output: complete Phase 19 user experience, e2e-proven, human-signed-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-license-activation-offline-verification/19-CONTEXT.md
@src/components/UpsellPanel.tsx
@src/components/Sidebar.tsx
@src/lib/platform/index.ts
@src/lib/entitlements/store.ts
@src/main.tsx
@test/e2e/helpers.ts
@test/e2e/entitlements.e2e.ts
@docs/HARNESS.md
</context>

<interfaces>
<!-- Existing contracts the executor builds against (verified from current source) -->

From src/lib/platform/index.ts (created by Plan 03):
```ts
export type LicenseProblem = "corrupt" | "tampered" | "foreignMachine" | "unsupportedAlg";
export type LicenseStatusPayload =
  | { state: "notActivated"; hasStoredKey: boolean }
  | { state: "licensed"; expiry: string | null; entitlements: string[] }
  | { state: "problem"; problem: LicenseProblem; hasStoredKey: boolean };
// platform.license.status() — pure-local; platform.license.activate(key: string | null)
// rejections carry { code: LicenseErrorCode }
```

From src/lib/entitlements/store.ts: `refreshEntitlements(): Promise<void>` — the proven D-32 live-flip path; `setEntitlementsForTest/resetEntitlementsForTest` test seams.

From src/components/UpsellPanel.tsx: `UpsellPanel({ icon, headingId })` — the D-22 "I have a license key" button is currently an inert stub with comment "Phase 19 wires key-paste activation here". `UpsellModal` owns Esc/scrim dismiss + focus trap + focus return. `BUY_LICENSE_URL` CTA stays a no-op (Phase 20).

From src/components/Sidebar.tsx (line ~596): the D-29 footer renders only when `!ents.has(ENT_ORDERING) || !ents.has(ENT_THEMING)`; it opens the shared UpsellModal. Comment marks it "Future home for Phase 19 key entry".

From src/lib/platform/index.ts: `setPlatformForTest(p: Platform)` / `resetPlatformForTest()` — vitest UI tests inject a license stub through this seam.

snapshot-store pattern to mirror (src/lib/entitlements/store.ts): module-level `current` + `listeners` Set + `getSnapshot`/`subscribe`/`refresh` + guarded test seams; consumed via `useSyncExternalStore` (see src/shell/useEntitlements.ts).

Error copy (locked decisions -> exact code mapping; calm tone, Claude's-discretion exact wording within these):
- `seatLimit` (D-36): "This key is already active on another Mac. Deactivate it on the other Mac first, then activate here." (names the resolution path; NO support link — deferred to Phase 21)
- `offline` (D-38): "You're offline — connect and try again."
- `serviceUnreachable` (D-38): "Can't reach the licensing service — try again shortly."
- `invalidKey`: "That key wasn't recognized — check it and try again."
- `suspended`: "This license isn't active anymore."
- `noStoredKey` / `activationFailed`: "Activation didn't complete — try again."

D-44 + LIC-04 reconciliation (decided at planning, record in code comment): "pre-filled from Keychain" is implemented as Rust-side stored-key reactivation — JS sees only `hasStoredKey: boolean`. When true, the problem-state field shows placeholder "Your saved key will be used" and an empty submit calls `platform.license.activate(null)`; pasting a key overrides. The raw key NEVER reaches JS (LIC-04 is architecturally prior).

e2e facts: app identifier `com.tinkerdev.app` (tauri.conf.json) -> app data dir `~/Library/Application Support/com.tinkerdev.app/`; machine.lic lives there. The dev free-tier toggle is the palette command literally named "Toggle free tier (dev)" (entitlements.e2e.ts shows the drive pattern + T-18-15 finally-cleanup). e2e runs via `scripts/e2e-spike.sh` on the real WKWebView (project memory: the UI gate IS this run; Chromium is only a preview).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: licenseUi store + activation form + panel states (D-33..D-39, D-44)</name>
  <files>src/lib/license/licenseUi.ts, src/lib/license/licenseUi.test.ts, src/components/UpsellPanel.tsx, src/components/UpsellPanel.test.tsx, src/main.tsx</files>
  <read_first>
    - src/components/UpsellPanel.tsx (current structure, D-19 static copy, the inert D-22 button, UpsellModal focus trap — preserve all existing behavior)
    - src/lib/entitlements/store.ts (the snapshot-store pattern to mirror exactly)
    - src/shell/useEntitlements.ts (useSyncExternalStore consumption pattern)
    - src/main.tsx (startup sequence — where refreshEntitlements is kicked off; refreshLicenseUi joins beside it)
    - src/lib/platform/index.ts (LicenseStatusPayload/LicenseErrorCode types + setPlatformForTest seam)
  </read_first>
  <behavior>
    - licenseUi.test: refreshLicenseUi() pulls platform.license.status() (via an injected test platform) and notifies subscribers only on change; default snapshot = { state: "notActivated", hasStoredKey: false }
    - UpsellPanel.test: clicking "I have a license key" reveals the inline form (input + Activate button) in place — same panel, no new dialog (D-33)
    - UpsellPanel.test: during a pending activate, the submit button is disabled and the aria-live="polite" status line reads an "Activating…"-style message (D-34)
    - UpsellPanel.test: resolve activate -> panel swaps to the "Licensed — thank you" dismissible state AND refreshEntitlements was called (D-35; spy via a test double or assert the entitlements store refresh side effect)
    - UpsellPanel.test: reject with { code: "seatLimit" } -> inline error below the field containing "other Mac" (resolution path named, D-36), field RETAINS its typed value (D-37)
    - UpsellPanel.test: { code: "offline" } and { code: "serviceUnreachable" } render two DIFFERENT messages (D-38)
    - UpsellPanel.test: submit with only-whitespace input does NOT call activate (D-39 trim-only client validation); leading/trailing whitespace around a key is trimmed before send
    - UpsellPanel.test: with licenseUi snapshot { state: "problem", problem: "corrupt", hasStoredKey: false } the panel renders the problem state — heading "couldn't be verified"-style, form pre-revealed, key field auto-focused, NO sales-pitch copy (D-44)
    - UpsellPanel.test: problem state with hasStoredKey: true -> empty-field submit calls platform.license.activate(null) (stored-key reactivation; D-44/LIC-04 reconciliation)
  </behavior>
  <action>
    1. Create `src/lib/license/licenseUi.ts` mirroring the entitlements snapshot store: `getLicenseUiSnapshot(): LicenseStatusPayload`, `subscribeLicenseUi`, `refreshLicenseUi()` (awaits `initPlatform()` then `platform.license.status()` — pure-local, D-45-safe), change-detection short-circuit, guarded `setLicenseUiForTest`/`resetLicenseUiForTest` (same isTestOrDev guard idiom). Default snapshot `{ state: "notActivated", hasStoredKey: false }`. Export a small `useLicenseUi()` hook (useSyncExternalStore) — either here or beside useEntitlements; follow the existing hook placement convention.
    2. In `src/main.tsx`: beside the existing `void refreshEntitlements()`, add `void refreshLicenseUi().catch(...)` — comment: LOCAL file read + verify only, zero network (D-45; the v1.6 amendment forbids launch-time network).
    3. Rework `UpsellPanel.tsx` into a small state machine while preserving the existing default copy byte-for-byte for the sales state: views = `sales` (current copy + buttons) | `form` (revealed by the D-22 button: labelled text input + "Activate" submit + status/error line region under the field) | `activating` | `licensed` | `problem`. Initial view derives from `useLicenseUi()`: state "problem" -> problem view (heading like "Your license file couldn't be verified" + one calm body line + the form pre-revealed, input auto-focused via ref; if hasStoredKey, placeholder "Your saved key will be used — paste a new key to replace it" and empty submit allowed -> activate(null)); otherwise sales view. NEVER show sales copy in problem state (D-44: a paying customer never sees the pitch).
    4. Submission: trim input; empty + !hasStoredKey -> no-op (D-39); call `platform.license.activate(trimmed || null)`; pending -> disable submit + status line "Activating…" in an `aria-live="polite"` element (one line under the field, no spinner chrome — D-34). Success -> `await refreshLicenseUi(); await refreshEntitlements();` then the `licensed` view: calm "Licensed — thank you." + a "Done" button; Esc continues to dismiss via UpsellModal (D-35 — unlock is live behind the panel, no restart). Errors -> map `{ code }` to the exact copy table in <interfaces>, render inline below the field in a calm red-tint with the SAME aria-live region, keep the field value (D-37). Unknown codes -> the activationFailed fallback line.
    5. Keyboard: Enter in the input submits; Esc dismisses (UpsellModal already owns Esc — don't double-handle). All new controls Tab-reachable with focus-visible rings matching the existing panel classes; the form must stay inside the UpsellModal focus trap (it queries buttons/inputs — verify the input is caught by its selector).
    6. Styling: reuse the panel's existing token classes (border-bd, bg-input-bg, text-tx-2, focus-visible:ring-accent); error tint via the app's existing calm red token (check tailwind config / existing error styles — match, don't invent).
    7. Extend `UpsellPanel.test.tsx` (existing tests must keep passing — the default sales render is unchanged) with the behavior list via setPlatformForTest license stubs + setLicenseUiForTest.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run src/components/UpsellPanel.test.tsx src/lib/license/</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'aria-live="polite"' src/components/UpsellPanel.tsx` matches (D-34/D-37 region)
    - `grep "activate(null)" src/components/UpsellPanel.tsx` (or equivalent null-arg call) matches — stored-key reactivation path exists
    - `grep "refreshEntitlements" src/components/UpsellPanel.tsx` matches in the success path (D-35)
    - `grep "other Mac" src/components/UpsellPanel.tsx` matches (D-36 resolution path named)
    - `grep -c "offline" src/components/UpsellPanel.tsx` and `grep "try again shortly"` both match — two distinct D-38 messages
    - `grep "refreshLicenseUi" src/main.tsx` matches with a comment containing "no network" or "local"
    - UpsellPanel tests cover all 9 behavior cases; full `pnpm vitest run` green; existing UpsellPanel/Sidebar/palette tests unmodified-and-green
    - `grep -rn "@tauri-apps" src/components/ src/lib/license/` returns 0
  </acceptance_criteria>
  <done>The shared panel carries the complete activation UX: reveal-in-place form, calm in-flight/success/error states, distinct problem state with stored-key reactivation — all locked decisions D-33..D-39 + D-44 implemented and unit-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-43 footer attention state + real-WKWebView e2e</name>
  <files>src/components/Sidebar.tsx, test/e2e/license.e2e.ts</files>
  <read_first>
    - src/components/Sidebar.tsx lines 590-630 (the D-29 footer button + UpsellModal wiring — extend, don't restructure)
    - test/e2e/entitlements.e2e.ts (the "Toggle free tier (dev)" palette drive pattern + finally-cleanup discipline T-18-15)
    - test/e2e/helpers.ts (assert/dispatchKey/navigateToTool/saveScreenshot — new spec MUST use these, no inline duplicates)
    - docs/HARNESS.md (e2e ports/preflight/session rules)
  </read_first>
  <behavior>
    - Sidebar vitest (extend existing Sidebar tests minimally): with licenseUi snapshot state "problem", the footer row renders the attention label EVEN when entitlements are FULL_SET (a Phase-19 release build has everything unlocked — the attention surface must not depend on the free-tier flip); clicking it opens the shared UpsellModal
    - Sidebar vitest: with state "notActivated"/"licensed" and FULL_SET, the footer is absent (current behavior preserved)
    - e2e: free-tier toggle -> footer "Unlock Pro" -> open panel -> "I have a license key" reveals the form -> type a key -> submit -> the aria-live region shows a status, then SOME inline error renders and the field retains its value (CE may or may not be running — assert error-region-non-empty + value retention, not a specific message)
    - e2e: seed a corrupt machine.lic into the app data dir -> reopen the panel -> the D-44 problem heading is visible (panel re-queries status on open) -> footer shows the attention state -> cleanup restores everything in finally
  </behavior>
  <action>
    1. Sidebar.tsx: consume `useLicenseUi()`. Footer render condition becomes: `licenseAttention || !ents.has(ENT_ORDERING) || !ents.has(ENT_THEMING)` where `licenseAttention = licenseUi.state === "problem"`. When attention: label "License needs attention" (calm, neutral tokens + the existing Lock icon or a neutral alert icon — no red alarm styling, D-43 is a hint not an interruption); otherwise the existing "Unlock Pro" label. Same onClick (opens the shared UpsellModal — the panel itself renders the problem state from Task 1). NO launch interruption anywhere (D-43: silent free-tier launch + footer hint is the entire discovery surface).
    2. UpsellPanel (small addition if not already in Task 1): on panel mount, `void refreshLicenseUi()` so opening the panel re-queries status (still pure-local) — this is what makes the e2e seeding deterministic and the D-44 state fresh.
    3. Write `test/e2e/license.e2e.ts` using helpers.ts exclusively, following the entitlements.e2e.ts session/cleanup shape:
       a. Flow A (form mechanics): drive the palette -> "Toggle free tier (dev)" -> assert footer button appears -> click -> modal open -> click "I have a license key" -> assert input visible -> set value "TEST-KEY-E2E" -> submit -> await the inline error region becoming non-empty -> assert input still has "TEST-KEY-E2E" (D-37). Screenshot via saveScreenshot.
       b. Flow B (fail-closed surfacing): from node context write garbage (e.g. "not a machine file") to `${os.homedir()}/Library/Application Support/com.tinkerdev.app/machine.lic` (back up any existing file first); close + reopen the panel (or toggle modal) so the mount re-query runs -> assert the problem heading text is visible AND the footer now reads the attention label -> screenshot.
       c. finally: restore/delete the seeded machine.lic (restore backup if one existed), untoggle free tier via the palette command, assert the default state returned (T-18-15 discipline).
    4. Run the REAL gate: `scripts/e2e-spike.sh` (the actual WKWebView; per project memory the green run must include the new spec — watch the preflight port rules). All 15 existing specs + license.e2e.ts green.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run src/components/ && bash scripts/e2e-spike.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep "needs attention" src/components/Sidebar.tsx` matches
    - `grep "licenseAttention\|state === \"problem\"" src/components/Sidebar.tsx` shows the footer condition includes the problem state independent of entitlements
    - `grep "helpers" test/e2e/license.e2e.ts` imports from ./helpers (no duplicated assert/dispatch utilities)
    - `grep "finally" test/e2e/license.e2e.ts` matches with machine.lic cleanup inside
    - `grep "com.tinkerdev.app" test/e2e/license.e2e.ts` matches (real app-data path seeding)
    - e2e run output shows license.e2e.ts green on the REAL WKWebView alongside all pre-existing specs (16 spec files total)
    - Full vitest + tsc + eslint green; decoder untouched
  </acceptance_criteria>
  <done>Fail-closed UX proven end-to-end on the real webview: silent launch + footer hint + problem panel (D-43/D-44), form mechanics + error rendering (D-37) — with deterministic seeding and full cleanup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase boundary — build, live walkthrough vs local CE, WCAG audit</name>
  <files>(no source changes — build + verification)</files>
  <read_first>
    - .planning/phases/19-license-activation-offline-verification/19-SPIKE-OUTCOME.md (note: the spike license's seat is bound to a synthetic fingerprint — mint a FRESH license for this walkthrough)
    - scripts/keygen-ce/README.md (CE bring-up + mint_license usage)
  </read_first>
  <action>
    AUTOMATE EVERYTHING BEFORE PAUSING (binding harness — the human only launches/tests/approves):
    1. Run the full gate: `pnpm exec tsc --noEmit && pnpm vitest run && pnpm exec eslint . && cargo test --manifest-path src-tauri/Cargo.toml && bash scripts/e2e-spike.sh` — all green before proceeding.
    2. Run `bash scripts/check-dev-strip.sh` against a fresh build — 0 hits for the dev toggle string (T-19-24).
    3. Ensure local CE is up (`docker compose -f scripts/keygen-ce/compose.yaml ps`; bring up via scripts/keygen-ce/README.md if down) and mint a FRESH walkthrough license via `scripts/keygen-ce/bootstrap.sh` (the spike license's seat is bound to a synthetic fingerprint — never reuse it).
    4. Run `pnpm tauri build` (expected final non-zero exit = absent updater-signing key; the bundle under `src-tauri/target/release/bundle/macos/` is the artifact — if the DMG step flakes, `hdiutil detach` mounted DMGs and retry per project memory).
    5. Run the `gsd-ui-review` WCAG-AA audit on the activation panel + footer states; attach results.
    6. Start `DEVTOOLS_KEYGEN_CA=scripts/keygen-ce/caddy-root.crt pnpm tauri dev` for the human's dev-build half, and hand off: built-app path, the fresh license key, CE status, audit results.
  </action>
  <what-built>
    Complete Phase 19: local Keygen CE + SPIKE record; Rust license module (fingerprint, Ed25519 fail-closed verify, Keychain, atomic machine.lic store); 4 Tauri commands behind the platform seam; activation UX in the shared upsell panel with fail-closed surfacing (D-33..D-45).
  </what-built>
  <how-to-verify>
    IMPORTANT release-build facts: in this phase's release build everything is unlocked (free-tier flip is Phase 21), so the panel's only release entry point is the D-43 attention footer; and the release build does NOT trust the local CE's self-signed CA (dev-only trust is compiled out — correct security posture), so the live activation step runs under `tauri dev`. The walkthrough therefore has two halves:

    DEV-BUILD half (live activation vs CE) — agent starts `DEVTOOLS_KEYGEN_CA=scripts/keygen-ce/caddy-root.crt pnpm tauri dev` for you:
    1. Palette -> "Toggle free tier (dev)" -> footer "Unlock Pro" -> open panel -> "I have a license key".
    2. Paste the fresh license key -> Activate. Expect: "Activating…" status line, then the "Licensed — thank you" state. (macOS may show a Keychain prompt — expected in dev builds.)
    3. Reopen the panel: it should NOT show the sales pitch flow as a problem; close, relaunch `tauri dev` with Wi-Fi OFF -> app launches silently, no attention footer, tools fully functional (LIC-03 offline launch; verify Console shows no network errors).
    4. Seat limit (LIC-02): agent runs `scripts/keygen-ce/spike.sh`-style second activation of YOUR key with a different fingerprint via curl — then in-app, deactivate is not wired (Phase 21), so instead: agent activates the key via curl on a synthetic fingerprint FIRST on a SECOND fresh license, then you paste that second key — expect the calm seat-limit message naming "the other Mac". Confirm the message reads calmly and the field keeps the key.
    5. Corrupt-lic fail-closed (LIC-06): agent corrupts `~/Library/Application Support/com.tinkerdev.app/machine.lic` -> relaunch -> NO crash, NO interruption, footer shows "License needs attention" -> open panel -> problem state ("couldn't be verified", field focused, saved-key affordance) -> Activate with empty field (stored key) -> back to Licensed.

    RELEASE-BUILD half (baseline + offline verify):
    6. Launch the built .app from `src-tauri/target/release/bundle/macos/`. With the dev-build's valid machine.lic + Keychain item in place (same identifier/app-data dir), launch with networking OFF: silent licensed launch — no attention footer, no prompts beyond a possible one-time Keychain ACL prompt (dev vs release signing — click Allow once), every tool works.
    7. Corrupt the machine.lic again -> relaunch release app -> attention footer appears, panel shows the problem state (the release-reachable D-43/D-44 path). Restore afterwards.
    8. Keychain check (LIC-04): `security find-generic-password -s com.tinkerdev.app.license` shows the item EXISTS (do NOT pass -w; the key value should never need to be printed). Confirm no `machine.lic`-adjacent file or prefs JSON contains the key: `grep -r "$(head -c 8 <<< 'first-8-chars-of-key')" ~/Library/Application\ Support/com.tinkerdev.app/` returns nothing.
    9. Review the attached gsd-ui-review WCAG-AA results for the panel form/status/error states and footer (keyboard path + aria-live are mandatory).
  </how-to-verify>
  <verify>
    <automated>bash scripts/check-dev-strip.sh && ls src-tauri/target/release/bundle/macos/ | grep -i "TinkerDev"</automated>
  </verify>
  <acceptance_criteria>
    - Full gate green (tsc + vitest + eslint + cargo test + real-WKWebView e2e 16 specs) before the pause
    - check-dev-strip.sh reports 0 hits against the fresh build
    - Built .app/.dmg present under src-tauri/target/release/bundle/macos/
    - Human walkthrough approved: live activation (dev), seat-limit message, offline licensed launch, corrupt-lic fail-closed (dev + release), Keychain item exists with no key on disk
    - gsd-ui-review WCAG-AA audit passing for the new surfaces
  </acceptance_criteria>
  <done>Phase 19 human-signed-off: walkthrough approved against the live local CE + passing WCAG-AA audit on a fresh tauri build.</done>
  <resume-signal>Type "approved" to close Phase 19, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> activate command | Pasted key string crosses webview -> Rust |
| on-disk machine.lic -> launch status | Untrusted file drives UI state via typed payloads only |
| dev walkthrough -> release artifact | Dev CA trust + dev toggle must be absent from the shipped bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-21 | I (Info disclosure) | key material in the webview/UI layer | mitigate | UI holds the pasted key only in transient component state for the one activate call; never persisted to prefs/localStorage, never logged, never echoed back from Rust; stored-key path uses activate(null) + hasStoredKey boolean only (D-44/LIC-04 reconciliation) |
| T-19-22 | D / fail-open | corrupt machine.lic crashing launch or unlocking | mitigate | Problem states arrive as typed payloads from the fail-closed Rust path; UI renders calm hint states (D-43/D-44); e2e Flow B + walkthrough steps 5/7 prove no-crash + free-tier behavior |
| T-19-23 | T (Tampering) | webview gate patching (user edits JS to fake licensed) | accept | Locked architecture: webview gating is UX-gating, not DRM; the Rust side remains the verification authority |
| T-19-24 | I | dev free-tier toggle / dev CA trust leaking into release | mitigate | Existing `scripts/check-dev-strip.sh` dist-grep proves "Toggle free tier (dev)" absent from the build (Phase 18 artifact — agent re-runs it before the checkpoint); CA trust is `#[cfg(debug_assertions)]`-gated (T-19-16) |
| T-19-25 | E (Elevation) | unprompted network from launch path | mitigate | Startup + panel-mount refreshes call only the pure-local license_status; the sole network call is the user-initiated activate (D-45); main.tsx comment + walkthrough offline steps verify |
| T-19-26 | S (Spoofing) | UI trusting its own state over the server for seats | mitigate | UI renders only server-derived typed error codes; zero client seat logic (T-19-17 upstream) |
</threat_model>

<verification>
- Full gate green: tsc + vitest (existing 816+ plus new) + eslint + cargo test + real-WKWebView e2e (16 specs)
- `scripts/check-dev-strip.sh` returns 0 hits against the fresh build
- decoder.ts + its 19 tests byte-for-byte untouched (`git diff v1.5..HEAD -- src/lib/protobuf/decoder.ts src/lib/protobuf/decoder.test.ts` empty for this phase's commits)
- Human walkthrough approved (live activation, seat-limit, offline launch, fail-closed, Keychain) + gsd-ui-review WCAG-AA passing
- Per-task harness honored: /simplify -> /codex:review -> unit green -> real-webview verification
</verification>

<success_criteria>
- ROADMAP Phase 19 success criteria 1-5 all observable: paste-key activation with live unlock (1), second-Mac calm rejection naming the path (2), fully-offline licensed launch (3), corrupt/tampered/foreign fail-closed with calm re-activation (4), key Keychain-only + SPIKE outcome recorded (5)
- LIC-01, LIC-02, LIC-03, LIC-04, LIC-06 complete; REQUIREMENTS.md traceability updatable to Phase 19 plans
- Phase 21 inherits: refresh/deactivate commands callable, expiry surfaced in payloads, entitlements embedded in machine.lic, the resolveEntitlements flip point untouched
</success_criteria>

<output>
After completion, create `.planning/phases/19-license-activation-offline-verification/19-04-SUMMARY.md`
</output>
