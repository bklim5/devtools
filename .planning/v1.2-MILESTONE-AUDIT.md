---
milestone: 1.2
milestone_name: Release Tooling
audited: 2026-06-03
status: passed
scores:
  requirements: 12/12
  phases: 3/3
  integration: 22/22 seams connected
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 11-build-and-publish
    items:
      - "WR-01 (advisory): post-publish curl verify lacks --fail and retry — a redirect-propagation lag can fail an otherwise-correct publish; HTTP errors surface as opaque JSON.parse failures"
      - "WR-02 (advisory): assertSingleSig reused for .dmg/.app.tar.gz globs emits signature-specific diagnostics that mislead on a missing DMG/tarball"
      - "WR-03 (advisory): stale-.sig clear + single-match assert rely on one exact path constant; an mtime freshness check would add defense-in-depth"
      - "IN-02 (advisory): comment/code drift on the accepted viewerPermission set (MAINTAIN)"
  - phase: 10-bump-and-tag
    items:
      - "WR-02/WR-03 (advisory): non-blocking fail-safe robustness notes from 10-REVIEW.md"
nyquist:
  compliant_phases: ["10"]
  partial_phases: ["11"]
  missing_phases: ["09"]
  overall: partial
  note: "Discovery-only, non-blocking. Phase 9 has no VALIDATION.md (pure core fully unit-covered by the existing tsc+vitest gate). Phase 11 VALIDATION.md is the planning validation-strategy doc (nyquist_compliant: false) reflecting the inherently manual-only live items (real publish + DST-02 round-trip are human gates); the automatable logic — publishPlan.ts (33 tests) + downloadProgress.ts (6 tests) — is fully covered. No 3 consecutive tasks lack automated verification."
parked_by_design:
  - "CI track (999.2): cross-repo PAT + minisign secrets in Actions, push/PR checks, tag-triggered CI release — explicitly deferred to a follow-on milestone, not a gap"
---

# Milestone v1.2 "Release Tooling" — Audit Report

**Status: PASSED** — 12/12 requirements satisfied, 3/3 phases verified, all cross-phase seams connected, the single E2E flow complete. The whole flow was additionally proven **live** this session (real v0.2.2 bump→tag→build→publish→updater round-trip).

## Definition of Done (from ROADMAP)

Replace the manual `docs/RELEASE.md` dance with two composable local helper scripts (`bump-and-tag` + `build-and-publish`) over a unit-tested pure core in `src/lib/release/`, with zero new runtime dependencies and the hero decoder + its 19 tests byte-for-byte untouched. CI parked. Phases 9–11 touch no app UI; Phase 11's real updater round-trip is the load-bearing DST-02 human sign-off.

## Requirements Coverage (3-source cross-reference)

All 12 REL requirements are **satisfied** — each confirmed by VERIFICATION.md (`✓ SATISFIED`) + SUMMARY `requirements-completed` frontmatter + REQUIREMENTS.md `[x]`.

| Req | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|-----|-------|--------------|---------------------|-----------------|-------|
| REL-01 | 10 | ✓ SATISFIED | 10-02/10-03 | [x] | satisfied |
| REL-02 | 9 | ✓ SATISFIED | 09-01 | [x] | satisfied |
| REL-03 | 10 | ✓ SATISFIED | 10-01/10-03 | [x] | satisfied |
| REL-04 | 10 | ✓ SATISFIED (live tag on origin) | 10-03 | [x] | satisfied |
| REL-05 | 11 | ✓ SATISFIED | 11-02/11-03 | [x] | satisfied |
| REL-06 | 9 (core) + 11 (delivery) | ✓ SATISFIED | 09-02/11-02/11-03 | [x] | satisfied |
| REL-07 | 11 | ✓ SATISFIED (live publish) | 11-02/11-03 | [x] | satisfied |
| REL-08 | 9 | ✓ SATISFIED | 09-01 | [x] | satisfied |
| REL-09 | 11 | ✓ SATISFIED | 11-02/11-03 | [x] | satisfied |
| REL-10 | 10 (mapped) | ✓ SATISFIED | 10-02 | [x] | satisfied |
| REL-11 | 10 (mapped) | ✓ SATISFIED | 10-02 | [x] | satisfied |
| REL-12 | 11 | ✓ SATISFIED | 11-02/11-03 | [x] | satisfied |

**Orphans:** none — every traceability-table ID is verified by its assigned phase. REL-06 is the only deliberately split requirement (Phase 9 pure `buildLatestJson` + Phase 11 delivery); both halves connected.

## Phase Verifications

| Phase | VERIFICATION status | Score |
|-------|---------------------|-------|
| 9 — Pure release core + housekeeping | passed | 5/5 |
| 10 — bump-and-tag driver | passed | 5/5 |
| 11 — build-and-publish driver + universal binary + safety rails | passed | 5/5 |

## Cross-Phase Integration

22 exports connected, **0 orphaned / 0 missing / 0 broken**. Verified seams:
- **Phase 9 → 10:** `bumpPlan.ts` imports `bumpSemver` + the three surgical `setXVersion` editors from `version.ts`; driver applies the lockstep 3-file write.
- **Phase 9 → 11:** `build-and-publish.mjs` imports `buildLatestJson` via `../src/lib/release/manifest.ts` (resolves from `scripts/`), producing the dual-key manifest from one `{url, signature}`.
- **Phase 10 → 11:** both drivers read the version from `package.json` (lockstep-intact at 0.2.2 across all 3 manifests); Phase 10 tags `vX.Y.Z`, Phase 11 publishes it.
- **Pure-core ↔ thin-shell:** both `.mjs` drivers import *and call* every pure helper; no decision logic leaked into the I/O shells.
- **Updater round-trip seam:** driver endpoint/curl-verify == `tauri.conf.json` updater endpoint; `manifest.ts` keys == the Tauri macOS updater's queried keys; minisign pubkey pinned at `tauri.conf.json:48`, `createUpdaterArtifacts: true` produces the globbed `.sig`. Loop closed.

## E2E Flow

**1/1 complete:** `pnpm release:bump <level>` (3-manifest lockstep bump + lockfile regen + annotated tag + push) → `pnpm release:publish` (universal build → lipo both-arch → fresh-sig dual-key `latest.json` → cross-repo gh publish assets-first/manifest-last → curl served-version verify) → updater minisign round-trip → relaunch. Every step's data dependency is satisfied by the prior step's output through the verified import graph. Proven live with v0.2.2 (arm64; Intel covered-by-construction).

## Security

Phase 11 threat-secured: `11-SECURITY.md`, 16/16 STRIDE threats closed, `threats_open: 0`, ASVS L2.

## Tech Debt (non-blocking, deferred)

- **Phase 11 hardening (from 11-REVIEW.md, 0 critical):** curl `--fail`+retry (WR-01), glob-diagnostic reuse (WR-02), `.sig` mtime freshness (WR-03), MAINTAIN comment drift (IN-02).
- **Phase 10:** WR-02/WR-03 fail-safe robustness notes (non-blocking).
- **Nyquist:** Phase 9 has no VALIDATION.md (pure core fully unit-covered); Phase 11 VALIDATION.md is partial by design (live items are human gates). Discovery-only — does not block. Optional: `/gsd-validate-phase 9` / `/gsd-validate-phase 11` to formalize.
- **CI track (999.2):** parked by design, not a gap.

## Verdict

No critical blockers, no unsatisfied requirements, no broken integration, no broken flows. The deferred items are advisory robustness hardening and CI (explicitly parked). **Milestone v1.2 is ready to archive.**
