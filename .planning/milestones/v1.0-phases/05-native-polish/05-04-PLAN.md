---
phase: 05-native-polish
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/05-native-polish/05-UI-REVIEW.md
  - .planning/phases/04-catalogue/04-HUMAN-UAT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [NAT-01, NAT-02]
user_setup: []

must_haves:
  truths:
    - "A fresh `tauri build` produces a runnable macOS bundle with single-instance, tray, global-shortcut and window-state plugins, and all six tools still function in the packaged WKWebView."
    - "On the packaged app, the global summon chord brings the app to front + focuses it from another application (NAT-01)."
    - "The tray icon + menu are present and work (Show focuses, Quit exits); a second launch focuses the existing window (NAT-02)."
    - "Window position + size persist across quit + relaunch (SHL-05 / D-11)."
    - "A gsd-ui-review WCAG-AA audit passes; the human signs off, batching the deferred Phase-4 UAT."
  artifacts:
    - path: ".planning/phases/05-native-polish/05-UI-REVIEW.md"
      provides: "WCAG-AA audit record for the Phase-5 packaged build"
    - path: ".planning/phases/04-catalogue/04-HUMAN-UAT.md"
      provides: "the deferred Phase-4 per-tool checks, resolved in the same sign-off session"
  key_links:
    - from: "packaged .app second launch"
      to: "existing window focus (single-instance)"
      via: "tauri_plugin_single_instance callback"
      pattern: "single_instance"
---

<objective>
Phase-5 boundary gate. After the Rust plugins (Plan 01), the seam (Plan 02), and the shell wiring (Plan 03) land, verify the WHOLE phase to the binding harness — a fresh `tauri build`, a `gsd-ui-review` WCAG-AA audit, the e2e suite, and a HUMAN packaged-build sign-off. This is the only `autonomous: false` plan in the phase (it carries the human checkpoint), mirroring Phase 4's `04-06`.

**CRITICAL — Phase 5's core behaviors are OS-level and NOT automatable by the WebDriver gate** (global hotkey from another app, tray click, single-instance second launch, geometry restore — see 05-VALIDATION §Manual-Only). So this sign-off is HEAVIER than prior phases and leans almost entirely on the human packaged-build UAT. Per STATE.md, the Phase-4 human sign-off is already DEFERRED (user AFK); this sign-off is EXPECTED to BATCH the deferred Phase-4 per-tool checks (04-HUMAN-UAT.md, status: partial) in the same session.

Purpose: prove the native polish ships under the same quality bar and surface any packaged-only macOS behavior (the focus regression A1, hide-vs-minimize, restore flash) before closing the phase.
Output: a passing WCAG-AA audit record + a green `tauri build` + human approval covering BOTH Phase 5 and the deferred Phase 4; ROADMAP/REQUIREMENTS/STATE updated to mark Phase 5 complete (and Phase 4 signed off) — ONLY after the user types "approved".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-native-polish/05-RESEARCH.md
@.planning/phases/05-native-polish/05-VALIDATION.md
@.planning/phases/04-catalogue/04-HUMAN-UAT.md

<!-- Reference: Phase-4 closing audit + sign-off for format/expectations: -->
<!-- .planning/phases/04-catalogue/04-06-PLAN.md (the mirrored closing-plan structure) -->
<!-- AA-token precedents to reuse: --tx-3 #868b95, accent #5b9bf8 -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + fresh tauri build + e2e + WCAG-AA audit + seam/release audits</name>
  <read_first>
    - .planning/phases/05-native-polish/05-VALIDATION.md §"Manual-Only Verifications" + §"Sampling Rate" (phase-gate definition)
    - .planning/phases/04-catalogue/04-06-PLAN.md (the closing-plan format being mirrored)
    - src-tauri/src/lib.rs (confirm single-instance FIRST + tray + the webdriver gate intact)
    - scripts/e2e-spike.sh (the real-WKWebView runner) + test/e2e/ (all specs incl. the new summon spec)
  </read_first>
  <action>
    1. **Full unit gate**: `pnpm vitest run` (decoder's 19 MUST be green, plus the platform-seam + summon suites), `npx tsc --noEmit`, `pnpm eslint .`. Fix any regression.
    2. **Seam discipline audit (FND-04)**: `grep -rn "@tauri-apps" src/ | grep -v "\.test\." | grep -v "//"` must show imports ONLY in `src/lib/platform/tauri.ts` (and the dynamic import/comments in index.ts). No tool/shell file imports @tauri-apps.
    3. **Release-exclusion audit (T-01-10/11, Pitfall 7)**: `cargo tree --release | grep -c webdriver` = 0; the `#[cfg(all(debug_assertions, feature = "webdriver"))]` gate in lib.rs unchanged.
    4. **Offline audit (T-05-03)**: confirm `src-tauri/tauri.conf.json` `security.csp` is unchanged (no new `connect-src`); the three new plugins make no network calls.
    5. **Capability least-privilege audit (T-05-01)**: capabilities/default.json grants exactly the four new perms (`global-shortcut:allow-register/unregister/is-registered`, `window-state:default`) — no wildcards.
    6. **Real-WKWebView e2e**: run `bash scripts/e2e-spike.sh`; all specs pass on webkit (incl. the new summon spec proving the app launches with the plugins + HashRouter deep-link works). The OS-global hotkey is NOT covered here (Manual-Only) — that is Task 2.
    7. **Fresh `tauri build`**: `pnpm tauri build` (or `npm run tauri build`) → runnable macOS bundle (.app/.dmg), exit 0. Launch the packaged app and smoke-test that all six tools still function (paste-instant, copy works, status bar) — catch packaged-only bugs.
    8. **gsd-ui-review WCAG-AA audit**: run the audit on the packaged build; for any finding apply the fix (reuse Phase-3/4 AA tokens — `--tx-3 #868b95`, `accent #5b9bf8` — not new values). Record in `.planning/phases/05-native-polish/05-UI-REVIEW.md` (mirror the Phase-4 UI-REVIEW format). Re-run until AA passes.
    Do NOT touch the decoder or its 19 tests; no new visual language (reuse design/DevTools Mockup.html tokens). Note: Phase 5 adds no UI surface inside the webview except possibly tray-driven window behavior, so the audit largely re-confirms the six tools still pass AA in the packaged build.
  </action>
  <verify>
    <automated>pnpm vitest run && npx tsc --noEmit && pnpm eslint . && [ "$(cargo tree --release --manifest-path src-tauri/Cargo.toml 2>/dev/null | grep -c webdriver)" = "0" ] && test -f .planning/phases/05-native-polish/05-UI-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` green (decoder 19 untouched + platform-seam + summon suites), `npx tsc --noEmit` exit 0, `pnpm eslint .` 0 errors.
    - Seam audit: `@tauri-apps` imports appear ONLY in `src/lib/platform/tauri.ts` (+ index.ts dynamic import/comments) — no tool/shell file.
    - `cargo tree --release --manifest-path src-tauri/Cargo.toml | grep -c webdriver` = 0; webdriver gate unchanged.
    - `security.csp` in tauri.conf.json unchanged (no new connect-src); capabilities grants are exactly the four least-privilege perms (no wildcard).
    - `bash scripts/e2e-spike.sh` exits 0 on webkit (all specs incl. summon).
    - `tauri build` exits 0 and produces a macOS bundle; all six tools function in the packaged app.
    - `.planning/phases/05-native-polish/05-UI-REVIEW.md` exists and records a PASSING WCAG-AA audit.
  </acceptance_criteria>
  <done>Full suite + tsc + lint green; seam/release/offline/capability audits clean; e2e passes; fresh tauri build runs; WCAG-AA audit passes and is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on the packaged Phase-5 build (batched with deferred Phase-4 UAT)</name>
  <action>
    Pause for the user to verify the freshly-built packaged macOS app. Present the build location and the verification steps below. Because Phase 5's core behaviors are OS-level (outside the webview), this is the primary verification for NAT-01/NAT-02/SHL-05 — there is no automated substitute. Per STATE.md the Phase-4 human sign-off is also deferred; present the Phase-4 per-tool checks (from 04-HUMAN-UAT.md) in the SAME session so the user can verify both at once. Do NOT proceed to Task 3 until the user explicitly types "approved". If the user reports issues, capture them for a `/gsd-plan-phase 5 --gaps` (and/or `/gsd-plan-phase 4 --gaps`) follow-up instead of marking complete.

    ADOPTED-DEFAULTS CONFIRMATION (flag prominently): the plans adopted four research defaults pending the user's confirmation — (D-01) summon chord `CommandOrControl+Shift+D`; (D-02) regular dock app + tray; (D-03) minimize/unminimize (not hide) with summon order unminimize→show→setFocus; (D-04) JS-seam shortcut handler (Rust fallback if focus is flaky). Ask the user to confirm or adjust each at sign-off; if they want changes (e.g. a different chord, accessory app, or hide-to-tray), capture as gaps.
  </action>
  <what-built>
    macOS native polish: a global summon chord (D-01 `CommandOrControl+Shift+D`) registered through the platform seam; single-instance (second launch focuses the existing window); a tray icon + menu (Show/Quit); and window-geometry persistence across relaunch — all behind `src/lib/platform/` with no @tauri-apps import outside tauri.ts. A fresh `tauri build` produced a runnable bundle; full unit suite (decoder 19 green), tsc, lint, the e2e suite, and a WCAG-AA audit all pass.
  </what-built>
  <how-to-verify>
    On the freshly-built packaged macOS app (NOT `tauri dev` — these behaviors only manifest in the real bundle):
    1. **Global summon (NAT-01)**: switch to a DIFFERENT app (e.g. Finder/Safari), press `Cmd+Shift+D` → DevTools comes to the front AND has keyboard focus (type into a tool input immediately). Try it when the window is minimized and when it's behind other apps. (This is the macOS focus-regression check, A1 — if it appears but lacks focus, that's the Pitfall-1 fallback signal.)
    2. **Tray (NAT-02)**: a tray icon is present in the menu bar; click it → a menu with "Show DevTools" + "Quit"; click Show with the window minimized → it restores + focuses; click Quit → app exits.
    3. **Single-instance (NAT-02)**: with the app running, launch the packaged `.app` AGAIN (double-click / `open`) → the existing window is focused, NO second window/instance appears. Try it with the window minimized too (Pitfall 5).
    4. **Window geometry (SHL-05 / D-11)**: move + resize the window, Quit fully, relaunch → it restores to the last position + size (and still opens to the last-used tool — Pitfall 6 / A5; no blank-window flash on launch).
    5. **Offline + tools intact**: all six tools still function (paste-instant, ≤1-keystroke copy, status bar); ⌘K still switches tools; no network access.
    6. **DEFERRED PHASE-4 CHECKS (batched)**: run the 5 pending per-tool checks from `.planning/phases/04-catalogue/04-HUMAN-UAT.md` (Unix Time / JWT / Hash / UUID-ULID + cross-cutting) on this same build.
    7. **Confirm the adopted defaults** (chord / dock-app / minimize / JS-handler) or state desired changes.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5 AND sign off the deferred Phase 4, or describe any issues / default changes (addressed via `/gsd-plan-phase 5 --gaps`).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 5 complete + sign off Phase 4 (ROADMAP / REQUIREMENTS / STATE / UAT) — after approval</name>
  <read_first>
    - .planning/REQUIREMENTS.md (NAT-01/NAT-02 + the Traceability table)
    - .planning/ROADMAP.md (Phase 5 entry + Progress table + Phase 4 sign-off note)
    - .planning/STATE.md (Current Position / Recent Activity / Next Step / verification-debt note)
    - .planning/phases/04-catalogue/04-HUMAN-UAT.md (the deferred per-tool checks to mark resolved)
  </read_first>
  <action>
    ONLY after the human types "approved":
    1. `.planning/REQUIREMENTS.md`: mark NAT-01 + NAT-02 as `[x]` Complete (note: plan + behavior), and update their Traceability rows from Pending → Complete. Promote SHL-05 from PARTIAL → Complete (the deferred window-geometry clause now ships via tauri-plugin-window-state, D-11) and update its Traceability row.
    2. `.planning/ROADMAP.md`: mark Phase 5 `[x]` complete in the Phases list + Phase Details; set the Progress table row `5. Native Polish` to its actual plan count · Complete · today's date. Update the Phase-4 row note to reflect the sign-off is now done (no longer deferred).
    3. `.planning/phases/04-catalogue/04-HUMAN-UAT.md`: mark the 5 pending checks resolved/approved (status → complete) since they were verified in this batched session — ONLY if the user actually confirmed them; if the user approved Phase 5 but flagged Phase-4 issues, leave 04-HUMAN-UAT open and capture the issues.
    4. `.planning/STATE.md`: update Current Position to "Phase 5 COMPLETE + signed off (Phase 4 sign-off batched + resolved)", record the sign-off + the confirmed/adjusted adopted defaults (D-01..04), clear the Phase-4 verification-debt note, and set the Next Step to "Phase 6 (Distribution)". Keep the OPEN BACKLOG protobuf decimal-byte-array note.
    Do NOT mark anything complete before approval.
  </action>
  <verify>
    <automated>grep -q "x.*NAT-01" .planning/REQUIREMENTS.md && grep -q "x.*NAT-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - After approval: NAT-01 + NAT-02 are `[x]` in REQUIREMENTS.md and Complete in the Traceability table; SHL-05 promoted to Complete.
    - ROADMAP.md Phase 5 marked `[x]` and the Progress table row updated to Complete with today's date; the Phase-4 deferred-sign-off note updated.
    - 04-HUMAN-UAT.md reflects the batched resolution (only if the user confirmed the Phase-4 checks).
    - STATE.md Current Position reflects "Phase 5 COMPLETE + signed off", records the confirmed adopted defaults, clears the Phase-4 verification debt, and Next Step points to Phase 6.
  </acceptance_criteria>
  <done>Phase 5 marked complete + Phase 4 signed off (if confirmed) across ROADMAP/REQUIREMENTS/STATE/UAT after human approval; Next Step set to Phase 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged webview vs dev | Production bundle behavior (macOS focus, window-state restore, single-instance) differs from `tauri dev` and from the WebDriver gate. |
| second process → running app | A second launch's argv/cwd reach the single-instance callback (untrusted). |
| build profile → release artifact | The debug-only webdriver server must stay out of the release bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | production-only macOS behavior divergence (focus regression A1, restore flash A5, hide-vs-minimize) | mitigate | Not automatable by the WebDriver gate — verified by the human packaged-build sign-off (Task 2 steps 1-4). Fresh `tauri build` + packaged smoke-test precede sign-off. |
| T-05-02 | Tampering / Spoofing | single-instance argv handling on the real second launch | mitigate | The Rust callback ignores argv (Plan 01); Task-2 single-instance check confirms a second launch only focuses, never acts on argv. |
| T-01-10 / T-01-11 | Elevation of Privilege | webdriver leaking into the released bundle | mitigate | Task-1 release audit `cargo tree --release | grep -c webdriver` = 0 before the build ships. |
| T-05-10 | Repudiation | closing the phase without a verifiable record | mitigate | WCAG-AA audit recorded in 05-UI-REVIEW.md; ROADMAP/REQUIREMENTS/STATE/UAT updated only after explicit "approved". |
</threat_model>

<verification>
- All automated gates green (vitest decoder-19, tsc, eslint, e2e); seam/release/offline/capability audits clean; fresh `tauri build` runs.
- WCAG-AA audit passes and is recorded; the human signs off on the packaged build covering NAT-01/NAT-02/SHL-05 (and the batched Phase-4 checks).
- Phase 5 marked complete (and Phase 4 signed off) across the planning docs after approval.
</verification>

<success_criteria>
- NAT-01 + NAT-02 Complete; SHL-05 promoted to Complete (window geometry ships).
- macOS native polish verified to the binding harness (review → unit → real-webview → phase-boundary human sign-off + WCAG-AA), with the OS-level behaviors human-verified.
- No regression to the decoder's 19 tests or any Phase-1..4 behavior; offline + seam discipline intact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-native-polish/05-04-SUMMARY.md`.
</output>
