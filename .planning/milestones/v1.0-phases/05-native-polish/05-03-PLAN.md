---
phase: 05-native-polish
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/shell/summon.ts
  - src/shell/summon.test.ts
  - src/main.tsx
  - test/e2e/summon.e2e.ts
autonomous: true
requirements: [NAT-01]
user_setup: []

must_haves:
  truths:
    - "On startup the app registers a single named global summon chord through the platform seam (not @tauri-apps directly)."
    - "When the chord fires, the app unminimizes → shows → focuses the window in that order (macOS focus-regression-safe)."
    - "The summon chord is a single named constant, trivially changeable in one place."
    - "Registration failure (chord already taken) degrades gracefully — it does not crash startup."
  artifacts:
    - path: "src/shell/summon.ts"
      provides: "SUMMON_CHORD constant + registerSummon() that wires the seam"
      contains: "SUMMON_CHORD"
    - path: "src/shell/summon.test.ts"
      provides: "unit coverage that registerSummon calls the seam (mocked), summon order is unminimize→show→setFocus"
      contains: "registerSummon"
    - path: "src/main.tsx"
      provides: "registerSummon() called once after initPlatform()"
      contains: "registerSummon"
  key_links:
    - from: "src/shell/summon.ts registerSummon"
      to: "platform.nativeShortcut.register + platform.window.*"
      via: "the platform seam (never @tauri-apps)"
      pattern: "platform\\.nativeShortcut\\.register"
    - from: "src/main.tsx"
      to: "registerSummon()"
      via: "after initPlatform() resolves"
      pattern: "registerSummon"
---

<objective>
Wire NAT-01 end-to-end in the shell: a single `summon.ts` module owns the named summon chord constant and a `registerSummon()` function that, after `initPlatform()`, registers the global hotkey through the platform seam and — on fire — summons the window via `platform.window` in the macOS-safe order. Called once from `main.tsx`. Consumes Plan 02's `platform.nativeShortcut` + `platform.window` seam; imports NO `@tauri-apps/*`.

Purpose: complete NAT-01 (global hotkey summons + focuses the app) using only the seam, keeping the chord trivially changeable and registration failures non-fatal.
Output: a tested `summon.ts`, the startup wiring, and an e2e spec covering the in-webview-observable surface (deep-link via hash route, app still launches with the chord registered). The OS-level "hotkey from another app focuses the window" is verified at the Plan-04 human sign-off (not automatable).

Wave 2: depends on Plan 02 (the seam). Plan 01 (Rust) runs in parallel in Wave 1.

ADOPTED DEFAULTS (pending user confirmation at sign-off) implemented here:
- **D-01 — summon chord = `CommandOrControl+Shift+D`** (`CommandOrControl` → Cmd on macOS; avoids Spotlight's Cmd+Space and the screenshot Cmd+Shift+3/4 chords). A single named `SUMMON_CHORD` constant — change one line to adjust.
- **D-04 — global-shortcut handler lives in the JS seam first** (clean seam discipline); if packaged-build focus proves flaky at sign-off, the swap to a Rust handler is isolated (registration is one function). The summon body calls `unminimize()` → `show()` → `setFocus()` (D-03 order, Tauri 2.3+ regression #12834).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-native-polish/05-RESEARCH.md
@.planning/phases/05-native-polish/05-VALIDATION.md

<interfaces>
<!-- Plan 02 delivers this seam surface (call it, never @tauri-apps): -->
```typescript
platform.nativeShortcut.register(accelerator: string, handler: () => void): Promise<void>
platform.nativeShortcut.unregister(accelerator: string): Promise<void>
platform.nativeShortcut.isRegistered(accelerator: string): Promise<boolean>
platform.window.unminimize(): Promise<void>
platform.window.show(): Promise<void>
platform.window.setFocus(): Promise<void>
```

initPlatform() (src/lib/platform/index.ts) is memoised — await it before registering.
main.tsx already calls `void initPlatform().catch(...)` before render; add the summon
registration onto that resolved promise.

Deep-link discipline (HashRouter ONLY — CLAUDE.md): to deep-link on summon, set
`window.location.hash = "#/tools/<id>"`. Any id from an untrusted source MUST be
validated through resolveStartupTool/getToolById (ENABLED_TOOLS only) first. For v1
the summon does NOT deep-link to a specific tool (it just summons the current window) —
keep the hash-navigation helper present but guarded, so a future deep-link reuses the
validated path. NEVER use BrowserRouter / location.assign to a path.

setPlatformForTest(stub) (index.ts) lets the unit test inject a vi.fn()-backed seam.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: summon.ts — chord constant + registerSummon() over the seam</name>
  <read_first>
    - src/lib/platform/index.ts (platform accessor + initPlatform + setPlatformForTest)
    - src/shell/resolveStartupTool.ts (the validated deep-link path to reuse if/when summon navigates)
    - .planning/phases/05-native-polish/05-RESEARCH.md §"Code Examples" (JS register + deep-link) + §"Common Pitfalls" 1/2
  </read_first>
  <behavior>
    - Test: `registerSummon()` calls `platform.nativeShortcut.register` with `SUMMON_CHORD` (inject a vi.fn() stub via setPlatformForTest).
    - Test: invoking the registered handler calls `platform.window.unminimize`, then `show`, then `setFocus` — IN THAT ORDER (assert call order).
    - Test: if `register` rejects (chord taken), `registerSummon()` resolves/does not throw (graceful degrade) — handler is wrapped so a thrown registration is caught and logged, not propagated.
    - Test: `SUMMON_CHORD` is the exact string `"CommandOrControl+Shift+D"` (a single named constant).
  </behavior>
  <action>
    Create `src/shell/summon.ts`:
    - Export `export const SUMMON_CHORD = "CommandOrControl+Shift+D";` with a comment that this is the single source of truth for the chord (D-01) — change one line to adjust.
    - Export `async function registerSummon(): Promise<void>` that:
      1. `await initPlatform();` (ensures the real Tauri impl is installed before registering).
      2. Calls `await platform.nativeShortcut.register(SUMMON_CHORD, summon)` where `summon` is an internal async fn that runs `await platform.window.unminimize(); await platform.window.show(); await platform.window.setFocus();` in that exact order (D-03 / Pitfall 1).
      3. Wraps the register call in try/catch — on failure (chord already taken, Pitfall 2) log a non-fatal warning (`console.warn`) and resolve, never throw (startup must not crash).
    - Keep a guarded private helper `deepLink(id: string)` that validates via `resolveStartupTool`/`getToolById` and sets `window.location.hash` ONLY for a known ENABLED tool — NOT called by the v1 summon, present for future use (HashRouter discipline, threat T-05-02). Do NOT import @tauri-apps anywhere in this file.
    Create `src/shell/summon.test.ts` covering the four behaviors above using `setPlatformForTest` with vi.fn()-backed `nativeShortcut`/`window` stubs (assert call order with `mock.invocationCallOrder` or sequential `expect`/captured-handler invocation).
    Run `pnpm vitest run src/shell/summon.test.ts` (pass) + `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>grep -q 'SUMMON_CHORD = "CommandOrControl+Shift+D"' src/shell/summon.ts && [ "$(grep -c '@tauri-apps' src/shell/summon.ts)" = "0" ] && pnpm vitest run src/shell/summon.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/shell/summon.ts` exports `SUMMON_CHORD === "CommandOrControl+Shift+D"` and `registerSummon`.
    - `registerSummon` calls `await initPlatform()` then `platform.nativeShortcut.register(SUMMON_CHORD, ...)`; the handler calls `platform.window.unminimize` → `show` → `setFocus` in that order.
    - A rejecting `register` does NOT throw out of `registerSummon` (try/catch + console.warn) — covered by a test.
    - `grep -c '@tauri-apps' src/shell/summon.ts` = 0 (seam only).
    - `pnpm vitest run src/shell/summon.test.ts` passes; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>summon.ts owns the named chord + a seam-only registerSummon with correct summon order + graceful register-failure handling, unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire registerSummon() into startup + real-WKWebView e2e</name>
  <read_first>
    - src/main.tsx (the existing `void initPlatform().catch(...)` preload before render)
    - test/e2e/uuid-ulid.e2e.ts (an existing e2e spec — the pattern/structure to mirror: navigate to a hash route, assert DOM)
    - scripts/e2e-spike.sh (the real-WKWebView runner) + wdio.conf.ts (globs test/e2e/*.e2e.ts)
    - .planning/phases/05-native-polish/05-VALIDATION.md §"Wave 0 Requirements" + §"Manual-Only Verifications"
  </read_first>
  <action>
    **A. `src/main.tsx`** — call `registerSummon()` once after the platform init resolves. Chain it onto the existing preload so it runs after `initPlatform()` and never blocks render:
    ```typescript
    void initPlatform()
      .then(() => registerSummon())
      .catch((err) => console.error("[platform] init/summon failed:", err));
    ```
    (Import `registerSummon` from `./shell/summon`.) registerSummon already awaits initPlatform internally — the chain just guarantees ordering and keeps the single catch.

    **B. `test/e2e/summon.e2e.ts`** — add a real-WKWebView spec (the standing harness rule: each behavior gets a `test/e2e/<name>.e2e.ts` run via `scripts/e2e-spike.sh`). Since the OS-global hotkey CANNOT be fired from WebDriver (it lives outside the webview — see VALIDATION Manual-Only), this spec asserts the WEBVIEW-OBSERVABLE surface only:
      1. The app launches with the three new Rust plugins + the summon registration WITHOUT a blank/crashed window — navigate to `#/tools/protobuf-decoder` and assert the hero tool's root renders (proves window-state `visible:false` + startup still reaches a tool, Pitfall 6 / A5).
      2. A hash-route deep-link works: set `#/tools/base64`, assert the Base64 tool renders (proves the HashRouter deep-link path the summon would use).
    Mirror the structure of an existing spec (selectors, waits). Then run `bash scripts/e2e-spike.sh` and confirm it passes on webkit (all existing specs + this one).

    Run `pnpm vitest run` + `npx tsc --noEmit` + `pnpm eslint .` to confirm no regression.
  </action>
  <verify>
    <automated>grep -q 'registerSummon' src/main.tsx && test -f test/e2e/summon.e2e.ts && pnpm vitest run && npx tsc --noEmit && pnpm eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.tsx` imports and calls `registerSummon()` exactly once, chained after `initPlatform()`, inside the existing single `.catch`.
    - `test/e2e/summon.e2e.ts` exists, navigates to at least two hash routes (`#/tools/protobuf-decoder` and `#/tools/base64`) and asserts each tool renders (window not blank with `visible:false`; HashRouter deep-link works).
    - `bash scripts/e2e-spike.sh` exits 0 on webkit (existing specs + summon spec).
    - `pnpm vitest run` green (decoder 19 untouched), `npx tsc --noEmit` exit 0, `pnpm eslint .` 0 errors.
  </acceptance_criteria>
  <done>Summon registration runs once at startup; the real-WKWebView e2e proves the app launches with the new plugins and the HashRouter deep-link path works; full gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS keyboard → app | The global chord originates at the OS level; the chosen chord could collide with another app's registration. |
| deep-link id → router | Any id used to deep-link on summon is untrusted input crossing into navigation. |
| shell → native | The summon flow must reach native control via the seam, never @tauri-apps directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | chord-collision silently disables summon | mitigate | `registerSummon` try/catch logs a non-fatal warning on register failure; the chord is a single named constant easily changed; `isRegistered` available for a future status check. Confirmed at human sign-off (OS-level). |
| T-05-08 | Tampering / Spoofing | deep-link target on summon | mitigate | The `deepLink` helper validates any id through `resolveStartupTool`/`getToolById` (ENABLED_TOOLS only) before setting `window.location.hash`; v1 summon does not deep-link. HashRouter only — never BrowserRouter/path navigation. |
| T-05-04 | Tampering | seam discipline in summon.ts | mitigate | `grep -c '@tauri-apps' src/shell/summon.ts` = 0; all native access via the platform seam (FND-04). |
</threat_model>

<verification>
- registerSummon registers `CommandOrControl+Shift+D` through the seam and summons via unminimize→show→setFocus; register failure is non-fatal.
- summon.ts imports no @tauri-apps; deep-link path validated through ENABLED_TOOLS; HashRouter only.
- main.tsx calls registerSummon once after init; e2e proves the app launches + HashRouter deep-link works; full gate green.
</verification>

<success_criteria>
- NAT-01 wired: a global chord summons + focuses the app (OS behavior confirmed at the Plan-04 human sign-off; seam + wiring + deep-link path automated-tested here).
- Decoder + 19 tests untouched; no @tauri-apps import outside tauri.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-native-polish/05-03-SUMMARY.md`.
</output>
