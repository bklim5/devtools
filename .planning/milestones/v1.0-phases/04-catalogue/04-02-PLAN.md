---
phase: 04-catalogue
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/tools/unix-time/UnixTimeTool.tsx
  - src/tools/unix-time/UnixTimeTool.test.tsx
  - src/tools/unix-time/index.ts
  - test/e2e/unix-time.e2e.ts
autonomous: true
requirements: [TIME-01]
user_setup: []

must_haves:
  truths:
    - "Pasting a unix timestamp (s or ms) instantly shows human-readable LOCAL and UTC datetimes plus ISO, with no convert button."
    - "The unit (s/ms) is auto-detected by magnitude with a manual override toggle (mirrors Protobuf detect+override)."
    - "An editable datetime/ISO field derives the timestamp back (two-way), and a live 'now' unix time is shown with ≤1-keystroke copy."
    - "Empty input is the neutral state, not an error; malformed input is a field-scoped explicit error."
    - "The tool renders the shared status bar and the registry component is the real UnixTimeTool (not makePlaceholder)."
  artifacts:
    - path: "src/tools/unix-time/UnixTimeTool.tsx"
      provides: "Two-way Unix Time converter UI over src/lib/timeFormat"
      min_lines: 60
    - path: "src/tools/unix-time/index.ts"
      provides: "Registry entry with component: UnixTimeTool"
      contains: "component: UnixTimeTool"
    - path: "test/e2e/unix-time.e2e.ts"
      provides: "Real-WKWebView gate for paste→datetime + copy"
  key_links:
    - from: "src/tools/unix-time/UnixTimeTool.tsx"
      to: "src/lib/timeFormat.ts"
      via: "import formatTimestamp/classifyUnit/toUnixFromIso"
      pattern: "@/lib/timeFormat"
    - from: "src/tools/unix-time/index.ts"
      to: "UnixTimeTool"
      via: "registry component swap"
      pattern: "component: UnixTimeTool"
---

<objective>
Ship the Unix Time tool (TIME-01) into the registry-driven shell. Paste a unix timestamp (s/ms) → human-readable LOCAL + UTC + ISO instantly; auto-detect the unit by magnitude with a manual override; an editable ISO/datetime field derives the timestamp back (two-way, D-06); a live "now" with ≤1-keystroke copy. All cross-cutting UX (paste-instant, visible focusable copy, status bar, WCAG-AA, layout-agnostic) reused from Phase 3.

Purpose: complete TIME-01; reuse the shared `timeFormat.ts` (Plan 01) so JWT (Plan 03) shares the same humanization.
Output: real UnixTimeTool swapped into the registry + its TDD + real-webview e2e gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-catalogue/04-CONTEXT.md
@.planning/phases/04-catalogue/04-RESEARCH.md

<interfaces>
<!-- From src/lib/timeFormat.ts (Plan 01 — consume, do NOT re-implement): -->
```typescript
export function formatTimestamp(ms: number): { iso: string; utc: string; local: string }; // throws on invalid
export function relativeTime(targetMs: number, nowMs?: number): string;
export function classifyUnit(value: number): "s" | "ms";
export function toUnixFromIso(isoOrDatetime: string): number; // throws on NaN
```
<!-- From src/components/StatusBar.tsx (Plan 01): -->
```typescript
export type ParseState = "ok" | "error" | "empty";
// <StatusBar parseState byteCount encoding? error? timingMs? />  (encoding optional — omit for this tool)
```
<!-- From src/components/CopyButton.tsx (Plan 01): -->
```typescript
export function CopyButton(props: { value: string; label: string; className?: string }): JSX.Element;
```
<!-- Reference for the detect+override toggle look + accent=selected discipline: src/tools/base64/Base64Tool.tsx AlphabetToggle (aria-pressed, accent-soft when active). -->
<!-- Registry entry to mutate (currently makePlaceholder). From src/tools/unix-time/index.ts: -->
```typescript
export const unixTimeTool: ToolDefinition = {
  id: "unix-time", name: "Unix Time", description: "...", category: "time",
  keywords: ["unix","time","timestamp","epoch"], icon: Clock,
  component: makePlaceholder("Unix Time"),  // ← swap to UnixTimeTool
  enabled: true,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UnixTimeTool — two-way converter over timeFormat (TIME-01)</name>
  <read_first>
    - src/lib/timeFormat.ts + src/lib/timeFormat.test.ts (the shared lib — consume it, do NOT duplicate)
    - src/tools/base64/Base64Tool.tsx (the thin-tool template: timed() wrapper, layout-agnostic panes, focus-ring classes, accent=selected toggle)
    - src/components/StatusBar.tsx + src/components/CopyButton.tsx (Plan 01 shared scaffold)
    - src/shell/useCopyFeedback.ts (already wrapped inside CopyButton)
    - .planning/phases/04-catalogue/04-CONTEXT.md D-05, D-06, D-20
    - src/tools/base64/Base64Tool.test.tsx (test style: jsdom pragma, platform store stub, fireEvent, getByLabelText)
  </read_first>
  <behavior>
    UnixTimeTool.test.tsx (// @vitest-environment jsdom):
    - typing "1469922850" (s) into the timestamp field renders a UTC string and the ISO "2016-07-30T23:54:10.000Z" (auto-detected as seconds)
    - typing "1469922850259" (ms) renders ISO "2016-07-30T23:54:10.259Z" (auto-detected as ms)
    - empty timestamp field → status parseState "empty", NO error node
    - typing "notanumber" → a field-scoped explicit error (aria-invalid + text-bad), status parseState "error", no crash
    - the unit override toggle (s/ms) is present with aria-pressed reflecting the active unit; forcing the other unit re-renders the datetime
    - the reverse field: setting an ISO like "2016-07-30T23:54:10.259Z" derives the timestamp 1469922850259 (ms) into the forward field
    - a "now" readout exists with a visible Copy button (button[aria-label^="Copy"])
  </behavior>
  <action>
    Create `src/tools/unix-time/UnixTimeTool.tsx` — layout-agnostic (responsive Tailwind, no fixed widths, UX-05), default-exported component:
    - **Forward pane**: a `<textarea>`/`<input>` for the timestamp. On every change (paste-instant, UX-01) parse the integer; if empty → neutral. Use `classifyUnit(value)` for the auto-detected unit unless the user has forced an override. Convert to ms (×1000 if unit is "s"). Call `formatTimestamp(ms)` and render LOCAL, UTC, and ISO rows — each row is a labelled output with a `<CopyButton value={...} label="..." />` (≤1 keystroke, visible, UX-02). Wrap the parse in try/catch; on `formatTimestamp` throw or non-numeric input show a field-scoped error node (`aria-invalid` + `text-bad`, never opacity-only, UX-04).
    - **Unit override toggle**: a small s/ms (D-20: µs/ns optional) segmented control mirroring Base64's `AlphabetToggle` — `aria-pressed`, active = `accent-soft`/`accent-line`/`text-accent` (accent = selected ONLY), idle neutral. Selecting a unit forces that interpretation.
    - **Reverse pane (D-06)**: an editable ISO/datetime `<input>`; on change call `toUnixFromIso(value)` (try/catch → field-scoped error on NaN) and write the resulting timestamp (in the active unit) into the forward field's value/state.
    - **Live "now" (D-06)**: `Date.now()` refreshed every 1s via `setInterval` (cleanup on unmount); render the current unix time (in the active unit) with a `<CopyButton>`.
    - **Status bar**: render `<StatusBar parseState={...} byteCount={0} error={firstError} timingMs={...} />` — omit `encoding`. Measure timing with a `timed()` wrapper like Base64. `byteCount` is not meaningful here → pass 0 (the bar still shows parse state · timing · error per UX-03).
    Do NOT import `@tauri-apps/*`; clipboard flows through CopyButton→platform seam. Do NOT duplicate date math — use timeFormat.ts.
    Create `src/tools/unix-time/UnixTimeTool.test.tsx` (jsdom pragma) covering <behavior>; stub the platform store like Base64Tool.test.tsx does.
  </action>
  <verify>
    <automated>npx vitest run src/tools/unix-time/UnixTimeTool.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/tools/unix-time/UnixTimeTool.tsx` is the default export and `grep -q '@/lib/timeFormat' src/tools/unix-time/UnixTimeTool.tsx` succeeds (consumes shared lib).
    - `grep -q '@tauri-apps' src/tools/unix-time/UnixTimeTool.tsx` finds NOTHING.
    - `npx vitest run src/tools/unix-time/UnixTimeTool.test.tsx` exits 0; the suite asserts ISO `2016-07-30T23:54:10.259Z` renders for input `1469922850259` and `2016-07-30T23:54:10.000Z` for `1469922850`.
    - The suite asserts an empty field → no error node, and `"notanumber"` → an aria-invalid field-scoped error.
    - At least one `button[aria-label^="Copy"]` is present and visible (no hover-only copy).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>UnixTimeTool renders forward (local+UTC+ISO) + override + reverse + live-now with focusable copy; suite + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Swap registry component + real-WKWebView e2e gate</name>
  <read_first>
    - src/tools/unix-time/index.ts (swap component: makePlaceholder → UnixTimeTool)
    - src/lib/tools/registry.ts (confirm unixTimeTool already in TOOLS — no array edit needed, just the index swap)
    - test/e2e/base64.e2e.ts (the e2e template: hash-navigate, $ selectors, assertions, screenshot)
    - scripts/e2e-spike.sh (the runner; understand it starts tauri dev --features webdriver and runs pnpm e2e)
  </read_first>
  <action>
    1. In `src/tools/unix-time/index.ts`: replace `component: makePlaceholder("Unix Time")` with `component: UnixTimeTool`, add `import UnixTimeTool from "./UnixTimeTool";`, and remove the now-unused `makePlaceholder` import. (unixTimeTool is ALREADY in `registry.ts`'s TOOLS array — do NOT edit registry.ts.)
    2. Create `test/e2e/unix-time.e2e.ts` mirroring `base64.e2e.ts`: navigate via `window.location.hash = "#/tools/unix-time"`; wait for the timestamp input to exist; type `1469922850259`; assert the ISO `2016-07-30T23:54:10.259Z` text appears in the rendered output; assert a `button[aria-label^="Copy"]` `isDisplayed()` (UX-02, the hover-only-copy gate); save a screenshot to `test/e2e/__screenshots__/unix-time-wkwebview.png`. Give the timestamp input and the ISO output stable selectors (ids/data-attributes) in the component so the spec can find them.
    3. Run the real-webview gate: `bash scripts/e2e-spike.sh` and confirm it passes on webkit.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'component: UnixTimeTool' src/tools/unix-time/index.ts` succeeds; `grep -q 'makePlaceholder' src/tools/unix-time/index.ts` finds NOTHING.
    - `test/e2e/unix-time.e2e.ts` exists and references `#/tools/unix-time` and asserts a `button[aria-label^="Copy"]` is displayed.
    - `bash scripts/e2e-spike.sh` exits 0 (real WKWebView gate; produces `test/e2e/__screenshots__/unix-time-wkwebview.png`).
    - `npm test` green (decoder 19 untouched), `npx tsc --noEmit` exit 0, `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>Registry renders the real Unix Time tool; the real-WKWebView e2e gate passes; full suite + tsc + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted timestamp/ISO → parser | Untrusted numeric/ISO strings cross into parsing + Date construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | timestamp/ISO parsing on adversarial input | mitigate | All parsing wrapped in try/catch via timeFormat.ts (which throws bounded explicit Errors on invalid); empty = neutral; malformed = field-scoped error, never a crash/hang. Tested with `"notanumber"`. |
| T-04-06 | Information Disclosure | logging/sending pasted values | accept | Offline, display-only; no network, no logging of input; clipboard write only on explicit user copy. |
</threat_model>

<verification>
- Unix Time tool renders in the shell at `#/tools/unix-time` (registry swap).
- Paste-instant forward (local+UTC+ISO), unit auto-detect+override, reverse derive, live now+copy all work.
- `npm test` green (decoder 19), tsc clean, lint 0; real-WKWebView e2e passes.
</verification>

<success_criteria>
- TIME-01 satisfied: paste s/ms → local+UTC datetimes + ISO, and the reverse (ISO→timestamp), plus live now.
- UX-01..05 honored (paste-instant, ≤1-keystroke visible copy, status bar, WCAG-AA, layout-agnostic).
- Real component swapped into the registry; real-webview gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalogue/04-02-SUMMARY.md`.
</output>
