---
phase: 04-catalogue
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/tools/uuid-ulid/decodeId.ts
  - src/tools/uuid-ulid/decodeId.test.ts
  - src/tools/uuid-ulid/UuidUlidTool.tsx
  - src/tools/uuid-ulid/UuidUlidTool.test.tsx
  - src/tools/uuid-ulid/index.ts
  - test/e2e/uuid-ulid.e2e.ts
autonomous: true
requirements: [UID-01]
user_setup: []

must_haves:
  truths:
    - "The tool generates UUID v4 + UUID v7 + ULID; one is generated on open and a single keystroke regenerates."
    - "An optional count produces a batch list, each entry copyable in ≤1 keystroke, plus a copy-all."
    - "Pasting a UUID or ULID auto-detects which it is and shows a full breakdown (UUID: version + variant + embedded timestamp for v1/v7; ULID: decoded timestamp + randomness)."
    - "Malformed input is flagged explicitly (D-08 pattern), never a crash; the registry renders the real UuidUlidTool."
  artifacts:
    - path: "src/tools/uuid-ulid/decodeId.ts"
      provides: "Auto-detect UUID vs ULID + full breakdown"
      exports: ["decodeId"]
    - path: "src/tools/uuid-ulid/UuidUlidTool.tsx"
      provides: "Generate (v4/v7/ULID) + batch + decode UI"
      min_lines: 60
    - path: "src/tools/uuid-ulid/index.ts"
      provides: "Registry entry component swapped to UuidUlidTool"
      contains: "component: UuidUlidTool"
  key_links:
    - from: "src/tools/uuid-ulid/UuidUlidTool.tsx"
      to: "src/lib/ulid.ts + src/lib/uuidv7.ts"
      via: "generateUlid / generateUuidV7 / crypto.randomUUID"
      pattern: "@/lib/ulid"
    - from: "src/tools/uuid-ulid/decodeId.ts"
      to: "src/lib/ulid.ts + src/lib/uuidv7.ts"
      via: "decodeUlid / decodeUuid"
      pattern: "decodeUuid"
---

<objective>
Ship the UUID/ULID tool (UID-01) into the registry. Generate UUID v4 (`crypto.randomUUID`) + UUID v7 (`generateUuidV7`, Plan 01) + ULID (`generateUlid`, Plan 01) (D-15); generate one on open, single keystroke regenerates, optional count → batch list each copyable + copy-all (D-16). Decode: auto-detect UUID vs ULID from a pasted string → full breakdown (UUID: version + variant + embedded timestamp for v1/v7; ULID: decoded timestamp + randomness) (D-17). Malformed flagged explicitly (D-08 pattern).

Purpose: complete UID-01; consume the Plan-01 `ulid.ts` + `uuidv7.ts` pure libs (no duplication).
Output: pure `decodeId.ts` + UuidUlidTool UI + component swap + real-webview e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-catalogue/04-CONTEXT.md
@.planning/phases/04-catalogue/04-RESEARCH.md
@.planning/phases/04-catalogue/04-VALIDATION.md

<interfaces>
<!-- Plan 01 pure libs (consume, do NOT re-implement). From src/lib/ulid.ts + src/lib/uuidv7.ts: -->
```typescript
// ulid.ts
export function generateUlid(nowMs?: number, rand?: Uint8Array): string;     // 26-char Crockford base32
export function decodeUlid(ulid: string): { tsMs: number; randomness: Uint8Array }; // throws on malformed
// uuidv7.ts
export function generateUuidV7(nowMs?: number, rand?: Uint8Array): string;   // dashed v7
export function buildUuidV7(unixMs: number, rand: Uint8Array): string;
export function decodeUuid(uuid: string): { version: number; variant: string; tsMs?: number }; // throws on malformed
```
<!-- Native: crypto.randomUUID() → v4 dashed string. -->
<!-- timeFormat.ts (Plan 01): formatTimestamp(ms) for humanizing the embedded/decoded timestamps. -->
<!-- StatusBar + CopyButton (Plan 01, src/components/). encoding optional → omit. -->
<!-- src/tools/uuid-ulid/index.ts created in Plan 01 with component: makePlaceholder("UUID / ULID") — swap to UuidUlidTool here. Do NOT edit registry.ts. -->
<!-- Reference: src/tools/protobuf-decoder per-node copy + src/tools/base64/Base64Tool.tsx toggle/look. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: decodeId pure module — auto-detect UUID vs ULID + breakdown (D-17)</name>
  <read_first>
    - src/lib/ulid.ts + src/lib/uuidv7.ts (Plan 01 — decodeUlid / decodeUuid; consume, do NOT re-implement)
    - .planning/phases/04-catalogue/04-RESEARCH.md §"ULID — Exact Spec" + §"UUID v7 — Exact Layout" (decode assertions, the detect rules)
    - .planning/phases/04-catalogue/04-CONTEXT.md D-17, D-08
    - src/tools/jwt/decodeJwt.ts (if it exists in this wave — same discriminated-union error-result style; otherwise mirror protobuf-decoder's result-object style)
  </read_first>
  <behavior>
    decodeId.test.ts:
    - decodeId("017f22e2-79b0-7cc3-98c4-dc0c0c180cc3") → { kind:"ok", type:"uuid", version:7, variant:"10", tsMs:1645557742000 }
    - decodeId(crypto.randomUUID()) → { kind:"ok", type:"uuid", version:4, variant:"10" } (no tsMs)
    - decodeId("01ARZ3NDEKTSV4RRFFQ69G5FAV") → { kind:"ok", type:"ulid", tsMs:1469922850259, randomness:<Uint8Array len 10> }
    - decodeId("") → { kind:"empty" } (neutral, not an error)
    - decodeId("not-an-id") and decodeId("01ARZ3NDEKTSV4RRFFQ69G5FA") (25 chars) → { kind:"error", message:<explicit> } (no crash)
  </behavior>
  <action>
    Create `src/tools/uuid-ulid/decodeId.ts` (pure, imports `@/lib/ulid` + `@/lib/uuidv7`):
    - `decodeId(input: string): DecodedId` discriminated union:
      `{ kind:"empty" }` | `{ kind:"error"; message:string }` |
      `{ kind:"ok"; type:"uuid"; version:number; variant:string; tsMs?:number }` |
      `{ kind:"ok"; type:"ulid"; tsMs:number; randomness:Uint8Array }`.
    - Detect: trim; empty → `{kind:"empty"}`. If it matches the UUID shape (32 hex nibbles with the 8-4-4-4-12 dash layout, case-insensitive) → try `decodeUuid` (catch → error). Else if it is 26 chars all in the Crockford alphabet → try `decodeUlid` (catch → error). Else → `{kind:"error", message:"Not a recognized UUID or ULID"}`.
    - Wrap so it NEVER throws past the boundary (T-04-14). All malformed input returns an explicit error result.
    Create `src/tools/uuid-ulid/decodeId.test.ts` with the <behavior> vectors.
  </action>
  <verify>
    <automated>npx vitest run src/tools/uuid-ulid/decodeId.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/tools/uuid-ulid/decodeId.ts` exports `decodeId` and `grep -q 'decodeUuid' src/tools/uuid-ulid/decodeId.ts && grep -q 'decodeUlid' src/tools/uuid-ulid/decodeId.ts` succeeds (consumes Plan-01 libs).
    - `npx vitest run src/tools/uuid-ulid/decodeId.test.ts` exits 0; the suite asserts the v7 vector → version 7 + tsMs 1645557742000, the ULID vector → tsMs 1469922850259, and that a 25-char / "not-an-id" input returns a `kind:"error"` (no throw).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>decodeId auto-detects UUID vs ULID and returns a full breakdown or an explicit error without throwing; suite + tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UuidUlidTool UI (generate v4/v7/ULID + batch + decode) + swap + e2e (UID-01/D-15/D-16)</name>
  <read_first>
    - src/tools/uuid-ulid/decodeId.ts (Task 1 output)
    - src/lib/ulid.ts + src/lib/uuidv7.ts (generateUlid / generateUuidV7) + crypto.randomUUID (v4)
    - src/lib/timeFormat.ts (formatTimestamp — humanize decoded/embedded timestamps)
    - src/components/StatusBar.tsx + src/components/CopyButton.tsx (Plan 01 — CopyButton for each generated/decoded row + copy-all)
    - src/tools/base64/Base64Tool.tsx (toggle look; accent=selected; focus-ring classes)
    - src/tools/uuid-ulid/index.ts (created in Plan 01 — swap component to UuidUlidTool)
    - .planning/phases/04-catalogue/04-CONTEXT.md D-15, D-16, D-17, D-18, D-20
    - test/e2e/base64.e2e.ts (e2e template)
  </read_first>
  <behavior>
    UuidUlidTool.test.tsx (// @vitest-environment jsdom):
    - on mount, at least one generated id is rendered (generate-on-open, D-16) with a button[aria-label^="Copy"]
    - a "Generate"/regenerate control exists; clicking it (or pressing Enter on the default-focused button) produces a DIFFERENT id (regeneration)
    - the kind toggle (UUID v4 / UUID v7 / ULID) is present (aria-pressed); selecting ULID generates a 26-char Crockford string; selecting UUID v7 generates a dashed v7 (version nibble 7)
    - setting a batch count of 3 renders 3 entries, each with its own copy button, plus a copy-all control
    - pasting the v7 vector "017f22e2-79b0-7cc3-98c4-dc0c0c180cc3" into the decode field shows version 7 and the humanized timestamp; pasting the ULID vector shows the decoded timestamp
    - pasting "not-an-id" shows an explicit error (text-bad), no crash
  </behavior>
  <action>
    1. Create `src/tools/uuid-ulid/UuidUlidTool.tsx` — layout-agnostic default export, two regions:
       - **Generate region**: a kind toggle (UUID v4 / UUID v7 / ULID) mirroring Base64's `AlphabetToggle` (aria-pressed, accent=selected only). Generate ONE on mount (`useEffect` once) per the selected kind: v4 = `crypto.randomUUID()`, v7 = `generateUuidV7()`, ULID = `generateUlid()`. A default-focused "Generate" `<button>` regenerates on click / Enter (≤1 keystroke, D-16). Each generated value is a row with a `<CopyButton value={id} label="UUID"|"ULID" />`. An optional **count** control (D-16/D-20 design discretion — e.g. a small number input, default 1): count>1 → render that many freshly-generated entries, each with its own copy, plus a **Copy all** button (newline-joined, via CopyButton or a dedicated button → platform.clipboard).
       - **Decode region**: a `<textarea>`/`<input>`; on every change (paste-instant) call `decodeId(value)`. `kind:"empty"` → neutral; `kind:"error"` → field-scoped error node (`aria-invalid`+`text-bad`); `kind:"ok"` → render the breakdown: for UUID show type/version/variant + (if tsMs) `formatTimestamp(tsMs)` humanized; for ULID show the decoded timestamp (`formatTimestamp(tsMs)`) + the randomness bytes (hex via bytesToHex). Each readout copyable where useful.
       - **Status bar**: `<StatusBar parseState byteCount={0} error={decodeError} timingMs={...} />` (omit encoding). parseState reflects the decode region.
       Randomness for generation MUST come from the Plan-01 libs / `crypto` (never Math.random). Do NOT import `@tauri-apps/*`; copy via CopyButton→platform seam. Do NOT re-implement ULID/UUID encode/decode — use the libs.
    2. In `src/tools/uuid-ulid/index.ts` (Plan 01): change `component: makePlaceholder("UUID / ULID")` → `component: UuidUlidTool`, add `import UuidUlidTool from "./UuidUlidTool";`, remove the unused makePlaceholder import. Do NOT touch registry.ts.
    3. Create `test/e2e/uuid-ulid.e2e.ts` mirroring base64.e2e.ts: navigate to `#/tools/uuid-ulid`; assert a generated id is present on open with a displayed copy button; click Generate and assert the value changes; paste the ULID vector into the decode field and assert the decoded timestamp text appears; screenshot to `uuid-ulid-wkwebview.png`. (crypto.randomUUID / getRandomValues secure-context is also exercised here on the real WKWebView, A1.) Then RUN `bash scripts/e2e-spike.sh`.
    Create `src/tools/uuid-ulid/UuidUlidTool.test.tsx` (jsdom) covering <behavior>; stub the platform store as Base64Tool.test.tsx does.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'component: UuidUlidTool' src/tools/uuid-ulid/index.ts` succeeds; `grep -q 'makePlaceholder' src/tools/uuid-ulid/index.ts` finds NOTHING.
    - `grep -q '@/lib/ulid' src/tools/uuid-ulid/UuidUlidTool.tsx && grep -q '@/lib/uuidv7' src/tools/uuid-ulid/UuidUlidTool.tsx` succeed (consumes Plan-01 libs, no duplication).
    - `grep -E 'Math.random' src/tools/uuid-ulid/UuidUlidTool.tsx` finds NOTHING.
    - `grep -q '@tauri-apps' src/tools/uuid-ulid/UuidUlidTool.tsx` finds NOTHING.
    - `npx vitest run src/tools/uuid-ulid/UuidUlidTool.test.tsx` exits 0; the suite asserts generate-on-open, regeneration changes the value, batch count 3 → 3 entries + copy-all, and decoding the v7 vector shows version 7.
    - `test/e2e/uuid-ulid.e2e.ts` exists, references `#/tools/uuid-ulid`, asserts a displayed copy button and a value change on Generate; `bash scripts/e2e-spike.sh` exits 0.
    - `npm test` green (decoder 19 untouched), `npx tsc --noEmit` exit 0, `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>UUID/ULID tool generates v4/v7/ULID (on open + 1-keystroke regen + batch + copy-all) and decodes pasted ids with full breakdown + explicit errors; component swapped; real-WKWebView gate green; full suite + tsc + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted id → decoder | Untrusted UUID/ULID string crosses into detect + decode. |
| randomness source | Generated ids must use a CSPRNG. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Denial of Service | decodeId on adversarial input | mitigate | Length/charset/hex validated before parsing; decodeId NEVER throws past its boundary; malformed → explicit error result; tested with 25-char / non-id input. |
| T-04-15 | Information Disclosure | weak randomness in generated ids | mitigate | Generation uses `crypto.randomUUID` (v4) + `crypto.getRandomValues` (v7/ULID via Plan-01 libs), never `Math.random`; grep-asserted. |
| T-04-16 | Information Disclosure | logging/sending pasted ids | accept | Offline, display-only; no network; clipboard write only on explicit user copy. |
</threat_model>

<verification>
- UUID/ULID renders in the shell at `#/tools/uuid-ulid` (component swapped; entry pre-registered in Plan 01).
- Generate v4/v7/ULID on open + 1-keystroke regen + batch + copy-all; decode auto-detect + breakdown + explicit errors.
- crypto generation confirmed on the REAL WKWebView (A1).
- `npm test` green (decoder 19), tsc clean, lint 0; real-WKWebView e2e passes.
</verification>

<success_criteria>
- UID-01 satisfied: generate UUIDs/ULIDs with one keystroke; decode a pasted UUID/ULID into its components.
- D-15 (v4+v7+ULID), D-16 (on-open + 1-keystroke regen + batch + copy-all), D-17 (auto-detect + breakdown) honored.
- UX-01..05 honored; real component swapped + real-webview gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalogue/04-05-SUMMARY.md`.
</output>
