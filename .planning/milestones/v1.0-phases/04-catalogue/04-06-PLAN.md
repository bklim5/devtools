---
phase: 04-catalogue
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04, 05]
files_modified:
  - .planning/phases/04-catalogue/04-UI-REVIEW.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [TIME-01, JWT-01, HASH-01, UID-01]
user_setup: []

must_haves:
  truths:
    - "All six tools (the two Phase-3 tools + the four new catalogue tools) are reachable from the sidebar/palette and render their real components — no placeholders remain."
    - "A fresh `tauri build` produces a runnable macOS bundle and every catalogue tool functions inside the packaged WKWebView (not just `tauri dev`)."
    - "A gsd-ui-review WCAG-AA audit passes across the four new tools (visible focus, AA contrast, no opacity-only disabled/error states)."
    - "The human signs off on the packaged build; the four cross-cutting behaviors (paste-instant, ≤1-keystroke copy, status bar, no-mouse switching + opens-to-last) continue to hold."
  artifacts:
    - path: ".planning/phases/04-catalogue/04-UI-REVIEW.md"
      provides: "WCAG-AA audit record for the four catalogue tools"
  key_links:
    - from: "registry TOOLS"
      to: "six real tool components"
      via: "no makePlaceholder remains for the six"
      pattern: "component:"
---

<objective>
Phase-4 boundary gate. After the four tool plans (02-05) land, verify the WHOLE phase to the binding harness: a fresh `tauri build`, a `gsd-ui-review` WCAG-AA audit across the four new tools, and a human sign-off on the packaged bundle — mirroring Phase 3's `03-04` closing plan. This is the only `autonomous: false` plan in the phase (it carries the human checkpoint).

Purpose: prove the six-tool v1 catalogue is complete and ships under the same quality bar; surface any production-only (packaged-webview) bugs before closing the phase.
Output: a passing WCAG-AA audit record + a green `tauri build` + human approval; ROADMAP/REQUIREMENTS/STATE updated to mark Phase 4 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-catalogue/04-CONTEXT.md
@.planning/phases/04-catalogue/04-VALIDATION.md

<!-- Reference: Phase-3 closing audit + sign-off for format/expectations: -->
<!-- .planning/phases/03-hero-protobuf-encoding-ux-constraints/03-04-SUMMARY.md -->
<!-- docs/phase-1-ui-review.md / 03-UI-REVIEW (the gsd-ui-review record format + the AA-fix precedents: --tx-3 #868b95, accent #5b9bf8) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + fresh tauri build + per-tool e2e + WCAG-AA audit</name>
  <read_first>
    - .planning/phases/04-catalogue/04-VALIDATION.md §"Manual-Only Verifications" + §"Sampling Rate" (phase-gate definition)
    - src/lib/tools/registry.ts (confirm all six tools registered with real components — no makePlaceholder for the six)
    - test/e2e/ (confirm unix-time/jwt/hash/uuid-ulid e2e specs exist from plans 02-05)
    - scripts/e2e-spike.sh (the real-WKWebView runner)
    - .planning/phases/03-hero-protobuf-encoding-ux-constraints/ (the Phase-3 UI-REVIEW format + AA-token fixes to reuse, e.g. --tx-3 #868b95, accent #5b9bf8)
  </read_first>
  <action>
    1. **Full unit gate**: `npm test` (full vitest — decoder's 19 MUST be green, plus all Phase-4 suites), `npx tsc --noEmit`, `npm run lint`. Fix any cross-tool regression.
    2. **Confirm no placeholders remain**: `grep -rn "makePlaceholder" src/tools/*/index.ts` should find NOTHING for unix-time/base64/protobuf-decoder/jwt/hash/uuid-ulid (all six render real components).
    3. **Real-WKWebView e2e for all four new tools**: run `bash scripts/e2e-spike.sh` and confirm the unix-time / jwt / hash / uuid-ulid specs pass on webkit (the hash spec is the load-bearing Web-Crypto secure-context check, A1). If the runner runs one spec at a time, run each; all must exit 0.
    4. **Fresh `tauri build`**: run the project's production build (e.g. `pnpm tauri build` / `npm run tauri build` per the repo) → confirm it produces a runnable macOS bundle (.app/.dmg), exit 0. Launch the packaged app and smoke-test each of the four new tools (paste-instant, a copy works, status bar shows) to catch packaged-only bugs (Phase-2/3 precedent: production-only bugs surfaced only on the real bundle).
    5. **gsd-ui-review WCAG-AA audit** across the four new tools: run the audit; for each finding (focus visibility, AA text contrast, opacity-only disabled/error) apply the fix (reuse the Phase-3 AA-token decisions — `--tx-3 #868b95`, `accent #5b9bf8` — rather than inventing new values). Record results in `.planning/phases/04-catalogue/04-UI-REVIEW.md` (mirror the Phase-3 UI-REVIEW format). Re-run after fixes until AA passes.
    Do NOT touch the decoder or its 19 tests. Reuse the shared `design/DevTools Mockup.html` tokens — no new visual language.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` green (decoder 19 untouched + all four new tools' suites), `npx tsc --noEmit` exit 0, `npm run lint` 0 errors.
    - `grep -rn "makePlaceholder" src/tools/unix-time/index.ts src/tools/jwt/index.ts src/tools/hash/index.ts src/tools/uuid-ulid/index.ts` finds NOTHING.
    - `bash scripts/e2e-spike.sh` exits 0 for the four new specs (incl. the hash SHA-256 secure-context assertion on the real WKWebView).
    - `tauri build` exits 0 and produces a macOS bundle; the four tools function in the packaged app.
    - `.planning/phases/04-catalogue/04-UI-REVIEW.md` exists and records a PASSING WCAG-AA audit (all blockers fixed) across the four tools.
  </acceptance_criteria>
  <done>Full suite + tsc + lint green; no placeholders remain; four real-WKWebView e2e specs pass; fresh tauri build runs; WCAG-AA audit passes and is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on the packaged Phase-4 build</name>
  <action>
    Pause for the user to verify the freshly-built packaged macOS app. Present the build location and the per-tool verification steps below; do NOT proceed to Task 3 (marking the phase complete) until the user explicitly types "approved". If the user reports issues, capture them for a `/gsd-plan-phase 4 --gaps` follow-up instead of marking complete.
  </action>
  <what-built>
    The four catalogue tools (Unix Time, JWT, Hash, UUID/ULID) shipped into the registry-driven shell under the identical cross-cutting UX constraints, completing the six-tool v1 catalogue. A fresh `tauri build` has produced a runnable macOS bundle; full unit suite (decoder 19 green), tsc, lint, the four real-WKWebView e2e specs, and a WCAG-AA audit all pass.
  </what-built>
  <how-to-verify>
    On the freshly-built packaged macOS app (not `tauri dev`):
    1. **Unix Time** (`#/tools/unix-time`): paste `1469922850259` → see local + UTC datetimes + ISO `2016-07-30T23:54:10.259Z` instantly; toggle s/ms; type an ISO into the reverse field → timestamp derives; the live "now" updates and copies.
    2. **JWT** (`#/tools/jwt`): paste a real JWT → header + payload pretty-printed, signature shown raw, `alg` shown; an expired token shows an "expired" flag; paste a 2-segment string → a clear error (no crash).
    3. **Hash** (`#/tools/hash`): type `abc` → MD5 `900150983cd24fb0d6963f7d28e17f72` + SHA-256 `ba7816bf...` appear (proves Web Crypto works in the packaged webview); switch input to hex `616263` → same digests; flip casing to uppercase; copy a digest in ≤1 keystroke.
    4. **UUID/ULID** (`#/tools/uuid-ulid`): an id appears on open; press Generate → it changes; switch to ULID/v7; set a batch count → multiple copyable entries + copy-all; paste a UUID and a ULID → correct breakdowns; paste garbage → clear error.
    5. **Cross-cutting**: every output has a visible (non-hover) focusable copy; each tool shows a status bar; ⌘K still switches tools with no mouse; relaunch opens to the last-used tool.
    Confirm the visuals match the Phase-3 look (cards/chips/status bar, accent only on selected/active).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 4, or describe any issues (which will be addressed via `/gsd-plan-phase 4 --gaps`).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Mark Phase 4 complete (ROADMAP / REQUIREMENTS / STATE) — after approval</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 entry + Progress table)
    - .planning/REQUIREMENTS.md (TIME-01/JWT-01/HASH-01/UID-01 + Traceability table)
    - .planning/STATE.md (Current Position / Recent Activity / Next Step)
  </read_first>
  <action>
    ONLY after the human types "approved":
    1. `.planning/REQUIREMENTS.md`: mark TIME-01, JWT-01, HASH-01, UID-01 as `[x]` Complete (with a short note: plan + tool), and update the Traceability table rows for Phase 4 from Pending → Complete.
    2. `.planning/ROADMAP.md`: mark Phase 4 `[x]` complete in the Phases list and the Phase Details; set the Progress table row `4. Catalogue` to `6/6` (or actual plan count) · Complete · with today's date.
    3. `.planning/STATE.md`: update Current Position to "Phase 4 COMPLETE + signed off", record the sign-off, and set the Next Step to "Phase 5 (Native Polish)". Note the BACKLOG protobuf decimal-byte-array item is still open.
    Do NOT mark anything complete before approval.
  </action>
  <verify>
    <automated>grep -q "x.*TIME-01" .planning/REQUIREMENTS.md && grep -q "x.*JWT-01" .planning/REQUIREMENTS.md && grep -q "x.*HASH-01" .planning/REQUIREMENTS.md && grep -q "x.*UID-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - After approval: TIME-01, JWT-01, HASH-01, UID-01 are `[x]` in REQUIREMENTS.md and Complete in the Traceability table.
    - ROADMAP.md Phase 4 marked `[x]` and the Progress table row updated to Complete with today's date.
    - STATE.md Current Position reflects "Phase 4 COMPLETE + signed off" and Next Step points to Phase 5.
  </acceptance_criteria>
  <done>Phase 4 marked complete across ROADMAP/REQUIREMENTS/STATE after human approval; Next Step set to Phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged webview vs dev | The production bundle's WKWebView may differ from `tauri dev` (secure-context, store backend). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | production-only behavior divergence (e.g. Web Crypto unavailable, A1) | mitigate | Fresh `tauri build` + per-tool real-WKWebView e2e + packaged smoke-test are the gate; the hash spec asserts a real SHA-256 digest (fails loudly if crypto.subtle is absent). Human sign-off on the packaged app before closing the phase. |
| T-04-18 | Repudiation | closing the phase without a verifiable record | mitigate | The WCAG-AA audit is recorded in 04-UI-REVIEW.md; ROADMAP/REQUIREMENTS/STATE updated only after explicit "approved". |
</threat_model>

<verification>
- All six tools render real components (no placeholders).
- Full suite (decoder 19) + tsc + lint green; four real-WKWebView e2e specs pass; fresh `tauri build` runs.
- WCAG-AA audit passes and is recorded; human signs off on the packaged build.
- Phase 4 marked complete across the planning docs.
</verification>

<success_criteria>
- The six-tool v1 catalogue is complete and verified to the binding harness (review → unit → real-webview → phase-boundary human sign-off + WCAG-AA).
- TIME-01, JWT-01, HASH-01, UID-01 all Complete.
- No regression to the decoder's 19 tests or any Phase-1/2/3 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalogue/04-06-SUMMARY.md`.
</output>
