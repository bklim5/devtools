---
phase: 04-catalogue
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/hash/HashTool.tsx
  - src/tools/hash/HashTool.test.tsx
  - src/tools/uuid-ulid/UuidUlidTool.tsx
  - src/tools/uuid-ulid/UuidUlidTool.test.tsx
  - src/components/CopyButton.tsx
  - test/e2e/hash.e2e.ts
autonomous: true
requirements: [HASH-01, UID-01]
user_setup: []

must_haves:
  truths:
    - "Hash tool treats input ALWAYS as text — there is no input-encoding selector and no hex/base64 decode path (G-04-1, supersedes D-11)."
    - "Hash tool layout does NOT flicker/reflow/jump while typing — all five digest rows occupy stable, fixed-height containers from the first keystroke (G-04-2)."
    - "UUID Count field can be cleared and retyped normally — the user can backspace below 1 and type 2-9 without select-then-replace (G-04-3)."
    - "UUID batch generation is hard-capped at 100 everywhere (input max, clamp, generation guard) — no path can produce more than 100 entries (G-04-4)."
    - "Every interactive button introduced in Phase 4 (Generate, Copy, Copy-all, KindToggle, CasingToggle) shows cursor: pointer on hover (G-04-5)."
  artifacts:
    - path: "src/tools/hash/HashTool.tsx"
      provides: "Text-only Hash tool with stable, fixed-height digest rows"
      contains: "utf8ToBytes"
    - path: "src/tools/uuid-ulid/UuidUlidTool.tsx"
      provides: "UUID/ULID tool with editable count field capped at 100"
      contains: "Math.min"
    - path: "src/components/CopyButton.tsx"
      provides: "Shared copy button with cursor-pointer"
      contains: "cursor-pointer"
  key_links:
    - from: "src/tools/hash/HashTool.tsx"
      to: "src/lib/bytes.ts"
      via: "utf8ToBytes only (no hexToBytes/base64ToBytes import)"
      pattern: "utf8ToBytes"
    - from: "src/tools/uuid-ulid/UuidUlidTool.tsx"
      to: "generateBatch"
      via: "Math.min(count, 100) clamp + max={100}"
      pattern: "100"
---

<objective>
Close exactly the five UX defects from Phase-4 human UAT (G-04-1..G-04-5) in the Hash and UUID/ULID tools. No new features, no scope creep, no re-litigating already-verified decisions. Two of the five (G-04-1 text-only, G-04-4 cap-100) are explicit product corrections that supersede/tighten Phase-4 decisions; the other three are pure UX polish (stable layout, editable count, pointer cursor).

Purpose: Make the two flagged tools pass the deferred packaged-build human sign-off without regressing any green automated gate (269/269 vitest incl. the immovable 19 decoder tests, tsc clean, eslint 0, the Hash + UUID real-WKWebView e2e specs).

Output: A text-only, flicker-free Hash tool; a UUID/ULID tool with an editable count hard-capped at 100; and `cursor-pointer` on every Phase-4 interactive button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-catalogue/04-HUMAN-UAT.md

<gap_source>
The five gaps come from `.planning/phases/04-catalogue/04-HUMAN-UAT.md` ## Gaps (tests 3 Hash + 4 UUID/ULID returned `issue`). G-04-1 (text-only Hash) explicitly SUPERSEDES CONTEXT D-11 (the UTF-8/hex/base64 input-encoding toggle). G-04-4 tightens the batch cap from 1000 → 100. The other three are UX polish.
</gap_source>

<binding_gates>
From CLAUDE.md harness (never skip, in this order, per task):
1. `/simplify` on the just-written changes (quality only).
2. `/codex:review` (`--wait --scope working-tree`) — address findings.
3. Unit green: `npx vitest run` exits 0 (the 19 decoder tests stay untouched and pass) AND `npx tsc --noEmit` clean AND `npm run lint` 0 errors.
4. Real-WKWebView re-verification: the Hash + UUID e2e specs via `bash scripts/e2e-spike.sh` (update assertions that changed — e.g. the dropped Hash encoding toggle).

Tools NEVER import `@tauri-apps/*` directly (clipboard via the `src/lib/platform/` seam / CopyButton). Do NOT touch `decoder.ts`/`bytes.ts`/`types.ts`/`registry.ts` internals or the 19 decoder tests. HashRouter only. Six tools only.
</binding_gates>

<interfaces>
Key contracts the executor will use directly — no codebase exploration needed.

From src/lib/bytes.ts (port-unchanged — import only what you need):
```typescript
export function utf8ToBytes(s: string): Uint8Array;   // never throws on a string
export function hexToBytes(hex: string): Uint8Array;  // (to be DROPPED from HashTool imports)
export function base64ToBytes(s: string, alphabet: Base64Alphabet): Uint8Array; // (to be DROPPED)
```

From src/tools/hash/hashes.ts (UNCHANGED by this plan — keep as-is):
```typescript
export function md5Hex(bytes: Uint8Array): string;
export function shaHex(algo: ShaAlgorithm, bytes: Uint8Array): Promise<string>;
export interface DigestRow { algo; hex: string }
export type ShaAlgorithm = "SHA-1" | "SHA-256" | "SHA-384" | "SHA-512";
```

From src/components/StatusBar.tsx (UNCHANGED — `encoding` is already optional):
```typescript
export interface StatusBarProps { parseState; byteCount; encoding?: string; error?; timingMs? }
```

From src/components/CopyButton.tsx (MODIFIED by Task 3 — add cursor-pointer to base class):
```typescript
export function CopyButton({ value, label, className }: CopyButtonProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hash — text-only input (G-04-1) + stable flicker-free layout (G-04-2)</name>
  <files>src/tools/hash/HashTool.tsx, src/tools/hash/HashTool.test.tsx, test/e2e/hash.e2e.ts</files>
  <read_first>
    - src/tools/hash/HashTool.tsx (the file being modified — current encoding toggle, parseInput, conditional digests section)
    - src/tools/hash/HashTool.test.tsx (drop encoding-toggle assertions; keep text→digest coverage)
    - src/tools/hash/hashes.ts (UNCHANGED — md5Hex/shaHex/DigestRow contracts)
    - src/components/StatusBar.tsx (encoding prop is optional — drop it from the Hash call)
    - test/e2e/hash.e2e.ts (real-WKWebView spec — selectors #hash-input, [data-algo], button[aria-label="Copy …"])
  </read_first>
  <behavior>
    Tests to keep/adjust in HashTool.test.tsx (text-only world):
    - Test: typing "abc" renders MD5 `900150983cd24fb0d6963f7d28e17f72` (sync) and (await) SHA-256 starting `ba7816bf8f01cfea`. KEEP.
    - Test: empty input → status "Empty", no digest VALUES shown, no error. (Rows MAY exist as fixed-height placeholders now — assert on digest text being empty / status "Empty", NOT on row absence.)
    - Test: casing toggle flips MD5 to uppercase and back, and uppercases the COPIED value. KEEP.
    - Test: each of the five digest rows has a focusable Copy button (no opacity-0). KEEP.
    - REMOVE: the "input-encoding toggle exposes UTF-8 / hex / base64" test (no toggle exists).
    - REMOVE: the "hex '616263' yields the same digests" test (no hex mode).
    - REMOVE: the "invalid input for the chosen encoding shows a field-scoped error" test (UTF-8 never errors; that path is dead).
  </behavior>
  <action>
G-04-1 (text-only):
- DELETE the `INPUT_ENCODINGS` const and the `InputEncoding` type (lines ~28-30).
- DELETE the `parseInput` function (lines ~32-42) and the `EncodingToggle` component (lines ~44-78).
- DELETE the `encoding` state: remove `const [encoding, setEncoding] = useState<InputEncoding>("UTF-8");`.
- DELETE the `<EncodingToggle value={encoding} onChange={setEncoding} />` usage in the Input header row (leave the `<label htmlFor="hash-input">Input</label>` — the header `flex items-center justify-between` may collapse to just the label; that's fine).
- Change the imports from `@/lib/bytes` to ONLY `import { utf8ToBytes } from "@/lib/bytes";` (drop `base64ToBytes` and `hexToBytes`).
- Replace the `useMemo` parse block with a direct, throw-free parse. Since UTF-8 of any string never throws, there is NO error path and NO try/catch:
  ```ts
  const bytes = useMemo<Uint8Array>(
    () => (raw === "" ? new Uint8Array(0) : utf8ToBytes(raw)),
    [raw],
  );
  ```
  Remove the `error` variable entirely. `bytes` is now never null.
- Update `md5Row` to drop the `bytes === null` branch: `() => (isEmpty ? null : { algo: "MD5", hex: md5Hex(bytes) })`.
- Update the SHA `useEffect` to drop `if (bytes === null) return;` (bytes is always a Uint8Array now).
- Update `parseState`: `const parseState: ParseState = isEmpty ? "empty" : "ok";` (no "error").
- DELETE the field-scoped error UI: remove `aria-invalid`/`aria-describedby` on the textarea and the `{error ? <p id="hash-input-error">…</p> : null}` block.
- Update the textarea placeholder to `"Paste text…"`.
- Update the StatusBar call: drop the `encoding={encoding}` prop and the `error={error}` prop (pass `error={undefined}` or omit; `byteCount={bytes.length}`).

G-04-2 (stable layout — no flicker/reflow):
- ALWAYS render the Digests section (remove the `{!isEmpty && !error ? (…) : null}` guard so it does not mount/unmount on each keystroke). When `isEmpty`, render the five rows as muted placeholders.
- Build a STABLE list of all five rows every render, regardless of empty/filled: `const ALL_ALGOS = ["MD5", "SHA-1", "SHA-256", "SHA-384", "SHA-512"] as const;` and map a fixed `{ algo, hex }` for each (hex = "" when empty or not-yet-resolved).
- Give each `DigestRowView` a FIXED ROW HEIGHT so an empty "" row and a filled 128-hex-char SHA-512 row occupy identical height (no reflow). Use a fixed line height + reserved min-height on the `<code>`, NOT a fixed pixel WIDTH (must stay layout-agnostic, UX-05). Concretely: render the digest text inside a `<code>` with `block min-w-0 break-all font-mono text-[13px] leading-5 min-h-[2.5rem] text-tx`, and when `hex === ""` show a muted placeholder (e.g. a `text-tx-3` "…" or an empty string with the reserved min-height) so the box never collapses. The Casing toggle stays in the Digests header (still visible when empty is fine, or keep it; do not hide it conditionally as that would reflow).
- Net effect: the Digests section and all five row containers are present from mount; typing only swaps the inner text, never the box geometry — no flicker.

Keep the Pitfall-3 stale-guard (`shaResult.src === bytes`) intact — it still prevents stale SHA rows.

Update HashTool.test.tsx per the <behavior> block (remove the three encoding tests; keep/adjust the rest; the empty test must assert on status/empty-values not row absence since rows now always render).

Update test/e2e/hash.e2e.ts: it does NOT reference the encoding toggle, so its assertions still hold (types "abc" → MD5 + SHA-256 → Copy MD5 visible). Re-read it to confirm no encoding-toggle selector exists; if a comment mentions the toggle, trim it. No assertion change is expected — but RUN it via the gate to confirm the stable-layout change didn't break the `[data-algo="MD5"] code` selector (it must still hold the digest text).
  </action>
  <verify>
    <automated>npx vitest run src/tools/hash/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INPUT_ENCODINGS" src/tools/hash/HashTool.tsx` returns 0.
    - `grep -c "InputEncoding" src/tools/hash/HashTool.tsx` returns 0.
    - `grep -c "EncodingToggle" src/tools/hash/HashTool.tsx` returns 0.
    - `grep -c "hexToBytes\|base64ToBytes" src/tools/hash/HashTool.tsx` returns 0.
    - `grep -c "utf8ToBytes" src/tools/hash/HashTool.tsx` returns >= 1.
    - `grep -c "Paste text" src/tools/hash/HashTool.tsx` returns >= 1.
    - `grep -c "min-h-\[" src/tools/hash/HashTool.tsx` returns >= 1 (reserved row height).
    - `grep -c "encoding=" src/tools/hash/HashTool.tsx` returns 0 (StatusBar no longer gets encoding).
    - `grep -ci "UTF-8\|hex\|base64" src/tools/hash/HashTool.test.tsx` shows the three encoding tests are gone (no "exposes UTF-8 / hex / base64" assertion, no hex "616263" test).
    - `npx vitest run src/tools/hash/` exits 0.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>
    Hash tool parses input only via `utf8ToBytes` (no toggle, no error path); all five digest rows render in fixed-height containers from mount so typing never reflows; HashTool tests updated and green; tsc clean. The hash e2e spec still passes on the real WKWebView.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UUID — editable count (G-04-3) + hard cap at 100 (G-04-4)</name>
  <files>src/tools/uuid-ulid/UuidUlidTool.tsx, src/tools/uuid-ulid/UuidUlidTool.test.tsx</files>
  <read_first>
    - src/tools/uuid-ulid/UuidUlidTool.tsx (the file being modified — count state, handleCount, generateBatch, the count <input>)
    - src/tools/uuid-ulid/UuidUlidTool.test.tsx (batch-count tests use fireEvent.change(#uuid-ulid-count, {value:"3"}) → must still pass)
    - test/e2e/uuid-ulid.e2e.ts (real-WKWebView spec — does NOT touch the count field; must still pass)
  </read_first>
  <behavior>
    Tests in UuidUlidTool.test.tsx (keep passing + add cap coverage):
    - KEEP: "a batch count of 3 renders 3 entries…" — fireEvent.change(count, {value:"3"}) → 3 rows. Must still work with the new string-state count.
    - KEEP: "copy-all writes every generated id newline-joined" — count "3" → 3 ids.
    - ADD: setting the count to "250" generates at most 100 entries (cap enforced): fireEvent.change(count, {value:"250"}) → `genValues(container)` length is 100 (or the rows length is 100). Asserts G-04-4.
    - ADD: clearing the count to "" does NOT immediately snap the field back to "1" (transient empty allowed): fireEvent.change(count, {value:""}) → the input's value is "" (not "1"). Asserts G-04-3. (Generation may keep the last valid ids or clamp to 1 — assert only that the FIELD shows "".)
  </behavior>
  <action>
G-04-4 (cap at 100 everywhere) — do this first, it's the high-severity guard:
- In `generateBatch`: change `const n = Math.max(1, Math.min(count, 1000));` → `const n = Math.max(1, Math.min(count, 100));`.
- On the count `<input>`: change `max={1000}` → `max={100}`.
- In `handleCount` (about to be rewritten below): the clamp uses 100, not 1000.
- Confirm via grep that NO `1000` literal remains in the file.

G-04-3 (editable count — allow transient empty / in-progress value):
- Change the count state to hold the RAW STRING so the field can be cleared and retyped: `const [countText, setCountText] = useState("1");`.
- Derive the effective numeric count for generation by clamping on read: a helper
  ```ts
  function clampCount(text: string): number {
    const n = Number.parseInt(text, 10);
    if (!Number.isFinite(n) || n < 1) return 1;
    return Math.min(n, 100);
  }
  ```
- Rewrite `handleCount(raw: string)` to NOT coerce the field back to 1 on every change. It stores the raw text and regenerates using the clamped value, but leaves the field text as typed (so the user can clear it / type 2-9):
  ```ts
  function handleCount(raw: string) {
    setCountText(raw);
    regenerate(kind, clampCount(raw));
  }
  ```
- The `<input>` binds `value={countText}` (string).
- Add an `onBlur` that normalizes an empty/invalid field back to a valid number string so it never stays blank after the user leaves it:
  ```ts
  onBlur={() => setCountText(String(clampCount(countText)))}
  ```
- Update `regenerate` and `handleKind` to use the clamped numeric count: `regenerate(next, clampCount(countText))` in `handleKind`; the `regenerate` default `c` param should be `clampCount(countText)` instead of `count`. Update the lazy `useState` initializer for `ids` to `generateBatch("uuid-v4", 1)` (unchanged — opens with one id).
- Remove the old numeric `count` state and the old `handleCount` clamp logic (the `Math.min(n, 1000)` line). Ensure no reference to the removed `count` numeric state remains (search for `count` usages — `generateBatch(k, c)` etc. now receive clamped numbers via `clampCount`).
- The on-open behavior is unchanged: one id generated via the lazy initializer.

Update UuidUlidTool.test.tsx per the <behavior> block: the existing fireEvent.change with "3" still drives `genValues` length 3 (clampCount("3")===3); ADD the "250 → 100" cap test and the "clear → field shows ''" test.

test/e2e/uuid-ulid.e2e.ts does NOT interact with the count field — re-read to confirm, expect NO change needed; RUN it through the gate to confirm the generate/decode selectors still hold.
  </action>
  <verify>
    <automated>npx vitest run src/tools/uuid-ulid/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "1000" src/tools/uuid-ulid/UuidUlidTool.tsx` returns 0.
    - `grep -c "Math.min" src/tools/uuid-ulid/UuidUlidTool.tsx` returns >= 1 AND the clamp uses 100 (`grep -c "Math.min(count, 100)\|Math.min(n, 100)" src/tools/uuid-ulid/UuidUlidTool.tsx` returns >= 1).
    - `grep -c "max={100}" src/tools/uuid-ulid/UuidUlidTool.tsx` returns >= 1.
    - `grep -c "countText\|clampCount" src/tools/uuid-ulid/UuidUlidTool.tsx` returns >= 1 (string-backed editable count).
    - `grep -c "onBlur" src/tools/uuid-ulid/UuidUlidTool.tsx` returns >= 1.
    - UuidUlidTool.test.tsx contains a cap test asserting a count of 250 yields 100 entries, and a clear test asserting the field can show "".
    - `npx vitest run src/tools/uuid-ulid/` exits 0.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>
    The count field can be cleared and retyped normally (transient empty allowed, normalized on blur); batch generation is hard-capped at 100 in the input `max`, the clamp, and `generateBatch`; no `1000` remains; tests cover both the cap and the clearable field and are green. The uuid-ulid e2e spec still passes on the real WKWebView.
  </done>
</task>

<task type="auto">
  <name>Task 3: cursor-pointer on every Phase-4 interactive button (G-04-5)</name>
  <files>src/components/CopyButton.tsx, src/tools/uuid-ulid/UuidUlidTool.tsx, src/tools/hash/HashTool.tsx</files>
  <read_first>
    - src/components/CopyButton.tsx (shared copy button — base class string; covers per-row Copy across all four tools)
    - src/tools/uuid-ulid/UuidUlidTool.tsx (Generate button, Copy-all button, KindToggle buttons)
    - src/tools/hash/HashTool.tsx (CasingToggle buttons — after Task 1's edits)
  </read_first>
  <action>
There is currently NO `cursor-pointer` anywhere in src/ (verified). Add it to every interactive `<button>` introduced/owned by the Phase-4 tools, using the least-repetitive approach:

- In `src/components/CopyButton.tsx`: add `cursor-pointer` to the base class string of the single `<button>` (the first entry in the joined className array, alongside `flex items-center gap-1.5 …`). This one edit covers EVERY per-row Copy button in Hash, UUID/ULID, Unix Time, and JWT (all use CopyButton) — maximum coverage, minimum repetition.
- In `src/tools/uuid-ulid/UuidUlidTool.tsx`:
  - Generate `<button>`: add `cursor-pointer` to its className.
  - Copy-all `<button>`: add `cursor-pointer` to its className.
  - KindToggle `<button>` (inside the KindToggle component): add `cursor-pointer` to the shared button className array.
- In `src/tools/hash/HashTool.tsx`:
  - CasingToggle `<button>` (inside the CasingToggle component): add `cursor-pointer` to the shared button className array.
  (The EncodingToggle was removed in Task 1 — nothing to do there.)

Keep it scoped: do NOT restyle unrelated shell chrome (Sidebar, CommandPalette, etc.) — this gap is about the Phase-4 tool buttons the user flagged. Use the Tailwind `cursor-pointer` utility consistently; do not introduce inline styles.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cursor-pointer" src/components/CopyButton.tsx` returns >= 1.
    - `grep -c "cursor-pointer" src/tools/uuid-ulid/UuidUlidTool.tsx` returns >= 1 (Generate + Copy-all + KindToggle covered).
    - `grep -c "cursor-pointer" src/tools/hash/HashTool.tsx` returns >= 1 (CasingToggle).
    - The Generate button's className in UuidUlidTool.tsx contains `cursor-pointer`.
    - `npx vitest run` exits 0 (full suite — decoder 19 untouched and green).
    - `npx tsc --noEmit` clean AND `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>
    Every interactive button the user touches in the Phase-4 tools (Generate, Copy, Copy-all, KindToggle, CasingToggle) shows the pointer cursor on hover; the change is scoped to those buttons (CopyButton handles all per-row copies via one edit); the full vitest suite, tsc, and lint are all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user paste → Hash input | untrusted text crosses into the hasher (now always UTF-8 bytes — never throws) |
| user input → UUID count field | untrusted numeric string crosses into batch generation (must be bounded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Denial of Service | UuidUlidTool batch count | mitigate | Hard-cap generation at 100 in `generateBatch` (`Math.min(n,100)`), the input `max={100}`, AND `clampCount` — three layers, no path can request unbounded entries (G-04-4, the high-severity defect). |
| T-04-07-02 | Tampering | Hash input parse | accept | Input is now always UTF-8 text; `utf8ToBytes` never throws on a string, so the removed error path cannot be reached — no untrusted-decode surface remains (G-04-1). No PII; offline; low value. |
| T-04-07-03 | Information disclosure | clipboard via CopyButton | accept | Clipboard writes go through the existing `src/lib/platform/` seam (no `@tauri-apps/*` in tools); cursor-only styling change introduces no new data path. |
</threat_model>

<verification>
Run the full binding harness, in order, after the three tasks:
1. `npx vitest run` → exits 0, full suite green INCLUDING the 19 decoder tests (`npx vitest run src/lib/protobuf/decoder.test.ts` → 19 passed) — they must be byte-for-byte untouched.
2. `npx tsc --noEmit` → clean.
3. `npm run lint` → 0 errors.
4. Real-WKWebView re-verification of the two changed tools: `bash scripts/e2e-spike.sh` → the Hash (`test/e2e/hash.e2e.ts`) and UUID/ULID (`test/e2e/uuid-ulid.e2e.ts`) specs pass on webkit (the other four specs continue to pass). Confirm the Hash spec's `[data-algo="MD5"] code` and SHA-256 selectors still resolve after the stable-layout refactor, and the UUID spec's generate/decode flow is unaffected by the count refactor.
5. Grep audit: no `@tauri-apps/*` import added to any tool; no `1000` in UuidUlidTool.tsx; no `INPUT_ENCODINGS`/`InputEncoding`/`EncodingToggle` in HashTool.tsx; `cursor-pointer` present in CopyButton.tsx + both tools.
</verification>

<success_criteria>
- G-04-1: Hash tool has no input-encoding selector; input is always parsed via `utf8ToBytes`; the hex/base64 decode + error path is gone.
- G-04-2: All five Hash digest rows render in fixed-height containers from mount; typing swaps text only, never geometry — no flicker/reflow.
- G-04-3: The UUID Count field can be cleared and retyped (transient empty allowed; normalized on blur) without select-then-replace.
- G-04-4: Batch count is hard-capped at 100 in the input `max`, the clamp, and `generateBatch`; no `1000` remains; no path exceeds 100.
- G-04-5: Generate, Copy, Copy-all, KindToggle, and CasingToggle buttons all show `cursor-pointer`.
- All binding gates green: vitest full suite (decoder 19 untouched), tsc clean, eslint 0, Hash + UUID e2e passing on the real WKWebView.
- No scope creep: no new features, no other tools touched, no decoder/bytes/types/registry edits, no `@tauri-apps/*` in tools, HashRouter unchanged, still six tools.
</success_criteria>

<output>
After completion, create `.planning/phases/04-catalogue/04-07-SUMMARY.md` recording: which gaps closed, the exact files touched, any deviations (Rule 1/2/3), the final gate numbers (vitest count, tsc/eslint, e2e pass count on webkit), and a note that the packaged-build human re-verification of Hash + UUID/ULID is the remaining step (folds into the deferred Phase-4 / Phase-5 human sign-off).
</output>
