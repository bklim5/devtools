---
phase: 01-scaffold-harness-proof
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lefthook.yml
  - docs/phase-0-notes.md
autonomous: true
requirements: [HRN-03, HRN-04]
user_setup: []

must_haves:
  truths:
    - "A lefthook pre-commit hook runs `pnpm tsc --noEmit` + `pnpm vitest run` and BLOCKS a commit with a type error or failing test"
    - "The block is proven via `pnpm lefthook run pre-commit` against a temporary type-error file (NOT a real commit) — no worktree contamination"
    - "It is documented that the UI gate and /codex:review remain manual per-task steps (lefthook covers the unit gate only)"
    - "`pnpm tauri build` produces a runnable unsigned macOS bundle"
    - "Build findings (signing/Gatekeeper surprises, bundle size, toolchain quirks) are recorded in docs/phase-0-notes.md"
  artifacts:
    - path: "lefthook.yml"
      provides: "Pre-commit unit gate (tsc --noEmit + vitest run), parallel"
      contains: "pre-commit"
    - path: "docs/phase-0-notes.md"
      provides: "HRN-04 build findings + gate-enforcement notes (automation path appended in Plan 04)"
      contains: "tauri build"
  key_links:
    - from: "lefthook.yml pre-commit"
      to: "pnpm tsc --noEmit + pnpm vitest run"
      via: "lefthook install hook → git pre-commit"
      pattern: "vitest run"
    - from: "pnpm tauri build"
      to: "src-tauri/target/release/bundle/macos/*.app"
      via: "tauri CLI macOS bundler"
      pattern: "tauri build"
---

<objective>
Install the mechanical unit gate (lefthook pre-commit running `tsc --noEmit` + `vitest run`, D-07) and prove it blocks bad commits — using `pnpm lefthook run pre-commit` against a temporary type-error file so no real commit contaminates the worktree — then run `pnpm tauri build` to produce a runnable unsigned macOS bundle (HRN-04, the FIRST/smoke build) and capture findings in `docs/phase-0-notes.md`. This plan touches build/tooling files only — it runs in parallel with Plan 02 with ZERO package.json/lockfile overlap (the lefthook package + the `prepare` script were installed in Plan 01, Wave 1; this plan only writes `lefthook.yml` and `docs/phase-0-notes.md`).

Purpose: HRN-03 demands the per-task DoD be enforced for every subsequent phase; lefthook makes the unit gate mechanical (not just agent discipline). HRN-04 demands a verified `tauri build` at each phase boundary; running it now surfaces signing/toolchain surprises early (the documented reason this build happens in Phase 1). NOTE: Plan 04 adds the debug-only WebDriver plugin AFTER this build, so the AUTHORITATIVE final release build (verifying the WebDriver server is absent) happens in Plan 04 — this is the first smoke build.
Output: A working blocking pre-commit hook (proven non-destructively), a runnable `.app`/`.dmg` bundle, and a `docs/phase-0-notes.md` recording build findings + the manual-gate boundary.

Implements: HRN-03 (gate enforcement, parallel plans never bypass), HRN-04 (runnable macOS bundle — first smoke; final authoritative build in Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-harness-proof/01-CONTEXT.md
@.planning/phases/01-scaffold-harness-proof/01-RESEARCH.md
@CLAUDE.md
@docs/harness-and-decisions.md

<interfaces>
<!-- Exact config the executor writes — from RESEARCH.md. -->

DEPENDENCIES + SCRIPTS ALREADY INSTALLED (Plan 01, Wave 1) — this plan runs NO `pnpm add` and edits NO
  package.json: lefthook@2.1.9 is installed and `"prepare": "lefthook install"` is already in package.json.
  THIS plan only WRITES `lefthook.yml` and then runs `pnpm lefthook install` to register the git hook.
  If lefthook is missing, that is a Plan-01 bug — report it; do NOT `pnpm add` here (would conflict with the
  parallel Plan 02's wave). This is the HIGH-2 de-conflict: Wave 2 has zero package.json/lockfile overlap.

lefthook.yml (RESEARCH Code Examples; D-07):
  pre-commit:
    parallel: true
    commands:
      typecheck:
        run: pnpm tsc --noEmit
      test:
        run: pnpm vitest run
  # Install (hook registration only — package + prepare script already in place from Plan 01):
  #   `pnpm lefthook install`
  # D-08: lefthook covers the UNIT gate only. /codex:review (disable-model-invocation)
  #       and the real-webview UI check stay MANUAL per-task DoD steps — a git hook cannot run them.

Non-destructive block proof (MED — avoid real commits in a possibly-dirty/parallel worktree):
  Use `pnpm lefthook run pre-commit` to execute the hook directly, NOT `git commit`. Create a temporary
  file with a deliberate type error (e.g. `src/__lefthook_probe__.ts` containing `const x: number = "oops";`),
  run `pnpm lefthook run pre-commit` → it MUST exit non-zero (tsc fails). Then DELETE the probe file and
  re-run `pnpm lefthook run pre-commit` → it MUST exit zero. This proves the gate has teeth without ever
  creating a commit that could capture unrelated changes.

tauri build (RESEARCH Pitfall 6; HRN-04):
  `pnpm tauri build`  → unsigned artifacts in:
    src-tauri/target/release/bundle/macos/*.app
    src-tauri/target/release/bundle/dmg/*.dmg
  Unsigned → Gatekeeper "unidentified developer"/"damaged" is EXPECTED in Phase 1 (signing = Phase 6).
  To launch locally: right-click → Open, or `xattr -dr com.apple.quarantine <App>.app`.
  First cargo build is slow (compiles all Tauri crates). cargo 1.83 present; if MSRV error → `rustup update stable`.
  This is the FIRST/smoke build (predates the WebDriver plugin). Plan 04 runs the AUTHORITATIVE final build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lefthook pre-commit unit gate and prove it BLOCKS (via `lefthook run`, no real commit)</name>
  <read_first>
    - .planning/phases/01-scaffold-harness-proof/01-RESEARCH.md (lefthook.yml Code Example; Don't Hand-Roll lefthook row; D-07/D-08)
    - docs/harness-and-decisions.md (§3 three-gate DoD — which gates are mechanical vs manual)
    - package.json (lefthook + "prepare" already present from Plan 01 — do NOT add or edit)
  </read_first>
  <action>
    1. Confirm lefthook is already installed (Plan 01) and `"prepare": "lefthook install"` is in package.json — do NOT `pnpm add` or edit package.json (HIGH-2: that would conflict with the parallel Plan 02). Run `pnpm lefthook install` to register the git hook.
    2. Create `lefthook.yml` exactly per the interface above: `pre-commit` with `parallel: true` and two commands — `typecheck: pnpm tsc --noEmit` and `test: pnpm vitest run`. Re-run `pnpm lefthook install` if needed so the hook picks up the config.
    3. PROVE the gate blocks NON-DESTRUCTIVELY (MED — no real commits): create a temporary probe file `src/__lefthook_probe__.ts` with a deliberate type error (`const x: number = "oops";`). Run `pnpm lefthook run pre-commit` → it MUST exit non-zero (tsc fails on the probe). Capture the failing output for the SUMMARY. Then DELETE `src/__lefthook_probe__.ts` and run `pnpm lefthook run pre-commit` again → it MUST exit zero (clean). This proves the hook runs and has teeth without creating any commit that could capture unrelated worktree changes.
    4. Add a clear note (in lefthook.yml comments and the SUMMARY) per D-08: the UI gate and `/codex:review` remain MANUAL per-task steps — lefthook enforces the unit gate only.
  </action>
  <verify>
    <automated>test -f lefthook.yml && grep -q "pnpm vitest run" lefthook.yml && grep -q "tsc --noEmit" lefthook.yml && test -f .git/hooks/pre-commit</automated>
  </verify>
  <acceptance_criteria>
    - `lefthook.yml` exists with `pre-commit` running both `pnpm tsc --noEmit` and `pnpm vitest run`
    - `.git/hooks/pre-commit` exists (lefthook installed the hook)
    - `pnpm lefthook run pre-commit` with a deliberate-type-error probe file exits NON-ZERO (capture output); after deleting the probe, `pnpm lefthook run pre-commit` exits ZERO — proven WITHOUT a real commit
    - No probe file remains: `! test -f src/__lefthook_probe__.ts`
    - `package.json` is UNCHANGED by this plan (lefthook + "prepare" came from Plan 01); `git diff --name-only` for this plan does NOT include package.json or pnpm-lock.yaml
    - lefthook.yml comment documents D-08 (UI gate + /codex:review stay manual)
  </acceptance_criteria>
  <done>lefthook pre-commit unit gate installed and demonstrated (via `pnpm lefthook run pre-commit`) to fail on a type error then pass clean — no real commit, no worktree contamination; package.json untouched (HIGH-2); the manual-gate boundary (UI + /codex:review) is documented. HRN-03 mechanical enforcement in place.</done>
</task>

<task type="auto">
  <name>Task 2: Run tauri build (first smoke), produce a runnable unsigned macOS bundle, capture findings in phase-0-notes.md</name>
  <read_first>
    - .planning/phases/01-scaffold-harness-proof/01-RESEARCH.md (Pitfall 6; Environment Availability — cargo 1.83/Xcode/macOS present; A6 unsigned-build assumption)
    - .planning/phases/01-scaffold-harness-proof/01-CONTEXT.md (deliverable: docs/phase-0-notes.md is the HRN-02/HRN-04 record)
    - src-tauri/tauri.conf.json (bundle config from Plan 01)
  </read_first>
  <action>
    1. Run `pnpm tauri build`. The first cargo build compiles all Tauri crates (slow — allow time). If a Tauri/crate MSRV error appears, run `rustup update stable` and retry (note it).
    2. Locate the artifacts: `src-tauri/target/release/bundle/macos/*.app` and `src-tauri/target/release/bundle/dmg/*.dmg`. Confirm the `.app` is runnable locally — `xattr -dr com.apple.quarantine <App>.app` then launch it (unsigned/Gatekeeper warnings are EXPECTED and in-scope-as-noted; signing is Phase 6, do NOT attempt to fix).
    3. Create `docs/phase-0-notes.md` (the phase deliverable) and record under an "HRN-04: tauri build (first smoke)" section: the exact artifact paths, bundle size of the `.app`/`.dmg`, build duration, cargo/rustc version used, any MSRV/toolchain quirks, and the unsigned-Gatekeeper behavior observed. Leave a clearly-marked placeholder section "HRN-02: macOS webview automation path" for Plan 04 to fill, AND a placeholder "HRN-04: FINAL release build (post-WebDriver)" section for Plan 04's authoritative build (so all deliverables live in one file).
    4. Note explicitly: this smoke build PREDATES the debug-only WebDriver plugin (added in Plan 04 behind `debug_assertions`). The AUTHORITATIVE final build that must verify the WebDriver server is absent from the release artifact happens in Plan 04 — record that hand-off here so it isn't lost.
  </action>
  <verify>
    <automated>ls src-tauri/target/release/bundle/macos/*.app >/dev/null 2>&1 && test -f docs/phase-0-notes.md && grep -qi "tauri build" docs/phase-0-notes.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls src-tauri/target/release/bundle/macos/*.app` shows a built `.app`
    - The `.app` launches locally after clearing quarantine (manual confirmation; record in notes)
    - `docs/phase-0-notes.md` exists with an "HRN-04" first-smoke section recording artifact paths, bundle size, build duration, toolchain version, and unsigned-Gatekeeper behavior
    - `docs/phase-0-notes.md` contains placeholder sections "HRN-02" and "HRN-04: FINAL release build (post-WebDriver)" for Plan 04
    - No code-signing/notarisation was attempted (out of scope — Phase 6)
  </acceptance_criteria>
  <done>`pnpm tauri build` produced a runnable unsigned macOS `.app`/`.dmg` (first smoke); findings (paths, size, duration, toolchain, Gatekeeper behavior) recorded in docs/phase-0-notes.md with HRN-02 + final-build placeholders for Plan 04, and an explicit note that Plan 04 owns the authoritative post-WebDriver release build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit ↔ repo | Untested/broken code attempting to enter the repo; the pre-commit gate is the control |
| release bundle ↔ end user | The shipped artifact must not expose dev-only surfaces (e.g. webdriver server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Broken/untyped code lands in the repo, eroding the decoder's green bar | mitigate | lefthook pre-commit runs tsc+vitest and BLOCKS the commit; proven non-destructively via `pnpm lefthook run pre-commit` on a temp type-error file (acceptance criterion) |
| T-01-08 | Information disclosure | Dev-only WebDriver server (port 4445) compiled into a release build → remote control surface | mitigate | This FIRST smoke build predates the webdriver plugin; Plan 04 gates it strictly under `[target.'cfg(debug_assertions)'.dependencies]` + `#[cfg(debug_assertions)]` and runs the AUTHORITATIVE final build that verifies its absence (RESEARCH Pitfall 4); hand-off noted here |
| T-01-09 | Spoofing | Unsigned bundle is trivially impersonable/altered | accept | Phase 1 ships an UNSIGNED dev bundle by design to surface toolchain surprises early; signing/notarisation is Phase 6 (DST-01) — explicitly out of scope here |
</threat_model>

<verification>
- `lefthook.yml` present; `.git/hooks/pre-commit` installed; `pnpm lefthook run pre-commit` rejects a type-error probe then passes clean (no real commit)
- package.json/pnpm-lock.yaml UNCHANGED by this plan (HIGH-2 — no overlap with parallel Plan 02)
- `pnpm tauri build` produces `src-tauri/target/release/bundle/macos/*.app`
- `docs/phase-0-notes.md` records HRN-04 first-smoke findings + HRN-02 + final-build placeholders
- Manual: the unsigned `.app` launches after `xattr -dr com.apple.quarantine`
</verification>

<success_criteria>
- lefthook unit gate installed and DEMONSTRATED to block (non-destructively, via `lefthook run`) bad commits (HRN-03)
- package.json untouched — Wave 2 has zero package.json/lockfile overlap with Plan 02 (HIGH-2)
- Manual-gate boundary (UI + /codex:review) documented per D-08
- `pnpm tauri build` produces a runnable unsigned macOS bundle (HRN-04 first smoke; final authoritative build is Plan 04)
- Build findings recorded in docs/phase-0-notes.md with the Plan-04 hand-off for the final build
- No signing attempted (Phase 6); no webdriver surface in this smoke build (it predates the plugin)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-harness-proof/01-03-SUMMARY.md` documenting: the non-destructive blocked-gate proof output (`lefthook run` on the probe), confirmation package.json/lockfile were untouched (HIGH-2), the bundle artifact paths + size + build duration, the explicit note that Plan 04 owns the authoritative post-WebDriver final build, and any toolchain quirks (cargo MSRV / rustup update).
</output>
</parameter>
</invoke>
