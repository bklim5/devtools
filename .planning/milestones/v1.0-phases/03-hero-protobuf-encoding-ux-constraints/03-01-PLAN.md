---
phase: 03-hero-protobuf-encoding-ux-constraints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shell/preferences.ts
  - src/shell/prefsStore.ts
  - src/shell/usePreferences.ts
  - src/shell/prefsStore.test.ts
  - src/shell/usePreferences.test.ts
autonomous: true
requirements: [PRO-06]
nyquist_compliant: true

must_haves:
  truths:
    - "The Preferences schema carries a protobufTreeStyle key defaulting to 'cards'"
    - "A rows/cards choice round-trips through the store seam and survives reload"
    - "A corrupt/absent protobufTreeStyle value coerces to 'cards' (untrusted-merge)"
    - "usePreferences exposes a setTreeStyle setter that writes on change only"
  artifacts:
    - path: "src/shell/preferences.ts"
      provides: "ProtobufTreeStyle type + protobufTreeStyle field + default 'cards'"
      contains: "protobufTreeStyle"
    - path: "src/shell/prefsStore.ts"
      provides: "coerceTreeStyle + protobufTreeStyle in mergePreferences"
      contains: "coerceTreeStyle"
    - path: "src/shell/usePreferences.ts"
      provides: "setTreeStyle setter (write-on-change via update)"
      contains: "setTreeStyle"
  key_links:
    - from: "src/shell/usePreferences.ts"
      to: "src/shell/prefsStore.ts"
      via: "savePreferences persists the merged blob including protobufTreeStyle"
      pattern: "protobufTreeStyle"
    - from: "src/shell/prefsStore.ts"
      to: "src/shell/preferences.ts"
      via: "mergePreferences coerces protobufTreeStyle over DEFAULT_PREFERENCES"
      pattern: "coerceTreeStyle\\(blob\\.protobufTreeStyle\\)"
---

<objective>
Extend the existing preferences seam with a single new persisted field, `protobufTreeStyle: "cards" | "rows"` (default `"cards"`), so the Phase-3 Protobuf tool can persist its rows/cards toggle (PRO-06, D-07) without widening the `Store` seam or touching any tool code. The schema was deliberately reserved for exactly this in Phase 2.

Purpose: Unblocks the protobuf tree toggle (consumed by plan 03-04) with a one-field, untrusted-merge-safe schema extension that round-trips through `platform.store`.
Output: `protobufTreeStyle` in the `Preferences` schema + default, a `coerceTreeStyle` coercer wired into `mergePreferences`, and a `setTreeStyle` setter on `usePreferences`, all covered by extended unit tests (Wave 0 RED → GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-CONTEXT.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-RESEARCH.md
@.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-VALIDATION.md

<interfaces>
<!-- VERIFIED current shapes — read these files (do not assume). -->

From src/shell/preferences.ts (current):
```typescript
export type ThemeName = "dark";
export interface Preferences {
  theme: ThemeName;
  accent: string;
  lastUsedId: string | null;
  recentToolIds: string[];
  // protobufTreeStyle to be ADDED here (the schema comment reserves this exact slot)
}
export const DEFAULT_PREFERENCES: Preferences = {
  theme: "dark", accent: "#3b82f6", lastUsedId: null, recentToolIds: [],
};
```

From src/shell/prefsStore.ts (current): `coerceTheme`/`coerceAccent`/`coerceLastUsedId`/`normalizeRecents` exist; `mergePreferences(stored): Preferences` merges field-by-field over `DEFAULT_PREFERENCES`, accepting only known fields. `loadPreferences()`/`savePreferences()` `await initPlatform()`.

From src/shell/usePreferences.ts (current): `update(patch: Partial<Preferences>)` sets state + `void savePreferences(next)` (write-on-change, guarded by `dirtyRef`/`prefsLoaded`). Existing setters: `setTheme`/`setAccent`/`setLastUsedId`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 RED): Extend prefs tests for protobufTreeStyle</name>
  <read_first>
    - src/shell/prefsStore.test.ts (current test conventions: `// @vitest-environment jsdom`, `setPlatformForTest(makeMemoryPlatform(store))`, `createStoreStub`, explicit `{ describe, it, expect }` imports — globals:false)
    - src/shell/usePreferences.test.ts (existing setter round-trip tests; `@testing-library/react` render + `waitFor`)
    - src/shell/prefsStore.ts (mergePreferences pattern — what a new coercer must look like)
    - src/shell/preferences.ts (current schema + DEFAULT_PREFERENCES)
  </read_first>
  <behavior>
    - mergePreferences({}) → protobufTreeStyle === "cards" (default when absent)
    - mergePreferences({ protobufTreeStyle: "rows" }) → "rows" (valid value preserved)
    - mergePreferences({ protobufTreeStyle: "banana" }) → "cards" (untrusted/unknown coerced to default; threat: hand-edited prefs.json)
    - mergePreferences({ protobufTreeStyle: 42 }) → "cards" (non-string coerced to default)
    - usePreferences: after setTreeStyle("rows"), preferences.protobufTreeStyle === "rows" AND a fresh load (re-mounted hook over the same store) reads "rows" back (round-trips through savePreferences)
  </behavior>
  <action>
Add the above cases. In prefsStore.test.ts add a `describe("protobufTreeStyle coercion")` exercising `mergePreferences` for: absent→"cards", "rows"→"rows", "banana"→"cards", `42`→"cards". In usePreferences.test.ts add a test that renders the hook, calls `setTreeStyle("rows")` inside `act`, asserts local `preferences.protobufTreeStyle === "rows"`, then re-renders/re-mounts a second `usePreferences` against the SAME memory store and asserts it loads `"rows"` (proving savePreferences persisted it). Match existing test setup verbatim (same imports, `beforeEach`/`afterEach` store stub + `setPlatformForTest`/`resetPlatformForTest`). These tests MUST reference `protobufTreeStyle`/`setTreeStyle` that do not exist yet — they are expected to FAIL to compile/run (RED). Do NOT implement the schema in this task.
  </action>
  <verify>
    <automated>npx vitest run src/shell/prefsStore.test.ts src/shell/usePreferences.test.ts 2>&1 | grep -qiE "protobufTreeStyle|setTreeStyle|fail|error" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "protobufTreeStyle" src/shell/prefsStore.test.ts returns ≥ 4
    - grep -q "setTreeStyle" src/shell/usePreferences.test.ts
    - The two test files reference protobufTreeStyle/setTreeStyle (RED state expected — implementation lands in Task 2). lefthook will block committing red; commit Task 1 + Task 2 together in the GREEN commit (Phase-2 precedent in STATE.md).
  </acceptance_criteria>
  <done>Failing (RED) prefs tests exist that pin the protobufTreeStyle default, the untrusted-merge coercion, and the setter round-trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Add protobufTreeStyle to schema, merge, and hook</name>
  <read_first>
    - src/shell/preferences.ts (add the type + field + default here)
    - src/shell/prefsStore.ts (add coercer + include in mergePreferences)
    - src/shell/usePreferences.ts (add setTreeStyle via the existing update() pattern)
  </read_first>
  <action>
1. preferences.ts: add `export type ProtobufTreeStyle = "cards" | "rows";`. Add `protobufTreeStyle: ProtobufTreeStyle;` to the `Preferences` interface (with a doc comment: "Protobuf tree layout (PRO-06, D-07). Persisted; default cards."). Add `protobufTreeStyle: "cards"` to `DEFAULT_PREFERENCES`.
2. prefsStore.ts: add `function coerceTreeStyle(value: unknown): ProtobufTreeStyle { return value === "rows" ? "rows" : "cards"; }` (import `ProtobufTreeStyle` from "./preferences"). In `mergePreferences`, add `protobufTreeStyle: coerceTreeStyle(blob.protobufTreeStyle),` to the returned object. Also add `protobufTreeStyle: DEFAULT_PREFERENCES.protobufTreeStyle` to the catch-fallback object in `loadPreferences` if it spreads explicit fields (it currently spreads `...DEFAULT_PREFERENCES` so this is automatically covered — verify, do not duplicate).
3. usePreferences.ts: import `ProtobufTreeStyle`; add `setTreeStyle: (style: ProtobufTreeStyle) => void;` to the `UsePreferences` interface; implement `const setTreeStyle = useCallback((style: ProtobufTreeStyle) => update({ protobufTreeStyle: style }), [update]);` and return it. Do NOT call the setter during render (Pitfall 5 — write-on-change only).
DO NOT touch `platform.store` directly anywhere; the change is entirely through the schema + merge + hook. DO NOT edit decoder.ts/bytes.ts/types.ts/registry.ts.
  </action>
  <verify>
    <automated>npx vitest run src/shell/prefsStore.test.ts src/shell/usePreferences.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/shell/prefsStore.test.ts src/shell/usePreferences.test.ts exits 0
    - npx tsc --noEmit exits 0
    - grep -q "protobufTreeStyle: \"cards\"" src/shell/preferences.ts (default is cards)
    - grep -q "coerceTreeStyle(blob.protobufTreeStyle)" src/shell/prefsStore.ts
    - grep -q "setTreeStyle" src/shell/usePreferences.ts
    - npx vitest run src/lib/protobuf/decoder.test.ts still reports 19 passing (untouched)
  </acceptance_criteria>
  <done>protobufTreeStyle is part of the typed Preferences, defaults to "cards", coerces unknown values to "cards", and round-trips via setTreeStyle; all prefs tests + tsc green; decoder's 19 tests untouched and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk prefs.json → app | User can hand-edit the persisted prefs blob; values read are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `mergePreferences` reading `protobufTreeStyle` from disk | mitigate | `coerceTreeStyle` accepts only `"rows"`, defaults everything else (incl. non-strings) to `"cards"` — same untrusted-merge pattern as Phase-2 (T-02-08). Unit-tested with `"banana"` and `42`. |
| T-03-02 | DoS | prefs blob size | accept | Single short enum string; no unbounded growth. Existing single-key blob model unchanged. |
</threat_model>

<verification>
- `npx vitest run src/shell/prefsStore.test.ts src/shell/usePreferences.test.ts` exits 0 (new cases green).
- `npx vitest run src/lib/protobuf/decoder.test.ts` reports 19 passing (port-unchanged bar intact).
- `npx tsc --noEmit` clean.
- Per-task harness in order: /simplify → /codex:review (--wait --scope working-tree) → vitest+tsc → real-webview UI (this plan has no UI surface; the toggle's real-webview persistence is verified in 03-04 against `tauri dev` per Pitfall 6).
</verification>

<success_criteria>
- `protobufTreeStyle: "cards" | "rows"` exists in `Preferences` with default `"cards"`.
- Unknown/corrupt values coerce to `"cards"` (untrusted-merge proven by test).
- `setTreeStyle` persists on change and round-trips through the store seam.
- No `@tauri-apps/*` import added; `Store` seam not widened; no port-unchanged file edited.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-protobuf-encoding-ux-constraints/03-01-SUMMARY.md`
</output>
