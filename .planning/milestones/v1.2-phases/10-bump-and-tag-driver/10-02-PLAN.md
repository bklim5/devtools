---
phase: 10-bump-and-tag-driver
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/lib/release/bumpPlan.ts
  - src/lib/release/bumpPlan.test.ts
autonomous: true
requirements: [REL-01, REL-10, REL-11]

must_haves:
  truths:
    - "A single computed next version (from bumpSemver) is threaded into all three manifest edits AND the tag name AND the commit message — the version is computed once, never re-read or recomputed"
    - "parseBumpArgs accepts exactly patch|minor|major (+ optional --dry-run) and rejects anything else (no explicit-version arg, no --no-push, no --skip-checks)"
    - "buildBumpPlan(currentVersion, level) returns the next version, the three {path, edit} manifest transforms, the tag name vX.Y.Z, the commit message chore(release): vX.Y.Z, and the push target — all derived from the one computed version"
    - "assertOnlyExpectedPaths flags any changed path outside the 5-file allowlist (3 manifests + 2 lockfiles) and passes when only allowlisted paths changed (incl. the pnpm-lock no-op case where fewer than 5 changed)"
    - "renderDryRunPlan produces a human-readable plan string containing the computed version, the file edits, the lockfile regen, the git commands, and the push target — with zero side effects"
    - "renderRecovery produces the literal copy-pasteable retry-push and undo (git tag -d / git reset) commands for the declined-confirm and push-failed paths"
    - "the confirm-default helper returns false for empty/no/non-y input and for the non-TTY case (default NO)"
  artifacts:
    - path: "src/lib/release/bumpPlan.ts"
      provides: "Pure, side-effect-free decision core for the bump driver: arg parsing, plan building, allowlist diff, dry-run/recovery text, confirm-default"
      exports: ["parseBumpArgs", "buildBumpPlan", "assertOnlyExpectedPaths", "renderDryRunPlan", "renderRecovery", "ALLOWED_PATHS", "isAffirmative"]
      min_lines: 60
    - path: "src/lib/release/bumpPlan.test.ts"
      provides: "Unit coverage for every pure helper incl. single-computed-version threading and the allowlist diff"
      contains: "buildBumpPlan"
  key_links:
    - from: "buildBumpPlan"
      to: "bumpSemver (Phase 9 version.ts)"
      via: "import { bumpSemver } and call it ONCE; thread the result into edits + tag + message"
      pattern: "bumpSemver"
---

<objective>
Author `src/lib/release/bumpPlan.ts` — the PURE, side-effect-free decision core of the bump driver — plus full vitest coverage, via TDD. This module owns everything that can be unit-tested without performing real git/network/fs actions: CLI arg parsing (D-01/D-02), building the bump plan from a SINGLE computed version (REL-01, the load-bearing single-source rule), the "only expected paths changed" allowlist diff (REL-11), the dry-run plan text (REL-10), the recovery-command text (D-09/D-10), and the confirm-default helper (D-05, non-TTY -> NO).

Purpose: The Phase 11 ethos — pure logic in `src/lib/`, I/O in a thin caller — means the bump driver's testable brain lives here, covered by the existing vitest/tsc/eslint gate, while Plan 03's `scripts/bump-and-tag.mjs` stays a thin shell around it. This gives REL-01/REL-10/REL-11 AUTOMATED coverage rather than only manual verification (RESEARCH §Validation Architecture / Wave 0).

Output: `src/lib/release/bumpPlan.ts` (pure exports, zero fs/git/process side effects) + `src/lib/release/bumpPlan.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-bump-and-tag-driver/10-CONTEXT.md
@.planning/phases/10-bump-and-tag-driver/10-RESEARCH.md
@src/lib/release/version.ts
@src/lib/release/version.test.ts

<interfaces>
<!-- Phase 9 core this module imports. Use directly — do NOT modify version.ts. -->

From src/lib/release/version.ts:
```typescript
export function bumpSemver(version: string, level: "major" | "minor" | "patch"): string;
export function setPackageJsonVersion(content: string, newVersion: string): string;
export function setTauriConfVersion(content: string, newVersion: string): string;
export function setCargoVersion(content: string, newVersion: string): string;
```
All four throw loudly on malformed input / 0 / >1 matches. The driver lets these
throws propagate (fail loud) — never try/catch-swallow.

The 5-path allowlist (RESEARCH §Q3), the authoritative set for assertOnlyExpectedPaths:
  "package.json", "src-tauri/tauri.conf.json", "src-tauri/Cargo.toml",
  "pnpm-lock.yaml", "src-tauri/Cargo.lock"
</interfaces>

<note>
NO app UI is touched — the real-WKWebView UI gate is N/A this phase. The other
three DoD steps (/simplify, /codex:review, vitest+tsc+eslint) apply. This module
MUST be pure: no `node:fs`, no `node:child_process`, no `process.argv` reads, no
console writes inside the helpers (renderDryRunPlan/renderRecovery RETURN strings;
the caller in Plan 03 prints them). Test env is `node`, `globals: false` — import
{ describe, it, expect } from "vitest" explicitly (match version.test.ts).
</note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD parseBumpArgs + isAffirmative (CLI grammar + confirm default)</name>
  <files>src/lib/release/bumpPlan.ts, src/lib/release/bumpPlan.test.ts</files>
  <read_first>
    - src/lib/release/version.test.ts (the test style to mirror: explicit vitest imports, it.each for tables)
    - .planning/phases/10-bump-and-tag-driver/10-CONTEXT.md §"CLI surface" (D-01 level-only patch|minor|major + --dry-run; D-02 only --dry-run, NO --no-push/--skip-checks)
    - .planning/phases/10-bump-and-tag-driver/10-RESEARCH.md §Q5 (isTTY -> NO; empty/non-y -> NO)
  </read_first>
  <behavior>
    parseBumpArgs(argv: string[]) -> { level: "patch"|"minor"|"major", dryRun: boolean }:
    - Test: ["patch"] -> { level: "patch", dryRun: false }
    - Test: ["minor", "--dry-run"] -> { level: "minor", dryRun: true }
    - Test: ["--dry-run", "major"] -> { level: "major", dryRun: true } (order-independent)
    - Test: [] (no level) -> throws /level/ (usage error)
    - Test: ["0.3.0"] -> throws (explicit-version arg rejected, D-01)
    - Test: ["patch", "--no-push"] -> throws (rejected flag, D-02)
    - Test: ["patch", "--skip-checks"] -> throws (rejected flag, D-02)
    - Test: ["foo"] -> throws (unknown level)
    isAffirmative(input: string) -> boolean:
    - Test: "y" -> true; "yes" -> true; "Y" -> true (case-insensitive, trimmed)
    - Test: "" -> false; "n" -> false; "no" -> false; "maybe" -> false
  </behavior>
  <action>
    Write the failing tests FIRST (RED), then implement. parseBumpArgs validates the level against the allowlist `["patch","minor","major"]` and treats `--dry-run` as the only recognized flag; any other token (a version string, an unknown flag like `--no-push`/`--skip-checks`, a second level) throws an Error whose message names the offending token and prints the accepted usage `patch|minor|major [--dry-run]`. isAffirmative trims + lowercases and returns true only for `"y"`/`"yes"`. Keep both pure (no process.argv read inside — the caller passes argv slice).
  </action>
  <acceptance_criteria>
    - `src/lib/release/bumpPlan.ts` exports `parseBumpArgs` and `isAffirmative`
    - `grep -q "no-push\|skip-checks" src/lib/release/bumpPlan.test.ts` (rejected-flag cases present)
    - `pnpm test -- bumpPlan` green for these cases
    - bumpPlan.ts contains NO `process.argv`, NO `node:fs`, NO `node:child_process` import
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- bumpPlan && ! grep -Eq "node:fs|node:child_process|process\.argv" src/lib/release/bumpPlan.ts && echo OK</automated>
  </verify>
  <done>parseBumpArgs enforces D-01/D-02 grammar (throws on explicit version, --no-push, --skip-checks); isAffirmative defaults to false for non-y input. Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD buildBumpPlan — single computed version threaded everywhere (REL-01)</name>
  <files>src/lib/release/bumpPlan.ts, src/lib/release/bumpPlan.test.ts</files>
  <read_first>
    - src/lib/release/version.ts (bumpSemver + the three setXVersion editors — import, do not modify)
    - .planning/phases/10-bump-and-tag-driver/10-CONTEXT.md §"Commit & tag shape" (D-03 commit msg, D-04 annotated tag vX.Y.Z) + §specifics (single-computed-version rule is load-bearing)
    - .planning/phases/10-bump-and-tag-driver/10-RESEARCH.md §"Pattern 1" + §Q6 (git command shapes, push target origin/master then tag)
  </read_first>
  <behavior>
    buildBumpPlan(currentVersion: string, level: BumpLevel) -> BumpPlan, where BumpPlan = {
      currentVersion, nextVersion, tag, commitMessage, pushTarget,
      manifests: Array<{ path, apply: (content: string) => string }>,
      gitCommands: string[]  // human-readable, for the dry-run plan
    }:
    - Test: buildBumpPlan("0.2.1", "patch").nextVersion === "0.2.2"
    - Test: buildBumpPlan("0.2.1", "minor").nextVersion === "0.3.0"
    - Test: buildBumpPlan("0.2.1", "major").nextVersion === "1.0.0"
    - Test: tag === `v${nextVersion}` (e.g. "v0.2.2")
    - Test: commitMessage === `chore(release): v${nextVersion}` (D-03)
    - Test: manifests has exactly 3 entries with paths package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml (in that order)
    - Test (load-bearing, single-source threading): apply each manifest.apply to a fixture and assert the SAME nextVersion lands in all three — and that nextVersion appears EXACTLY ONCE in the plan's computation (the version is computed once via bumpSemver, reused; assert by checking tag, commitMessage, and all three applied outputs all carry the identical string)
    - Test: gitCommands includes the annotated tag form `git tag -a v0.2.2 -m "v0.2.2"` (D-04) and the two-push order `git push origin master` before `git push origin v0.2.2` (RESEARCH §Q6)
    - Test: buildBumpPlan("bad", "patch") throws (propagates bumpSemver's /Invalid semver/)
  </behavior>
  <action>
    RED first. Implement buildBumpPlan to call `bumpSemver(currentVersion, level)` EXACTLY ONCE and store the result in a single `nextVersion` const; derive `tag = "v"+nextVersion`, `commitMessage = "chore(release): " + tag`, `pushTarget = "origin"`, and the three `manifests` entries whose `apply` closures call the matching setXVersion with that ONE nextVersion. Build `gitCommands` as literal strings using the same nextVersion/tag. Do NOT re-read or recompute the version anywhere. Let setXVersion/bumpSemver throws propagate.
    The manifest `apply` closures take file CONTENT (string) and return new content — they perform NO fs themselves (Plan 03's driver reads/writes the files). This keeps buildBumpPlan pure and unit-testable against string fixtures.
  </action>
  <acceptance_criteria>
    - `grep -q "bumpSemver" src/lib/release/bumpPlan.ts` and bumpSemver is called exactly once (single source)
    - bumpPlan.ts contains the literal `chore(release): ` prefix and constructs the tag as `v` + version
    - bumpPlan.ts gitCommands contain `git tag -a` and both `git push origin master` and `git push origin v` (tag) forms
    - `pnpm test -- bumpPlan` green incl. the "same nextVersion in all three manifests" assertion
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- bumpPlan && grep -q "chore(release): " src/lib/release/bumpPlan.ts && grep -q "git tag -a" src/lib/release/bumpPlan.ts && echo OK</automated>
  </verify>
  <done>buildBumpPlan computes the version once and threads it into all 3 manifest edits + the vX.Y.Z annotated tag + the chore(release): commit message + the git command list. Tests prove the single-source threading.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD assertOnlyExpectedPaths + renderDryRunPlan + renderRecovery (REL-10/REL-11 text + allowlist)</name>
  <files>src/lib/release/bumpPlan.ts, src/lib/release/bumpPlan.test.ts</files>
  <read_first>
    - .planning/phases/10-bump-and-tag-driver/10-RESEARCH.md §Q3 (the porcelain allowlist diff; pnpm-lock may be a no-op so FEWER than 5 paths is valid) + §Q6 (the literal recovery block) + §"Pattern 3" (dry-run text)
    - .planning/phases/10-bump-and-tag-driver/10-CONTEXT.md §"Failure recovery" (D-09 retry+undo, D-10 declined-confirm identical)
  </read_first>
  <behavior>
    ALLOWED_PATHS (exported Set/array) = the 5 paths from <interfaces>.
    assertOnlyExpectedPaths(changedPaths: string[]) -> void (throws on stray):
    - Test: ["package.json","src-tauri/tauri.conf.json","src-tauri/Cargo.toml","src-tauri/Cargo.lock"] (4, pnpm-lock no-op) -> does NOT throw (RESEARCH §Q1/§P2: pnpm-lock no-op is valid)
    - Test: all 5 -> does NOT throw
    - Test: [...allowed, "src/App.tsx"] -> throws /unexpected/ naming src/App.tsx
    - Test: [] -> throws (nothing changed — a bump must change at least the 3 manifests; assert at least the 3 manifests are present, or define the empty case as an error and test it)
    renderDryRunPlan(plan: BumpPlan) -> string:
    - Test: contains the nextVersion, the three manifest paths, a lockfile-regen mention, the git commands, and the push target "origin"
    - Test: the returned value is a string and the function performs no side effects (call it twice, identical output; no throw)
    renderRecovery(plan: BumpPlan) -> string:
    - Test: contains `git push origin master && git push origin v<next>` (retry), `git tag -d v<next>`, `git reset --hard HEAD~1`, and `git reset --soft HEAD~1` (D-09 literal block)
  </behavior>
  <action>
    RED first. Implement the three helpers as pure string functions. assertOnlyExpectedPaths diffs the input against ALLOWED_PATHS and throws an Error listing every stray path; it also requires the 3 manifests be present (a real bump always edits them) — fewer lockfiles is fine, a missing manifest or any stray path is an error. renderDryRunPlan and renderRecovery build multi-line strings from the plan fields (reuse plan.gitCommands so the dry-run and the real run print identical commands). NO console.log inside — return the string. The recovery block must be copy-pasteable verbatim (RESEARCH §Q6).
  </action>
  <acceptance_criteria>
    - bumpPlan.ts exports `ALLOWED_PATHS`, `assertOnlyExpectedPaths`, `renderDryRunPlan`, `renderRecovery`
    - `grep -q "git tag -d" src/lib/release/bumpPlan.ts` and `grep -q "git reset --soft" src/lib/release/bumpPlan.ts` (recovery block present)
    - bumpPlan.ts contains NO `console.` call (helpers return strings)
    - `pnpm test -- bumpPlan` green incl. the 4-path (pnpm-lock no-op) accepted case and the stray-path rejected case
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- bumpPlan && grep -q "git tag -d" src/lib/release/bumpPlan.ts && ! grep -q "console\." src/lib/release/bumpPlan.ts && echo OK</automated>
  </verify>
  <done>Allowlist diff accepts the pnpm-lock no-op (4 paths) and rejects strays; dry-run + recovery text built as pure return-strings carrying the single computed version and the literal recovery commands.</done>
</task>

<task type="auto">
  <name>Task 4: Full gate + purity audit of the core module</name>
  <read_first>
    - .planning/phases/10-bump-and-tag-driver/10-VALIDATION.md (the gate: vitest + tsc --noEmit + eslint)
    - src/lib/release/bumpPlan.ts (the module just authored — audit for side effects)
  </read_first>
  <action>
    Run the full D-07 gate one-shot and audit module purity. The core MUST be importable with zero side effects (Plan 03's driver and the tests both import it):
    - `pnpm test` (whole suite green incl. decoder 19/19, version.test.ts, bumpPlan.test.ts)
    - `pnpm exec tsc --noEmit`
    - `pnpm lint`
    - Purity audit: confirm `src/lib/release/bumpPlan.ts` imports nothing from `node:fs`, `node:child_process`, `node:process`/`process.argv`, and contains no top-level executable statement (only declarations/exports) and no `console.` call.
  </action>
  <acceptance_criteria>
    - `pnpm test` exits 0; `pnpm exec tsc --noEmit` exits 0; `pnpm lint` exits 0
    - `bumpPlan.ts` matches none of: `node:fs`, `node:child_process`, `process.argv`, `console.`
    - The decoder's 19 tests still pass (unchanged)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test && pnpm exec tsc --noEmit && pnpm lint && ! grep -Eq "node:fs|node:child_process|process\.argv|console\." src/lib/release/bumpPlan.ts && echo OK</automated>
  </verify>
  <done>bumpPlan core fully covered by vitest, type-clean, lint-clean, and provably pure (no fs/child_process/process/console). Decoder untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> driver | The level/flag string is untrusted text; it ultimately names a git tag/branch operation |
| computed version -> shelled git/cargo (downstream, Plan 03) | The version string is interpolated into tag names; must carry no shell metacharacters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | parseBumpArgs | mitigate | Allowlist the level to exactly patch|minor|major and the only flag to --dry-run; throw on anything else (no arbitrary string flows downstream) — Task 1 |
| T-10-04 | Tampering | buildBumpPlan version string | mitigate | The tag/version derives solely from bumpSemver's output, which is validated to strict MAJOR.MINOR.PATCH (Phase 9 SEMVER_RE) — no shell metacharacters are representable; Task 2 |
| T-10-05 | Elevation/Safety | renderDryRunPlan / renderRecovery | accept | Pure string builders with no side effects; the only risk is misleading text, mitigated by tests asserting exact command content (Task 3) |
| T-10-06 | Info disclosure | plan/recovery output | mitigate | No env/secret is read or interpolated in this module (signing env is Phase 11); helpers only render version + git commands — Task 3 |
</threat_model>

<verification>
- `pnpm test -- bumpPlan` green for all helpers.
- Full suite + tsc + eslint green.
- bumpPlan.ts is provably pure (grep audit: no fs/child_process/process.argv/console).
- bumpSemver called exactly once in buildBumpPlan (single-source rule).
</verification>

<success_criteria>
- The pure decision core exists with full unit coverage, giving REL-01/REL-10/REL-11 automated verification.
- parseBumpArgs enforces D-01/D-02 grammar; buildBumpPlan threads one computed version into 3 manifests + tag + commit message; assertOnlyExpectedPaths handles the pnpm-lock no-op; dry-run + recovery text are pure return-strings.
- Zero new dependencies; decoder + its 19 tests untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/10-bump-and-tag-driver/10-02-SUMMARY.md`
</output>
