---
phase: 10-bump-and-tag-driver
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - scripts/bump-and-tag.mjs
  - package.json
autonomous: false
requirements: [REL-01, REL-03, REL-04, REL-10, REL-11]

must_haves:
  truths:
    - "pnpm release:bump patch|minor|major writes the identical next version into package.json + src-tauri/tauri.conf.json + src-tauri/Cargo.toml ([package] only) from one computed version, touching no other file content"
    - "After the bump, pnpm-lock.yaml (if changed) and Cargo.lock are regenerated and staged in the same commit, and git status --porcelain is empty before the tag is created"
    - "The bump creates the annotated vX.Y.Z tag (version from the written file, never typed twice) and, after a y/N confirm, pushes the commit then the tag to origin (bklim5/devtools)"
    - "pnpm release:bump <level> --dry-run prints the full plan and changes zero files and performs zero git/network actions (git status --porcelain identical before and after)"
    - "Preflights fail fast and abort non-zero BEFORE any write when the tree is dirty, the branch is not master, the target tag exists local or remote, or vitest/tsc/eslint are not green"
    - "On a declined confirm or a push failure, the script keeps the local commit+tag, exits non-zero, and prints the literal retry-push and undo commands (never a tool-initiated git reset)"
  artifacts:
    - path: "scripts/bump-and-tag.mjs"
      provides: "Thin Node ESM I/O driver: preflights -> read+edit 3 manifests -> regen+stage lockfiles -> commit -> annotated tag -> print plan -> y/N -> push, with --dry-run short-circuit and recovery output"
      contains: "execFileSync"
      min_lines: 80
    - path: "package.json"
      provides: "release:bump script wired to tsx"
      contains: "release:bump"
  key_links:
    - from: "scripts/bump-and-tag.mjs"
      to: "src/lib/release/bumpPlan.ts + version.ts (via tsx)"
      via: "import the pure core; the .mjs only does fs/git/cargo/pnpm I/O + prompting"
      pattern: "bumpPlan"
    - from: "scripts/bump-and-tag.mjs"
      to: "git/pnpm/cargo CLIs"
      via: "execFileSync with argv arrays (never execSync string interpolation)"
      pattern: "execFileSync"
---

<objective>
Author `scripts/bump-and-tag.mjs` — the thin I/O caller that turns the pure Plan 02 core into the real `pnpm release:bump` maintainer command — and wire the `release:bump` script. The driver reads the three manifests, edits them via the Plan 02 plan, regenerates + stages the lockfiles (stage-only-if-changed for the pnpm no-op), commits, creates the annotated `vX.Y.Z` tag, prints the push plan, asks a TTY-guarded y/N, then pushes commit-then-tag to `origin` — guarded by `--dry-run` (zero side effects) and fail-fast preflights, with copy-pasteable recovery output on decline/failure.

Purpose: Delivers REL-01 (lockstep 3-file write, wired to real fs), REL-03 (lockfile regen+stage, clean tagged tree), REL-04 (annotated tag + push to private origin), REL-10 (--dry-run), REL-11 (preflights) for the bump half. All the testable decision logic already lives in Plan 02's pure core; this script is deliberately thin orchestration + I/O + the two human-facing print surfaces.

Output: `scripts/bump-and-tag.mjs`; `package.json` `release:bump` script; a verified `--dry-run` proving zero side effects; a human-gated real push (the documented manual-only verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-bump-and-tag-driver/10-CONTEXT.md
@.planning/phases/10-bump-and-tag-driver/10-RESEARCH.md
@.planning/phases/10-bump-and-tag-driver/10-VALIDATION.md
@src/lib/release/version.ts
@package.json

<interfaces>
<!-- Plan 02 pure core this driver consumes (import via tsx, explicit .ts extension). -->

From src/lib/release/bumpPlan.ts:
```typescript
export function parseBumpArgs(argv: string[]): { level: "patch"|"minor"|"major"; dryRun: boolean };
export function buildBumpPlan(currentVersion: string, level: "patch"|"minor"|"major"): BumpPlan;
export function assertOnlyExpectedPaths(changedPaths: string[]): void; // throws on stray
export function renderDryRunPlan(plan: BumpPlan): string;
export function renderRecovery(plan: BumpPlan): string;
export function isAffirmative(input: string): boolean;
export const ALLOWED_PATHS: ReadonlySet<string> | string[];
// BumpPlan = { currentVersion, nextVersion, tag, commitMessage, pushTarget,
//              manifests: {path, apply:(content:string)=>string}[], gitCommands: string[] }
```

<!-- Phase 9 core (also available; buildBumpPlan already uses it internally). -->
From src/lib/release/version.ts: bumpSemver, setPackageJsonVersion, setTauriConfVersion, setCargoVersion.
</interfaces>

<verified_commands>
<!-- RESEARCH verified ALL of these in-repo this session. Copy verbatim. -->

Read current version: parse "version" from package.json (the value fed to buildBumpPlan).

Preflights (read-only, ALL before the first write — D-08):
  execFileSync("git", ["status", "--porcelain"])              # must be empty
  execFileSync("git", ["rev-parse", "--abbrev-ref", "HEAD"])  # must === "master"
  execFileSync("git", ["rev-parse", "-q", "--verify", "refs/tags/v<next>"])  # exit 0 == tag EXISTS locally (abort); non-zero == absent (ok)
  execFileSync("git", ["ls-remote", "--tags", "origin", "v<next>"])          # NON-EMPTY stdout == exists on remote (abort). If origin unreachable -> ABORT with recovery message (RESEARCH Q6 / A3 safe default)
Gate (one-shot, non-watch):
  execFileSync("pnpm", ["test"])              # = vitest run
  execFileSync("pnpm", ["exec", "tsc", "--noEmit"])
  execFileSync("pnpm", ["lint"])              # = eslint .

Lockfile regen (AFTER manifest writes):
  execFileSync("pnpm", ["install", "--lockfile-only", "--offline"])           # EXPECT no-op (pnpm-lock does not store root version, RESEARCH Q1)
  execFileSync("cargo", ["update", "-p", "devtools-app", "--offline"], { cwd: "src-tauri" })   # surgical 1-package relock (NOT generate-lockfile)
  # dry-run preview form: cargo update -p devtools-app --offline --dry-run

Stage-only-if-changed + commit + tag (after allowlist check passes):
  # compute changed = git status --porcelain | slice(3); stage only those in ALLOWED_PATHS
  execFileSync("git", ["add", ...changedAllowed])
  execFileSync("git", ["commit", "-m", plan.commitMessage])          # chore(release): vX.Y.Z
  execFileSync("git", ["tag", "-a", plan.tag, "-m", plan.tag])       # annotated vX.Y.Z (D-04)
  # assert clean: git status --porcelain empty BEFORE pushing

Push (only after y/N == yes — commit FIRST, then tag):
  execFileSync("git", ["push", "origin", "master"])
  execFileSync("git", ["push", "origin", plan.tag])

Confirm (RESEARCH Q5): node:readline/promises; if (!stdin.isTTY) return false; else isAffirmative(answer).
</verified_commands>

<note>
NO app UI is touched — the real-WKWebView UI gate is N/A. The other three DoD
steps (/simplify, /codex:review, vitest+tsc+eslint) apply. The script is `.mjs`
(package.json is type:module); run via `tsx` so it can import the `.ts` core with
no build step. Use `execFileSync` (argv arrays) NEVER `execSync` with an
interpolated string (shell-injection guard, threat T-10-04). NEVER `git add -A`
or `git commit -a` (stage the explicit allowlist). NEVER run `git reset` or
`git tag -d` from the script (D-09/D-10 — print the command, never execute it).
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/bump-and-tag.mjs + wire the release:bump script</name>
  <files>scripts/bump-and-tag.mjs, package.json</files>
  <read_first>
    - src/lib/release/bumpPlan.ts (the pure core to wrap — parseBumpArgs/buildBumpPlan/assertOnlyExpectedPaths/renderDryRunPlan/renderRecovery/isAffirmative)
    - src/lib/release/version.ts (the editors buildBumpPlan threads; the driver applies plan.manifests[].apply to file content)
    - .planning/phases/10-bump-and-tag-driver/10-RESEARCH.md sections Q1-Q6 + Pitfalls P1/P4/P5/P7 (the verified commands and the no-op/cwd/gate/staging gotchas)
    - .planning/phases/10-bump-and-tag-driver/10-CONTEXT.md (all of D-01..D-11)
    - scripts/e2e-spike.sh (repo convention for scripts/ helpers)
    - package.json (where to add the release:bump script, alongside test/lint/build)
  </read_first>
  <action>
    Implement the driver as the ordered pipeline (RESEARCH Pattern 2), importing the Plan 02 core via `import { ... } from "../src/lib/release/bumpPlan.ts"`. Pipeline:

    1. Parse args: `const { level, dryRun } = parseBumpArgs(process.argv.slice(2));` (let throws print usage + exit non-zero).
    2. Read current version from package.json; `const plan = buildBumpPlan(current, level);`.
    3. Read-only preflights (ALL before any write — D-08, copy the verified commands above):
       clean tree, branch===master, tag absent local AND remote (abort if origin unreachable), then the gate vitest+tsc+eslint (D-06/D-07). Any failure -> print the reason, exit non-zero, NO writes.
    4. If `dryRun`: print `renderDryRunPlan(plan)` and EXIT 0. Perform zero fs writes / zero git writes / zero cargo/pnpm writes (do NOT call the lockfile regen; the pnpm one is a no-op anyway, the cargo one would write — skip it). After the dry-run path, Task 2 asserts `git status --porcelain` is unchanged.
    5. Writes (reversible, local): for each `plan.manifests` entry, read the file, `apply` it, write it back (`node:fs` readFileSync/writeFileSync).
    6. Lockfiles: `pnpm install --lockfile-only --offline` (expect no-op) then `cargo update -p devtools-app --offline` with `{ cwd: "src-tauri" }`.
    7. Allowlist check: compute changed paths from `git status --porcelain` (slice(3)); `assertOnlyExpectedPaths(changed)` — abort BEFORE commit on any stray.
    8. Stage-only-if-changed: `git add` exactly the changed paths that are in ALLOWED_PATHS (so the pnpm-lock no-op is simply absent). Commit `plan.commitMessage`. Create annotated tag `git tag -a plan.tag -m plan.tag`.
    9. Assert `git status --porcelain` is EMPTY (criterion #2) before pushing.
    10. Print the push plan (what + where: commit + tag -> origin). y/N confirm via node:readline/promises guarded by `stdin.isTTY` (non-TTY -> false). Use `isAffirmative` from the core.
    11. If NO (declined) -> print `renderRecovery(plan)`, exit non-zero (D-10). Keep commit+tag.
    12. If YES -> push commit then tag (two pushes, commit first). On push failure (catch) -> print `renderRecovery(plan)`, exit non-zero (D-09). NEVER run `git reset`/`git tag -d` — only print them.

    Wire `package.json`: add `"release:bump": "tsx scripts/bump-and-tag.mjs"` to scripts (alongside `test`/`lint`). Keep the file `.mjs`. Use `execFileSync` for every CLI call (argv arrays). Let the setXVersion/bumpSemver throws propagate with a clear message.
  </action>
  <acceptance_criteria>
    - `package.json` contains the line `"release:bump": "tsx scripts/bump-and-tag.mjs"`
    - `scripts/bump-and-tag.mjs` contains `execFileSync` and does NOT contain a bare `execSync(` call (only `execFileSync`)
    - `scripts/bump-and-tag.mjs` contains `--lockfile-only` and contains `devtools-app` and does NOT contain `generate-lockfile`
    - `scripts/bump-and-tag.mjs` contains `node:readline/promises` and `isTTY`
    - `scripts/bump-and-tag.mjs` imports from `bumpPlan` (uses the Plan 02 core, not re-derived logic)
    - `scripts/bump-and-tag.mjs` contains the `["tag", "-a"` annotated-tag argv form
    - No `git reset` / `git tag -d` appears as an execFileSync argv array (those commands appear ONLY inside the printed renderRecovery text from the core)
    - `pnpm lint` is clean (the new .mjs passes eslint)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "release:bump" package.json && grep -q "execFileSync" scripts/bump-and-tag.mjs && ! grep -Eq "[^F]execSync\(" scripts/bump-and-tag.mjs && grep -q -- "--lockfile-only" scripts/bump-and-tag.mjs && grep -q "devtools-app" scripts/bump-and-tag.mjs && ! grep -q "generate-lockfile" scripts/bump-and-tag.mjs && grep -q "node:readline/promises" scripts/bump-and-tag.mjs && grep -q "isTTY" scripts/bump-and-tag.mjs && grep -q "bumpPlan" scripts/bump-and-tag.mjs && pnpm lint</automated>
  </verify>
  <done>scripts/bump-and-tag.mjs implements the full preflight->bump->lockfile->commit->tag->confirm->push pipeline with --dry-run short-circuit and recovery output, wired to pnpm release:bump, using execFileSync argv arrays and the Plan 02 pure core.</done>
</task>

<task type="auto">
  <name>Task 2: Verify --dry-run has zero side effects and preflights abort fast (REL-10, REL-11)</name>
  <files>scripts/bump-and-tag.mjs</files>
  <read_first>
    - scripts/bump-and-tag.mjs (the driver just authored)
    - .planning/phases/10-bump-and-tag-driver/10-RESEARCH.md §"Pattern 3" + §Pitfalls P1 (dry-run must not write) + §P6 (clean tree)
    - .planning/phases/10-bump-and-tag-driver/10-VALIDATION.md §"Per-Task Verification Map" + §"Manual-Only Verifications"
  </read_first>
  <action>
    Exercise the non-irreversible paths of the driver to prove REL-10 (zero side effects) and REL-11 (fail-fast preflights) WITHOUT performing any commit/tag/push. Run each check from a clean tree and confirm the tree is unchanged afterward.

    A. DRY-RUN, ZERO SIDE EFFECTS (REL-10):
       - Snapshot: `git status --porcelain > /tmp/before.txt`
       - Run `pnpm release:bump patch --dry-run` — confirm it prints a plan that shows ONE computed version everywhere (the bumped version in all three manifest lines, the `vX.Y.Z` tag, and the `chore(release): vX.Y.Z` message), the lockfile-regen step, the git commands, and `origin` as the push target.
       - Snapshot again: `git status --porcelain > /tmp/after.txt`; assert `/tmp/before.txt` and `/tmp/after.txt` are identical (zero files changed). Also confirm no new tag exists: `git tag -l vX.Y.Z` empty.

    B. PREFLIGHT ABORTS (REL-11) — each must exit non-zero with NO write:
       - Dirty tree: create a reversible scratch change (e.g. append a line to README.md), run `pnpm release:bump patch`, confirm it aborts non-zero citing the dirty tree BEFORE any version write; then revert the scratch change immediately.
       - Wrong branch: `git switch -c scratch-preflight`, run `pnpm release:bump patch`, confirm it aborts non-zero citing branch != master; `git switch master && git branch -D scratch-preflight`.
       - Existing tag: temporarily create the target tag locally (`git tag v<computed-next>`), run `pnpm release:bump patch`, confirm it aborts non-zero citing tag exists; `git tag -d v<computed-next>`.
       - Confirm that after EVERY abort, `git status --porcelain` shows no manifest/lockfile changes (the abort happened before writes).

    Do NOT proceed to the real push in this task — that is the human-gated checkpoint (Task 3). Restore the tree to clean after every sub-check; if any sub-check leaves a stray change, undo it.
  </action>
  <acceptance_criteria>
    - `pnpm release:bump patch --dry-run` exits 0, prints a plan containing the next version + the tag `v<next>` + `chore(release): v<next>` + `origin`, and leaves `git status --porcelain` byte-identical to before
    - No `v<next>` tag is created by the dry-run (`git tag -l` unchanged)
    - The dirty-tree, non-master, and existing-tag scenarios each cause a non-zero exit with a clear reason and ZERO manifest/lockfile writes
    - The tree is clean (`git status --porcelain` empty) after this task completes
  </acceptance_criteria>
  <verify>
    <automated>git status --porcelain > /tmp/bt_before.txt; pnpm release:bump patch --dry-run; git status --porcelain > /tmp/bt_after.txt; diff /tmp/bt_before.txt /tmp/bt_after.txt && echo DRYRUN_CLEAN</automated>
  </verify>
  <done>--dry-run proven to change zero files and perform zero git/network actions; the three git preflights each abort non-zero before any write; tree clean afterward.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-gated real bump + push (REL-04 manual-only verification)</name>
  <files>scripts/bump-and-tag.mjs</files>
  <action>
    This is the one documented manual-only verification (10-VALIDATION.md): the real
    `git push` of the commit + annotated tag to the private `origin` is irreversible
    and network-bound, so it cannot be safely automated. The executor automates
    everything up to the push (already covered by Tasks 1-2) and then PAUSES for the
    maintainer to run a real `pnpm release:bump <level>`, inspect the bumped tree +
    annotated tag, exercise the y/N confirm (including a declined-then-recover pass),
    and approve the push to `bklim5/devtools`. Do not script the push.
  </action>
  <what-built>
    `scripts/bump-and-tag.mjs` (wired to `pnpm release:bump`) does the full lockstep
    bump across the three manifests from one computed version, regenerates + stages
    the lockfiles, commits `chore(release): vX.Y.Z`, creates the annotated `vX.Y.Z`
    tag, prints the push plan, asks y/N, and on yes pushes the commit then the tag
    to the private `origin` (`bklim5/devtools`). Preflights and `--dry-run` are
    already automated-verified (Task 2). The real push is irreversible/network and
    is the one documented manual-only verification (10-VALIDATION.md).
  </what-built>
  <how-to-verify>
    1. Confirm the tree is clean: `git status --porcelain` is empty, branch is `master`.
    2. Run a real bump, e.g. `pnpm release:bump patch`.
    3. Observe: the three manifests now show the SAME next version (`grep '"version"' package.json src-tauri/tauri.conf.json` and the `[package]` line in `src-tauri/Cargo.toml`); `Cargo.lock` devtools-app entry updated; `git status --porcelain` is EMPTY before the prompt (clean, reproducible tree); the commit is `chore(release): vX.Y.Z`; the annotated tag `vX.Y.Z` exists (`git tag -n vX.Y.Z`).
    4. At the y/N prompt: optionally answer `n` first — confirm the script keeps the local commit+tag and prints the literal retry-push + undo (`git tag -d` / `git reset`) commands and exits non-zero. Then either follow its printed retry command, or re-run is blocked by the existing-tag preflight (expected).
    5. When ready to publish: answer `y` (or run the printed retry) — confirm `git push origin master` then `git push origin vX.Y.Z` succeed and the commit + tag appear on `bklim5/devtools`.
    6. Confirm no secret/token was echoed anywhere in the output.
  </how-to-verify>
  <verify>
    <automated>MANUAL — irreversible network push to private origin; verified by the maintainer per how-to-verify (Tasks 1-2 cover all automatable behavior).</automated>
  </verify>
  <done>The maintainer has run a real bump, confirmed one computed version across all 3 manifests + annotated tag + clean tree, exercised the declined-confirm recovery path, and pushed the commit + tag to bklim5/devtools with no secret echoed.</done>
  <resume-signal>Type "approved" once the bump, commit, annotated tag, clean tree, and push-to-origin are confirmed (or describe any issue).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> driver | The level/flag string is untrusted text that drives git tag/push operations |
| computed version -> shelled git/cargo | The version/tag string is passed to git/cargo subprocesses |
| working tree -> private origin | The push is the single irreversible network action against the live private remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | shelled git/pnpm/cargo calls | mitigate | Use `execFileSync` with argv ARRAYS (never `execSync` with an interpolated string); the version/tag derives from the validated `bumpSemver` output (strict MAJOR.MINOR.PATCH — no shell metacharacters representable) — Task 1 |
| T-10-07 | Safety/Elevation | irreversible push (wrong branch / dup tag / dirty tree) | mitigate | Fail-fast preflights (clean tree, branch==master, tag absent local+remote) run BEFORE any write, plus the TTY-guarded y/N confirm before the push (D-05/D-08) — Tasks 1 & 2 |
| T-10-08 | Repudiation/Safety | tool-initiated destructive reset | mitigate | The script NEVER executes `git reset`/`git tag -d`; on decline/failure it only PRINTS the recovery commands (D-09/D-10) — verified by acceptance grep in Task 1 |
| T-10-09 | Info disclosure | plan/recovery/console output | mitigate | No env/secret is read or echoed in the bump half (signing env is Phase 11); output contains only version + git commands — Task 1, confirmed in Task 3 step 6 |
| T-10-10 | Tampering | tagging a dirty/unexpected tree | mitigate | `assertOnlyExpectedPaths` allowlist diff + explicit-path staging + clean-tree assertion before the tag (D-11) — Task 1 |
</threat_model>

<verification>
- Automated (Task 1): grep acceptance — release:bump wired, execFileSync (no execSync), --lockfile-only + devtools-app (no generate-lockfile), readline/promises + isTTY, imports bumpPlan, annotated tag form, no reset/tag -d execution; `pnpm lint` clean.
- Automated (Task 2): `--dry-run` leaves `git status --porcelain` byte-identical (zero side effects); the three preflight scenarios abort non-zero before any write.
- Manual (Task 3): the real bump produces a clean tagged tree with one computed version in all three manifests + annotated tag, and pushes commit+tag to private origin; declined-confirm keeps local work + prints recovery; no secret echoed.
</verification>

<success_criteria>
- `pnpm release:bump patch|minor|major` performs the lockstep 3-manifest write from one computed version, regenerates + stages the lockfiles, commits, and creates the annotated `vX.Y.Z` tag against a clean tree, then pushes commit+tag to `origin` after a y/N confirm.
- `--dry-run` changes zero files and performs zero git/network actions.
- Preflights abort fast before any write on dirty tree / non-master / existing tag / failing gate.
- Decline or push-failure keeps local work and prints copy-pasteable recovery; no tool-initiated reset; no secret echoed.
- Zero new dependencies; decoder + its 19 tests untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/10-bump-and-tag-driver/10-03-SUMMARY.md`
</output>
