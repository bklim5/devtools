---
phase: 10-bump-and-tag-driver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.lock
autonomous: true
requirements: [REL-03]

must_haves:
  truths:
    - "The deferred Cargo.lock devtools-app entry is committed at 0.2.1 (matching Cargo.toml), so a fresh checkout has a clean working tree"
    - "git status --porcelain is empty after the housekeeping commit (no pre-existing dirty Cargo.lock to trip the bump driver's clean-tree preflight)"
    - "Only the devtools-app package entry changed in Cargo.lock; no other package versions or checksums shifted"
  artifacts:
    - path: "src-tauri/Cargo.lock"
      provides: "Reconciled lockfile with devtools-app at 0.2.1, committed (no longer dirty in the working tree)"
      contains: "version = \"0.2.1\""
  key_links:
    - from: "Cargo.toml [package].version (0.2.1, Phase 9)"
      to: "Cargo.lock devtools-app entry"
      via: "the committed lock now matches the manifest, completing Phase 9's deferred regen"
      pattern: "devtools-app"
---

<objective>
Commit the deferred `Cargo.lock` `0.1.0 -> 0.2.1` reconcile that Phase 9 left uncommitted in the working tree, as a SEPARATE housekeeping commit — BEFORE any bump-driver logic exists. This completes Phase 9's deliberately-deferred lockfile regen (STATE.md / RESEARCH §P6) so a fresh checkout is clean and the bump driver's clean-tree preflight (D-08) does not falsely abort on the very first real run.

Purpose: RESEARCH §P6 (HIGHEST RISK) — `Cargo.lock` is `0.1.0` at HEAD but `0.2.1` uncommitted in the working tree. If this rides along inside the `chore(release):` commit it muddies criterion #1 ("no other file content touched"); if left dirty it trips the clean-tree preflight. A dedicated housekeeping commit isolates it cleanly.

Output: `src-tauri/Cargo.lock` committed with `devtools-app` at `0.2.1`; `git status --porcelain` empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-bump-and-tag-driver/10-CONTEXT.md
@.planning/phases/10-bump-and-tag-driver/10-RESEARCH.md

<note>
This phase touches NO app UI — the per-task real-WKWebView UI gate is N/A. The
other three Definition-of-Done steps (/simplify, /codex:review, unit gate
vitest+tsc+eslint) still apply. There is no code change in THIS plan beyond a
lockfile commit, so /simplify is a no-op here; still run the unit gate to confirm
the tree is green and nothing regressed.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and commit the deferred Cargo.lock reconcile as housekeeping</name>
  <read_first>
    - src-tauri/Cargo.toml (confirm [package].version = "0.2.1" at line 3 — the target the lock must match)
    - src-tauri/Cargo.lock (the devtools-app entry; confirm it currently shows 0.2.1 in the working tree)
    - .planning/phases/10-bump-and-tag-driver/10-RESEARCH.md §"Runtime State Inventory" + §P6 (the dirty-lock finding and why a SEPARATE commit, not folded into the bump)
  </read_first>
  <action>
    Confirm the working-tree state matches RESEARCH §P6, then commit ONLY the lockfile reconcile as a standalone housekeeping commit. Do NOT regenerate the lock here — Phase 9 already produced the correct 0.2.1 working-tree content; this plan only COMMITS it.

    1. Verify HEAD vs worktree:
       - `git show HEAD:src-tauri/Cargo.lock | grep -A1 'name = "devtools-app"'` → expect `version = "0.1.0"`
       - `grep -A1 'name = "devtools-app"' src-tauri/Cargo.lock` → expect `version = "0.2.1"`
       - `git status --porcelain` → expect exactly one line: ` M src-tauri/Cargo.lock`
    2. Confirm the diff is ONLY the devtools-app own-version line (no other package versions/checksums shifted):
       - `git diff src-tauri/Cargo.lock` → the only changed `version = "..."` hunk must be inside the `name = "devtools-app"` block (0.1.0 -> 0.2.1). If ANY other package entry differs, STOP and report — do not commit unrelated churn (D-11 / RESEARCH §P3).
    3. Stage and commit EXACTLY this file (explicit path, never `git add -A`):
       - `git add src-tauri/Cargo.lock`
       - `git commit -m "chore(release): reconcile Cargo.lock devtools-app 0.1.0 -> 0.2.1 (deferred from Phase 9)"`
    4. Confirm clean tree: `git status --porcelain` → empty output.

    If step 2 finds extra churn, the working-tree lock was regenerated too broadly at some point — report the stray packages and stop; do NOT silently commit them.
  </action>
  <acceptance_criteria>
    - `git show HEAD:src-tauri/Cargo.lock | grep -A1 'name = "devtools-app"'` returns `version = "0.2.1"` (after the commit, HEAD now carries the reconcile)
    - `git status --porcelain` produces EMPTY output (clean tree)
    - The most recent commit (`git log -1 --format=%s`) is the `chore(release): reconcile Cargo.lock devtools-app 0.1.0 -> 0.2.1 (deferred from Phase 9)` message
    - `git show HEAD --stat` lists ONLY `src-tauri/Cargo.lock` as changed
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain)" && git show HEAD:src-tauri/Cargo.lock | grep -A1 'name = "devtools-app"' | grep -q 'version = "0.2.1"' && echo OK</automated>
  </verify>
  <done>Cargo.lock devtools-app committed at 0.2.1 in a dedicated housekeeping commit; working tree clean; only the lockfile changed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the unit/type/lint gate is green on the clean tree</name>
  <read_first>
    - package.json (the `test`, `lint` scripts — `test` = `vitest run`, `lint` = `eslint .`)
    - .planning/phases/10-bump-and-tag-driver/10-VALIDATION.md (the sampling-rate contract: vitest + tsc --noEmit + eslint)
  </read_first>
  <action>
    Run the full D-07 gate to confirm the clean tree passes the same bar the bump driver will later enforce on itself. No code changed, so this is a regression guard. Run the one-shot (non-watch) forms only:
    - `pnpm test` (= `vitest run`; the immovable 19 decoder tests + the Phase 9 release-core tests must stay green)
    - `pnpm exec tsc --noEmit`
    - `pnpm lint`
    Abort and report if any is non-zero. Do NOT use `pnpm build` (it runs `vite build`) — only `tsc --noEmit` for the type check.
  </action>
  <acceptance_criteria>
    - `pnpm test` exits 0 with the full suite green (decoder 19/19 + release-lib tests)
    - `pnpm exec tsc --noEmit` exits 0 with no output
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test && pnpm exec tsc --noEmit && pnpm lint</automated>
  </verify>
  <done>vitest + tsc --noEmit + eslint all green on the clean, reconciled tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> git history | A lockfile diff is committed; a stray edit could be tagged later if not isolated now |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | the housekeeping commit (Cargo.lock) | mitigate | Diff-verify the change is ONLY the devtools-app own-version line before committing (step 2); explicit `git add src-tauri/Cargo.lock` (never `git add -A`) so no stray file is swept in |
| T-10-02 | Tampering | bump-commit purity (downstream) | mitigate | Isolating the deferred lock into a SEPARATE commit keeps the later `chore(release):` commit free of unrelated diffs (criterion #1) |
</threat_model>

<verification>
- `git status --porcelain` empty after the housekeeping commit.
- HEAD `Cargo.lock` shows devtools-app at 0.2.1; no other package entries changed.
- Full unit/type/lint gate green.
</verification>

<success_criteria>
- The deferred Cargo.lock reconcile is committed as a standalone housekeeping commit (NOT folded into a future bump commit).
- A fresh checkout / current tree is clean — the bump driver's D-08 clean-tree preflight will not falsely abort.
- vitest + tsc + eslint green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-bump-and-tag-driver/10-01-SUMMARY.md`
</output>
