// Throwaway Phase-1 placeholder. Plan 02 ports the real router/App; Task 3 of
// this plan applies the dark-window styling. Kept minimal so the scaffold builds.
function App() {
  return (
    <main className="flex min-h-screen items-center justify-center font-sans">
      <h1>DevTools — scaffold</h1>
    </main>
  );
}

export default App;
